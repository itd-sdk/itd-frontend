const __vite__mapDeps = (
  i,
  m = __vite__mapDeps,
  d = m.f ||
    (m.f = [
      "assets/index-Cs8C4zj9.js",
      "assets/IconNotificationMention-e79ywpKW.js",
      "assets/IconInfo-CcKxyY8p.js",
      "assets/index-PK1FQ786.css",
      "assets/index-DDf9w1fw.js",
      "assets/index-DldLfQtW.css",
      "assets/VoiceInput-CnkjU1-_.js",
      "assets/IconPlay-CbwDz6zO.js",
      "assets/VoiceInput-DZdGic7O.css",
      "assets/PostMediaVideo-DPEVDT0F.js",
      "assets/PostMediaVideo-BU5GkgA2.css",
      "assets/index-CpKibEpf.js",
      "assets/IconCheckCircle-DsXIWG-y.js",
      "assets/index-DUNfAr-5.css",
      "assets/index-BfAQ7MuJ.js",
      "assets/index-CMb0_kOg.css",
      "assets/index-DxaTj9Qm.js",
      "assets/index-dlzu-GCD.css",
      "assets/index-BL62Y3xN.js",
      "assets/index-cCML6m2F.css",
      "assets/index-CtxhrCFP.js",
      "assets/index-OH_PF-Xj.css",
      "assets/index-B9669l9n.js",
      "assets/IconChevronLeft-Dk6onnGB.js",
      "assets/index-CBuCdRiz.css",
      "assets/index-2izXOrDU.js",
      "assets/index-CR-zXVgs.css",
      "assets/index-DrHNb29y.js",
      "assets/index-D97fxzoE.css",
      "assets/index-BH27YdR9.js",
      "assets/IconCheck-aKs2NOQh.js",
      "assets/index-BugMCLi7.css",
      "assets/index-DDpAeEm4.js",
      "assets/index-fVyEeTxZ.css",
      "assets/index-CiWj-mK_.js",
      "assets/index-CvwY5MQ0.css",
      "assets/index-Cwi6RGS5.js",
      "assets/index-A-p77jj4.css",
      "assets/index-be4I7QTS.js",
      "assets/index-DPEA_KpL.css",
      "assets/index-CqMzHFVy.js",
      "assets/index-FiJYfR7B.css",
      "assets/index-BGzFHrA0.js",
      "assets/index-Br4kO3ru.css",
      "assets/index-B3jmvXK3.js",
      "assets/index-KIeJZKvI.css",
      "assets/index-DKGMn3gR.js",
      "assets/index-BVrhoziR.css",
      "assets/index-BpMxfl41.js",
      "assets/index-DLl-nvUc.css",
      "assets/index-B2WBWaZg.js",
      "assets/index-CeNvjz_E.css",
      "assets/index-LYjm6vTJ.js",
      "assets/index-Y8oASWOB.css",
      "assets/index-C6ljCNWs.js",
      "assets/index-CGMxHk7J.css",
      "assets/index-Bz7li-X9.js",
      "assets/index-Bt-cCGG-.js",
      "assets/index-Bkp0Nmzn.css",
      "assets/index-scvl2LQN.js",
      "assets/index-DUFXb9MU.css",
      "assets/IconEyeOff-DkfQwtOQ.js",
      "assets/index-BlqwaJ7Q.css",
      "assets/index-BbSCDQFq.js",
      "assets/index-DIoOHPp1.css",
      "assets/index-iy_jnniV.js",
      "assets/index-BLGv7ZNS.css",
      "assets/index-_I6Z1HoX.js",
      "assets/index-DWYK_8EB.css",
      "assets/index-tLqCcjk5.js",
      "assets/index-Bt_F-ZCF.css",
    ])
) => i.map(i => d[i]);
(() => {
  try {
    const e =
      typeof window !== "undefined"
        ? window
        : typeof global !== "undefined"
        ? global
        : typeof globalThis !== "undefined"
        ? globalThis
        : typeof self !== "undefined"
        ? self
        : {};
    e.SENTRY_RELEASE = { id: "1.1.0" };
    const t = new e.Error().stack;

    if (t) {
      (e._sentryDebugIds = e._sentryDebugIds || {});
      (e._sentryDebugIds[t] = "65d8de20-0ccc-47ba-8f2e-22b404aa50a8");
      (e._sentryDebugIdIdentifier = "sentry-dbid-65d8de20-0ccc-47ba-8f2e-22b404aa50a8");
    }
  } catch {}
})();
(() => {
  const t = document.createElement("link").relList;
  if (t && t.supports && t.supports("modulepreload")) {
    return;
  }
  for (const o of document.querySelectorAll('link[rel="modulepreload"]')) {
    r(o);
  }
  new MutationObserver((o) => {
    for (const s of o) {
      if (s.type === "childList") {
        for (const a of s.addedNodes) {
          if (a.tagName === "LINK" && a.rel === "modulepreload") {
            r(a);
          }
        }
      }
    }
  }).observe(document, { childList: true, subtree: true });
  function n(o) {
    const s = {};

    if (o.integrity) {
      (s.integrity = o.integrity);
    }

    if (o.referrerPolicy) {
      (s.referrerPolicy = o.referrerPolicy);
    }

    if (o.crossOrigin === "use-credentials") {
      (s.credentials = "include");
    } else if (o.crossOrigin === "anonymous") {
      (s.credentials = "omit");
    } else {
      (s.credentials = "same-origin");
    }

    return s;
  }
  function r(o) {
    if (o.ep) {
      return;
    }
    o.ep = true;
    const s = n(o);
    fetch(o.href, s);
  }
})();
let Bn;
let q;
let Sa;
let Wt;
let Ss;
let ba;
let Ca;
let Ta;
let Ho;
let lo;
let uo;
let Ia;
const Mn = {};
const ka = [];
const Dl = /acit|ex(?:s|g|n|p|$)|rph|grid|ows|mnc|ntw|ine[ch]|zoo|^ord|itera/i;
const Array_isArray = Array.isArray;
function nt(e, t) {
  for (const n in t) {
    e[n] = t[n];
  }
  return e;
}
function Bo(e) {
  if (e && e.parentNode) {
    e.parentNode.removeChild(e);
  }
}
function Je(e, t, n) {
  let r;
  let o;
  let s;
  const a = {};
  for (s in t) {
    if (s == "key") {
      (r = t[s]);
    } else if (s == "ref") {
      (o = t[s]);
    } else {
      (a[s] = t[s]);
    }
  }

  if (arguments.length > 2) {
    (a.children = arguments.length > 3 ? Bn.call(arguments, 2) : n);
  }

  if (typeof e == "function" && e.defaultProps != null) {
    for (s in e.defaultProps) {
      if (a[s] === undefined) {
        (a[s] = e.defaultProps[s]);
      }
    }
  }

  return An(e, a, r, o, null);
}
function An(e, t, n, r, o) {
  const s = {
    type: e,
    props: t,
    key: n,
    ref: r,
    __k: null,
    __: null,
    __b: 0,
    __e: null,
    __c: null,
    constructor: undefined,
    __v: o ?? ++Sa,
    __i: -1,
    __u: 0,
  };

  if (o == null && q.vnode != null) {
    q.vnode(s);
  }

  return s;
}
function Ul() {
  return { current: null };
}
function Te(e) {
  return e.children;
}

class qe {
  constructor(e, t) {
    (this.props = e);
    (this.context = t);
  }

  setState(e, t) {
    let n;

    (n = this.__s != null && this.__s != this.state
      ? this.__s
      : (this.__s = nt({}, this.state)));

    if (typeof e == "function") {
      (e = e(nt({}, n), this.props));
    }

    if (e) {
      nt(n, e);
    }

    if (e != null && this.__v) {
      t && this._sb.push(t);
      fo(this);
    }
  }

  forceUpdate(e) {
    if (this.__v) {
      (this.__e = true);
      e && this.__h.push(e);
      fo(this);
    }
  }
}

function on(e, t) {
  if (t == null) {
    return e.__ ? on(e.__, e.__i + 1) : null;
  }
  let n;
  for (; t < e.__k.length; t++) {
    if ((n = e.__k[t]) != null && n.__e != null) {
      return n.__e;
    }
  }
  return typeof e.type == "function" ? on(e) : null;
}
function Na(e) {
  let t;
  let n;
  if ((e = e.__) != null && e.__c != null) {
    e.__e = null;
    e.__c.base = null;

    for (t = 0; t < e.__k.length; t++) {
      if ((n = e.__k[t]) != null && n.__e != null) {
        e.__e = e.__c.base = n.__e;
        break;
      }
    }

    return Na(e);
  }
}
function fo(e) {
  if (((!e.__d && (e.__d = true) && Wt.push(e) && !yr.__r++) || Ss != q.debounceRendering)) {
    ((Ss = q.debounceRendering) || ba)(yr);
  }
}
function yr() {
  let e;
  let t;
  let n;
  let r;
  let o;
  let s;
  let a;
  let c = 1;

  while (Wt.length) {
    if (Wt.length > c) {
      Wt.sort(Ca);
    }

    (e = Wt.shift());
    (c = Wt.length);

    if (e.__d) {
      (n = undefined);
      (r = undefined);
      (o = (r = (t = e).__v).__e);
      (s = []);
      (a = []);

      t.__P &&
        (((n = nt({}, r)).__v = r.__v + 1),
        q.vnode && q.vnode(n),
        Vo(
          t.__P,
          n,
          r,
          t.__n,
          t.__P.namespaceURI,
          32 & r.__u ? [o] : null,
          s,
          o ?? on(r),
          !!(32 & r.__u),
          a
        ),
        (n.__v = r.__v),
        (n.__.__k[n.__i] = n),
        Aa(s, n, a),
        (r.__e = r.__ = null),
        n.__e != o && Na(n));
    }
  }

  yr.__r = 0;
}
function Ra(e, t, n, r, o, s, a, c, l, u, f) {
  let d;
  let p;
  let h;
  let m;
  let g;
  let T;
  let _;
  const y = (r && r.__k) || ka;
  const t_length = t.length;
  l = Fl(n, t, y, l, t_length);

  for (d = 0; d < t_length; d++) {
    if ((h = n.__k[d]) != null) {
      (p = h.__i == -1 ? Mn : y[h.__i] || Mn);
      (h.__i = d);
      (T = Vo(e, h, p, o, s, a, c, l, u, f));
      (m = h.__e);

      h.ref &&
        p.ref != h.ref &&
        (p.ref && Wo(p.ref, null, h), f.push(h.ref, h.__c || m, h));

      g == null && m != null && (g = m);

      (_ = !!(4 & h.__u)) || p.__k === h.__k
        ? (l = Pa(h, l, e, _))
        : typeof h.type == "function" && T !== undefined
        ? (l = T)
        : m && (l = m.nextSibling);

      (h.__u &= -7);
    }
  }

  (n.__e = g);
  return l;
}
function Fl(e, t, n, r, o) {
  let s;
  let a;
  let c;
  let l;
  let u;
  const n_length = n.length;
  let d = n_length;
  let p = 0;
  e.__k = new Array(o);

  for (s = 0; s < o; s++) {
    if ((a = t[s]) != null && typeof a != "boolean" && typeof a != "function") {
      typeof a == "string" ||
          typeof a == "number" ||
          typeof a == "bigint" ||
          a.constructor == String
            ? (a = e.__k[s] = An(null, a, null, null, null))
            : Array_isArray(a)
            ? (a = e.__k[s] = An(Te, { children: a }, null, null, null))
            : a.constructor === undefined && a.__b > 0
            ? (a = e.__k[s] =
                An(a.type, a.props, a.key, a.ref ? a.ref : null, a.__v))
            : (e.__k[s] = a);

      (l = s + p);
      (a.__ = e);
      (a.__b = e.__b + 1);
      (c = null);
      (u = a.__i = Hl(a, n, l, d)) != -1 && (d--, (c = n[u]) && (c.__u |= 2));

      c == null || c.__v == null
        ? (u == -1 && (o > n_length ? p-- : o < n_length && p++),
          typeof a.type != "function" && (a.__u |= 4))
        : u != l &&
          (u == l - 1
            ? p--
            : u == l + 1
            ? p++
            : (u > l ? p-- : p++, (a.__u |= 4)));
    } else {
      (e.__k[s] = null);
    }
  }

  if (d) {
    for (s = 0; s < n_length; s++) {
      if ((c = n[s]) != null &&
        (2 & c.__u) == 0) {
        c.__e == r && (r = on(c));
        xa(c, c);
      }
    }
  }
  return r;
}
function Pa(e, t, n, r) {
  let o;
  let s;
  if (typeof e.type == "function") {
    o = e.__k;

    for (s = 0; o && s < o.length; s++) {
      if (o[s]) {
        (o[s].__ = e);
        (t = Pa(o[s], t, n, r));
      }
    }

    return t;
  }

  if (e.__e != t) {
    r &&
        (t && e.type && !t.parentNode && (t = on(e)),
        n.insertBefore(e.__e, t || null));

    (t = e.__e);
  }

  do {
    t = t && t.nextSibling;
  } while (t != null && t.nodeType == 8);
  return t;
}
function Ke(e, t) {
  (t = t || []);

  if (e != null && typeof e != "boolean") {
    if (Array_isArray(e)) {
      e.some(n => {
              Ke(n, t);
            });
    } else {
      t.push(e);
    }
  }

  return t;
}
function Hl(e, t, n, r) {
  let o;
  let s;
  let a;

  const {
    key,
    type
  } = e;

  let t_n = t[n];
  const f = t_n != null && (2 & t_n.__u) == 0;
  if ((t_n === null && key == null) || (f && key == t_n.key && type == t_n.type)) {
    return n;
  }
  if (r > (f ? 1 : 0)) {
    o = n - 1;

    for (s = n + 1; o >= 0 || s < t.length; ) {
      if ((t_n = t[(a = o >= 0 ? o-- : s++)]) != null &&
      (2 & t_n.__u) == 0 &&
      key == t_n.key &&
      type == t_n.type) {
        return a;
      }
    }
  }
  return -1;
}
function bs(e, t, n) {
  if (t[0] == "-") {
    e.setProperty(t, n ?? "");
  } else {
    (e[t] = n == null ? "" : typeof n != "number" || Dl.test(t) ? n : `${n}px`);
  }
}
function Jn(e, t, n, r, o) {
  let s;
  let a;
  e: if (t == "style") {
    if (typeof n == "string") {
      e.style.cssText = n;
    } else {
      if (typeof r == "string") {
        (e.style.cssText = r = "");
      }

      if (r) {
        for (t in r) {
          if (!n || t in n) {
            bs(e.style, t, "");
          }
        }
      }

      if (n) {
        for (t in n) {
          if (!r || n[t] != r[t]) {
            bs(e.style, t, n[t]);
          }
        }
      }
    }
  } else if (t[0] == "o" && t[1] == "n") {
    (s = t != (t = t.replace(Ta, "$1")));
    (a = t.toLowerCase());

    (t = a in e || t == "onFocusOut" || t == "onFocusIn"
      ? a.slice(2)
      : t.slice(2));

    if (!e.l) {
      (e.l = {});
    }

    (e.l[t + s] = n);

    if (n) {
      if (r) {
        (n.u = r.u);
      } else {
        (n.u = Ho);
        e.addEventListener(t, s ? uo : lo, s);
      }
    } else {
      e.removeEventListener(t, s ? uo : lo, s);
    }
  } else {
    if (o == "http://www.w3.org/2000/svg") {
      t = t.replace(/xlink(H|:h)/, "h").replace(/sName$/, "s");
    } else if (t != "width" &&
    t != "height" &&
    t != "href" &&
    t != "list" &&
    t != "form" &&
    t != "tabIndex" &&
    t != "download" &&
    t != "rowSpan" &&
    t != "colSpan" &&
    t != "role" &&
    t != "popover" &&
    t in e) {
      try {
        e[t] = n ?? "";
        break e;
      } catch {}
    }

    if (typeof n != "function") {
      if (n == null || (n === false && t[4] != "-")) {
        e.removeAttribute(t);
      } else {
        e.setAttribute(t, t == "popover" && n == 1 ? "" : n);
      }
    }
  }
}
function Cs(e) {
  return function (t) {
    if (this.l) {
      const n = this.l[t.type + e];
      if (t.t == null) {
        t.t = Ho++;
      } else if (t.t < n.u) {
        return;
      }
      return n(q.event ? q.event(t) : t);
    }
  };
}
function Vo(e, t, n, r, o, s, a, c, l, u) {
  let f;
  let d;
  let p;
  let h;
  let m;
  let g;
  let T;
  let _;
  let y;
  let w;
  let I;
  let S;
  let v;
  let C;
  let b;
  let E;
  let R;
  const t_type = t.type;
  if (t.constructor !== undefined) {
    return null;
  }

  if (128 & n.__u) {
    (l = !!(32 & n.__u));
    (s = [(c = t.__e = n.__e)]);
  }

  if ((f = q.__b)) {
    f(t);
  }

  e: if (typeof t_type == "function") {
    try {
      (_ = t.props);
      (y = "prototype" in t_type && t_type.prototype.render);
      (w = (f = t_type.contextType) && r[f.__c]);
      (I = f ? (w ? w.props.value : f.__) : r);

      if (n.__c) {
        (T = (d = t.__c = n.__c).__ = d.__E);
      } else {
        y
              ? (t.__c = d = new t_type(_, I))
              : ((t.__c = d = new qe(_, I)),
                (d.constructor = t_type),
                (d.render = Vl));

        w && w.sub(d);
        d.state || (d.state = {});
        (d.__n = r);
        (p = d.__d = true);
        (d.__h = []);
        (d._sb = []);
      }

      if (y && d.__s == null) {
        (d.__s = d.state);
      }

      if (y &&
        t_type.getDerivedStateFromProps != null) {
        d.__s == d.state && (d.__s = nt({}, d.__s));
        nt(d.__s, t_type.getDerivedStateFromProps(_, d.__s));
      }

      (h = d.props);
      (m = d.state);
      (d.__v = t);

      if (p) {
        if (y &&
          t_type.getDerivedStateFromProps == null &&
          d.componentWillMount != null) {
          d.componentWillMount();
        }

        if (y && d.componentDidMount != null) {
          d.__h.push(d.componentDidMount);
        }
      } else {
        if (y &&
            t_type.getDerivedStateFromProps == null &&
            _ !== h &&
            d.componentWillReceiveProps != null) {
          d.componentWillReceiveProps(_, I);
        }

        if (t.__v == n.__v ||
          (!d.__e &&
            d.shouldComponentUpdate != null &&
            d.shouldComponentUpdate(_, d.__s, I) === false)) {
          if (t.__v != n.__v) {
            (d.props = _);
            (d.state = d.__s);
            (d.__d = false);
          }

          t.__e = n.__e;
          t.__k = n.__k;

          t.__k.some($ => {
            if ($) {
              ($.__ = t);
            }
          });

          for (S = 0; S < d._sb.length; S++) {
            d.__h.push(d._sb[S]);
          }

          (d._sb = []);

          if (d.__h.length) {
            a.push(d);
          }

          break e;
        }

        if (d.componentWillUpdate != null) {
          d.componentWillUpdate(_, d.__s, I);
        }

        if (y &&
          d.componentDidUpdate != null) {
          d.__h.push(() => {
            d.componentDidUpdate(h, m, g);
          });
        }
      }

      (d.context = I);
      (d.props = _);
      (d.__P = e);
      (d.__e = false);
      (v = q.__r);
      (C = 0);

      if (y) {
        d.state = d.__s;
        d.__d = false;

        if (v) {
          v(t);
        }

        f = d.render(d.props, d.state, d.context);

        for (b = 0; b < d._sb.length; b++) {
          d.__h.push(d._sb[b]);
        }

        d._sb = [];
      } else {
        do {
          (d.__d = false);

          if (v) {
            v(t);
          }

          (f = d.render(d.props, d.state, d.context));
          (d.state = d.__s);
        } while (d.__d && ++C < 25);
      }

      (d.state = d.__s);

      if (d.getChildContext != null) {
        (r = nt(nt({}, r), d.getChildContext()));
      }

      if (y &&
        !p &&
        d.getSnapshotBeforeUpdate != null) {
        (g = d.getSnapshotBeforeUpdate(h, m));
      }

      (E = f);

      if (f != null &&
        f.type === Te &&
        f.key == null) {
        (E = Oa(f.props.children));
      }

      (c = Ra(e, Array_isArray(E) ? E : [E], t, n, r, o, s, a, c, l, u));
      (d.base = t.__e);
      (t.__u &= -161);

      if (d.__h.length) {
        a.push(d);
      }

      if (T) {
        (d.__E = d.__ = null);
      }
    } catch ($) {
      (t.__v = null);

      if (l || s != null) {
        if ($.then) {
          for (t.__u |= l ? 160 : 128; c && c.nodeType == 8 && c.nextSibling; ) {
            c = c.nextSibling;
          }
          (s[s.indexOf(c)] = null);
          (t.__e = c);
        } else {
          for (R = s.length; R--; ) {
            Bo(s[R]);
          }
          po(t);
        }
      } else {
        (t.__e = n.__e);
        (t.__k = n.__k);

        if (!$.then) {
          po(t);
        }
      }

      q.__e($, t, n);
    }
  } else {
    if (s == null && t.__v == n.__v) {
      (t.__k = n.__k);
      (t.__e = n.__e);
    } else {
      (c = t.__e = Bl(n.__e, t, n, r, o, s, a, l, u));
    }
  }

  if ((f = q.diffed)) {
    f(t);
  }

  return 128 & t.__u ? undefined : c;
}
function po(e) {
  if (e && e.__c) {
    (e.__c.__e = true);
  }

  if (e && e.__k) {
    e.__k.forEach(po);
  }
}
function Aa(e, t, n) {
  for (let r = 0; r < n.length; r++) {
    Wo(n[r], n[++r], n[++r]);
  }

  if (q.__c) {
    q.__c(t, e);
  }

  e.some(o => {
    try {
      (e = o.__h);
      (o.__h = []);

      e.some(s => {
        s.call(o);
      });
    } catch (s) {
      q.__e(s, o.__v);
    }
  });
}
function Oa(e) {
  return typeof e != "object" || e == null || (e.__b && e.__b > 0)
    ? e
    : Array_isArray(e)
    ? e.map(Oa)
    : nt({}, e);
}
function Bl(e, t, n, r, o, s, a, c, l) {
  let u;
  let f;
  let d;
  let p;
  let h;
  let m;
  let g;
  let T = n.props || Mn;

  const {
    props,
    type
  } = t;

  if (type == "svg") {
    (o = "http://www.w3.org/2000/svg");
  } else if (type == "math") {
    (o = "http://www.w3.org/1998/Math/MathML");
  } else if (!o) {
    (o = "http://www.w3.org/1999/xhtml");
  }

  if (s != null) {
    for (u = 0; u < s.length; u++) {
      if (
        (h = s[u]) &&
        "setAttribute" in h == !!type &&
        (type ? h.localName == type : h.nodeType == 3)
      ) {
        (e = h);
        (s[u] = null);
        break;
      }
    }
  }

  if (e == null) {
    if (type == null) {
      return document.createTextNode(props);
    }
    (e = document.createElementNS(o, type, props.is && props));

    if (c) {
      q.__m && q.__m(t, s);
      (c = false);
    }

    (s = null);
  }
  if (type == null) {
    if (T !== props && (!c || e.data != props)) {
      (e.data = props);
    }
  } else {
    (s = s && Bn.call(e.childNodes));

    if (!c && s != null) {
      T = {};

      for (u = 0; u < e.attributes.length; u++) {
        T[(h = e.attributes[u]).name] = h.value;
      }
    }

    for (u in T) {
      (h = T[u]);

      if (u != "children") {
        if (u == "dangerouslySetInnerHTML") {
          d = h;
        } else if (!(u in props)) {
          if ((u == "value" && "defaultValue" in props) ||
          (u == "checked" && "defaultChecked" in props)) {
            continue;
          }
          Jn(e, u, null, h, o);
        }
      }
    }
    for (u in props) {
      (h = props[u]);

      switch (u) {
      case "children":
        (p = h);
        break;
      case "dangerouslySetInnerHTML":
        (f = h);
        break;
      case "value":
        (m = h);
        break;
      case "checked":
        (g = h);
        break;
      default:
        Jn(e, u, h, T[u], o);
        break;
      }
    }
    if (f) {
      if (!c && (!d || f.__html != d.__html && f.__html != e.innerHTML)) {
        (e.innerHTML = f.__html);
      }

      (t.__k = []);
    } else {
      if (d) {
        (e.innerHTML = "");
      }

      Ra(
        t.type == "template" ? e.content : e,
        Array_isArray(p) ? p : [p],
        t,
        n,
        r,
        type == "foreignObject" ? "http://www.w3.org/1999/xhtml" : o,
        s,
        a,
        s ? s[0] : n.__k && on(n, 0),
        c,
        l
      );

      if (s != null) {
        for (u = s.length; u--; ) {
          Bo(s[u]);
        }
      }
    }

    if (!c) {
      (u = "value");

      type == "progress" && m == null
        ? e.removeAttribute("value")
        : m != null &&
          (m !== e[u] ||
            (type == "progress" && !m) ||
            (type == "option" && m != T[u])) &&
          Jn(e, u, m, T[u], o);

      (u = "checked");
      g != null && g != e[u] && Jn(e, u, g, T[u], o);
    }
  }
  return e;
}
function Wo(e, t, n) {
  try {
    if (typeof e == "function") {
      const r = typeof e.__u == "function";

      if (r) {
        e.__u();
      }

      if (!r || t != null) {
        (e.__u = e(t));
      }
    } else {
      e.current = t;
    }
  } catch (o) {
    q.__e(o, n);
  }
}
function xa(e, t, n) {
  let r;
  let o;

  if (q.unmount) {
    q.unmount(e);
  }

  if ((r = e.ref)) {
    if (!r.current || r.current == e.__e) {
      Wo(r, null, t);
    }
  }

  if ((r = e.__c) != null) {
    if (r.componentWillUnmount) {
      try {
        r.componentWillUnmount();
      } catch (s) {
        q.__e(s, t);
      }
    }
    r.base = null;
    r.__P = null;
  }

  if ((r = e.__k)) {
    for (o = 0; o < r.length; o++) {
      if (r[o]) {
        xa(r[o], t, n || typeof e.type != "function");
      }
    }
  }

  if (!n) {
    Bo(e.__e);
  }

  e.__c = undefined;
  e.__ = undefined;
  e.__e = undefined;
}
function Vl(e, t, n) {
  return this.constructor(e, n);
}
function $n(e, t, n) {
  let r;
  let o;
  let s;
  let a;

  if (t == document) {
    (t = document.documentElement);
  }

  if (q.__) {
    q.__(e, t);
  }

  (o = (r = typeof n == "function") ? null : (n && n.__k) || t.__k);
  (s = []);
  (a = []);

  Vo(
    t,
    (e = ((!r && n) || t).__k = Je(Te, null, [e])),
    o || Mn,
    Mn,
    t.namespaceURI,
    !r && n ? [n] : o ? null : t.firstChild ? Bn.call(t.childNodes) : null,
    s,
    !r && n ? n : o ? o.__e : t.firstChild,
    r,
    a
  );

  Aa(s, e, a);
}
function La(e, t) {
  $n(e, t, La);
}
function Ma(e, t, n) {
  let r;
  let o;
  let s;
  let a;
  const c = nt({}, e.props);

  if (e.type && e.type.defaultProps) {
    (a = e.type.defaultProps);
  }

  for (s in t) {
    if (s == "key") {
      (r = t[s]);
    } else if (s == "ref") {
      (o = t[s]);
    } else {
      (c[s] = t[s] === undefined && a != null ? a[s] : t[s]);
    }
  }

  if (arguments.length > 2) {
    (c.children = arguments.length > 3 ? Bn.call(arguments, 2) : n);
  }

  return An(e.type, c, r || e.key, o || e.ref, null);
}
function fn(e) {
  function t(n) {
    if (!this.getChildContext) {
      (r = new Set());
      o = {};
      o[t.__c] = this;
      (this.getChildContext = () => o);

      (this.componentWillUnmount = () => {
          r = null;
        });

      (this.shouldComponentUpdate = function (s) {
        if (this.props.value != s.value) {
          r.forEach(a => {
            (a.__e = true);
            fo(a);
          });
        }
      });

      (this.sub = s => {
          r.add(s);
          const s_componentWillUnmount = s.componentWillUnmount;
          s.componentWillUnmount = () => {
            if (r) {
              r.delete(s);
            }

            if (s_componentWillUnmount) {
              s_componentWillUnmount.call(s);
            }
          };
        });
    }

    return n.children;
  }
  (t.__c = `__cC${Ia++}`);
  (t.__ = e);
  t.Provider = t;
  t.__l = t;

  (t.Consumer = (n, r) => n.children(r)).contextType = t;

  return t;
}
(Bn = ka.slice);

(q = {
    __e(e, t, n, r) {
      let o;
      let s;
      let a;

      while ((t = t.__)) {
        if ((o = t.__c) && !o.__) {
          try {
            if ((s = o.constructor) &&
                s.getDerivedStateFromError != null) {
              o.setState(s.getDerivedStateFromError(e));
              (a = o.__d);
            }

            if (o.componentDidCatch != null) {
              o.componentDidCatch(e, r || {});
              (a = o.__d);
            }

            if (a) {
              return (o.__E = o);
            }
          } catch (c) {
            e = c;
          }
        }
      }

      throw e;
    },
  });

(Sa = 0);

(qe.prototype.render = Te);
(Wt = []);

(ba = typeof Promise == "function"
  ? Promise.prototype.then.bind(Promise.resolve())
  : setTimeout);

(Ca = (e, t) => e.__v.__b - t.__v.__b);

(yr.__r = 0);
(Ta = /(PointerCapture)$|Capture$/i);
(Ho = 0);
(lo = Cs(false));
(uo = Cs(true));
(Ia = 0);
let Wl = 0;
function i(e, t, n, r, o, s) {
  if (!t) {
    (t = {});
  }

  let a;
  let c;
  let l = t;
  if ("ref" in l) {
    (l = {});

    for (c in t) {
      if (c == "ref") {
        (a = t[c]);
      } else {
        (l[c] = t[c]);
      }
    }
  }
  const u = {
    type: e,
    props: l,
    key: n,
    ref: a,
    __k: null,
    __: null,
    __b: 0,
    __e: null,
    __c: null,
    constructor: undefined,
    __v: --Wl,
    __i: -1,
    __u: 0,
    __source: o,
    __self: s,
  };
  if (typeof e == "function" && (a = e.defaultProps)) {
    for (c in a) {
      if (l[c] === undefined) {
        (l[c] = a[c]);
      }
    }
  }

  if (q.vnode) {
    q.vnode(u);
  }

  return u;
}
const z = typeof __SENTRY_DEBUG__ === "undefined" || __SENTRY_DEBUG__;
const te = globalThis;
const zt = "10.52.0";
function Ir() {
  kr(te);
  return te;
}
function kr(e) {
  const t = (e.__SENTRY__ = e.__SENTRY__ || {});
  (t.version = t.version || zt);
  (t[zt] = t[zt] || {});
  return t[zt];
}
function pn(e, t, n = te) {
  const r = (n.__SENTRY__ = n.__SENTRY__ || {});
  const o = (r[zt] = r[zt] || {});
  return o[e] || (o[e] = t());
}
const jl = ["debug", "info", "warn", "error", "log", "assert", "trace"];
const zl = "Sentry Logger ";
const vr = {};
function hn(e) {
  if (!("console" in te)) {
    return e();
  }
  const te_console = te.console;
  const n = {};
  const r = Object.keys(vr);
  r.forEach((o) => {
    const vr_o = vr[o];
    (n[o] = te_console[o]);
    (te_console[o] = vr_o);
  });
  try {
    return e();
  } finally {
    r.forEach((o) => {
      te_console[o] = n[o];
    });
  }
}
function ql() {
  zo().enabled = true;
}
function Gl() {
  zo().enabled = false;
}
function $a() {
  return zo().enabled;
}
function Yl(...e) {
  jo("log", ...e);
}
function Xl(...e) {
  jo("warn", ...e);
}
function Kl(...e) {
  jo("error", ...e);
}
function jo(e, ...t) {
  if (z &&
    $a()) {
    hn(() => {
      te.console[e](`${zl}[${e}]:`, ...t);
    });
  }
}
function zo() {
  return z ? pn("loggerSettings", () => ({
    enabled: false
  })) : { enabled: false };
}

const F = {
    enable: ql,
    disable: Gl,
    isEnabled: $a,
    log: Yl,
    warn: Xl,
    error: Kl,
  };

const Da = 50;
const Gt = "?";
const Ts = /\(error: (.*)\)/;
const Is = /captureMessage|captureException/;
function Ua(...e) {
  const t = e.sort((n, r) => n[0] - r[0]).map(n => n[1]);
  return (n, r = 0, o = 0) => {
    const s = [];

    const a = n.split(`
`);

    for (let c = r; c < a.length; c++) {
      let a_c = a[c];

      if (a_c.length > 1024) {
        (a_c = a_c.slice(0, 1024));
      }

      const u = Ts.test(a_c) ? a_c.replace(Ts, "$1") : a_c;
      if (!u.includes("Error: ")) {
        for (const f of t) {
          const d = f(u);
          if (d) {
            s.push(d);
            break;
          }
        }
        if (s.length >= Da + o) {
          break;
        }
      }
    }
    return Jl(s.slice(o));
  };
}
function Zl(e) {
  return Array.isArray(e) ? Ua(...e) : e;
}
function Jl(e) {
  if (!e.length) {
    return [];
  }
  const t = Array.from(e);

  if (/sentryWrapped/.test(Qn(t).function || "")) {
    t.pop();
  }

  t.reverse();

  if (Is.test(Qn(t).function || "")) {
    t.pop();
    Is.test(Qn(t).function || "") && t.pop();
  }

  return t
    .slice(0, Da)
    .map(n => ({
    ...n,
    filename: n.filename || Qn(t).filename,
    function: n.function || Gt
  }));
}
function Qn(e) {
  return e[e.length - 1] || {};
}
const Fr = "<anonymous>";
function Nt(e) {
  try {
    return !e || typeof e != "function" ? Fr : e.name || Fr;
  } catch {
    return Fr;
  }
}
function ks(e) {
  const e_exception = e.exception;
  if (e_exception) {
    const n = [];
    try {
      e_exception.values.forEach((r) => {
        if (r.stacktrace.frames) {
          n.push(...r.stacktrace.frames);
        }
      });

      return n;
    } catch {
      return;
    }
  }
}
function Fa(e) {
  return "__v_isVNode" in e && e.__v_isVNode ? "[VueVNode]" : "[VueViewModel]";
}
const In = {};
const Ns = {};
function Kt(e, t) {
  (In[e] = In[e] || []);
  In[e].push(t);

  return () => {
    const In_e = In[e];
    if (In_e) {
      const r = In_e.indexOf(t);

      if (r !== -1) {
        In_e.splice(r, 1);
      }
    }
  };
}
function Zt(e, t) {
  if (!Ns[e]) {
    Ns[e] = true;
    try {
      t();
    } catch (n) {
      if (z) {
        F.error(`Error while instrumenting ${e}`, n);
      }
    }
  }
}
function Ze(e, t) {
  const n = e && In[e];
  if (n) {
    for (const r of n) {
      try {
        r(t);
      } catch (o) {
        if (z) {
          F.error(
            `Error while triggering instrumentation handler.
Type: ${e}
Name: ${Nt(r)}
Error:`,
            o
          );
        }
      }
    }
  }
}
let Hr = null;
function Ql(e) {
  const t = "error";
  Kt(t, e);
  Zt(t, eu);
}
function eu(...args) {
  (Hr = te.onerror);

  (te.onerror = function (e, t, n, r, o) {
    Ze("error", { column: r, error: o, line: n, msg: e, url: t });
    return Hr ? Hr.apply(this, args) : false;
  });

  (te.onerror.__SENTRY_INSTRUMENTED__ = true);
}
let Br = null;
function tu(e) {
  const t = "unhandledrejection";
  Kt(t, e);
  Zt(t, nu);
}
function nu(...args) {
  (Br = te.onunhandledrejection);

  (te.onunhandledrejection = function (e) {
    Ze("unhandledrejection", e);
    return Br ? Br.apply(this, args) : true;
  });

  (te.onunhandledrejection.__SENTRY_INSTRUMENTED__ = true);
}
const Ha = Object.prototype.toString;
function Wn(e) {
  switch (Ha.call(e)) {
    case "[object Error]":
    case "[object Exception]":
    case "[object DOMException]":
    case "[object WebAssembly.Exception]":
      {
        return true;
      }
    default:
      {
        return Rt(e, Error);
      }
  }
}
function mn(e, t) {
  return Ha.call(e) === `[object ${t}]`;
}
function Ba(e) {
  return mn(e, "ErrorEvent");
}
function Rs(e) {
  return mn(e, "DOMError");
}
function ru(e) {
  return mn(e, "DOMException");
}
function vt(e) {
  return mn(e, "String");
}
function qo(e) {
  return (
    typeof e == "object" &&
    e !== null &&
    "__sentry_template_string__" in e &&
    "__sentry_template_values__" in e
  );
}
function Nr(e) {
  return (
    e === null || qo(e) || (typeof e != "object" && typeof e != "function")
  );
}
function Dn(e) {
  return mn(e, "Object");
}
function Rr(e) {
  return typeof Event !== "undefined" && Rt(e, Event);
}
function ou(e) {
  return typeof Element !== "undefined" && Rt(e, Element);
}
function su(e) {
  return mn(e, "RegExp");
}
function jn(e) {
  return !!(e?.then && typeof e.then == "function");
}
function iu(e) {
  return (
    Dn(e) &&
    "nativeEvent" in e &&
    "preventDefault" in e &&
    "stopPropagation" in e
  );
}
function Rt(e, t) {
  try {
    return e instanceof t;
  } catch {
    return false;
  }
}
function Va(e) {
  return !!(
    typeof e == "object" &&
    e !== null &&
    (e.__isVue || e._isVue || e.__v_isVNode)
  );
}
function Wa(e) {
  return typeof Request !== "undefined" && Rt(e, Request);
}
const Go = te;
const au = 80;
function ja(e, t = {}) {
  if (!e) {
    return "<unknown>";
  }
  try {
    let n = e;
    const r = 5;
    const o = [];
    let s = 0;
    let a = 0;
    const c = " > ";
    const c_length = c.length;
    let u;
    const f = Array.isArray(t) ? t : t.keyAttrs;
    const d = (!Array.isArray(t) && t.maxStringLength) || au;

    while (n &&
         s++ < r &&
         ((u = cu(n, f)),
         !(u === "html" || (s > 1 && a + o.length * c_length + u.length >= d)))) {
      o.push(u);
      (a += u.length);
      (n = n.parentNode);
    }

    return o.reverse().join(c);
  } catch {
    return "<unknown>";
  }
}
function cu(e, t) {
  const n = e;
  const r = [];
  if (!n?.tagName) {
    return "";
  }
  if (Go.HTMLElement && n instanceof HTMLElement && n.dataset) {
    if (n.dataset.sentryComponent) {
      return n.dataset.sentryComponent;
    }
    if (n.dataset.sentryElement) {
      return n.dataset.sentryElement;
    }
  }
  r.push(n.tagName.toLowerCase());
  const o = t?.length
    ? t.filter(s => n.getAttribute(s)).map(s => [s, n.getAttribute(s)])
    : null;
  if (o?.length) {
    o.forEach((s) => {
      r.push(`[${s[0]}="${s[1]}"]`);
    });
  } else {
    if (n.id) {
      r.push(`#${n.id}`);
    }

    const n_className = n.className;
    if (n_className && vt(n_className)) {
      const a = n_className.split(/\s+/);
      for (const c of a) {
        r.push(`.${c}`);
      }
    }
  }
  for (const s of ["aria-label", "type", "name", "title", "alt"]) {
    const a = n.getAttribute(s);

    if (a) {
      r.push(`[${s}="${a}"]`);
    }
  }
  return r.join("");
}
function Yo() {
  try {
    return Go.document.location.href;
  } catch {
    return "";
  }
}
function lu(e, t = 5) {
  if (!Go.HTMLElement) {
    return null;
  }
  let n = e;
  for (let r = 0; r < t; r++) {
    if (!n) {
      return null;
    }
    if (n instanceof HTMLElement) {
      if (n.dataset.sentryComponent) {
        return n.dataset.sentryComponent;
      }
      if (n.dataset.sentryElement) {
        return n.dataset.sentryElement;
      }
    }
    n = n.parentNode;
  }
  return null;
}
function Ve(e, t, n) {
  if (!(t in e)) {
    return;
  }
  const e_t = e[t];
  if (typeof e_t != "function") {
    return;
  }
  const o = n(e_t);

  if (typeof o == "function") {
    za(o, e_t);
  }

  try {
    e[t] = o;
  } catch {
    if (z) {
      F.log(`Failed to replace method "${t}" in object`, e);
    }
  }
}
function Pt(e, t, n) {
  try {
    Object.defineProperty(e, t, { value: n, writable: true, configurable: true });
  } catch {
    if (z) {
      F.log(
        `Failed to add non-enumerable property "${String(t)}" to object`,
        e
      );
    }
  }
}
function za(e, t) {
  try {
    const n = t.prototype || {};
    e.prototype = n;
    t.prototype = n;
    Pt(e, "__sentry_original__", t);
  } catch {}
}
function Xo(e) {
  return e.__sentry_original__;
}
function qa(e) {
  if (Wn(e)) {
    return { message: e.message, name: e.name, stack: e.stack, ...As(e) };
  }
  if (Rr(e)) {
    const t = {
      type: e.type,
      target: Ps(e.target),
      currentTarget: Ps(e.currentTarget),
      ...As(e),
    };

    if (typeof CustomEvent !== "undefined" && Rt(e, CustomEvent)) {
      (t.detail = e.detail);
    }

    return t;
  } else {
    return e;
  }
}
function Ps(e) {
  try {
    return ou(e) ? ja(e) : Object.prototype.toString.call(e);
  } catch {
    return "<unknown>";
  }
}
function As(e) {
  return typeof e == "object" && e !== null
    ? Object.fromEntries(Object.entries(e))
    : {};
}
function uu(e) {
  const t = Object.keys(qa(e));
  t.sort();
  return t[0] ? t.join(", ") : "[object has no keys]";
}
let en;
function Pr(e) {
  if (en !== undefined) {
    return en ? en(e) : e();
  }
  const t = Symbol.for("__SENTRY_SAFE_RANDOM_ID_WRAPPER__");
  const n = te;
  return t in n && typeof n[t] == "function"
    ? ((en = n[t]), en(e))
    : ((en = null), e());
}
function wr() {
  return Pr(() => Math.random());
}
function Ar() {
  return Pr(() => Date.now());
}
function ho(e, t = 0) {
  return typeof e != "string" || t === 0 || e.length <= t
    ? e
    : `${e.slice(0, t)}...`;
}
function Os(e, t) {
  if (!Array.isArray(e)) {
    return "";
  }
  const n = [];

  for (const o of e) {
    try {
      if (Va(o)) {
        n.push(Fa(o));
      } else {
        n.push(String(o));
      }
    } catch {
      n.push("[value cannot be serialized]");
    }
  }

  return n.join(t);
}
function On(e, t, n = false) {
  return vt(e)
    ? su(t)
      ? t.test(e)
      : vt(t)
      ? n
        ? e === t
        : e.includes(t)
      : typeof t == "function"
      ? t(e)
      : false
    : false;
}
function Or(e, t = [], n = false) {
  return t.some(r => On(e, r, n));
}
function du() {
  const e = te;
  return e.crypto || e.msCrypto;
}
let Vr;
function fu() {
  return wr() * 16;
}
function Ge(e = du()) {
  try {
    if (e?.randomUUID) {
      return Pr(() => e.randomUUID()).replace(/-/g, "");
    }
  } catch {}

  if (!Vr) {
    (Vr = `10000000100040008000${100000000000/* 1e11 */}`);
  }

  return Vr.replace(/[018]/g, t => (t ^ ((fu() & 15) >> (t / 4))).toString(16));
}
function Ga(e) {
  return e.exception?.values?.[0];
}
function jt(e) {
  const { message, event_id } = e;
  if (message) {
    return message;
  }
  const r = Ga(e);
  return r
    ? r.type && r.value
      ? `${r.type}: ${r.value}`
      : r.type || r.value || event_id || "<unknown>"
    : event_id || "<unknown>";
}
function mo(e, t, n) {
  const r = (e.exception = e.exception || {});
  const o = (r.values = r.values || []);
  const s = (o[0] = o[0] || {});

  if (!s.value) {
    (s.value = t || "");
  }

  if (!s.type) {
    (s.type = "Error");
  }
}
function sn(e, t) {
  const n = Ga(e);
  if (!n) {
    return;
  }
  const r = { type: "generic", handled: true };
  const n_mechanism = n.mechanism;
  (n.mechanism = { ...r, ...n_mechanism, ...t });

  if (t && "data" in t) {
    const s = { ...n_mechanism?.data, ...t.data };
    n.mechanism.data = s;
  }
}
function xs(e) {
  if (pu(e)) {
    return true;
  }
  try {
    Pt(e, "__sentry_captured__", true);
  } catch {}
  return false;
}
function pu(e) {
  try {
    return e.__sentry_captured__;
  } catch {}
}
const Ya = 1000/* 1e3 */;
function zn() {
  return Ar() / Ya;
}
function hu() {
  const { performance } = te;
  if (!performance?.now || !performance.timeOrigin) {
    return zn;
  }
  const e_timeOrigin = performance.timeOrigin;
  return () => (e_timeOrigin + Pr(() => performance.now())) / Ya;
}
let Ls;
function wt() {
  return (Ls ?? (Ls = hu()))();
}
function mu(e) {
  const t = wt();

  const n = {
    sid: Ge(),
    init: true,
    timestamp: t,
    started: t,
    duration: 0,
    status: "ok",
    errors: 0,
    ignoreDuration: false,
    toJSON: () => _u(n),
  };

  if (e) {
    an(n, e);
  }

  return n;
}
function an(e, t = {}) {
  if (t.user) {
    !e.ipAddress && t.user.ip_address && (e.ipAddress = t.user.ip_address);

    !e.did &&
      !t.did &&
      (e.did = t.user.id || t.user.email || t.user.username);
  }

  (e.timestamp = t.timestamp || wt());

  if (t.abnormal_mechanism) {
    (e.abnormal_mechanism = t.abnormal_mechanism);
  }

  if (t.ignoreDuration) {
    (e.ignoreDuration = t.ignoreDuration);
  }

  if (t.sid) {
    (e.sid = t.sid.length === 32 ? t.sid : Ge());
  }

  if (t.init !== undefined) {
    (e.init = t.init);
  }

  if (!e.did && t.did) {
    (e.did = `${t.did}`);
  }

  if (typeof t.started == "number") {
    (e.started = t.started);
  }

  if (e.ignoreDuration) {
    e.duration = undefined;
  } else if (typeof t.duration == "number") {
    e.duration = t.duration;
  } else {
    const n = e.timestamp - e.started;
    e.duration = n >= 0 ? n : 0;
  }

  if (t.release) {
    (e.release = t.release);
  }

  if (t.environment) {
    (e.environment = t.environment);
  }

  if (!e.ipAddress && t.ipAddress) {
    (e.ipAddress = t.ipAddress);
  }

  if (!e.userAgent && t.userAgent) {
    (e.userAgent = t.userAgent);
  }

  if (typeof t.errors == "number") {
    (e.errors = t.errors);
  }

  if (t.status) {
    (e.status = t.status);
  }
}
function gu(e, t) {
  let n = {};

  if (e.status === "ok") {
    (n = { status: "exited" });
  }

  an(e, n);
}
function _u(e) {
  return {
    sid: `${e.sid}`,
    init: e.init,
    started: new Date(e.started * 1000/* 1e3 */).toISOString(),
    timestamp: new Date(e.timestamp * 1000/* 1e3 */).toISOString(),
    status: e.status,
    errors: e.errors,
    did:
      typeof e.did == "number" || typeof e.did == "string"
        ? `${e.did}`
        : undefined,
    duration: e.duration,
    abnormal_mechanism: e.abnormal_mechanism,
    attrs: {
      release: e.release,
      environment: e.environment,
      ip_address: e.ipAddress,
      user_agent: e.userAgent,
    },
  };
}
function qn(e, t, n = 2) {
  if (!t || typeof t != "object" || n <= 0) {
    return t;
  }
  if (e && Object.keys(t).length === 0) {
    return e;
  }
  const r = { ...e };
  for (const o in t) {
    if (Object.prototype.hasOwnProperty.call(t, o)) {
      (r[o] = qn(r[o], t[o], n - 1));
    }
  }
  return r;
}
function Ms() {
  return Ge();
}
function Xa() {
  return Ge().substring(16);
}
const go = "_sentrySpan";
function $s(e, t) {
  if (t) {
    Pt(e, go, t);
  } else {
    delete e[go];
  }
}
function Ds(e) {
  return e[go];
}
const yu = 100;
class rt {
  constructor() {
    (this._notifyingListeners = false);
    (this._scopeListeners = []);
    (this._eventProcessors = []);
    (this._breadcrumbs = []);
    (this._attachments = []);
    (this._user = {});
    (this._tags = {});
    (this._attributes = {});
    (this._extra = {});
    (this._contexts = {});
    (this._sdkProcessingMetadata = {});
    (this._propagationContext = { traceId: Ms(), sampleRand: wr() });
  }
  clone() {
    const t = new rt();
    (t._breadcrumbs = [...this._breadcrumbs]);
    (t._tags = { ...this._tags });
    (t._attributes = { ...this._attributes });
    (t._extra = { ...this._extra });
    (t._contexts = { ...this._contexts });

    if (this._contexts.flags) {
      (t._contexts.flags = { values: [...this._contexts.flags.values] });
    }

    (t._user = this._user);
    (t._level = this._level);
    (t._session = this._session);
    (t._transactionName = this._transactionName);
    (t._fingerprint = this._fingerprint);
    (t._eventProcessors = [...this._eventProcessors]);
    (t._attachments = [...this._attachments]);
    (t._sdkProcessingMetadata = { ...this._sdkProcessingMetadata });
    (t._propagationContext = { ...this._propagationContext });
    (t._client = this._client);
    (t._lastEventId = this._lastEventId);
    (t._conversationId = this._conversationId);
    $s(t, Ds(this));
    return t;
  }
  setClient(t) {
    this._client = t;
  }
  setLastEventId(t) {
    this._lastEventId = t;
  }
  getClient() {
    return this._client;
  }
  lastEventId() {
    return this._lastEventId;
  }
  addScopeListener(t) {
    this._scopeListeners.push(t);
  }
  addEventProcessor(t) {
    this._eventProcessors.push(t);
    return this;
  }
  setUser(t) {
    (this._user = t || {
      email: undefined,
      id: undefined,
      ip_address: undefined,
      username: undefined,
    });

    if (this._session) {
      an(this._session, { user: t });
    }

    this._notifyScopeListeners();
    return this;
  }
  getUser() {
    return this._user;
  }
  setConversationId(t) {
    (this._conversationId = t || undefined);
    this._notifyScopeListeners();
    return this;
  }
  setTags(t) {
    (this._tags = { ...this._tags, ...t });
    this._notifyScopeListeners();
    return this;
  }
  setTag(t, n) {
    return this.setTags({ [t]: n });
  }
  setAttributes(t) {
    (this._attributes = { ...this._attributes, ...t });
    this._notifyScopeListeners();
    return this;
  }
  setAttribute(t, n) {
    return this.setAttributes({ [t]: n });
  }
  removeAttribute(t) {
    if (t in this._attributes) {
      delete this._attributes[t];
      this._notifyScopeListeners();
    }

    return this;
  }
  setExtras(t) {
    (this._extra = { ...this._extra, ...t });
    this._notifyScopeListeners();
    return this;
  }
  setExtra(t, n) {
    (this._extra = { ...this._extra, [t]: n });
    this._notifyScopeListeners();
    return this;
  }
  setFingerprint(t) {
    (this._fingerprint = t);
    this._notifyScopeListeners();
    return this;
  }
  setLevel(t) {
    (this._level = t);
    this._notifyScopeListeners();
    return this;
  }
  setTransactionName(t) {
    (this._transactionName = t);
    this._notifyScopeListeners();
    return this;
  }
  setContext(t, n) {
    if (n === null) {
      delete this._contexts[t];
    } else {
      (this._contexts[t] = n);
    }

    this._notifyScopeListeners();
    return this;
  }
  setSession(t) {
    if (t) {
      (this._session = t);
    } else {
      delete this._session;
    }

    this._notifyScopeListeners();
    return this;
  }
  getSession() {
    return this._session;
  }
  update(t) {
    if (!t) {
      return this;
    }
    const n = typeof t == "function" ? t(this) : t;
    const r = n instanceof rt ? n.getScopeData() : Dn(n) ? t : undefined;

    const {
      tags,
      attributes,
      extra,
      user,
      contexts,
      level,
      fingerprint = [],
      propagationContext,
      conversationId,
    } = r || {};

    (this._tags = { ...this._tags, ...tags });
    (this._attributes = { ...this._attributes, ...attributes });
    (this._extra = { ...this._extra, ...extra });
    (this._contexts = { ...this._contexts, ...contexts });

    if (user && Object.keys(user).length) {
      (this._user = user);
    }

    if (level) {
      (this._level = level);
    }

    if (fingerprint.length) {
      (this._fingerprint = fingerprint);
    }

    if (propagationContext) {
      (this._propagationContext = propagationContext);
    }

    if (conversationId) {
      (this._conversationId = conversationId);
    }

    return this;
  }
  clear() {
    (this._breadcrumbs = []);
    (this._tags = {});
    (this._attributes = {});
    (this._extra = {});
    (this._user = {});
    (this._contexts = {});
    (this._level = undefined);
    (this._transactionName = undefined);
    (this._fingerprint = undefined);
    (this._session = undefined);
    (this._conversationId = undefined);
    $s(this, undefined);
    (this._attachments = []);
    this.setPropagationContext({ traceId: Ms(), sampleRand: wr() });
    this._notifyScopeListeners();
    return this;
  }
  addBreadcrumb(t, n) {
    const r = typeof n == "number" ? n : yu;
    if (r <= 0) {
      return this;
    }
    const o = {
      timestamp: zn(),
      ...t,
      message: t.message ? ho(t.message, 2048) : t.message,
    };
    this._breadcrumbs.push(o);

    if (this._breadcrumbs.length > r) {
      (this._breadcrumbs = this._breadcrumbs.slice(-r));
      this._client?.recordDroppedEvent("buffer_overflow", "log_item");
    }

    this._notifyScopeListeners();
    return this;
  }
  getLastBreadcrumb() {
    return this._breadcrumbs[this._breadcrumbs.length - 1];
  }
  clearBreadcrumbs() {
    (this._breadcrumbs = []);
    this._notifyScopeListeners();
    return this;
  }
  addAttachment(t) {
    this._attachments.push(t);
    return this;
  }
  clearAttachments() {
    (this._attachments = []);
    return this;
  }
  getScopeData() {
    return {
      breadcrumbs: this._breadcrumbs,
      attachments: this._attachments,
      contexts: this._contexts,
      tags: this._tags,
      attributes: this._attributes,
      extra: this._extra,
      user: this._user,
      level: this._level,
      fingerprint: this._fingerprint || [],
      eventProcessors: this._eventProcessors,
      propagationContext: this._propagationContext,
      sdkProcessingMetadata: this._sdkProcessingMetadata,
      transactionName: this._transactionName,
      span: Ds(this),
      conversationId: this._conversationId,
    };
  }
  setSDKProcessingMetadata(t) {
    (this._sdkProcessingMetadata = qn(this._sdkProcessingMetadata, t, 2));
    return this;
  }
  setPropagationContext(t) {
    (this._propagationContext = t);
    return this;
  }
  getPropagationContext() {
    return this._propagationContext;
  }
  captureException(t, n) {
    const r = n?.event_id || Ge();
    if (!this._client) {
      if (z) {
        F.warn("No client configured on scope - will not capture exception!");
      }

      return r;
    }
    const o = new Error("Sentry syntheticException");

    this._client.captureException(
      t,
      { originalException: t, syntheticException: o, ...n, event_id: r },
      this
    );

    return r;
  }
  captureMessage(t, n, r) {
    const o = r?.event_id || Ge();
    if (!this._client) {
      if (z) {
        F.warn("No client configured on scope - will not capture message!");
      }

      return o;
    }
    const s = r?.syntheticException ?? new Error(t);

    this._client.captureMessage(
      t,
      n,
      { originalException: t, syntheticException: s, ...r, event_id: o },
      this
    );

    return o;
  }
  captureEvent(t, n) {
    const r = t.event_id || n?.event_id || Ge();
    return this._client
      ? (this._client.captureEvent(t, { ...n, event_id: r }, this), r)
      : (z && F.warn("No client configured on scope - will not capture event!"),
        r);
  }
  _notifyScopeListeners() {
    if (!this._notifyingListeners) {
      (this._notifyingListeners = true);

      this._scopeListeners.forEach((t) => {
        t(this);
      });

      (this._notifyingListeners = false);
    }
  }
}
function vu() {
  return pn("defaultCurrentScope", () => new rt());
}
function wu() {
  return pn("defaultIsolationScope", () => new rt());
}

const Us = e => e instanceof Promise && !e[Ka];

const Ka = Symbol("chained PromiseLike");

const Eu = (e, t, n) => {
  const r = e.then(
    o => {
      t(o);
      return o;
    },
    (o) => {
      n(o);
      throw o;
    }
  );
  return Us(r) && Us(e) ? r : Su(e, r);
};

const Su = (e, t) => {
  if (!t) {
    return e;
  }
  let n = false;
  for (const r in e) {
    if (r in t) {
      continue;
    }
    n = true;
    const e_r = e[r];

    if (typeof e_r == "function") {
      Object.defineProperty(t, r, {
            value: (...s) => e_r.apply(e, s),
            enumerable: true,
            configurable: true,
            writable: true,
          });
    } else {
      (t[r] = e_r);
    }
  }

  if (n) {
    Object.assign(t, { [Ka]: true });
  }

  return t;
};

class bu {
  constructor(t, n) {
    let r;

    if (t) {
      (r = t);
    } else {
      (r = new rt());
    }

    let o;

    if (n) {
      (o = n);
    } else {
      (o = new rt());
    }

    (this._stack = [{ scope: r }]);
    (this._isolationScope = o);
  }
  withScope(t) {
    const n = this._pushScope();
    let r;
    try {
      r = t(n);
    } catch (o) {
      this._popScope();
      throw o;
    }
    return jn(r)
      ? Eu(
          r,
          () => this._popScope(),
          () => this._popScope()
        )
      : (this._popScope(), r);
  }
  getClient() {
    return this.getStackTop().client;
  }
  getScope() {
    return this.getStackTop().scope;
  }
  getIsolationScope() {
    return this._isolationScope;
  }
  getStackTop() {
    return this._stack[this._stack.length - 1];
  }
  _pushScope() {
    const t = this.getScope().clone();
    this._stack.push({ client: this.getClient(), scope: t });
    return t;
  }
  _popScope() {
    return this._stack.length <= 1 ? false : !!this._stack.pop();
  }
}
function cn() {
  const e = Ir();
  const t = kr(e);
  return (t.stack = t.stack || new bu(vu(), wu()));
}
function Cu(e) {
  return cn().withScope(e);
}
function Tu(e, t) {
  const n = cn();
  return n.withScope(() => {
    (n.getStackTop().scope = e);
    return t(e);
  });
}
function Fs(e) {
  return cn().withScope(() => e(cn().getIsolationScope()));
}
function Iu() {
  return {
    withIsolationScope: Fs,
    withScope: Cu,
    withSetScope: Tu,
    withSetIsolationScope: (e, t) => Fs(t),
    getCurrentScope: () => cn().getScope(),
    getIsolationScope: () => cn().getIsolationScope(),
  };
}
function Ko(e) {
  const t = kr(e);
  return t.acs ? t.acs : Iu();
}
function st() {
  const e = Ir();
  return Ko(e).getCurrentScope();
}
function it() {
  const e = Ir();
  return Ko(e).getIsolationScope();
}
function ku() {
  return pn("globalScope", () => new rt());
}
function Zo(...e) {
  const t = Ir();
  const n = Ko(t);
  if (e.length === 2) {
    const [r, o] = e;
    return r ? n.withSetScope(r, o) : n.withScope(o);
  }
  return n.withScope(e[0]);
}
function Ie() {
  return st().getClient();
}
function Nu(e) {
  const { traceId, parentSpanId, propagationSpanId } = e.getPropagationContext();
  const s = { trace_id: traceId, span_id: propagationSpanId || Xa() };

  if (parentSpanId) {
    (s.parent_span_id = parentSpanId);
  }

  return s;
}
const Ru = "sentry.source";
const Pu = "sentry.sample_rate";
const Au = "sentry.previous_trace_sample_rate";
const Ou = "sentry.op";
const xu = "sentry.origin";
const Za = "sentry.profile_id";
const Ja = "sentry.exclusive_time";
const Lu = "gen_ai.conversation.id";
const Mu = 0;
const $u = 1;
function Du(e) {
  if (e) {
    if (typeof e == "object" && "deref" in e && typeof e.deref == "function") {
      try {
        return e.deref();
      } catch {
        return;
      }
    }
    return e;
  }
}
const Uu = "_sentryScope";
const Fu = "_sentryIsolationScope";
function Qa(e) {
  const t = e;
  return { scope: t[Uu], isolationScope: Du(t[Fu]) };
}
const Hs = "sentry-";
function Hu(e) {
  const t = Bu(e);
  if (!t) {
    return;
  }
  const n = Object.entries(t).reduce((r, [o, s]) => {
    if (o.startsWith(Hs)) {
      const a = o.slice(Hs.length);
      r[a] = s;
    }
    return r;
  }, {});
  if (Object.keys(n).length > 0) {
    return n;
  }
}
function Bu(e) {
  if (!(!e || (!vt(e) && !Array.isArray(e)))) {
    return Array.isArray(e)
      ? e.reduce((t, n) => {
      const r = Bs(n);

      Object.entries(r).forEach(([o, s]) => {
        t[o] = s;
      });

      return t;
    }, {})
      : Bs(e);
  }
}
function Bs(e) {
  return e
    .split(",")
    .map((t) => {
    const n = t.indexOf("=");
    if (n === -1) {
      return [];
    }
    const r = t.slice(0, n);
    const o = t.slice(n + 1);
    return [r, o].map((s) => {
      try {
        return decodeURIComponent(s.trim());
      } catch {
        return;
      }
    });
  })
    .reduce((t, [n, r]) => {
    if (n && r) {
      (t[n] = r);
    }

    return t;
  }, {});
}
const Vu = /^o(\d+)\./;

const Wu =
  /^(?:(\w+):)\/\/(?:(\w+)(?::(\w+)?)?@)((?:\[[:.%\w]+\]|[\w.-]+))(?::(\d+))?\/(.+)/;

function ju(e) {
  return e === "http" || e === "https";
}
function gn(e, t = false) {
  const {
    host,
    path,
    pass,
    port,
    projectId,
    protocol,
    publicKey,
  } = e;
  return `${protocol}://${publicKey}${t && pass ? `:${pass}` : ""}@${host}${port ? `:${port}` : ""}/${
    path && `${path}/`
  }${projectId}`;
}
function zu(e) {
  const t = Wu.exec(e);
  if (!t) {
    hn(() => {
      console.error(`Invalid Sentry Dsn: ${e}`);
    });
    return;
  }
  const [n, r, o = "", s = "", a = "", c = ""] = t.slice(1);
  let l = "";
  let u = c;
  const f = u.split("/");

  if (f.length > 1) {
    (l = f.slice(0, -1).join("/"));
    (u = f.pop());
  }

  if (u) {
    const d = u.match(/^\d+/);

    if (d) {
      (u = d[0]);
    }
  }

  return ec({
    host: s,
    pass: o,
    path: l,
    projectId: u,
    port: a,
    protocol: n,
    publicKey: r,
  });
}
function ec(e) {
  return {
    protocol: e.protocol,
    publicKey: e.publicKey || "",
    pass: e.pass || "",
    host: e.host,
    port: e.port || "",
    path: e.path || "",
    projectId: e.projectId,
  };
}
function qu(e) {
  if (!z) {
    return true;
  }
  const { port, projectId, protocol } = e;
  return ["protocol", "publicKey", "host", "projectId"].find(a => e[a] ? false : (F.error(`Invalid Sentry Dsn: ${a} missing`), true)
  )
    ? false
    : projectId.match(/^\d+$/)
    ? ju(protocol)
      ? port && isNaN(parseInt(port, 10))
        ? (F.error(`Invalid Sentry Dsn: Invalid port ${port}`), false)
        : true
      : (F.error(`Invalid Sentry Dsn: Invalid protocol ${protocol}`), false)
    : (F.error(`Invalid Sentry Dsn: Invalid projectId ${projectId}`), false);
}
function Gu(e) {
  return e.match(Vu)?.[1];
}
function Yu(e) {
  const t = e.getOptions();
  const { host } = e.getDsn() || {};
  let r;

  if (t.orgId) {
    (r = String(t.orgId));
  } else if (host) {
    (r = Gu(host));
  }

  return r;
}
function tc(e) {
  const t = typeof e == "string" ? zu(e) : ec(e);
  if (!(!t || !qu(t))) {
    return t;
  }
}
function Xu(e) {
  if (typeof e == "boolean") {
    return Number(e);
  }
  const t = typeof e == "string" ? parseFloat(e) : e;
  if (!(typeof t != "number" || isNaN(t) || t < 0 || t > 1)) {
    return t;
  }
}
const nc = 1;
function Ku(e) {
  const { spanId, traceId, isRemote } = e.spanContext();
  const o = isRemote ? spanId : xr(e).parent_span_id;
  const s = Qa(e).scope;
  const a = isRemote ? s?.getPropagationContext().propagationSpanId || Xa() : spanId;
  return { parent_span_id: o, span_id: a, trace_id: traceId };
}
function Zu(e) {
  if (e && e.length > 0) {
    return e.map(
      (
        {
          context: { spanId: t, traceId: n, traceFlags: r, ...o },
          attributes: s,
        }
      ) => ({
        span_id: t,
        trace_id: n,
        sampled: r === nc,
        attributes: s,
        ...o
      })
    );
  }
}
function Ws(e) {
  if (typeof e == "number") {
    return js(e);
  }

  if (Array.isArray(e)) {
    return e[0] + e[1] / 1000000000/* 1e9 */;
  }

  if (e instanceof Date) {
    return js(e.getTime());
  }

  return wt();
}
function js(e) {
  return e > 9999999999 ? e / 1000/* 1e3 */ : e;
}
function xr(e) {
  if (ed(e)) {
    return e.getSpanJSON();
  }
  const { spanId, traceId } = e.spanContext();
  if (Qu(e)) {
    const {
      attributes,
      startTime,
      name,
      endTime,
      status,
      links,
    } = e;
    return {
      span_id: spanId,
      trace_id: traceId,
      data: attributes,
      description: name,
      parent_span_id: Ju(e),
      start_timestamp: Ws(startTime),
      timestamp: Ws(endTime) || undefined,
      status: nd(status),
      op: attributes[Ou],
      origin: attributes[xu],
      links: Zu(links),
    };
  }
  return { span_id: spanId, trace_id: traceId, start_timestamp: 0, data: {} };
}
function Ju(e) {
  return "parentSpanId" in e
    ? e.parentSpanId
    : "parentSpanContext" in e
    ? e.parentSpanContext?.spanId
    : undefined;
}
function Qu(e) {
  const t = e;
  return (
    !!t.attributes && !!t.startTime && !!t.name && !!t.endTime && !!t.status
  );
}
function ed(e) {
  return typeof e.getSpanJSON == "function";
}
function td(e) {
  const { traceFlags } = e.spanContext();
  return traceFlags === nc;
}
function nd(e) {
  if (!(!e || e.code === Mu)) {
    return e.code === $u ? "ok" : e.message || "internal_error";
  }
}
const rd = "_sentryRootSpan";
const rc = od;
function od(e) {
  return e[rd] || e;
}
function zs() {
  if (!Vs) {
    hn(() => {
        console.warn(
          "[Sentry] Returning null from `beforeSendSpan` is disallowed. To drop certain spans, configure the respective integrations directly or use `ignoreSpans`."
        );
      });

    (Vs = true);
  }
}
function sd(e) {
  if (typeof __SENTRY_TRACING__ == "boolean" && !__SENTRY_TRACING__) {
    return false;
  }
  const t = Ie()?.getOptions();
  return !!t && (t.tracesSampleRate != null || !!t.tracesSampler);
}
function qs(e) {
  F.log(
    `Ignoring span ${e.op} - ${e.description} because it matches \`ignoreSpans\`.`
  );
}
function Gs(e, t) {
  if (!t?.length) {
    return false;
  }
  for (const n of t) {
    if (cd(n)) {
      if (e.description && On(e.description, n)) {
        if (z) {
          qs(e);
        }

        return true;
      }
      continue;
    }
    const r = !!n.attributes && Object.keys(n.attributes).length > 0;
    if (!n.name && !n.op && !r) {
      continue;
    }
    const o = n.name ? e.description && On(e.description, n.name) : true;
    const s = n.op ? e.op && On(e.op, n.op) : true;

    const a = n.attributes
      ? Object.entries(n.attributes).every(([c, l]) => id(e.attributes?.[c], l)
        )
      : true;

    if (o && s && a) {
      if (z) {
        qs(e);
      }

      return true;
    }
  }
  return false;
}
function id(e, t) {
  return typeof e == "string" && (typeof t == "string" || t instanceof RegExp)
    ? On(e, t)
    : Array.isArray(e) && Array.isArray(t)
    ? e.length === t.length && e.every((n, r) => n === t[r])
    : e === t;
}
function ad(e, t) {
  const {
    parent_span_id,
    span_id
  } = t;

  if (parent_span_id) {
    for (const o of e) {
      if (o.parent_span_id === span_id) {
        (o.parent_span_id = parent_span_id);
      }
    }
  }
}
function cd(e) {
  return typeof e == "string" || e instanceof RegExp;
}
const Jo = "production";
const ld = "_frozenDsc";
function oc(e, t) {
  const n = t.getOptions();
  const { publicKey } = t.getDsn() || {};

  const o = {
    environment: n.environment || Jo,
    release: n.release,
    public_key: publicKey,
    trace_id: e,
    org_id: Yu(t),
  };

  t.emit("createDsc", o);
  return o;
}
function ud(e, t) {
  const n = t.getPropagationContext();
  return n.dsc || oc(n.traceId, e);
}
function dd(e) {
  const t = Ie();
  if (!t) {
    return {};
  }
  const n = rc(e);

  const {
    data,
    description
  } = xr(n);

  const s = n.spanContext().traceState;
  const a = s?.get("sentry.sample_rate") ?? data[Pu] ?? data[Au];
  function c(m) {
    if ((typeof a == "number" || typeof a == "string")) {
      (m.sample_rate = `${a}`);
    }

    return m;
  }
  const n_ld = n[ld];
  if (n_ld) {
    return c(n_ld);
  }
  const u = s?.get("sentry.dsc");
  const f = u && Hu(u);
  if (f) {
    return c(f);
  }
  const d = oc(e.spanContext().traceId, t);
  const p = data[Ru] ?? data["sentry.span.source"];

  if (p !== "url" && description) {
    (d.transaction = description);
  }

  if (sd()) {
    (d.sampled = String(td(n)));

    (d.sample_rand = s?.get("sentry.sample_rand") ??
    Qa(n).scope?.getPropagationContext().sampleRand.toString());
  }

  c(d);
  t.emit("createDsc", d, n);
  return d;
}
function fd(e) {
  return !!e && typeof e == "function" && "_streamed" in e && !!e._streamed;
}
const pd = Symbol.for("sentry.skipNormalization");
const hd = Symbol.for("sentry.overrideNormalizationDepth");
function md(e) {
  return !!e[pd];
}
function gd(e) {
  const e_hd = e[hd];
  return typeof e_hd == "number" ? e_hd : undefined;
}
function ht(e, t = 100, n = Infinity) {
  try {
    return _o("", e, t, n);
  } catch (r) {
    return { ERROR: `**non-serializable** (${r})` };
  }
}
function sc(e, t = 3, n = 100 * 1024) {
  const r = ht(e, t);
  return wd(r) > n ? sc(e, t - 1, n) : r;
}
function _o(e, t, n = Infinity, r = Infinity, o = Ed()) {
  const [s, a] = o;
  if (t == null ||
  ["boolean", "string"].includes(typeof t) ||
  (typeof t == "number" && Number.isFinite(t))) {
    return t;
  }
  const c = _d(e, t);
  if (!c.startsWith("[object ")) {
    return c;
  }
  if (md(t)) {
    return t;
  }
  const l = gd(t);
  const u = l !== undefined ? l : n;
  if (u === 0) {
    return c.replace("object ", "");
  }
  if (s(t)) {
    return "[Circular ~]";
  }
  const f = t;
  if (f && typeof f.toJSON == "function") {
    try {
      const m = f.toJSON();
      return _o("", m, u - 1, r, o);
    } catch {}
  }
  const d = Array.isArray(t) ? [] : {};
  let p = 0;
  const h = qa(t);
  for (const m in h) {
    if (!Object.prototype.hasOwnProperty.call(h, m)) {
      continue;
    }
    if (p >= r) {
      d[m] = "[MaxProperties ~]";
      break;
    }
    const h_m = h[m];
    (d[m] = _o(m, h_m, u - 1, r, o));
    p++;
  }
  a(t);
  return d;
}
function _d(e, t) {
  try {
    if (e === "domain" && t && typeof t == "object" && t._events) {
      return "[Domain]";
    }
    if (e === "domainEmitter") {
      return "[DomainEmitter]";
    }
    if (typeof global !== "undefined" && t === global) {
      return "[Global]";
    }
    if (typeof window !== "undefined" && t === window) {
      return "[Window]";
    }
    if (typeof document !== "undefined" && t === document) {
      return "[Document]";
    }
    if (Va(t)) {
      return Fa(t);
    }
    if (iu(t)) {
      return "[SyntheticEvent]";
    }
    if (typeof t == "number" && !Number.isFinite(t)) {
      return `[${t}]`;
    }
    if (typeof t == "function") {
      return `[Function: ${Nt(t)}]`;
    }
    if (typeof t == "symbol") {
      return `[${String(t)}]`;
    }
    if (typeof t == "bigint") {
      return `[BigInt: ${String(t)}]`;
    }
    const n = yd(t);
    return /^HTML(\w*)Element$/.test(n)
      ? `[HTMLElement: ${n}]`
      : `[object ${n}]`;
  } catch (n) {
    return `**non-serializable** (${n})`;
  }
}
function yd(e) {
  const t = Object.getPrototypeOf(e);
  return t?.constructor ? t.constructor.name : "null prototype";
}
function vd(e) {
  return ~-encodeURI(e).split(/%..|./).length;
}
function wd(e) {
  return vd(JSON.stringify(e));
}
function Ed() {
  const e = new WeakSet();
  function t(r) {
    return e.has(r) ? true : (e.add(r), false);
  }
  function n(r) {
    e.delete(r);
  }
  return [t, n];
}
function _n(e, t = []) {
  return [e, t];
}
function Sd(e, t) {
  const [n, r] = e;
  return [n, [...r, t]];
}
function yo(e, t) {
  const [, n] = e;
  for (const r of n) {
    const o = r[0].type;
    if (t(r, o)) {
      return true;
    }
  }
  return false;
}
function bd(e, t) {
  return yo(e, (n, r) => t.includes(r));
}
function vo(e) {
  const t = kr(te);
  return t.encodePolyfill ? t.encodePolyfill(e) : new TextEncoder().encode(e);
}
function Cd(e) {
  const [t, n] = e;
  let r = JSON.stringify(t);
  function o(s) {
    if (typeof r == "string") {
      (r = typeof s == "string" ? r + s : [vo(r), s]);
    } else {
      r.push(typeof s == "string" ? vo(s) : s);
    }
  }
  for (const s of n) {
    const [a, c] = s;

    o(`
${JSON.stringify(a)}
`);

    if (typeof c == "string" || c instanceof Uint8Array) {
      o(c);
    } else {
      let l;
      try {
        l = JSON.stringify(c);
      } catch {
        l = JSON.stringify(ht(c));
      }
      o(l);
    }
  }
  return typeof r == "string" ? r : Td(r);
}
function Td(e) {
  const t = e.reduce((o, s) => o + s.length, 0);

  const n = new Uint8Array(t);
  let r = 0;
  for (const o of e) {
    n.set(o, r);
    (r += o.length);
  }
  return n;
}
function Id(e) {
  const t = typeof e.data == "string" ? vo(e.data) : e.data;
  return [
    {
      type: "attachment",
      length: t.length,
      filename: e.filename,
      content_type: e.contentType,
      attachment_type: e.attachmentType,
    },
    t,
  ];
}
const ic = {
  sessions: "session",
  event: "error",
  client_report: "internal",
  user_report: "default",
  profile_chunk: "profile",
  replay_event: "replay",
  replay_recording: "replay",
  check_in: "monitor",
  raw_security: "security",
  log: "log_item",
  trace_metric: "metric",
};
function kd(e) {
  return e in ic;
}
function Ys(e) {
  return kd(e) ? ic[e] : e;
}
function ac(e) {
  if (!e?.sdk) {
    return;
  }
  const { name, version } = e.sdk;
  return { name: name, version: version };
}
function Nd(e, t, n, r) {
  const o = e.sdkProcessingMetadata?.dynamicSamplingContext;
  return {
    event_id: e.event_id,
    sent_at: new Date().toISOString(),
    ...(t && { sdk: t }),
    ...(!!n && r && { dsn: gn(r) }),
    ...(o && { trace: o }),
  };
}
function Rd(e, t) {
  if (!t) {
    return e;
  }
  const n = e.sdk || {};

  (e.sdk = {
    ...n,
    name: n.name || t.name,
    version: n.version || t.version,
    integrations: [...(e.sdk?.integrations || []), ...(t.integrations || [])],
    packages: [...(e.sdk?.packages || []), ...(t.packages || [])],
    settings:
      e.sdk?.settings || t.settings
        ? { ...e.sdk?.settings, ...t.settings }
        : undefined,
  });

  return e;
}
function Pd(e, t, n, r) {
  const o = ac(n);

  const s = {
    sent_at: new Date().toISOString(),
    ...(o && { sdk: o }),
    ...(!!r && t && { dsn: gn(t) }),
  };

  const a =
    "aggregates" in e
      ? [{ type: "sessions" }, e]
      : [{ type: "session" }, e.toJSON()];

  return _n(s, [a]);
}
function Ad(e, t, n, r) {
  const o = ac(n);
  const s = e.type && e.type !== "replay_event" ? e.type : "event";
  Rd(e, n?.sdk);
  const a = Nd(e, o, r, t);
  delete e.sdkProcessingMetadata;
  return _n(a, [[{ type: s }, e]]);
}
function Od(e, t) {
  const {
    fingerprint,
    span,
    breadcrumbs,
    sdkProcessingMetadata,
  } = t;
  xd(e, t);

  if (span) {
    $d(e, span);
  }

  Dd(e, fingerprint);
  Ld(e, breadcrumbs);
  Md(e, sdkProcessingMetadata);
}
function Xs(e, t) {
  const {
    extra,
    tags,
    attributes,
    user,
    contexts,
    level,
    sdkProcessingMetadata,
    breadcrumbs,
    fingerprint,
    eventProcessors,
    attachments,
    propagationContext,
    transactionName,
    span,
  } = t;
  En(e, "extra", extra);
  En(e, "tags", tags);
  En(e, "attributes", attributes);
  En(e, "user", user);
  En(e, "contexts", contexts);
  (e.sdkProcessingMetadata = qn(e.sdkProcessingMetadata, sdkProcessingMetadata, 2));

  if (level) {
    (e.level = level);
  }

  if (transactionName) {
    (e.transactionName = transactionName);
  }

  if (span) {
    (e.span = span);
  }

  if (breadcrumbs.length) {
    (e.breadcrumbs = [...e.breadcrumbs, ...breadcrumbs]);
  }

  if (fingerprint.length) {
    (e.fingerprint = [...e.fingerprint, ...fingerprint]);
  }

  if (eventProcessors.length) {
    (e.eventProcessors = [...e.eventProcessors, ...eventProcessors]);
  }

  if (attachments.length) {
    (e.attachments = [...e.attachments, ...attachments]);
  }

  (e.propagationContext = { ...e.propagationContext, ...propagationContext });
}
function En(e, t, n) {
  e[t] = qn(e[t], n, 1);
}
function cc(e, t) {
  const n = ku().getScopeData();

  if (e) {
    Xs(n, e.getScopeData());
  }

  if (t) {
    Xs(n, t.getScopeData());
  }

  return n;
}
function xd(e, t) {
  const {
    extra,
    tags,
    user,
    contexts,
    level,
    transactionName,
  } = t;

  if (Object.keys(extra).length) {
    (e.extra = { ...extra, ...e.extra });
  }

  if (Object.keys(tags).length) {
    (e.tags = { ...tags, ...e.tags });
  }

  if (Object.keys(user).length) {
    (e.user = { ...user, ...e.user });
  }

  if (Object.keys(contexts).length) {
    (e.contexts = { ...contexts, ...e.contexts });
  }

  if (level) {
    (e.level = level);
  }

  if (transactionName && e.type !== "transaction") {
    (e.transaction = transactionName);
  }
}
function Ld(e, t) {
  const n = [...(e.breadcrumbs || []), ...t];
  e.breadcrumbs = n.length ? n : undefined;
}
function Md(e, t) {
  e.sdkProcessingMetadata = { ...e.sdkProcessingMetadata, ...t };
}
function $d(e, t) {
  (e.contexts = { trace: Ku(t), ...e.contexts });

  (e.sdkProcessingMetadata = {
      dynamicSamplingContext: dd(t),
      ...e.sdkProcessingMetadata,
    });

  const n = rc(t);
  const r = xr(n).description;

  if (r && !e.transaction && e.type === "transaction") {
    (e.transaction = r);
  }
}
function Dd(e, t) {
  (e.fingerprint = e.fingerprint
    ? Array.isArray(e.fingerprint)
      ? e.fingerprint
      : [e.fingerprint]
    : []);

  if (t) {
    (e.fingerprint = e.fingerprint.concat(t));
  }

  if (!e.fingerprint.length) {
    delete e.fingerprint;
  }
}
function Wr(e) {
  if (!e) {
    return {};
  }
  const t = e.match(
    /^(([^:/?#]+):)?(\/\/([^/?#]*))?([^?#]*)(\?([^#]*))?(#(.*))?$/
  );
  if (!t) {
    return {};
  }
  const n = t[6] || "";
  const r = t[8] || "";
  return {
    host: t[4],
    path: t[5],
    protocol: t[2],
    search: n,
    hash: r,
    relative: t[5] + n + r,
  };
}
function Ud(e, t = true) {
  if (e.startsWith("data:")) {
    const n = e.match(/^data:([^;,]+)/);
    const r = n ? n[1] : "text/plain";
    const o = e.includes(";base64,");
    const s = e.indexOf(",");
    let a = "";
    if (t && s !== -1) {
      const c = e.slice(s + 1);
      a = c.length > 10 ? `${c.slice(0, 10)}... [truncated]` : c;
    }
    return `data:${r}${o ? ",base64" : ""}${a ? `,${a}` : ""}`;
  }
  return e;
}
function lc(e, t) {
  const n = e.attributes ?? (e.attributes = {});
  Object.entries(t).forEach(([r, o]) => {
    if (o != null && !(r in n)) {
      (n[r] = o);
    }
  });
}
const jr = 0;
const Ks = 1;
const Zs = 2;
function Gn(e) {
  return new Un((t) => {
    t(e);
  });
}
function Qo(e) {
  return new Un((t, n) => {
    n(e);
  });
}
class Un {
  constructor(t) {
    (this._state = jr);
    (this._handlers = []);
    this._runExecutor(t);
  }
  then(t, n) {
    return new Un((r, o) => {
      this._handlers.push([
        false,
        (s) => {
          if (!t) {
            r(s);
          } else {
            try {
              r(t(s));
            } catch (a) {
              o(a);
            }
          }
        },
        (s) => {
          if (!n) {
            o(s);
          } else {
            try {
              r(n(s));
            } catch (a) {
              o(a);
            }
          }
        },
      ]);

      this._executeHandlers();
    });
  }
  catch(t) {
    return this.then(n => n, t);
  }
  finally(t) {
    return new Un((n, r) => {
      let o;
      let s;
      return this.then(
        (a) => {
          (s = false);
          (o = a);

          if (t) {
            t();
          }
        },
        (a) => {
          (s = true);
          (o = a);

          if (t) {
            t();
          }
        }
      ).then(() => {
        if (s) {
          r(o);
          return;
        }
        n(o);
      });
    });
  }
  _executeHandlers() {
    if (this._state === jr) {
      return;
    }
    const t = this._handlers.slice();
    (this._handlers = []);

    t.forEach((n) => {
      if (!n[0]) {
        this._state === Ks && n[1](this._value);
        this._state === Zs && n[2](this._value);
        (n[0] = true);
      }
    });
  }
  _runExecutor(t) {
    const n = (s, a) => {
        if (this._state === jr) {
          if (jn(a)) {
            a.then(r, o);
            return;
          }
          (this._state = s);
          (this._value = a);
          this._executeHandlers();
        }
      };

    const r = (s) => {
      n(Ks, s);
    };

    const o = (s) => {
      n(Zs, s);
    };

    try {
      t(r, o);
    } catch (s) {
      o(s);
    }
  }
}
function Fd(e, t, n, r = 0) {
  try {
    const o = wo(t, n, e, r);
    return jn(o) ? o : Gn(o);
  } catch (o) {
    return Qo(o);
  }
}
function wo(e, t, n, r) {
  const n_r = n[r];
  if (!e || !n_r) {
    return e;
  }
  const s = n_r({ ...e }, t);

  if (z && s === null) {
    F.log(`Event processor "${n_r.id || "?"}" dropped event`);
  }

  return jn(s) ? s.then(a => wo(a, t, n, r + 1)) : wo(s, t, n, r + 1);
}
let xt;
let Js;
let Qs;
let St;
function Hd(e) {
  const {
    _sentryDebugIds,
    _debugIds
  } = te;

  if (!_sentryDebugIds && !_debugIds) {
    return {};
  }
  const r = _sentryDebugIds ? Object.keys(_sentryDebugIds) : [];
  const o = _debugIds ? Object.keys(_debugIds) : [];
  if (St && r.length === Js && o.length === Qs) {
    return St;
  }
  (Js = r.length);
  (Qs = o.length);
  (St = {});

  if (!xt) {
    (xt = {});
  }

  const s = (a, c) => {
    for (const l of a) {
      const c_l = c[l];
      const f = xt?.[l];
      if (f && St && c_l) {
        (St[f[0]] = c_l);

        if (xt) {
          (xt[l] = [f[0], c_l]);
        }
      } else if (c_l) {
        const d = e(l);
        for (let p = d.length - 1; p >= 0; p--) {
          const m = d[p]?.filename;
          if (m && St && xt) {
            (St[m] = c_l);
            (xt[l] = [m, c_l]);
            break;
          }
        }
      }
    }
  };

  if (_sentryDebugIds) {
    s(r, _sentryDebugIds);
  }

  if (_debugIds) {
    s(o, _debugIds);
  }

  return St;
}
function Bd(e, t, n, r, o, s) {
  const { normalizeDepth = 3, normalizeMaxBreadth = 1000/* 1e3 */ } = e;

  const l = {
    ...t,
    event_id: t.event_id || n.event_id || Ge(),
    timestamp: t.timestamp || zn(),
  };

  const u = n.integrations || e.integrations.map(_ => _.name);

  Vd(l, e);
  zd(l, u);

  if (o) {
    o.emit("applyFrameMetadata", t);
  }

  if (t.type === undefined) {
    Wd(l, e.stackParser);
  }

  const f = Gd(r, n.captureContext);

  if (n.mechanism) {
    sn(l, n.mechanism);
  }

  const d = o ? o.getEventProcessors() : [];
  const p = cc(s, f);
  const h = [...(n.attachments || []), ...p.attachments];

  if (h.length) {
    (n.attachments = h);
  }

  Od(l, p);
  const m = [...d, ...p.eventProcessors];
  return (n.data && n.data.__sentry__ === true ? Gn(l) : Fd(m, l, n)).then(
    _ => {
      if (_) {
        jd(_);
      }

      return typeof normalizeDepth == "number" && normalizeDepth > 0 ? qd(_, normalizeDepth, normalizeMaxBreadth) : _;
    }
  );
}
function Vd(e, t) {
  const { environment, release, dist, maxValueLength } = t;
  (e.environment = e.environment || environment || Jo);

  if (!e.release && release) {
    (e.release = release);
  }

  if (!e.dist && dist) {
    (e.dist = dist);
  }

  const e_request = e.request;

  if (e_request?.url && maxValueLength) {
    (e_request.url = ho(e_request.url, maxValueLength));
  }

  if (maxValueLength) {
    e.exception?.values?.forEach((c) => {
      if (c.value) {
        (c.value = ho(c.value, maxValueLength));
      }
    });
  }
}
function Wd(e, t) {
  const n = Hd(t);
  e.exception?.values?.forEach((r) => {
    r.stacktrace?.frames?.forEach((o) => {
      if (o.filename) {
        (o.debug_id = n[o.filename]);
      }
    });
  });
}
function jd(e) {
  const t = {};

  e.exception?.values?.forEach((r) => {
      r.stacktrace?.frames?.forEach((o) => {
        if (o.debug_id) {
          o.abs_path
              ? (t[o.abs_path] = o.debug_id)
              : o.filename && (t[o.filename] = o.debug_id);

          delete o.debug_id;
        }
      });
    });

  if (Object.keys(t).length === 0) {
    return;
  }

  (e.debug_meta = e.debug_meta || {});
  (e.debug_meta.images = e.debug_meta.images || []);
  const n = e.debug_meta.images;
  Object.entries(t).forEach(([r, o]) => {
    n.push({ type: "sourcemap", code_file: r, debug_id: o });
  });
}
function zd(e, t) {
  if (t.length > 0) {
    (e.sdk = e.sdk || {});
    (e.sdk.integrations = [...(e.sdk.integrations || []), ...t]);
  }
}
function qd(e, t, n) {
  if (!e) {
    return null;
  }
  const r = {
    ...e,
    ...(e.breadcrumbs && {
      breadcrumbs: e.breadcrumbs.map(o => ({
        ...o,
        ...(o.data && { data: ht(o.data, t, n) })
      })),
    }),
    ...(e.user && { user: ht(e.user, t, n) }),
    ...(e.contexts && { contexts: ht(e.contexts, t, n) }),
    ...(e.extra && { extra: ht(e.extra, t, n) }),
  };

  if (e.contexts?.trace &&
    r.contexts) {
    (r.contexts.trace = e.contexts.trace);

    e.contexts.trace.data &&
      (r.contexts.trace.data = ht(e.contexts.trace.data, t, n));
  }

  if (e.spans) {
    (r.spans = e.spans.map(o => ({
      ...o,
      ...(o.data && { data: ht(o.data, t, n) })
    })));
  }

  if (e.contexts?.flags &&
    r.contexts) {
    (r.contexts.flags = ht(e.contexts.flags, 3, n));
  }

  return r;
}
function Gd(e, t) {
  if (!t) {
    return e;
  }
  const n = e ? e.clone() : new rt();
  n.update(t);
  return n;
}
function Yd(e) {
  if (e) {
    return Xd(e) ? { captureContext: e } : Zd(e) ? { captureContext: e } : e;
  }
}
function Xd(e) {
  return e instanceof rt || typeof e == "function";
}
const Kd = [
  "user",
  "level",
  "extra",
  "contexts",
  "tags",
  "fingerprint",
  "propagationContext",
];
function Zd(e) {
  return Object.keys(e).some(t => Kd.includes(t));
}
function uc(e, t) {
  return st().captureException(e, Yd(t));
}
function dc(e, t) {
  return st().captureEvent(e, t);
}
function Jd(e, t) {
  it().setContext(e, t);
}
function ei(e) {
  it().setUser(e);
}
function Qd() {
  return it().lastEventId();
}
function ti(e) {
  const t = it();
  const { user } = cc(t, st());
  const { userAgent } = te.navigator || {};
  const o = mu({ user: user, ...(userAgent && { userAgent: userAgent }), ...e });
  const s = t.getSession();

  if (s?.status === "ok") {
    an(s, { status: "exited" });
  }

  fc();
  t.setSession(o);
  return o;
}
function fc() {
  const e = it();
  const n = st().getSession() || e.getSession();

  if (n) {
    gu(n);
  }

  pc();
  e.setSession();
}
function pc() {
  const e = it();
  const t = Ie();
  const n = e.getSession();

  if (n && t) {
    t.captureSession(n);
  }
}
function zr(e = false) {
  if (e) {
    fc();
    return;
  }
  pc();
}
const ef = "7";
function hc(e) {
  const t = e.protocol ? `${e.protocol}:` : "";
  const n = e.port ? `:${e.port}` : "";
  return `${t}//${e.host}${n}${e.path ? `/${e.path}` : ""}/api/`;
}
function tf(e) {
  return `${hc(e)}${e.projectId}/envelope/`;
}
function nf(e, t) {
  const n = { sentry_version: ef };

  if (e.publicKey) {
    (n.sentry_key = e.publicKey);
  }

  if (t) {
    (n.sentry_client = `${t.name}/${t.version}`);
  }

  return new URLSearchParams(n).toString();
}
function rf(e, t, n) {
  return t || `${tf(e)}?${nf(e, n)}`;
}
function of(e, t) {
  const n = tc(e);
  if (!n) {
    return "";
  }
  const r = `${hc(n)}embed/error-page/`;
  let o = `dsn=${gn(n)}`;
  for (const s in t) {
    if (s !== "dsn" && s !== "onClose") {
      if (s === "user") {
        const t_user = t.user;
        if (!t_user) {
          continue;
        }

        if (t_user.name) {
          (o += `&name=${encodeURIComponent(t_user.name)}`);
        }

        if (t_user.email) {
          (o += `&email=${encodeURIComponent(t_user.email)}`);
        }
      } else {
        o += `&${encodeURIComponent(s)}=${encodeURIComponent(t[s])}`;
      }
    }
  }
  return `${r}?${o}`;
}
const ni = [];
function sf(e) {
  const t = {};

  e.forEach((n) => {
    const { name } = n;
    const t_r = t[r];

    if (!t_r || t_r.isDefaultInstance || !n.isDefaultInstance) {
      (t[r] = n);
    }
  });

  return Object.values(t);
}
function af(e) {
  const t = e.defaultIntegrations || [];
  const e_integrations = e.integrations;
  t.forEach((o) => {
    o.isDefaultInstance = true;
  });
  let r;
  if (Array.isArray(e_integrations)) {
    r = [...t, ...e_integrations];
  } else if (typeof e_integrations == "function") {
    const o = e_integrations(t);
    r = Array.isArray(o) ? o : [o];
  } else {
    r = t;
  }
  return sf(r);
}
function cf(e, t) {
  const n = {};

  t.forEach((r) => {
    if (r?.beforeSetup) {
      r.beforeSetup(e);
    }
  });

  t.forEach((r) => {
    if (r) {
      mc(e, r, n);
    }
  });

  return n;
}
function ri(e, t) {
  for (const n of t) {
    if (n?.afterAllSetup) {
      n.afterAllSetup(e);
    }
  }
}
function mc(e, t, n) {
  if (n[t.name]) {
    if (z) {
      F.log(`Integration skipped because it was already installed: ${t.name}`);
    }

    return;
  }
  (n[t.name] = t);

  if (!ni.includes(t.name) &&
    typeof t.setupOnce == "function") {
    t.setupOnce();
    ni.push(t.name);
  }

  if (t.setup && typeof t.setup == "function") {
    t.setup(e);
  }

  if (typeof t.preprocessEvent == "function") {
    const r = t.preprocessEvent.bind(t);
    e.on("preprocessEvent", (o, s) => r(o, s, e));
  }

  if (typeof t.processEvent == "function") {
    const r = t.processEvent.bind(t);

    const o = Object.assign((s, a) => r(s, a, e), { id: t.name });

    e.addEventProcessor(o);
  }

  ["processSpan", "processSegmentSpan"].forEach((r) => {
    const t_r = t[r];

    if (typeof t_r == "function") {
      e.on(r, s => t_r.call(t, s, e));
    }
  });

  if (z) {
    F.log(`Integration installed: ${t.name}`);
  }
}
function lf() {
  return typeof __SENTRY_BROWSER_BUNDLE__ !== "undefined" && !!__SENTRY_BROWSER_BUNDLE__;
}
function uf() {
  return "npm";
}
function df() {
  return (!lf() && Object.prototype.toString.call(typeof process !== "undefined" ? process : 0) ===
    "[object process]");
}
function gc() {
  return typeof window !== "undefined" && (!df() || ff());
}
function ff() {
  return te.process?.type === "renderer";
}
function pf(e, t) {
  const n = t ? "auto" : "never";
  return [
    {
      type: "log",
      item_count: e.length,
      content_type: "application/vnd.sentry.items.log+json",
    },
    {
      version: 2,
      ...(gc() && { ingest_settings: { infer_ip: n, infer_user_agent: n } }),
      items: e,
    },
  ];
}
function hf(e, t, n, r, o) {
  const s = {};

  if (t?.sdk) {
    (s.sdk = { name: t.sdk.name, version: t.sdk.version });
  }

  if (n && r) {
    (s.dsn = gn(r));
  }

  return _n(s, [pf(e, o)]);
}
function Eo(e, t) {
  const n = t ?? mf(e) ?? [];
  if (n.length === 0) {
    return;
  }
  const r = e.getOptions();
  const o = hf(n, r._metadata, r.tunnel, e.getDsn(), r.sendDefaultPii);
  _c().set(e, []);
  e.emit("flushLogs");
  e.sendEnvelope(o);
}
function mf(e) {
  return _c().get(e);
}
function _c() {
  return pn("clientToLogBufferMap", () => new WeakMap());
}
function gf(e, t) {
  const n = t ? "auto" : "never";
  return [
    {
      type: "trace_metric",
      item_count: e.length,
      content_type: "application/vnd.sentry.items.trace-metric+json",
    },
    {
      version: 2,
      ...(gc() && { ingest_settings: { infer_ip: n, infer_user_agent: n } }),
      items: e,
    },
  ];
}
function _f(e, t, n, r, o) {
  const s = {};

  if (t?.sdk) {
    (s.sdk = { name: t.sdk.name, version: t.sdk.version });
  }

  if (n && r) {
    (s.dsn = gn(r));
  }

  return _n(s, [gf(e, o)]);
}
function yc(e, t) {
  const n = t ?? yf(e) ?? [];
  if (n.length === 0) {
    return;
  }
  const r = e.getOptions();
  const o = _f(n, r._metadata, r.tunnel, e.getDsn(), r.sendDefaultPii);
  vc().set(e, []);
  e.emit("flushMetrics");
  e.sendEnvelope(o);
}
function yf(e) {
  return vc().get(e);
}
function vc() {
  return pn("clientToMetricBufferMap", () => new WeakMap());
}
function wc(e) {
  if (typeof e == "object" && typeof e.unref == "function") {
    e.unref();
  }

  return e;
}
const es = Symbol.for("SentryBufferFullError");
function ts(e = 100) {
  const t = new Set();
  function n() {
    return t.size < e;
  }
  function r(a) {
    t.delete(a);
  }
  function o(a) {
    if (!n()) {
      return Qo(es);
    }
    const c = a();
    t.add(c);

    c.then(
      () => r(c),
      () => r(c)
    );

    return c;
  }
  function s(a) {
    if (!t.size) {
      return Gn(true);
    }
    const c = Promise.allSettled(Array.from(t)).then(() => true);
    if (!a) {
      return c;
    }
    const l = [c, new Promise(u => wc(setTimeout(() => u(false), a)))];
    return Promise.race(l);
  }
  return {
    get $() {
      return Array.from(t);
    },
    add: o,
    drain: s,
  };
}
const vf = 60 * 1000/* 1e3 */;
function wf(e, t = Ar()) {
  const n = parseInt(`${e}`, 10);
  if (!isNaN(n)) {
    return n * 1000/* 1e3 */;
  }
  const r = Date.parse(`${e}`);
  return isNaN(r) ? vf : r - t;
}
function Ef(e, t) {
  return e[t] || e.all || 0;
}
function Sf(e, t, n = Ar()) {
  return Ef(e, t) > n;
}
function bf(e, { statusCode: t, headers: n }, r = Ar()) {
  const o = { ...e };
  const s = n?.["x-sentry-rate-limits"];
  const a = n?.["retry-after"];
  if (s) {
    for (const c of s.trim().split(",")) {
      const [l, u, , , f] = c.split(":", 5);
      const d = parseInt(l, 10);
      const p = (isNaN(d) ? 60 : d) * 1000/* 1e3 */;
      if (!u) {
        o.all = r + p;
      } else {
        for (const h of u.split(";")) {
          if (h === "metric_bucket") {
            if ((!f || f.split(";").includes("custom"))) {
              (o[h] = r + p);
            }
          } else {
            (o[h] = r + p);
          }
        }
      }
    }
  } else {
    if (a) {
      (o.all = r + wf(a, r));
    } else if (t === 429) {
      (o.all = r + 60 * 1000/* 1e3 */);
    }
  }
  return o;
}
const Ec = 64;
function Cf(e, t, n = ts(e.bufferSize || Ec)) {
  let r = {};
  const o = a => n.drain(a);
  function s(a) {
    const c = [];

    yo(a, (d, p) => {
      const h = Ys(p);

      if (Sf(r, h)) {
        e.recordDroppedEvent("ratelimit_backoff", h);
      } else {
        c.push(d);
      }
    });

    if (c.length === 0) {
      return Promise.resolve({});
    }

    const l = _n(a[0], c);

    const u = (d) => {
      if (bd(l, ["client_report"])) {
        if (z) {
          F.warn(
            `Dropping client report. Will not send outcomes (reason: ${d}).`
          );
        }

        return;
      }
      yo(l, (p, h) => {
        e.recordDroppedEvent(d, Ys(h));
      });
    };

    const f = () => t({ body: Cd(l) }).then(
      d => d.statusCode === 413
        ? (z &&
            F.error(
              "Sentry responded with status code 413. Envelope was discarded due to exceeding size limits."
            ),
          u("send_error"),
          d)
        : (z &&
            d.statusCode !== undefined &&
            (d.statusCode < 200 || d.statusCode >= 300) &&
            F.warn(
              `Sentry responded with status code ${d.statusCode} to sent event.`
            ),
          (r = bf(r, d)),
          d),
      (d) => {
        u("network_error");

        if (z) {
          F.error("Encountered error running transport request:", d);
        }

        throw d;
      }
    );

    return n.add(f).then(
      d => d,
      (d) => {
        if (d === es) {
          if (z) {
            F.error("Skipped sending event because buffer is full.");
          }

          u("queue_overflow");
          return Promise.resolve({});
        }
        throw d;
      }
    );
  }
  return { send: s, flush: o };
}
function Tf(e, t, n) {
  const r = [
    { type: "client_report" },
    { timestamp: zn(), discarded_events: e },
  ];
  return _n(t ? { dsn: t } : {}, [r]);
}
function Sc(e) {
  const t = [];

  if (e.message) {
    t.push(e.message);
  }

  try {
    const n = e.exception.values[e.exception.values.length - 1];

    if (n?.value) {
      t.push(n.value);
      n.type && t.push(`${n.type}: ${n.value}`);
    }
  } catch {}
  return t;
}
function If(e) {
  const {
    trace_id,
    parent_span_id,
    span_id,
    status,
    origin,
    data,
    op,
  } = e.contexts?.trace ?? {};
  return {
    data: data ?? {},
    description: e.transaction,
    op: op,
    parent_span_id: parent_span_id,
    span_id: span_id ?? "",
    start_timestamp: e.start_timestamp ?? 0,
    status: status,
    timestamp: e.timestamp,
    trace_id: trace_id ?? "",
    origin: origin,
    profile_id: data?.[Za],
    exclusive_time: data?.[Ja],
    measurements: e.measurements,
    is_segment: true,
  };
}
function kf(e) {
  return {
    type: "transaction",
    timestamp: e.timestamp,
    start_timestamp: e.start_timestamp,
    transaction: e.description,
    contexts: {
      trace: {
        trace_id: e.trace_id,
        span_id: e.span_id,
        parent_span_id: e.parent_span_id,
        op: e.op,
        status: e.status,
        origin: e.origin,
        data: {
          ...e.data,
          ...(e.profile_id && { [Za]: e.profile_id }),
          ...(e.exclusive_time && { [Ja]: e.exclusive_time }),
        },
      },
    },
    measurements: e.measurements,
  };
}
const oi = "Not capturing exception because it's already been captured.";
const si = "Discarded session because of missing or non-string release";
const bc = Symbol.for("SentryInternalError");
const Cc = Symbol.for("SentryDoNotSendEventError");
const Nf = 5000/* 5e3 */;
function pr(e) {
  return { message: e, [bc]: true };
}
function qr(e) {
  return { message: e, [Cc]: true };
}
function ii(e) {
  return !!e && typeof e == "object" && bc in e;
}
function ai(e) {
  return !!e && typeof e == "object" && Cc in e;
}
function ci(e, t, n, r, o) {
  let s = 0;
  let a;
  let c = false;

  e.on(n, () => {
    (s = 0);
    clearTimeout(a);
    (c = false);
  });

  e.on(t, (l) => {
    (s += r(l));

    if (s >= 800000/* 8e5 */) {
      o(e);
    } else if (!c) {
      (c = true);

      (a = wc(
            setTimeout(() => {
              o(e);
            }, Nf)
          ));
    }
  });

  e.on("flush", () => {
    o(e);
  });
}
class Rf {
  constructor(t) {
    (this._options = t);
    (this._integrations = {});
    (this._numProcessing = 0);
    (this._outcomes = {});
    (this._hooks = {});
    (this._eventProcessors = []);
    (this._promiseBuffer = ts(t.transportOptions?.bufferSize ?? Ec));

    if (t.dsn) {
      (this._dsn = tc(t.dsn));
    } else if (z) {
      F.warn("No DSN provided, client will not send events.");
    }

    if (this._dsn) {
      const r = rf(this._dsn, t.tunnel, t._metadata ? t._metadata.sdk : undefined);
      this._transport = t.transport({
        tunnel: this._options.tunnel,
        recordDroppedEvent: this.recordDroppedEvent.bind(this),
        ...t.transportOptions,
        url: r,
      });
    }

    (this._options.enableLogs = this._options.enableLogs ?? this._options._experiments?.enableLogs);

    if (this._options.enableLogs) {
      ci(this, "afterCaptureLog", "flushLogs", xf, Eo);
    }

    if ((this._options.enableMetrics ??
      this._options._experiments?.enableMetrics ?? true)) {
      ci(this, "afterCaptureMetric", "flushMetrics", Of, yc);
    }
  }
  captureException(t, n, r) {
    const o = Ge();
    if (xs(t)) {
      if (z) {
        F.log(oi);
      }

      return o;
    }
    const s = { event_id: o, ...n };

    this._process(
      () => this.eventFromException(t, s)
        .then(a => this._captureEvent(a, s, r))
        .then(a => a),
      "error"
    );

    return s.event_id;
  }
  captureMessage(t, n, r, o) {
    const s = { event_id: Ge(), ...r };
    const a = qo(t) ? t : String(t);
    const c = Nr(t);
    const l = c ? this.eventFromMessage(a, n, s) : this.eventFromException(t, s);

    this._process(
      () => l.then(u => this._captureEvent(u, s, o)),
      c ? "unknown" : "error"
    );

    return s.event_id;
  }
  captureEvent(t, n, r) {
    const o = Ge();
    if (n?.originalException && xs(n.originalException)) {
      if (z) {
        F.log(oi);
      }

      return o;
    }
    const s = { event_id: o, ...n };

    const {
      capturedSpanScope,
      capturedSpanIsolationScope
    } = t.sdkProcessingMetadata || {};

    const u = li(t.type);

    this._process(() => this._captureEvent(t, s, capturedSpanScope || r, capturedSpanIsolationScope), u);

    return s.event_id;
  }
  captureSession(t) {
    this.sendSession(t);
    an(t, { init: false });
  }
  getDsn() {
    return this._dsn;
  }
  getOptions() {
    return this._options;
  }
  getSdkMetadata() {
    return this._options._metadata;
  }
  getTransport() {
    return this._transport;
  }
  async flush(t) {
    const n = this._transport;
    this.emit("flush");

    if (!n) {
      return true;
    }

    const r = await this._isClientDoneProcessing(t);
    const o = await n.flush(t);
    return r && o;
  }
  async close(t) {
    Eo(this);
    const n = await this.flush(t);
    (this.getOptions().enabled = false);
    this.emit("close");
    return n;
  }
  getEventProcessors() {
    return this._eventProcessors;
  }
  addEventProcessor(t) {
    this._eventProcessors.push(t);
  }
  init() {
    if ((this._isEnabled() || this._options.integrations.some(({ name: t }) => t.startsWith("Spotlight")
    ))) {
      this._setupIntegrations();
    }
  }
  getIntegrationByName(t) {
    return this._integrations[t];
  }
  addIntegration(t) {
    const n = this._integrations[t.name];

    if (!n && t.beforeSetup) {
      t.beforeSetup(this);
    }

    mc(this, t, this._integrations);

    if (!n) {
      ri(this, [t]);
    }
  }
  sendEvent(t, n = {}) {
    this.emit("beforeSendEvent", t, n);
    let r = Ad(t, this._dsn, this._options._metadata, this._options.tunnel);
    for (const o of n.attachments || []) {
      r = Sd(r, Id(o));
    }
    this.sendEnvelope(r).then(o => this.emit("afterSendEvent", t, o));
  }
  sendSession(t) {
    const { release, environment = Jo } = this._options;
    if ("aggregates" in t) {
      const s = t.attrs || {};
      if (!s.release && !release) {
        if (z) {
          F.warn(si);
        }

        return;
      }
      (s.release = s.release || release);
      (s.environment = s.environment || environment);
      (t.attrs = s);
    } else {
      if (!t.release && !release) {
        if (z) {
          F.warn(si);
        }

        return;
      }
      (t.release = t.release || release);
      (t.environment = t.environment || environment);
    }
    this.emit("beforeSendSession", t);
    const o = Pd(t, this._dsn, this._options._metadata, this._options.tunnel);
    this.sendEnvelope(o);
  }
  recordDroppedEvent(t, n, r = 1) {
    if (this._options.sendClientReports) {
      const o = `${t}:${n}`;

      if (z) {
        F.log(`Recording outcome: "${o}"${r > 1 ? ` (${r} times)` : ""}`);
      }

      (this._outcomes[o] = (this._outcomes[o] || 0) + r);
    }
  }
  on(t, n) {
    const r = (this._hooks[t] = this._hooks[t] || new Set());

    const o = (...s) => n(...s);

    r.add(o);

    return () => {
      r.delete(o);
    };
  }
  emit(t, ...n) {
    const r = this._hooks[t];

    if (r) {
      r.forEach(o => o(...n));
    }
  }
  async sendEnvelope(t) {
    this.emit("beforeEnvelope", t);

    if (this._isEnabled() && this._transport) {
      try {
        return await this._transport.send(t);
      } catch (n) {
        if (z) {
          F.error("Error while sending envelope:", n);
        }

        return {};
      }
    }

    if (z) {
      F.error("Transport disabled");
    }

    return {};
  }
  registerCleanup(t) {}
  dispose() {}
  _setupIntegrations() {
    const { integrations } = this._options;
    (this._integrations = cf(this, integrations));
    ri(this, integrations);
  }
  _updateSessionFromEvent(t, n) {
    let r = n.level === "fatal";
    let o = false;
    const s = n.exception?.values;
    if (s) {
      (o = true);
      (r = false);
      for (const l of s) {
        if (l.mechanism?.handled === false) {
          r = true;
          break;
        }
      }
    }
    const a = t.status === "ok";

    if (((a && t.errors === 0) || (a && r))) {
      an(t, {
          ...(r && { status: "crashed" }),
          errors: t.errors || Number(o || r),
        });

      this.captureSession(t);
    }
  }
  async _isClientDoneProcessing(t) {
    let n = 0;

    while (!t || n < t) {
      await new Promise(r => setTimeout(r, 1));

      if (!this._numProcessing) {
        return true;
      }

      n++;
    }

    return false;
  }
  _isEnabled() {
    return this.getOptions().enabled !== false && this._transport !== undefined;
  }
  _prepareEvent(t, n, r, o) {
    const s = this.getOptions();
    const a = Object.keys(this._integrations);

    if (!n.integrations && a?.length) {
      (n.integrations = a);
    }

    this.emit("preprocessEvent", t, n);

    if (!t.type) {
      o.setLastEventId(t.event_id || n.event_id);
    }

    return Bd(s, t, n, r, this, o).then((c) => {
      if (c === null) {
        return c;
      }
      this.emit("postprocessEvent", c, n);

      (c.contexts = {
          trace: { ...c.contexts?.trace, ...Nu(r) },
          ...c.contexts,
        });

      const l = ud(this, r);

      (c.sdkProcessingMetadata = {
        dynamicSamplingContext: l,
        ...c.sdkProcessingMetadata,
      });

      return c;
    });
  }
  _captureEvent(t, n = {}, r = st(), o = it()) {
    if (z &&
      So(t)) {
      F.log(`Captured error event \`${Sc(t)[0] || "<unknown>"}\``);
    }

    return this._processEvent(t, n, r, o).then(
      s => s.event_id,
      (s) => {
        if (z) {
          if (ai(s)) {
            F.log(s.message);
          } else if (ii(s)) {
            F.warn(s.message);
          } else {
            F.warn(s);
          }
        }
      }
    );
  }
  _processEvent(t, n, r, o) {
    const s = this.getOptions();
    const { sampleRate } = s;
    const c = Tc(t);
    const l = So(t);
    const f = `before send for type \`${t.type || "error"}\``;
    const d = typeof sampleRate === "undefined" ? undefined : Xu(sampleRate);
    if (l && typeof d == "number" && wr() > d) {
      this.recordDroppedEvent("sample_rate", "error");

      return Qo(
        qr(
          `Discarding event because it's not included in the random sample (sampling rate = ${sampleRate})`
        )
      );
    }
    const p = li(t.type);
    return this._prepareEvent(t, n, r, o)
      .then((h) => {
        if (h === null) {
          this.recordDroppedEvent("event_processor", p);
          throw qr("An event processor returned `null`, will not send event.");
        }
        if (n.data?.__sentry__ === true) {
          return h;
        }
        const g = Af(this, s, h, n);
        return Pf(g, f);
      })
      .then((h) => {
      if (h === null) {
        this.recordDroppedEvent("before_send", p);

        if (c) {
          const _ = 1 + (t.spans || []).length;
          this.recordDroppedEvent("before_send", "span", _);
        }

        throw qr(`${f} returned \`null\`, will not send event.`);
      }
      const m = r.getSession() || o.getSession();

      if (l && m) {
        this._updateSessionFromEvent(m, h);
      }

      if (c) {
        const T = h.sdkProcessingMetadata?.spanCountBeforeProcessing || 0;
        const _ = h.spans ? h.spans.length : 0;
        const y = T - _;

        if (y > 0) {
          this.recordDroppedEvent("before_send", "span", y);
        }
      }

      const h_transaction_info = h.transaction_info;
      if (c && h_transaction_info && h.transaction !== t.transaction) {
        const T = "custom";
        h.transaction_info = { ...h_transaction_info, source: T };
      }
      this.sendEvent(h, n);
      return h;
    })
      .then(null, (h) => {
        throw ai(h) || ii(h)
          ? h
          : (this.captureException(h, {
              mechanism: { handled: false, type: "internal" },
              data: { __sentry__: true },
              originalException: h,
            }),
            pr(`Event processing pipeline threw an error, original event will not be sent. Details have been sent as a new event.
Reason: ${h}`));
      });
  }
  _process(t, n) {
    this._numProcessing++;

    this._promiseBuffer.add(t).then(
      r => {
        this._numProcessing--;
        return r;
      },
      r => {
        this._numProcessing--;

        if (r === es) {
          this.recordDroppedEvent("queue_overflow", n);
        }

        return r;
      }
    );
  }
  _clearOutcomes() {
    const t = this._outcomes;
    (this._outcomes = {});

    return Object.entries(t).map(([n, r]) => {
      const [o, s] = n.split(":");
      return { reason: o, category: s, quantity: r };
    });
  }
  _flushOutcomes() {
    if (z) {
      F.log("Flushing outcomes...");
    }

    const t = this._clearOutcomes();
    if (t.length === 0) {
      if (z) {
        F.log("No outcomes to send");
      }

      return;
    }
    if (!this._dsn) {
      if (z) {
        F.log("No dsn provided, will not send outcomes");
      }

      return;
    }

    if (z) {
      F.log("Sending outcomes:", t);
    }

    const n = Tf(t, this._options.tunnel && gn(this._dsn));
    this.sendEnvelope(n);
  }
}
function li(e) {
  return e === "replay_event" ? "replay" : e || "error";
}
function Pf(e, t) {
  const n = `${t} must return \`null\` or a valid event.`;
  if (jn(e)) {
    return e.then(
      (r) => {
        if (!Dn(r) && r !== null) {
          throw pr(n);
        }
        return r;
      },
      (r) => {
        throw pr(`${t} rejected with ${r}`);
      }
    );
  }
  if (!Dn(e) && e !== null) {
    throw pr(n);
  }
  return e;
}
function Af(e, t, n, r) {
  const { beforeSend, beforeSendTransaction, ignoreSpans } = t;
  const c = !fd(t.beforeSendSpan) && t.beforeSendSpan;
  let l = n;
  if (So(l) && beforeSend) {
    return beforeSend(l, r);
  }
  if (Tc(l)) {
    if (c || ignoreSpans) {
      const u = If(l);
      if (ignoreSpans?.length &&
      Gs({ description: u.description, op: u.op, attributes: u.data }, ignoreSpans)) {
        return null;
      }
      if (c) {
        const f = c(u);

        if (f) {
          (l = qn(n, kf(f)));
        } else {
          zs();
        }
      }
      if (l.spans) {
        const f = [];
        const l_spans = l.spans;
        for (const h of l_spans) {
          if (
            ignoreSpans?.length &&
            Gs({ description: h.description, op: h.op, attributes: h.data }, ignoreSpans)
          ) {
            ad(l_spans, h);
            continue;
          }
          if (c) {
            const m = c(h);

            if (m) {
              f.push(m);
            } else {
              zs();
              f.push(h);
            }
          } else {
            f.push(h);
          }
        }
        const p = l.spans.length - f.length;

        if (p) {
          e.recordDroppedEvent("before_send", "span", p);
        }

        (l.spans = f);
      }
    }
    if (beforeSendTransaction) {
      if (l.spans) {
        const u = l.spans.length;
        l.sdkProcessingMetadata = {
          ...n.sdkProcessingMetadata,
          spanCountBeforeProcessing: u,
        };
      }
      return beforeSendTransaction(l, r);
    }
  }
  return l;
}
function So(e) {
  return e.type === undefined;
}
function Tc(e) {
  return e.type === "transaction";
}
function Of(e) {
  let t = 0;

  if (e.name) {
    (t += e.name.length * 2);
  }

  (t += 8);
  return t + Ic(e.attributes);
}
function xf(e) {
  let t = 0;

  if (e.message) {
    (t += e.message.length * 2);
  }

  return t + Ic(e.attributes);
}
function Ic(e) {
  if (!e) {
    return 0;
  }
  let t = 0;

  Object.values(e).forEach((n) => {
    if (Array.isArray(n)) {
      (t += n.length * ui(n[0]));
    } else if (Nr(n)) {
      (t += ui(n));
    } else {
      (t += 100);
    }
  });

  return t;
}
function ui(e) {
  return typeof e == "string"
    ? e.length * 2
    : typeof e == "number"
    ? 8
    : typeof e == "boolean"
    ? 4
    : 0;
}
function Lf(e) {
  return (
    Wn(e) &&
    "__sentry_fetch_url_host__" in e &&
    typeof e.__sentry_fetch_url_host__ == "string"
  );
}
function di(e) {
  return Lf(e) ? `${e.message} (${e.__sentry_fetch_url_host__})` : e.message;
}
function Mf(e, t) {
  if (t.debug === true) {
    if (z) {
      F.enable();
    } else {
      hn(() => {
              console.warn(
                "[Sentry] Cannot initialize SDK with `debug` option using a non-debug bundle."
              );
            });
    }
  }

  st().update(t.initialScope);
  const r = new e(t);
  $f(r);
  r.init();
  return r;
}
function $f(e) {
  st().setClient(e);
}
function Df(e) {
  if ("aggregates" in e) {
    if (e.attrs?.ip_address === undefined) {
      (e.attrs = { ...e.attrs, ip_address: "{{auto}}" });
    }
  } else if (e.ipAddress === undefined) {
    (e.ipAddress = "{{auto}}");
  }
}
function kc(e, t, n = [t], r = "npm") {
  const o = ((e._metadata = e._metadata || {}).sdk = e._metadata.sdk || {});

  if (!o.name) {
    (o.name = `sentry.javascript.${t}`);

    (o.packages = n.map(s => ({
      name: `${r}:@sentry/${s}`,
      version: zt
    })));

    (o.version = zt);
  }
}
const Uf = 100;
function Yt(e, t) {
  const n = Ie();
  const r = it();
  if (!n) {
    return;
  }
  const { beforeBreadcrumb = null, maxBreadcrumbs = Uf } = n.getOptions();
  if (maxBreadcrumbs <= 0) {
    return;
  }
  const c = { timestamp: zn(), ...e };

  const l = beforeBreadcrumb ? hn(() => beforeBreadcrumb(c, t)) : c;

  if (l !== null) {
    n.emit && n.emit("beforeAddBreadcrumb", l, t);
    r.addBreadcrumb(l, maxBreadcrumbs);
  }
}
let fi;
const Ff = "FunctionToString";
const pi = new WeakMap();

const Bf = () => ({
  name: Ff,

  setupOnce() {
    fi = Function.prototype.toString;
    try {
      Function.prototype.toString = function (...e) {
        const t = Xo(this);
        const n = pi.has(Ie()) && t !== undefined ? t : this;
        return fi.apply(n, e);
      };
    } catch {}
  },

  setup(e) {
    pi.set(e, true);
  }
});

const Vf = [
  /^Script error\.?$/,
  /^Javascript error: Script error\.? on line 0$/,
  /^ResizeObserver loop completed with undelivered notifications.$/,
  /^Cannot redefine property: googletag$/,
  /^Can't find variable: gmo$/,
  /^undefined is not an object \(evaluating 'a\.[A-Z]'\)$/,
  /can't redefine non-configurable property "solana"/,
  /vv\(\)\.getRestrictions is not a function/,
  /Can't find variable: _AutofillCallbackHandler/,
  /Object Not Found Matching Id:\d+, MethodName:simulateEvent/,
  /^Java exception was raised during method invocation$/,
];

const Wf = "EventFilters";

const jf = (e = {}) => {
  let t;
  return {
    name: Wf,
    setup(n) {
      const r = n.getOptions();
      t = hi(e, r);
    },
    processEvent(n, r, o) {
      if (!t) {
        const s = o.getOptions();
        t = hi(e, s);
      }
      return qf(n, t) ? null : n;
    },
  };
};

const zf = (e = {}) => ({
  ...jf(e),
  name: "InboundFilters"
});

function hi(e = {}, t = {}) {
  return {
    allowUrls: [...(e.allowUrls || []), ...(t.allowUrls || [])],
    denyUrls: [...(e.denyUrls || []), ...(t.denyUrls || [])],
    ignoreErrors: [
      ...(e.ignoreErrors || []),
      ...(t.ignoreErrors || []),
      ...(e.disableErrorDefaults ? [] : Vf),
    ],
    ignoreTransactions: [
      ...(e.ignoreTransactions || []),
      ...(t.ignoreTransactions || []),
    ],
  };
}
function qf(e, t) {
  if (e.type) {
    if (e.type === "transaction" && Yf(e, t.ignoreTransactions)) {
      if (z) {
        F.warn(`Event dropped due to being matched by \`ignoreTransactions\` option.
Event: ${jt(e)}`);
      }

      return true;
    }
  } else {
    if (Gf(e, t.ignoreErrors)) {
      if (z) {
        F.warn(`Event dropped due to being matched by \`ignoreErrors\` option.
Event: ${jt(e)}`);
      }

      return true;
    }
    if (Jf(e)) {
      if (z) {
        F.warn(`Event dropped due to not having an error message, error type or stacktrace.
Event: ${jt(e)}`);
      }

      return true;
    }
    if (Xf(e, t.denyUrls)) {
      if (z) {
        F.warn(`Event dropped due to being matched by \`denyUrls\` option.
Event: ${jt(e)}.
Url: ${Er(e)}`);
      }

      return true;
    }
    if (!Kf(e, t.allowUrls)) {
      if (z) {
        F.warn(`Event dropped due to not being matched by \`allowUrls\` option.
Event: ${jt(e)}.
Url: ${Er(e)}`);
      }

      return true;
    }
  }
  return false;
}
function Gf(e, t) {
  return t?.length ? Sc(e).some(n => Or(n, t)) : false;
}
function Yf(e, t) {
  if (!t?.length) {
    return false;
  }
  const e_transaction = e.transaction;
  return e_transaction ? Or(e_transaction, t) : false;
}
function Xf(e, t) {
  if (!t?.length) {
    return false;
  }
  const n = Er(e);
  return n ? Or(n, t) : false;
}
function Kf(e, t) {
  if (!t?.length) {
    return true;
  }
  const n = Er(e);
  return n ? Or(n, t) : true;
}
function Zf(e = []) {
  for (let t = e.length - 1; t >= 0; t--) {
    const e_t = e[t];
    if (e_t && e_t.filename !== "<anonymous>" && e_t.filename !== "[native code]") {
      return e_t.filename || null;
    }
  }
  return null;
}
function Er(e) {
  try {
    const n = [...(e.exception?.values ?? [])]
      .reverse()
      .find(
        r => r.mechanism?.parent_id === undefined && r.stacktrace?.frames?.length
      )?.stacktrace?.frames;
    return n ? Zf(n) : null;
  } catch {
    if (z) {
      F.error(`Cannot extract url for event ${jt(e)}`);
    }

    return null;
  }
}
function Jf(e) {
  return e.exception?.values?.length
    ? !e.message &&
        !e.exception.values.some(
          t => t.stacktrace || (t.type && t.type !== "Error") || t.value
        )
    : false;
}
function Qf(e, t, n, r, o, s) {
  if (!o.exception?.values || !s || !Rt(s.originalException, Error)) {
    return;
  }
  const a =
    o.exception.values.length > 0
      ? o.exception.values[o.exception.values.length - 1]
      : undefined;

  if (a) {
    (o.exception.values = bo(
        e,
        t,
        r,
        s.originalException,
        n,
        o.exception.values,
        a,
        0
      ));
  }
}
function bo(e, t, n, r, o, s, a, c) {
  if (s.length >= n + 1) {
    return s;
  }
  let l = [...s];
  if (Rt(r[o], Error)) {
    mi(a, c, r);
    const u = e(t, r[o]);
    const l_length = l.length;
    gi(u, o, l_length, c);
    (l = bo(e, t, n, r[o], o, [u, ...l], u, l_length));
  }

  if (Nc(r)) {
    r.errors.forEach((u, f) => {
      if (Rt(u, Error)) {
        mi(a, c, r);
        const d = e(t, u);
        const l_length = l.length;
        gi(d, `errors[${f}]`, l_length, c);
        (l = bo(e, t, n, u, o, [d, ...l], d, l_length));
      }
    });
  }

  return l;
}
function Nc(e) {
  return Array.isArray(e.errors);
}
function mi(e, t, n) {
  e.mechanism = {
    handled: true,
    type: "auto.core.linked_errors",
    ...(Nc(n) && { is_exception_group: true }),
    ...e.mechanism,
    exception_id: t,
  };
}
function gi(e, t, n, r) {
  e.mechanism = {
    handled: true,
    ...e.mechanism,
    type: "chained",
    source: t,
    exception_id: n,
    parent_id: r,
  };
}
function ep(e) {
  const t = "console";
  const n = Kt(t, e);
  Zt(t, tp);
  return n;
}
function tp() {
  if ("console" in te) {
    jl.forEach(e => {
      if (e in te.console) {
        Ve(te.console, e, t => {
          (vr[e] = t);

          return (...n) => {
            Ze("console", { args: n, level: e });
            vr[e]?.apply(te.console, n);
          };
        });
      }
    });
  }
}
function np(e) {
  return e === "warn"
    ? "warning"
    : ["fatal", "error", "warning", "log", "info", "debug"].includes(e)
    ? e
    : "log";
}
const rp = "Dedupe";

const sp = () => {
  let e;
  return {
    name: rp,
    processEvent(t) {
      if (t.type) {
        return t;
      }
      try {
        if (ip(t, e)) {
          if (z) {
            F.warn(
              "Event dropped due to being a duplicate of previously captured event."
            );
          }

          return null;
        }
      } catch {}
      return (e = t);
    },
  };
};

function ip(e, t) {
  return t ? !!(ap(e, t) || cp(e, t)) : false;
}
function ap(e, t) {
  const e_message = e.message;
  const t_message = t.message;
  return !(
    (!e_message && !t_message) ||
    (e_message && !t_message) ||
    (!e_message && t_message) ||
    e_message !== t_message ||
    !Pc(e, t) ||
    !Rc(e, t)
  );
}
function cp(e, t) {
  const n = _i(t);
  const r = _i(e);
  return !(
    !n ||
    !r ||
    n.type !== r.type ||
    n.value !== r.value ||
    !Pc(e, t) ||
    !Rc(e, t)
  );
}
function Rc(e, t) {
  let n = ks(e);
  let r = ks(t);
  if (!n && !r) {
    return true;
  }
  if ((n && !r) || (!n && r) || ((n = n), (r = r), r.length !== n.length)) {
    return false;
  }
  for (let o = 0; o < r.length; o++) {
    const r_o = r[o];
    const n_o = n[o];
    if (r_o.filename !== n_o.filename ||
    r_o.lineno !== n_o.lineno ||
    r_o.colno !== n_o.colno ||
    r_o.function !== n_o.function) {
      return false;
    }
  }
  return true;
}
function Pc(e, t) {
  let e_fingerprint = e.fingerprint;
  let t_fingerprint = t.fingerprint;
  if (!e_fingerprint && !t_fingerprint) {
    return true;
  }
  if ((e_fingerprint && !t_fingerprint) || (!e_fingerprint && t_fingerprint)) {
    return false;
  }
  (e_fingerprint = e_fingerprint);
  (t_fingerprint = t_fingerprint);
  try {
    return e_fingerprint.join("") === t_fingerprint.join("");
  } catch {
    return false;
  }
}
function _i(e) {
  return e.exception?.values?.[0];
}
const lp = "ConversationId";

const dp = () => ({
  name: lp,

  setup(e) {
    e.on("spanStart", (t) => {
      const n = st().getScopeData();
      const r = it().getScopeData();
      const o = n.conversationId || r.conversationId;
      if (o) {
        const { op, data, description } = xr(t);
        if (!op?.startsWith("gen_ai.") &&
        !data["ai.operationId"] &&
        !description?.startsWith("ai.")) {
          return;
        }
        t.setAttribute(Lu, o);
      }
    });
  }
});

function Ac(e) {
  if (e !== undefined) {
    return e >= 400 && e < 500 ? "warning" : e >= 500 ? "error" : undefined;
  }
}
const Fn = te;
function fp() {
  return "history" in Fn && !!Fn.history;
}
function pp() {
  if (!("fetch" in Fn)) {
    return false;
  }
  try {
    new Headers();
    new Request("data:,");
    new Response();
    return true;
  } catch {
    return false;
  }
}
function Co(e) {
  return (e && /^function\s+\w+\(\)\s+\{\s+\[native code\]\s+\}$/.test(e.toString()));
}
function hp() {
  if (typeof EdgeRuntime == "string") {
    return true;
  }
  if (!pp()) {
    return false;
  }
  if (Co(Fn.fetch)) {
    return true;
  }
  let e = false;
  const Fn_document = Fn.document;
  if (Fn_document && typeof Fn_document.createElement == "function") {
    try {
      const n = Fn_document.createElement("iframe");
      (n.hidden = true);
      Fn_document.head.appendChild(n);

      if (n.contentWindow?.fetch) {
        (e = Co(n.contentWindow.fetch));
      }

      Fn_document.head.removeChild(n);
    } catch (n) {
      if (z) {
        F.warn(
          "Could not create sandbox iframe for pure fetch check, bailing to window.fetch: ",
          n
        );
      }
    }
  }
  return e;
}
function mp(e, t) {
  const n = "fetch";
  const r = Kt(n, e);

  Zt(n, () => gp(undefined, t));

  return r;
}
function gp(e, t = false) {
  if (!t || hp()) {
    Ve(te, "fetch", n => (...r) => {
      const o = new Error();
      const { method, url } = _p(r);

      const c = {
        args: r,
        fetchData: { method: method, url: url },
        startTimestamp: wt() * 1000/* 1e3 */,
        virtualError: o,
        headers: yp(r),
      };

      Ze("fetch", { ...c });

      return n.apply(te, r).then(
        async l => {
          Ze("fetch", { ...c, endTimestamp: wt() * 1000/* 1e3 */, response: l });
          return l;
        },
        (l) => {
          Ze("fetch", { ...c, endTimestamp: wt() * 1000/* 1e3 */, error: l });

          if (Wn(l) &&
            l.stack === undefined) {
            (l.stack = o.stack);
            Pt(l, "framesToPop", 1);
          }

          const f =
            Ie()?.getOptions().enhanceFetchErrorMessages ?? "always";
          if (f !== false &&
          l instanceof TypeError &&
          (l.message === "Failed to fetch" ||
            l.message === "Load failed" ||
            l.message ===
              "NetworkError when attempting to fetch resource.")) {
            try {
              const h = new URL(c.fetchData.url).host;

              if (f === "always") {
                (l.message = `${l.message} (${h})`);
              } else {
                Pt(l, "__sentry_fetch_url_host__", h);
              }
            } catch {}
          }
          throw l;
        }
      );
    });
  }
}
function hr(e, t) {
  return !!e && typeof e == "object" && !!e[t];
}
function yi(e) {
  return typeof e == "string"
    ? e
    : e
    ? hr(e, "url")
      ? e.url
      : e.toString
      ? e.toString()
      : ""
    : "";
}
function _p(e) {
  if (e.length === 0) {
    return { method: "GET", url: "" };
  }
  if (e.length === 2) {
    const [n, r] = e;
    return {
      url: yi(n),
      method: hr(r, "method")
        ? String(r.method).toUpperCase()
        : Wa(n) && hr(n, "method")
        ? String(n.method).toUpperCase()
        : "GET",
    };
  }
  const [t] = e;
  return {
    url: yi(t),
    method: hr(t, "method") ? String(t.method).toUpperCase() : "GET",
  };
}
function yp(e) {
  const [t, n] = e;
  try {
    if (typeof n == "object" && n !== null && "headers" in n && n.headers) {
      return new Headers(n.headers);
    }
    if (Wa(t)) {
      return new Headers(t.headers);
    }
  } catch {}
}
const ie = te;
let To = 0;
function Oc() {
  return To > 0;
}
function vp() {
  To++;

  setTimeout(() => {
    To--;
  });
}
function ln(e, t = {}) {
  function n(o) {
    return typeof o == "function";
  }
  if (!n(e)) {
    return e;
  }
  try {
    const e_sentry_wrapped = e.__sentry_wrapped__;
    if (e_sentry_wrapped) {
      return typeof e_sentry_wrapped == "function" ? e_sentry_wrapped : e;
    }
    if (Xo(e)) {
      return e;
    }
  } catch {
    return e;
  }
  const r = function (...o) {
    try {
      const s = o.map(a => ln(a, t));
      return e.apply(this, s);
    } catch (s) {
      vp();

      Zo((a) => {
        a.addEventProcessor(
          c => {
            if (t.mechanism) {
              mo(c, undefined);
              sn(c, t.mechanism);
            }

            (c.extra = { ...c.extra, arguments: o });
            return c;
          }
        );

        uc(s);
      });

      throw s;
    }
  };
  try {
    for (const o in e) {
      if (Object.prototype.hasOwnProperty.call(e, o)) {
        (r[o] = e[o]);
      }
    }
  } catch {}
  za(r, e);
  Pt(e, "__sentry_wrapped__", r);
  try {
    if (Object.getOwnPropertyDescriptor(r, "name").configurable) {
      Object.defineProperty(r, "name", {
        get() {
          return e.name;
        },
      });
    }
  } catch {}
  return r;
}
function vi() {
  const e = Yo();
  const { referrer } = ie.document || {};
  const { userAgent } = ie.navigator || {};
  const r = { ...(referrer && { Referer: referrer }), ...(userAgent && { "User-Agent": userAgent }) };
  return { url: e, headers: r };
}
function ns(e, t) {
  const n = rs(e, t);
  const r = { type: Cp(t), value: Tp(t) };

  if (n.length) {
    (r.stacktrace = { frames: n });
  }

  if (r.type === undefined &&
    r.value === "") {
    (r.value = "Unrecoverable error caught");
  }

  return r;
}
function wp(e, t, n, r) {
  const s = Ie()?.getOptions().normalizeDepth;
  const a = Pp(t);
  const c = { __serialized__: sc(t, s) };
  if (a) {
    return { exception: { values: [ns(e, a)] }, extra: c };
  }
  const l = {
    exception: {
      values: [
        {
          type: Rr(t) ? t.constructor.name : r ? "UnhandledRejection" : "Error",
          value: Np(t, { isUnhandledRejection: r }),
        },
      ],
    },
    extra: c,
  };
  if (n) {
    const u = rs(e, n);

    if (u.length) {
      (l.exception.values[0].stacktrace = { frames: u });
    }
  }
  return l;
}
function Gr(e, t) {
  return { exception: { values: [ns(e, t)] } };
}
function rs(e, t) {
  const n = t.stacktrace || t.stack || "";
  const r = Sp(t);
  const o = bp(t);
  try {
    return e(n, r, o);
  } catch {}
  return [];
}
const Ep = /Minified React error #\d+;/i;
function Sp(e) {
  return e && Ep.test(e.message) ? 1 : 0;
}
function bp(e) {
  return typeof e.framesToPop == "number" ? e.framesToPop : 0;
}
function xc(e) {
  return typeof WebAssembly !== "undefined" && typeof WebAssembly.Exception !== "undefined"
    ? e instanceof WebAssembly.Exception
    : false;
}
function Cp(e) {
  const t = e?.name;
  return !t && xc(e)
    ? e.message && Array.isArray(e.message) && e.message.length == 2
      ? e.message[0]
      : "WebAssembly.Exception"
    : t;
}
function Tp(e) {
  const t = e?.message;
  return xc(e)
    ? Array.isArray(e.message) && e.message.length == 2
      ? e.message[1]
      : "wasm exception"
    : t
    ? t.error && typeof t.error.message == "string"
      ? di(t.error)
      : di(e)
    : "No error message";
}
function Ip(e, t, n, r) {
  const o = n?.syntheticException || undefined;
  const s = os(e, t, o, r);
  sn(s);
  (s.level = "error");

  if (n?.event_id) {
    (s.event_id = n.event_id);
  }

  return Gn(s);
}
function kp(e, t, n = "info", r, o) {
  const s = r?.syntheticException || undefined;
  const a = Io(e, t, s, o);
  (a.level = n);

  if (r?.event_id) {
    (a.event_id = r.event_id);
  }

  return Gn(a);
}
function os(e, t, n, r, o) {
  let s;
  if (Ba(t) && t.error) {
    return Gr(e, t.error);
  }
  if (Rs(t) || ru(t)) {
    const a = t;
    if ("stack" in t) {
      s = Gr(e, t);
    } else {
      const c = a.name || (Rs(a) ? "DOMError" : "DOMException");
      const l = a.message ? `${c}: ${a.message}` : c;
      (s = Io(e, l, n, r));
      mo(s, l);
    }

    if ("code" in a) {
      (s.tags = { ...s.tags, "DOMException.code": `${a.code}` });
    }

    return s;
  }

  if (Wn(t)) {
    return Gr(e, t);
  }

  if (Dn(t) || Rr(t)) {
    (s = wp(e, t, n, o));
    sn(s, { synthetic: true });
    return s;
  }

  (s = Io(e, t, n, r));
  mo(s, `${t}`);
  sn(s, { synthetic: true });
  return s;
}
function Io(e, t, n, r) {
  const o = {};
  if (r && n) {
    const s = rs(e, n);

    if (s.length) {
      (o.exception = { values: [{ value: t, stacktrace: { frames: s } }] });
    }

    sn(o, { synthetic: true });
  }
  if (qo(t)) {
    const { __sentry_template_string__, __sentry_template_values__ } = t;
    (o.logentry = { message: __sentry_template_string__, params: __sentry_template_values__ });
    return o;
  }
  (o.message = t);
  return o;
}
function Np(e, { isUnhandledRejection: t }) {
  const n = uu(e);
  const r = t ? "promise rejection" : "exception";
  return Ba(e)
    ? `Event \`ErrorEvent\` captured as ${r} with message \`${e.message}\``
    : Rr(e)
    ? `Event \`${Rp(e)}\` (type=${e.type}) captured as ${r}`
    : `Object captured as ${r} with keys: ${n}`;
}
function Rp(e) {
  try {
    const t = Object.getPrototypeOf(e);
    return t ? t.constructor.name : undefined;
  } catch {}
}
function Pp(e) {
  return Object.values(e).find(t => t instanceof Error);
}
class Ap extends Rf {
  constructor(t) {
    const n = Op(t);
    const r = ie.SENTRY_SDK_SOURCE || uf();
    kc(n, "browser", ["browser"], r);

    if (n._metadata?.sdk) {
      (n._metadata.sdk.settings = {
          infer_ip: n.sendDefaultPii ? "auto" : "never",
          ...n._metadata.sdk.settings,
        });
    }

    super(n);

    const {
        sendDefaultPii,
        sendClientReports,
        enableLogs,
        _experiments,
        enableMetrics,
      } = this._options;

    const u = enableMetrics ?? _experiments?.enableMetrics ?? true;

    if (ie.document &&
      (sendClientReports || enableLogs || u)) {
      ie.document.addEventListener("visibilitychange", () => {
        if (ie.document.visibilityState === "hidden") {
          sendClientReports && this._flushOutcomes();
          enableLogs && Eo(this);
          u && yc(this);
        }
      });
    }

    if (sendDefaultPii) {
      this.on("beforeSendSession", Df);
    }
  }
  eventFromException(t, n) {
    return Ip(this._options.stackParser, t, n, this._options.attachStacktrace);
  }
  eventFromMessage(t, n = "info", r) {
    return kp(
      this._options.stackParser,
      t,
      n,
      r,
      this._options.attachStacktrace
    );
  }
  _prepareEvent(t, n, r, o) {
    (t.platform = t.platform || "javascript");
    return super._prepareEvent(t, n, r, o);
  }
}
function Op(e) {
  return {
    release:
      typeof __SENTRY_RELEASE__ == "string"
        ? __SENTRY_RELEASE__
        : ie.SENTRY_RELEASE?.id,
    sendClientReports: true,
    parentSpanIsAlwaysRootSpan: true,
    ...e,
  };
}
const xp = typeof __SENTRY_DEBUG__ === "undefined" || __SENTRY_DEBUG__;
const Me = te;
const Lp = 1000/* 1e3 */;
let wi;
let ko;
let No;
function Mp(e) {
  Kt("dom", e);
  Zt("dom", $p);
}
function $p() {
  if (!Me.document) {
    return;
  }
  const e = Ze.bind(null, "dom");
  const t = Ei(e, true);
  Me.document.addEventListener("click", t, false);
  Me.document.addEventListener("keypress", t, false);

  ["EventTarget", "Node"].forEach((n) => {
    const o = Me[n]?.prototype;

    if (o?.hasOwnProperty?.("addEventListener")) {
      Ve(o, "addEventListener", s => (function(a, c, l) {
        if (a === "click" || a == "keypress") {
          try {
            const u = (this.__sentry_instrumentation_handlers__ =
                this.__sentry_instrumentation_handlers__ || {});

            const f = (u[a] = u[a] || { refCount: 0 });
            if (!f.handler) {
              const d = Ei(e);
              (f.handler = d);
              s.call(this, a, d, l);
            }
            f.refCount++;
          } catch {}
        }
        return s.call(this, a, c, l);
      }));

      Ve(o, "removeEventListener", s => (function(a, c, l) {
        if (a === "click" || a == "keypress") {
          try {
            const u = this.__sentry_instrumentation_handlers__ || {};
            const u_a = u[a];

            if (u_a) {
              u_a.refCount--;

              u_a.refCount <= 0 &&
                (s.call(this, a, u_a.handler, l),
                (u_a.handler = undefined),
                delete u[a]);

              Object.keys(u).length === 0 &&
                delete this.__sentry_instrumentation_handlers__;
            }
          } catch {}
        }
        return s.call(this, a, c, l);
      }));
    }
  });
}
function Dp(e) {
  if (e.type !== ko) {
    return false;
  }
  try {
    if (!e.target || e.target._sentryId !== No) {
      return false;
    }
  } catch {}
  return true;
}
function Up(e, t) {
  return e !== "keypress"
    ? false
    : t?.tagName
    ? !(
        t.tagName === "INPUT" ||
        t.tagName === "TEXTAREA" ||
        t.isContentEditable
      )
    : true;
}
function Ei(e, t = false) {
  return (n) => {
    if (!n || n._sentryCaptured) {
      return;
    }
    const r = Fp(n);
    if (Up(n.type, r)) {
      return;
    }
    Pt(n, "_sentryCaptured", true);

    if (r && !r._sentryId) {
      Pt(r, "_sentryId", Ge());
    }

    const o = n.type === "keypress" ? "input" : n.type;

    if (!Dp(n)) {
      e({ event: n, name: o, global: t });
      (ko = n.type);
      (No = r ? r._sentryId : undefined);
    }

    clearTimeout(wi);

    (wi = Me.setTimeout(() => {
      (No = undefined);
      (ko = undefined);
    }, Lp));
  };
}
function Fp(e) {
  try {
    return e.target;
  } catch {
    return null;
  }
}
let er;
function Lc(e) {
  const t = "history";
  Kt(t, e);
  Zt(t, Hp);
}
function Hp() {
  Me.addEventListener("popstate", () => {
    const t = Me.location.href;
    const n = er;
    (er = t);

    if (n === t) {
      return;
    }

    Ze("history", { from: n, to: t });
  });

  if (!fp()) {
    return;
  }

  function e(t) {
    return function (...n) {
      const r = n.length > 2 ? n[2] : undefined;
      if (r) {
        const o = er;
        const s = Bp(String(r));
        (er = s);

        if (o === s) {
          return t.apply(this, n);
        }

        Ze("history", { from: o, to: s });
      }
      return t.apply(this, n);
    };
  }
  Ve(Me.history, "pushState", e);
  Ve(Me.history, "replaceState", e);
}
function Bp(e) {
  try {
    return new URL(e, Me.location.origin).toString();
  } catch {
    return e;
  }
}
const mr = {};
function Vp(e) {
  const mr_e = mr[e];
  if (mr_e) {
    return mr_e;
  }
  let n = Me[e];
  if (Co(n)) {
    return (mr[e] = n.bind(Me));
  }
  const {
    document
  } = Me;
  if (document && typeof document.createElement == "function") {
    try {
      const o = document.createElement("iframe");
      (o.hidden = true);
      document.head.appendChild(o);
      const o_contentWindow = o.contentWindow;

      if (o_contentWindow?.[e]) {
        (n = o_contentWindow[e]);
      }

      document.head.removeChild(o);
    } catch (o) {
      if (xp) {
        F.warn(
          `Could not create sandbox iframe for ${e} check, bailing to window.${e}: `,
          o
        );
      }
    }
  }
  return n && (mr[e] = n.bind(Me));
}
function Wp(e) {
  mr[e] = undefined;
}
const kn = "__sentry_xhr_v3__";
function jp(e) {
  Kt("xhr", e);
  Zt("xhr", zp);
}
function zp() {
  if (!Me.XMLHttpRequest) {
    return;
  }
  const XMLHttpRequest_prototype = XMLHttpRequest.prototype;

  (XMLHttpRequest_prototype.open = new Proxy(XMLHttpRequest_prototype.open, {
    apply(t, n, r) {
      const o = new Error();
      const s = wt() * 1000/* 1e3 */;
      const a = vt(r[0]) ? r[0].toUpperCase() : undefined;
      const c = qp(r[1]);
      if (!a || !c) {
        return t.apply(n, r);
      }
      (n[kn] = { method: a, url: c, request_headers: {} });

      if (a === "POST" &&
        c.match(/sentry_key/)) {
        (n.__sentry_own_request__ = true);
      }

      const l = () => {
        const n_kn = n[kn];
        if (n_kn && n.readyState === 4) {
          try {
            n_kn.status_code = n.status;
          } catch {}
          const f = {
            endTimestamp: wt() * 1000/* 1e3 */,
            startTimestamp: s,
            xhr: n,
            virtualError: o,
          };
          Ze("xhr", f);
        }
      };

      if ("onreadystatechange" in n && typeof n.onreadystatechange == "function") {
        (n.onreadystatechange = new Proxy(n.onreadystatechange, {
              apply(u, f, d) {
                l();
                return u.apply(f, d);
              },
            }));
      } else {
        n.addEventListener("readystatechange", l);
      }

      (n.setRequestHeader = new Proxy(n.setRequestHeader, {
        apply(u, f, d) {
          const [p, h] = d;
          const f_kn = f[kn];

          if (f_kn && vt(p) && vt(h)) {
            (f_kn.request_headers[p.toLowerCase()] = h);
          }

          return u.apply(f, d);
        },
      }));

      return t.apply(n, r);
    },
  }));

  (XMLHttpRequest_prototype.send = new Proxy(XMLHttpRequest_prototype.send, {
      apply(t, n, r) {
        const n_kn = n[kn];
        if (!n_kn) {
          return t.apply(n, r);
        }

        if (r[0] !== undefined) {
          (n_kn.body = r[0]);
        }

        const s = { startTimestamp: wt() * 1000/* 1e3 */, xhr: n };
        Ze("xhr", s);
        return t.apply(n, r);
      },
    }));
}
function qp(e) {
  if (vt(e)) {
    return e;
  }
  try {
    return e.toString();
  } catch {}
}
const Gp = 40;
function Yp(e, t = Vp("fetch")) {
  let n = 0;
  let r = 0;
  async function o(s) {
    const a = s.body.length;
    (n += a);
    r++;
    const c = {
      body: s.body,
      method: "POST",
      referrerPolicy: "strict-origin",
      headers: e.headers,
      keepalive: n <= 60000/* 6e4 */ && r < 15,
      ...e.fetchOptions,
    };
    try {
      const l = await t(e.url, c);
      return {
        statusCode: l.status,
        headers: {
          "x-sentry-rate-limits": l.headers.get("X-Sentry-Rate-Limits"),
          "retry-after": l.headers.get("Retry-After"),
        },
      };
    } catch (l) {
      Wp("fetch");
      throw l;
    } finally {
      (n -= a);
      r--;
    }
  }
  return Cf(e, o, ts(e.bufferSize || Gp));
}
const un = typeof __SENTRY_DEBUG__ === "undefined" || __SENTRY_DEBUG__;
const Xp = 30;
const Kp = 50;
function Ro(e, t, n, r) {
  const o = { filename: e, function: t === "<anonymous>" ? Gt : t, in_app: true };

  if (n !== undefined) {
    (o.lineno = n);
  }

  if (r !== undefined) {
    (o.colno = r);
  }

  return o;
}
const Zp = /^\s*at (\S+?)(?::(\d+))(?::(\d+))\s*$/i;

const Jp =
  /^\s*at (?:(.+?\)(?: \[.+\])?|.*?) ?\((?:address at )?)?(?:async )?((?:<anonymous>|[-a-z]+:|.*bundle|\/)?.*?)(?::(\d+))?(?::(\d+))?\)?\s*$/i;

const Qp = /\((\S*)(?::(\d+))(?::(\d+))\)/;
const eh = /at (.+?) ?\(data:(.+?),/;

const th = (e) => {
  const t = e.match(eh);
  if (t) {
    return { filename: `<data:${t[2]}>`, function: t[1] };
  }
  const n = Zp.exec(e);
  if (n) {
    const [, o, s, a] = n;
    return Ro(o, Gt, Number(s), Number(a));
  }
  const r = Jp.exec(e);
  if (r) {
    if (r[2]?.indexOf("eval") === 0) {
      const c = Qp.exec(r[2]);

      if (c) {
        (r[2] = c[1]);
        (r[3] = c[2]);
        (r[4] = c[3]);
      }
    }
    const [s, a] = Mc(r[1] || Gt, r[2]);
    return Ro(a, s, r[3] ? +r[3] : undefined, r[4] ? +r[4] : undefined);
  }
};

const nh = [Xp, th];

const rh =
  /^\s*(.*?)(?:\((.*?)\))?(?:^|@)?((?:[-a-z]+)?:\/.*?|\[native code\]|[^@]*(?:bundle|\d+\.js)|\/[\w\-. /=]+)(?::(\d+))?(?::(\d+))?\s*$/i;

const oh = /(\S+) line (\d+)(?: > eval line \d+)* > eval/i;

const sh = (e) => {
  const t = rh.exec(e);
  if (t) {
    if (t[3] && t[3].includes(" > eval")) {
      const s = oh.exec(t[3]);

      if (s) {
        (t[1] = t[1] || "eval");
        (t[3] = s[1]);
        (t[4] = s[2]);
        (t[5] = "");
      }
    }
    let [,,, r] = t;
    let o = t[1] || Gt;
    ([o, r] = Mc(o, r));
    return Ro(r, o, t[4] ? +t[4] : undefined, t[5] ? +t[5] : undefined);
  }
};

const ih = [Kp, sh];
const ah = [nh, ih];
const ch = Ua(...ah);

const Mc = (e, t) => {
  const n = e.includes("safari-extension");
  const r = e.includes("safari-web-extension");
  return n || r
    ? [
        e.includes("@") ? e.split("@")[0] : Gt,
        n ? `safari-extension:${t}` : `safari-web-extension:${t}`,
      ]
    : [e, t];
};

const tr = 1024;
const lh = "Breadcrumbs";

const dh = (e = {}) => {
  const t = {
    console: true,
    dom: true,
    fetch: true,
    history: true,
    sentry: true,
    xhr: true,
    ...e,
  };
  return {
    name: lh,
    setup(n) {
      if (t.console) {
        ep(hh(n));
      }

      if (t.dom) {
        Mp(ph(n, t.dom));
      }

      if (t.xhr) {
        jp(mh(n));
      }

      if (t.fetch) {
        mp(gh(n));
      }

      if (t.history) {
        Lc(_h(n));
      }

      if (t.sentry) {
        n.on("beforeSendEvent", fh(n));
      }
    },
  };
};

function fh(e) {
  return n => {
    if (Ie() === e) {
      Yt(
        {
          category: `sentry.${
            n.type === "transaction" ? "transaction" : "event"
          }`,
          event_id: n.event_id,
          level: n.level,
          message: jt(n),
        },
        { event: n }
      );
    }
  };
}
function ph(e, t) {
  return r => {
    if (Ie() !== e) {
      return;
    }
    let o;
    let s;
    let a = typeof t == "object" ? t.serializeAttribute : undefined;

    let c =
      typeof t == "object" && typeof t.maxStringLength == "number"
        ? t.maxStringLength
        : undefined;

    if (c &&
      c > tr) {
      un &&
          F.warn(
            `\`dom.maxStringLength\` cannot exceed ${tr}, but a value of ${c} was configured. Sentry will use ${tr} instead.`
          );

      (c = tr);
    }

    if (typeof a == "string") {
      (a = [a]);
    }

    try {
      const r_event = r.event;
      const f = yh(r_event) ? r_event.target : r_event;
      (o = ja(f, { keyAttrs: a, maxStringLength: c }));
      (s = lu(f));
    } catch {
      o = "<unknown>";
    }
    if (o.length === 0) {
      return;
    }
    const l = { category: `ui.${r.name}`, message: o };

    if (s) {
      (l.data = { "ui.component_name": s });
    }

    Yt(l, { event: r.event, name: r.name, global: r.global });
  };
}
function hh(e) {
  return function (n) {
    if (Ie() !== e) {
      return;
    }
    const r = {
      category: "console",
      data: { arguments: n.args, logger: "console" },
      level: np(n.level),
      message: Os(n.args, " "),
    };
    if (n.level === "assert") {
      if (n.args[0] === false) {
        (r.message = `Assertion failed: ${
          Os(n.args.slice(1), " ") || "console.assert"
        }`);

        (r.data.arguments = n.args.slice(1));
      } else {
        return;
      }
    }
    Yt(r, { input: n.args, level: n.level });
  };
}
function mh(e) {
  return n => {
    if (Ie() !== e) {
      return;
    }
    const { startTimestamp, endTimestamp } = n;
    const s = n.xhr[kn];
    if (!startTimestamp || !endTimestamp || !s) {
      return;
    }
    const { method, url, status_code, body } = s;
    const f = { method: method, url: url, status_code: status_code };
    const d = { xhr: n.xhr, input: body, startTimestamp: startTimestamp, endTimestamp: endTimestamp };
    const p = { category: "xhr", data: f, type: "http", level: Ac(status_code) };
    e.emit("beforeOutgoingRequestBreadcrumb", p, d);
    Yt(p, d);
  };
}
function gh(e) {
  return n => {
    if (Ie() !== e) {
      return;
    }
    const { startTimestamp, endTimestamp } = n;
    if (endTimestamp &&
    !(n.fetchData.url.match(/sentry_key/) && n.fetchData.method === "POST")) {
      if (n.error) {
        const s = {
            data: n.error,
            input: n.args,
            startTimestamp: startTimestamp,
            endTimestamp: endTimestamp,
          };

        const a = {
          category: "fetch",
          data: n.fetchData,
          level: "error",
          type: "http",
        };

        e.emit("beforeOutgoingRequestBreadcrumb", a, s);
        Yt(a, s);
      } else {
        const n_response = n.response;
        const a = { ...n.fetchData, status_code: n_response?.status };

        const c = {
          input: n.args,
          response: n_response,
          startTimestamp: startTimestamp,
          endTimestamp: endTimestamp,
        };

        const l = {
          category: "fetch",
          data: a,
          type: "http",
          level: Ac(a.status_code),
        };

        e.emit("beforeOutgoingRequestBreadcrumb", l, c);
        Yt(l, c);
      }
    }
  };
}
function _h(e) {
  return n => {
    if (Ie() !== e) {
      return;
    }

    let {
      from,
      to: to_2
    } = n;

    const s = Wr(ie.location.href);
    let a = from ? Wr(from) : undefined;
    const c = Wr(to_2);

    if (!a?.path) {
      (a = s);
    }

    if (s.protocol === c.protocol && s.host === c.host) {
      (to_2 = c.relative);
    }

    if (s.protocol === a.protocol && s.host === a.host) {
      (from = a.relative);
    }

    Yt({ category: "navigation", data: { from: from, to: to_2 } });
  };
}
function yh(e) {
  return !!e && !!e.target;
}

const vh =
    "EventTarget,Window,Node,ApplicationCache,AudioTrackList,BroadcastChannel,ChannelMergerNode,CryptoOperation,EventSource,FileReader,HTMLUnknownElement,IDBDatabase,IDBRequest,IDBTransaction,KeyOperation,MediaController,MessagePort,ModalWindow,Notification,SVGElementInstance,Screen,SharedWorker,TextTrack,TextTrackCue,TextTrackList,WebSocket,WebSocketWorker,Worker,XMLHttpRequest,XMLHttpRequestEventTarget,XMLHttpRequestUpload".split(
      ","
    );

const wh = "BrowserApiErrors";

const Sh = (e = {}) => {
  const t = {
    XMLHttpRequest: true,
    eventTarget: true,
    requestAnimationFrame: true,
    setInterval: true,
    setTimeout: true,
    unregisterOriginalCallbacks: false,
    ...e,
  };
  return {
    name: wh,
    setupOnce() {
      if (t.setTimeout) {
        Ve(ie, "setTimeout", Si);
      }

      if (t.setInterval) {
        Ve(ie, "setInterval", Si);
      }

      if (t.requestAnimationFrame) {
        Ve(ie, "requestAnimationFrame", bh);
      }

      if (t.XMLHttpRequest &&
        "XMLHttpRequest" in ie) {
        Ve(XMLHttpRequest.prototype, "send", Ch);
      }

      const t_eventTarget = t.eventTarget;

      if (t_eventTarget) {
        (Array.isArray(t_eventTarget) ? t_eventTarget : vh).forEach(o => Th(o, t));
      }
    },
  };
};

function Si(e) {
  return function (...t) {
    const [n] = t;

    (t[0] = ln(n, {
      mechanism: {
        handled: false,
        type: `auto.browser.browserapierrors.${Nt(e)}`,
      },
    }));

    return e.apply(this, t);
  };
}
function bh(e) {
  return function (t) {
    return e.apply(this, [
      ln(t, {
        mechanism: {
          data: { handler: Nt(e) },
          handled: false,
          type: "auto.browser.browserapierrors.requestAnimationFrame",
        },
      }),
    ]);
  };
}
function Ch(e) {
  return function (...t) {
    const n = this;

    ["onload", "onerror", "onprogress", "onreadystatechange"].forEach((o) => {
      if (o in n &&
        typeof n[o] == "function") {
        Ve(n, o, s => {
          const a = {
              mechanism: {
                data: { handler: Nt(s) },
                handled: false,
                type: `auto.browser.browserapierrors.xhr.${o}`,
              },
            };

          const c = Xo(s);

          if (c) {
            (a.mechanism.data.handler = Nt(c));
          }

          return ln(s, a);
        });
      }
    });

    return e.apply(this, t);
  };
}
function Th(e, t) {
  const r = ie[e]?.prototype;

  if (r?.hasOwnProperty?.("addEventListener")) {
    Ve(r, "addEventListener", o => (function(s, a, c) {
      try {
        if (Ih(a)) {
          (a.handleEvent = ln(a.handleEvent, {
              mechanism: {
                data: { handler: Nt(a), target: e },
                handled: false,
                type: "auto.browser.browserapierrors.handleEvent",
              },
            }));
        }
      } catch {}

      if (t.unregisterOriginalCallbacks) {
        kh(this, s, a);
      }

      return o.apply(this, [
        s,
        ln(a, {
          mechanism: {
            data: { handler: Nt(a), target: e },
            handled: false,
            type: "auto.browser.browserapierrors.addEventListener",
          },
        }),
        c,
      ]);
    }));

    Ve(r, "removeEventListener", o => (function(s, a, c) {
      try {
        const a_sentry_wrapped = a.__sentry_wrapped__;

        if (a_sentry_wrapped) {
          o.call(this, s, a_sentry_wrapped, c);
        }
      } catch {}
      return o.call(this, s, a, c);
    }));
  }
}
function Ih(e) {
  return typeof e.handleEvent == "function";
}
function kh(e, t, n) {
  if (e &&
    typeof e == "object" &&
    "removeEventListener" in e &&
    typeof e.removeEventListener == "function") {
    e.removeEventListener(t, n);
  }
}

const Nh = (e = {}) => {
    const t = e.lifecycle ?? "route";
    return {
      name: "BrowserSession",
      setupOnce() {
        if (typeof ie.document === "undefined") {
          if (un) {
            F.warn(
              "Using the `browserSessionIntegration` in non-browser environments is not supported."
            );
          }

          return;
        }
        ti({ ignoreDuration: true });
        zr();
        const n = it();
        let r = n.getUser();

        n.addScopeListener((o) => {
          const s = o.getUser();

          if ((r?.id !== s?.id || r?.ip_address !== s?.ip_address)) {
            zr();
            (r = s);
          }
        });

        if (t === "route") {
          Lc(({ from: o, to: s }) => {
            if (o !== s) {
              ti({ ignoreDuration: true });
              zr();
            }
          });
        }
      },
    };
  };

const Rh = "CultureContext";

const Ah = () => ({
  name: Rh,

  preprocessEvent(e) {
    const t = bi();

    if (t) {
      (e.contexts = {
          ...e.contexts,
          culture: { ...t, ...e.contexts?.culture },
        });
    }
  },

  processSegmentSpan(e) {
    const t = bi();

    if (t) {
      lc(e, {
        "culture.locale": t.locale,
        "culture.timezone": t.timezone,
        "culture.calendar": t.calendar,
      });
    }
  }
});

function bi() {
  try {
    const ie_Intl = ie.Intl;
    if (!ie_Intl) {
      return;
    }
    const t = ie_Intl.DateTimeFormat().resolvedOptions();
    return { locale: t.locale, timezone: t.timeZone, calendar: t.calendar };
  } catch {
    return;
  }
}
const Oh = "GlobalHandlers";

const Lh = (e = {}) => {
  const t = { onerror: true, onunhandledrejection: true, ...e };
  return {
    name: Oh,
    setupOnce() {
      Error.stackTraceLimit = 50;
    },
    setup(n) {
      if (t.onerror) {
        Mh(n);
        Ci("onerror");
      }

      if (t.onunhandledrejection) {
        $h(n);
        Ci("onunhandledrejection");
      }
    },
  };
};

function Mh(e) {
  Ql((t) => {
    const { stackParser, attachStacktrace } = $c();
    if (Ie() !== e || Oc()) {
      return;
    }
    const { msg, url, line, column, error } = t;
    const u = Fh(os(stackParser, error || msg, undefined, attachStacktrace, false), url, line, column);
    (u.level = "error");

    dc(u, {
      originalException: error,
      mechanism: {
        handled: false,
        type: "auto.browser.global_handlers.onerror",
      },
    });
  });
}
function $h(e) {
  tu((t) => {
    const { stackParser, attachStacktrace } = $c();
    if (Ie() !== e || Oc()) {
      return;
    }
    const o = Dh(t);
    const s = Nr(o) ? Uh(o) : os(stackParser, o, undefined, attachStacktrace, true);
    (s.level = "error");

    dc(s, {
      originalException: o,
      mechanism: {
        handled: false,
        type: "auto.browser.global_handlers.onunhandledrejection",
      },
    });
  });
}
function Dh(e) {
  if (Nr(e)) {
    return e;
  }
  try {
    if ("reason" in e) {
      return e.reason;
    }
    if ("detail" in e && "reason" in e.detail) {
      return e.detail.reason;
    }
  } catch {}
  return e;
}
function Uh(e) {
  return {
    exception: {
      values: [
        {
          type: "UnhandledRejection",
          value: `Non-Error promise rejection captured with value: ${String(
            e
          )}`,
        },
      ],
    },
  };
}
function Fh(e, t, n, r) {
  const o = (e.exception = e.exception || {});
  const s = (o.values = o.values || []);
  const a = (s[0] = s[0] || {});
  const c = (a.stacktrace = a.stacktrace || {});
  const l = (c.frames = c.frames || []);

  if (l.length === 0) {
    l.push({
      colno: r,
      lineno: n,
      filename: Hh(t) ?? Yo(),
      function: Gt,
      in_app: true,
    });
  }

  return e;
}
function Ci(e) {
  if (un) {
    F.log(`Global Handler attached: ${e}`);
  }
}
function $c() {
  return Ie()?.getOptions() || { stackParser: () => [], attachStacktrace: false };
}
function Hh(e) {
  if (!(!vt(e) || e.length === 0)) {
    return e.startsWith("data:") ? `<${Ud(e, false)}>` : e;
  }
}

const Bh = () => ({
  name: "HttpContext",

  preprocessEvent(e) {
    if (!ie.navigator && !ie.location && !ie.document) {
      return;
    }
    const t = vi();
    const n = { ...t.headers, ...e.request?.headers };
    e.request = { ...t, ...e.request, headers: n };
  },

  processSegmentSpan(e) {
    if (!ie.navigator && !ie.location && !ie.document) {
      return;
    }
    const t = vi();
    lc(e, {
      "url.full": t.url || undefined,
      "http.request.header.user_agent": t.headers["User-Agent"],
      "http.request.header.referer": t.headers.Referer,
    });
  }
});

const Vh = "cause";
const Wh = 5;
const jh = "LinkedErrors";

const qh = (e = {}) => {
  const t = e.limit || Wh;
  const n = e.key || Vh;
  return {
    name: jh,
    preprocessEvent(r, o, s) {
      const a = s.getOptions();
      Qf(ns, a.stackParser, n, t, r, o);
    },
  };
};

function Gh() {
  return Yh()
    ? (un &&
        hn(() => {
          console.error(
            "[Sentry] You cannot use Sentry.init() in a browser extension, see: https://docs.sentry.io/platforms/javascript/best-practices/browser-extensions/"
          );
        }),
      true)
    : false;
}
function Yh() {
  if (typeof ie.window === "undefined") {
    return false;
  }
  const e = ie;
  if (e.nw || !(e.chrome || e.browser)?.runtime?.id) {
    return false;
  }
  const n = Yo();
  return !(
    ie === ie.top &&
    /^(?:chrome-extension|moz-extension|ms-browser-extension|safari-web-extension):\/\//.test(
      n
    )
  );
}
function Xh(e) {
  return [zf(), Bf(), dp(), Sh(), dh(), Lh(), qh(), sp(), Bh(), Ah(), Nh()];
}
function Kh(e = {}) {
  const t = !e.skipBrowserExtensionCheck && Gh();
  let n = e.defaultIntegrations == null ? Xh() : e.defaultIntegrations;
  const r = {
    ...e,
    enabled: t ? false : e.enabled,
    stackParser: Zl(e.stackParser || ch),
    integrations: af({ integrations: e.integrations, defaultIntegrations: n }),
    transport: e.transport || Yp,
  };
  return Mf(Ap, r);
}
function Ti(e = {}) {
  const ie_document = ie.document;
  const n = ie_document?.head || ie_document?.body;
  if (!n) {
    if (un) {
      F.error("[showReportDialog] Global document not defined");
    }

    return;
  }
  const r = st();
  const s = Ie()?.getDsn();
  if (!s) {
    if (un) {
      F.error("[showReportDialog] DSN not configured");
    }

    return;
  }

  const a = {
      ...e,
      user: { ...r.getUser(), ...e.user },
      eventId: e.eventId || Qd(),
    };

  const c = ie.document.createElement("script");
  (c.async = true);
  (c.crossOrigin = "anonymous");
  (c.src = of(s, a));
  const { onLoad, onClose } = a;

  if (onLoad) {
    (c.onload = onLoad);
  }

  if (onClose) {
    const f = (d) => {
      if (d.data === "__sentry_reportdialog_closed__") {
        try {
          onClose();
        } finally {
          ie.removeEventListener("message", f);
        }
      }
    };
    ie.addEventListener("message", f);
  }

  n.appendChild(c);
}
let At;
let de;
let Yr;
let Ii;
let dn = 0;
const Dc = [];
const pe = q;

const {
  __b,
  __r,
  diffed,
  __c,
  unmount,
  __: __1
} = pe;

function yn(e, t) {
  if (pe.__h) {
    pe.__h(de, e, dn || t);
  }

  (dn = 0);
  const n = de.__H || (de.__H = { __: [], __h: [] });

  if (e >= n.__.length) {
    n.__.push({});
  }

  return n.__[e];
}
function A(e) {
  (dn = 1);
  return ss(Hc, e);
}
function ss(e, t, n) {
  const r = yn(At++, 2);
  (r.t = e);

  if (!r.__c &&
    ((r.__ = [
      n ? n(t) : Hc(undefined, t),
      c => {
        const l = r.__N ? r.__N[0] : r.__[0];
        const u = r.t(l, c);

        if (l !== u) {
          (r.__N = [u, r.__[1]]);
          r.__c.setState({});
        }
      },
    ]),
    (r.__c = de),
    !de.__f)) {
    const o = function (c, l, u) {
      if (!r.__c.__H) {
        return true;
      }
      const f = r.__c.__H.__.filter(p => !!p.__c);
      if (f.every(p => !p.__N)) {
        return !shouldComponentUpdate || shouldComponentUpdate.call(this, c, l, u);
      }
      let d = r.__c.props !== c;

      f.forEach(p => {
        if (p.__N) {
          const h = p.__[0];
          (p.__ = p.__N);
          (p.__N = undefined);

          if (h !== p.__[0]) {
            (d = true);
          }
        }
      });

      return (shouldComponentUpdate && shouldComponentUpdate.call(this, c, l, u)) || d;
    };
    de.__f = true;

    var {
      shouldComponentUpdate,
      componentWillUpdate
    } = de;

    (de.componentWillUpdate = function (c, l, u) {
      if (this.__e) {
        const f = shouldComponentUpdate;
        (shouldComponentUpdate = undefined);
        o(c, l, u);
        (shouldComponentUpdate = f);
      }

      if (componentWillUpdate) {
        componentWillUpdate.call(this, c, l, u);
      }
    });

    (de.shouldComponentUpdate = o);
  }

  return r.__N || r.__;
}
function D(e, t) {
  const n = yn(At++, 3);

  if (!pe.__s && as(n.__H, t)) {
    (n.__ = e);
    (n.u = t);
    de.__H.__h.push(n);
  }
}
function vn(e, t) {
  const n = yn(At++, 4);

  if (!pe.__s && as(n.__H, t)) {
    (n.__ = e);
    (n.u = t);
    de.__h.push(n);
  }
}
function N(e) {
  (dn = 5);

  return ge(() => ({
    current: e
  }), []);
}
function is(e, t, n) {
  (dn = 6);

  vn(
    () => {
      if (typeof e == "function") {
        const r = e(t());
        return () => {
          e(null);

          if (r && typeof r == "function") {
            r();
          }
        };
      }
      if (e) {
        (e.current = t());

        return () => e.current = null;
      }
    },
    n == null ? n : n.concat(e)
  );
}
function ge(e, t) {
  const n = yn(At++, 7);

  if (as(n.__H, t)) {
    (n.__ = e());
    (n.__H = t);
    (n.__h = e);
  }

  return n.__;
}
function k(e, t) {
  (dn = 8);

  return ge(() => e, t);
}
function Lr(e) {
  const t = de.context[e.__c];
  const n = yn(At++, 9);
  (n.c = e);
  return t ? (n.__ == null && ((n.__ = true), t.sub(de)), t.props.value) : e.__;
}
function Uc(e, t) {
  if (pe.useDebugValue) {
    pe.useDebugValue(t ? t(e) : e);
  }
}
function Fc() {
  const e = yn(At++, 11);
  if (!e.__) {
    for (var t = de.__v; t !== null && !t.__m && t.__ !== null; ) {
      t = t.__;
    }
    const n = t.__m || (t.__m = [0, 0]);
    e.__ = `P${n[0]}-${n[1]++}`;
  }
  return e.__;
}
function Zh() {
  for (let e; (e = Dc.shift()); ) {
    if (e.__P && e.__H) {
      try {
        e.__H.__h.forEach(gr);
        e.__H.__h.forEach(Po);
        (e.__H.__h = []);
      } catch (t) {
        (e.__H.__h = []);
        pe.__e(t, e.__v);
      }
    }
  }
}

(pe.__b = e => {
  (de = null);

  if (__b) {
    __b(e);
  }
});

(pe.__ = (e, t) => {
  if (e && t.__k && t.__k.__m) {
    (e.__m = t.__k.__m);
  }

  if (__1) {
    __1(e, t);
  }
});

(pe.__r = e => {
  if (__r) {
    __r(e);
  }

  (At = 0);
  const t = (de = e.__c).__H;

  if (t) {
    if (Yr === de) {
      (t.__h = []);
      (de.__h = []);

      t.__.forEach(n => {
        if (n.__N) {
          (n.__ = n.__N);
        }

        n.u = undefined;
        n.__N = undefined;
      });
    } else {
      t.__h.forEach(gr);
      t.__h.forEach(Po);
      (t.__h = []);
      (At = 0);
    }
  }

  (Yr = de);
});

(pe.diffed = e => {
  if (diffed) {
    diffed(e);
  }

  const e_c = e.__c;

  if (e_c &&
    e_c.__H) {
    e_c.__H.__h.length &&
        ((Dc.push(e_c) !== 1 && Ii === pe.requestAnimationFrame) ||
          ((Ii = pe.requestAnimationFrame) || Jh)(Zh));

    e_c.__H.__.forEach(n => {
      if (n.u) {
        (n.__H = n.u);
      }

      (n.u = undefined);
    });
  }

  Yr = null;
  de = null;
});

(pe.__c = (e, t) => {
  t.some(n => {
    try {
      n.__h.forEach(gr);

      (n.__h = n.__h.filter(r => !r.__ || Po(r)));
    } catch (r) {
      t.some(o => {
        if (o.__h) {
          (o.__h = []);
        }
      });

      (t = []);
      pe.__e(r, n.__v);
    }
  });

  if (__c) {
    __c(e, t);
  }
});

(pe.unmount = e => {
  if (unmount) {
    unmount(e);
  }

  let t;
  const e_c = e.__c;

  if (e_c &&
    e_c.__H) {
    e_c.__H.__.forEach(r => {
        try {
          gr(r);
        } catch (o) {
          t = o;
        }
      });

    (e_c.__H = undefined);
    t && pe.__e(t, e_c.__v);
  }
});

const xi = typeof requestAnimationFrame == "function";
function Jh(e) {
  let t;

  const n = () => {
    clearTimeout(r);

    if (xi) {
      cancelAnimationFrame(t);
    }

    setTimeout(e);
  };

  var r = setTimeout(n, 35);

  if (xi) {
    (t = requestAnimationFrame(n));
  }
}
function gr(e) {
  const t = de;
  const e_c = e.__c;

  if (typeof e_c == "function") {
    (e.__c = undefined);
    e_c();
  }

  (de = t);
}
function Po(e) {
  const t = de;
  (e.__c = e.__());
  (de = t);
}
function as(e, t) {
  return !e ||
  e.length !== t.length ||
  t.some((n, r) => n !== e[r]);
}
function Hc(e, t) {
  return typeof t == "function" ? t(e) : t;
}
function Bc(e, t) {
  for (const n in t) {
    e[n] = t[n];
  }
  return e;
}
function Ao(e, t) {
  for (const n in e) {
    if (n !== "__source" && !(n in t)) {
      return true;
    }
  }
  for (const r in t) {
    if (r !== "__source" && e[r] !== t[r]) {
      return true;
    }
  }
  return false;
}
function Vc(e, t) {
  const n = t();
  const r = A({ t: { __: n, u: t } });
  const o = r[0].t;
  const [, s] = r;

  vn(
    () => {
      (o.__ = n);
      (o.u = t);

      if (Xr(o)) {
        s({ t: o });
      }
    },
    [e, n, t]
  );

  D(
    () => {
      if (Xr(o)) {
        s({ t: o });
      }

      return e(() => {
        if (Xr(o)) {
          s({ t: o });
        }
      });
    },
    [e]
  );

  return n;
}
function Xr(e) {
  let t;
  let n;

  const {
    u,
    __: _
  } = e;

  try {
    const s = u();
    return !(
      ((t = _) === (n = s) && (t !== 0 || 1 / t == 1 / n)) ||
      (t != t && n != n)
    );
  } catch {
    return true;
  }
}
function Wc(e) {
  e();
}
function jc(e) {
  return e;
}
function zc() {
  return [false, Wc];
}
const qc = vn;

class Oo {
  constructor(e, t) {
    (this.props = e);
    (this.context = t);
  }

  shouldComponentUpdate(e, t) {
      return Ao(this.props, e) || Ao(this.state, t);
    }
}

function Mr(e, t) {
  function n(o) {
    const s = this.props.ref;
    const a = s == o.ref;

    if (!a && s) {
      if (s.call) {
        s(null);
      } else {
        (s.current = null);
      }
    }

    if (t) {
      if (!!t(this.props, o)) {
        return !a;
      }
    }

    return Ao(this.props, o);
  }
  function r(o) {
    (this.shouldComponentUpdate = n);
    return Je(e, o);
  }
  (r.displayName = `Memo(${e.displayName || e.name})`);
  (r.prototype.isReactComponent = true);
  (r.__f = true);
  (r.type = e);
  return r;
}
((Oo.prototype = new qe()).isPureReactComponent = true);

const {
  __b: _b_1,
  __e,
  unmount: unmount_2,
  event,
  vnode,
  __r: _r_1,
  diffed: diffed_2
} = q;

q.__b = e => {
  if (e.type && e.type.__f && e.ref) {
    (e.props.ref = e.ref);
    (e.ref = null);
  }

  if (_b_1) {
    _b_1(e);
  }
};
const Qh =
  (typeof Symbol !== "undefined" && Symbol.for && Symbol.for("react.forward_ref")) ||
  3911;
function Gc(e) {
  class t {
    constructor(n) {
      const r = Bc({}, n);
      delete r.ref;
      return e(r, n.ref || null);
    }

    static componentWillUnmount() {
      $n(null, t.v);
      (t.v = null);
      (t.h = null);
    }
  }

  (t.$$typeof = Qh);
  (t.render = e);
  t.prototype.isReactComponent = true;
  t.__f = true;
  (t.displayName = `ForwardRef(${e.displayName || e.name})`);
  return t;
}

const Mi = (e, t) => e == null ? null : Ke(Ke(e).map(t));

const em = {
  map: Mi,
  forEach: Mi,
  count(e) {
    return e ? Ke(e).length : 0;
  },
  only(e) {
    const t = Ke(e);
    if (t.length !== 1) {
      throw "Children.only";
    }
    return t[0];
  },
  toArray: Ke,
};

q.__e = (e, t, n, r) => {
  if (e.then) {
    let o;
    for (let s = t; (s = s.__); ) {
      if ((o = s.__c) && o.__c) {
        if (t.__e == null) {
          (t.__e = n.__e);
          (t.__k = n.__k);
        }

        return o.__c(e, t);
      }
    }
  }
  __e(e, t, n, r);
};
function Yc(e, t, n) {
  if (e) {
    e.__c &&
        e.__c.__H &&
        (e.__c.__H.__.forEach(r => {
      if (typeof r.__c == "function") {
        r.__c();
      }
    }),
        (e.__c.__H = null));

    (e = Bc({}, e)).__c != null &&
      (e.__c.__P === n && (e.__c.__P = t), (e.__c.__e = true), (e.__c = null));

    (e.__k = e.__k &&
    e.__k.map(r => Yc(r, t, n)));
  }

  return e;
}
function Xc(e, t, n) {
  if (e &&
    n) {
    (e.__v = null);

    (e.__k = e.__k &&
    e.__k.map(r => Xc(r, t, n)));

    e.__c &&
      e.__c.__P === t &&
      (e.__e && n.appendChild(e.__e), (e.__c.__e = true), (e.__c.__P = n));
  }

  return e;
}

class Ee {
  constructor() {
    (this.__u = 0);
    (this.o = null);
    (this.__b = null);
  }

  componentWillUnmount() {
      this.o = [];
    }

  render(e, t) {
    if (this.__b) {
      if (this.__v.__k) {
        const n = document.createElement("div");
        const r = this.__v.__k[0].__c;
        this.__v.__k[0] = Yc(this.__b, n, (r.__O = r.__P));
      }
      this.__b = null;
    }
    const o = t.__a && Je(Te, null, e.fallback);

    if (o) {
      (o.__u &= -33);
    }

    return [Je(Te, null, t.__a ? null : e.children), o];
  }
}

function Kc(e) {
  const t = e.__.__c;
  return t && t.__a && t.__a(e);
}
function Z(e) {
  let t;
  let n;
  let r;
  let o = null;
  function s(a) {
    if (!t) {
      (e()).then(
        c => {
          if (c) {
            (o = c.default || c);
          }

          (r = true);
        },
        c => {
          (n = c);
          (r = true);
        }
      );
    }

    if (n) {
      throw n;
    }

    if (!r) {
      throw t;
    }
    return o ? Je(o, a) : null;
  }
  (s.displayName = "Lazy");
  (s.__f = true);
  return s;
}

class Nn {
  constructor() {
    (this.i = null);
    (this.l = null);
  }

  render(e) {
    (this.i = null);
    (this.l = new Map());
    const t = Ke(e.children);

    if (e.revealOrder && e.revealOrder[0] === "b") {
      t.reverse();
    }

    for (let n = t.length; n--; ) {
      this.l.set(t[n], (this.i = [1, 0, this.i]));
    }
    return e.children;
  }
}

(q.unmount = e => {
  const e_c = e.__c;

  if (e_c && e_c.__R) {
    e_c.__R();
  }

  if (e_c && 32 & e.__u) {
    (e.type = null);
  }

  if (unmount_2) {
    unmount_2(e);
  }
});

((Ee.prototype = new qe()).__c = function (e, t) {
  const t_c = t.__c;
  const r = this;

  if (r.o == null) {
    (r.o = []);
  }

  r.o.push(t_c);
  const o = Kc(r.__v);

  const a = () => {
    if (!s) {
      (s = true);
      (t_c.__R = null);
      o ? o(c) : c();
    }
  };

  t_c.__R = a;
  var c = () => {
    if (!--r.__u) {
      if (r.state.__a) {
        const l = r.state.__a;
        r.__v.__k[0] = Xc(l, l.__c.__P, l.__c.__O);
      }
      let u;
      for (r.setState({ __a: (r.__b = null) }); (u = r.o.pop()); ) {
        u.forceUpdate();
      }
    }
  };

  if (!r.__u++ && 32 & t.__u) {
    r.setState({ __a: (r.__b = r.__v.__k[0]) });
  }

  e.then(a, a);
});

const Di = (e, t, n) => {
  if (++n[1] === n[0]) {
    e.l.delete(t);
  }

  if (e.props.revealOrder && (e.props.revealOrder[0] !== "t" || !e.l.size)) {
    for (n = e.i; n; ) {
      while (n.length > 3) {
        n.pop()();
      }

      if (n[1] < n[0]) {
        break;
      }
      e.i = n = n[2];
    }
  }
};
function nm(e) {
  (this.getChildContext = () => e.context);

  return e.children;
}
function rm(e) {
  const t = this;
  const e_h = e.h;

  if (t.h && t.h !== e_h) {
    t.componentWillUnmount();
  }

  if (!t.v) {
    for (var r = t.__v; r !== null && !r.__m && r.__ !== null; ) {
      r = r.__;
    }
    (t.h = e_h);

    (t.v = {
        nodeType: 1,
        parentNode: e_h,
        childNodes: [],
        __k: { __m: r.__m },
        contains() {
          return true;
        },
        insertBefore(o, s) {
          this.childNodes.push(o);
          t.h.insertBefore(o, s);
        },
        removeChild(o) {
          this.childNodes.splice(this.childNodes.indexOf(o) >>> 1, 1);
          t.h.removeChild(o);
        },
      });
  }

  $n(Je(nm, { context: t.context }, e.__v), t.v);
}

export function $(e, t) {
  const n = Je(rm, { __v: e, h: t });
  (n.containerInfo = t);
  return n;
}

((Nn.prototype = new qe()).__a = function (e) {
  const t = this;
  const n = Kc(t.__v);
  const r = t.l.get(e);
  r[0]++;

  return o => {
    const s = () => {
      if (t.props.revealOrder) {
        r.push(o);
        Di(t, e, r);
      } else {
        o();
      }
    };

    if (n) {
      n(s);
    } else {
      s();
    }
  };
});

(Nn.prototype.componentDidUpdate = Nn.prototype.componentDidMount =
    function () {
      const e = this;
      this.l.forEach((t, n) => {
        Di(e, n, t);
      });
    });

const Zc =
    (typeof Symbol !== "undefined" && Symbol.for && Symbol.for("react.element")) || 60103;

const om =
  /^(?:accent|alignment|arabic|baseline|cap|clip(?!PathU)|color|dominant|fill|flood|font|glyph(?!R)|horiz|image(!S)|letter|lighting|marker(?!H|W|U)|overline|paint|pointer|shape|stop|strikethrough|stroke|text(?!L)|transform|underline|unicode|units|v|vector|vert|word|writing|x(?!C))[A-Z]/;

const sm = /^on(Ani|Tra|Tou|BeforeInp|Compo)/;
const im = /[A-Z0-9]/g;
const am = typeof document !== "undefined";

const cm = e => (
  typeof Symbol !== "undefined" && typeof Symbol() == "symbol"
    ? /fil|che|rad/
    : /fil|che|ra/
).test(e);

function Jc(e, t, n) {
  if (t.__k == null) {
    (t.textContent = "");
  }

  $n(e, t);

  if (typeof n == "function") {
    n();
  }

  return e ? e.__c : null;
}
function lm(e, t, n) {
  La(e, t);

  if (typeof n == "function") {
    n();
  }

  return e ? e.__c : null;
}
(qe.prototype.isReactComponent = {});

[
  "componentWillMount",
  "componentWillReceiveProps",
  "componentWillUpdate",
].forEach(e => {
  Object.defineProperty(qe.prototype, e, {
    configurable: true,
    get() {
      return this[`UNSAFE_${e}`];
    },
    set(t) {
      Object.defineProperty(this, e, {
        configurable: true,
        writable: true,
        value: t,
      });
    },
  });
});

function um() {}
function dm() {
  return this.cancelBubble;
}
function fm() {
  return this.defaultPrevented;
}
q.event = e => {
  if (event) {
    (e = event(e));
  }

  (e.persist = um);
  (e.isPropagationStopped = dm);
  (e.isDefaultPrevented = fm);
  (e.nativeEvent = e);
  return e.nativeEvent;
};
let cs;

const pm = {
  enumerable: false,
  configurable: true,
  get() {
    return this.class;
  },
};

q.vnode = e => {
  if (typeof e.type == "string") {
    (t => {
      const {
        props,
        type
      } = t;

      const o = {};
      const s = !type.includes("-");
      for (let a in props) {
        let props_a = props[a];
        if (
          !(
            (a === "value" && "defaultValue" in props && props_a == null) ||
            (am && a === "children" && type === "noscript") ||
            a === "class" ||
            a === "className"
          )
        ) {
          let l = a.toLowerCase();

          if (a === "defaultValue" && "value" in props && props.value == null) {
            (a = "value");
          } else if (a === "download" && props_a === true) {
            (props_a = "");
          } else if (l === "translate" && props_a === "no") {
            (props_a = false);
          } else if (l[0] === "o" && l[1] === "n") {
            if (l === "ondoubleclick") {
              (a = "ondblclick");
            } else if (l !== "onchange" ||
                  (type !== "input" && type !== "textarea") ||
                  cm(props.type)) {
              if (l === "onfocus") {
                (a = "onfocusin");
              } else if (l === "onblur") {
                (a = "onfocusout");
              } else if (sm.test(a)) {
                (a = l);
              }
            } else {
              (l = a = "oninput");
            }
          } else if (s && om.test(a)) {
            (a = a.replace(im, "-$&").toLowerCase());
          } else if (props_a === null) {
            (props_a = undefined);
          }

          if (l === "oninput" && o[(a = l)]) {
            (a = "oninputCapture");
          }

          (o[a] = props_a);
        }
      }

      if (type == "select" &&
        o.multiple &&
        Array.isArray(o.value)) {
        (o.value = Ke(props.children).forEach(u => {
            u.props.selected = o.value.includes(u.props.value);
          }));
      }

      if (type == "select" &&
        o.defaultValue != null) {
        (o.value = Ke(props.children).forEach(u => {
            u.props.selected = o.multiple
              ? o.defaultValue.includes(u.props.value)
              : o.defaultValue == u.props.value;
          }));
      }

      if (props.class && !props.className) {
        (o.class = props.class);
        Object.defineProperty(o, "className", pm);
      } else if (((props.className && !props.class) || (props.class && props.className))) {
        (o.class = o.className = props.className);
      }

      (t.props = o);
    })(e);
  }

  (e.$$typeof = Zc);

  if (vnode) {
    vnode(e);
  }
};
q.__r = e => {
  if (_r_1) {
    _r_1(e);
  }

  (cs = e.__c);
};
q.diffed = e => {
  if (diffed_2) {
    diffed_2(e);
  }

  const {
    props,
    __e: _e
  } = e;

  if (_e != null &&
    e.type === "textarea" &&
    "value" in props &&
    props.value !== _e.value) {
    (_e.value = props.value == null ? "" : props.value);
  }

  (cs = null);
};

const hm = {
    ReactCurrentDispatcher: {
      current: {
        readContext(e) {
          return cs.__n[e.__c].props.value;
        },
        useCallback: k,
        useContext: Lr,
        useDebugValue: Uc,
        useDeferredValue: jc,
        useEffect: D,
        useId: Fc,
        useImperativeHandle: is,
        useInsertionEffect: qc,
        useLayoutEffect: vn,
        useMemo: ge,
        useReducer: ss,
        useRef: N,
        useState: A,
        useSyncExternalStore: Vc,
        useTransition: zc,
      },
    },
  };

const Qc = "18.3.1";
function mm(e) {
  return Je.bind(null, e);
}
function Yn(e) {
  return !!e && e.$$typeof === Zc;
}
function gm(e) {
  return Yn(e) && e.type === Te;
}
function _m(e) {
  return (
    !!e &&
    !!e.displayName &&
    (typeof e.displayName == "string" || e.displayName instanceof String) &&
    e.displayName.startsWith("Memo(")
  );
}
function ym(e) {
  return Yn(e) ? Ma(...arguments) : e;
}
function el(e) {
  return !!e.__k && ($n(null, e), true);
}
function vm(e) {
  return (e && (e.base || (e.nodeType === 1 && e))) || null;
}

const wm = (e, t) => e(t);

const Em = (e, t) => e(t);

const tl = Te;
const Sm = Yn;

const Rn = {
  useState: A,
  useId: Fc,
  useReducer: ss,
  useEffect: D,
  useLayoutEffect: vn,
  useInsertionEffect: qc,
  useTransition: zc,
  useDeferredValue: jc,
  useSyncExternalStore: Vc,
  startTransition: Wc,
  useRef: N,
  useImperativeHandle: is,
  useMemo: ge,
  useCallback: k,
  useContext: Lr,
  useDebugValue: Uc,
  version: "18.3.1",
  Children: em,
  render: Jc,
  hydrate: lm,
  unmountComponentAtNode: el,
  createPortal: $,
  createElement: Je,
  createContext: fn,
  createFactory: mm,
  cloneElement: ym,
  createRef: Ul,
  Fragment: Te,
  isValidElement: Yn,
  isElement: Sm,
  isFragment: gm,
  isMemo: _m,
  findDOMNode: vm,
  Component: qe,
  PureComponent: Oo,
  memo: Mr,
  forwardRef: Gc,
  flushSync: Em,
  unstable_batchedUpdates: wm,
  StrictMode: tl,
  Suspense: Ee,
  SuspenseList: Nn,
  lazy: Z,
  __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED: hm,
};

function bm(e) {
  const t = { ...e };
  kc(t, "react");
  Jd("react", { version: Qc });
  return Kh(t);
}
function Cm(e) {
  const t = e.match(/^([^.]+)/);
  return t !== null && parseInt(t[0]) >= 17;
}
function Tm(e, t) {
  const n = new WeakSet();
  function r(o, s) {
    if (!n.has(o)) {
      if (o.cause) {
        n.add(o);
        return r(o.cause, s);
      }
      o.cause = s;
    }
  }
  r(e, t);
}
function Im(e, { componentStack: t }, n) {
  if (Cm(Qc) && Wn(e) && t) {
    const r = new Error(e.message);
    (r.name = `React ErrorBoundary ${e.name}`);
    (r.stack = t);
    Tm(e, r);
  }
  return Zo(r => {
    r.setContext("react", { componentStack: t });
    return uc(e, n);
  });
}
const km = typeof __SENTRY_DEBUG__ === "undefined" || __SENTRY_DEBUG__;
const Kr = { componentStack: null, error: null, eventId: null };
class Nm extends qe {
  constructor(t) {
    super(t);
    (this.state = Kr);
    (this._openFallbackReportDialog = true);
    const n = Ie();

    if (n &&
      t.showDialog) {
      (this._openFallbackReportDialog = false);

      (this._cleanupHook = n.on("afterSendEvent", (r) => {
        if (!r.type &&
          this._lastEventId &&
          r.event_id === this._lastEventId) {
          Ti({ ...t.dialogOptions, eventId: this._lastEventId });
        }
      }));
    }
  }
  componentDidCatch(t, n) {
    const { componentStack } = n;

    const {
      beforeCapture,
      onError,
      showDialog,
      dialogOptions,
    } = this.props;

    Zo((l) => {
      if (beforeCapture) {
        beforeCapture(l, t, componentStack);
      }

      const u =
          this.props.handled != null
            ? this.props.handled
            : !!this.props.fallback;

      const f = Im(t, n, {
        mechanism: { handled: u, type: "auto.function.react.error_boundary" },
      });

      if (onError) {
        onError(t, componentStack, f);
      }

      if (showDialog) {
        (this._lastEventId = f);
        this._openFallbackReportDialog && Ti({ ...dialogOptions, eventId: f });
      }

      this.setState({ error: t, componentStack: componentStack, eventId: f });
    });
  }
  componentDidMount() {
    const { onMount } = this.props;

    if (onMount) {
      onMount();
    }
  }
  componentWillUnmount() {
    const { error, componentStack, eventId } = this.state;
    const { onUnmount } = this.props;

    if (onUnmount) {
      if (this.state === Kr) {
        onUnmount(null, null, null);
      } else {
        onUnmount(error, componentStack, eventId);
      }
    }

    if (this._cleanupHook) {
      this._cleanupHook();
      (this._cleanupHook = undefined);
    }
  }
  resetErrorBoundary() {
    const { onReset } = this.props;
    const { error, componentStack, eventId } = this.state;

    if (onReset) {
      onReset(error, componentStack, eventId);
    }

    this.setState(Kr);
  }
  render() {
    const { fallback, children } = this.props;
    const r = this.state;
    if (r.componentStack === null) {
      return typeof children == "function" ? children() : children;
    }
    const o =
      typeof fallback == "function"
        ? Je(fallback, {
            error: r.error,
            componentStack: r.componentStack,
            resetError: () => this.resetErrorBoundary(),
            eventId: r.eventId,
          })
        : fallback;
    return Yn(o)
      ? o
      : (fallback && km && F.warn("fallback did not produce a valid ReactElement"),
        null);
  }
}
bm({
  dsn: "https://693c388031bcee4cd87e917055abf6a2@sentry.xn--d1ah4a.com/2",
  environment: "production",
  enabled: true,
  sendDefaultPii: true,
  tracesSampleRate: 0.1,
  release: "1.1.0",
});
function Rm(e) {
  return {
    render(t) {
      Jc(t, e);
    },
    unmount() {
      el(e);
    },
  };
}
const Pm = "modulepreload";

const Am = e => `/${e}`;

const Vi = {};

const X = (t, n, r) => {
  let o = Promise.resolve();
  if (n && n.length > 0) {
    let l = u => Promise.all(
      u.map(f => Promise.resolve(f).then(
        d => ({
          status: "fulfilled",
          value: d
        }),
        d => ({
          status: "rejected",
          reason: d
        })
      )
      )
    );
    document.getElementsByTagName("link");
    const a = document.querySelector("meta[property=csp-nonce]");
    const c = a?.nonce || a?.getAttribute("nonce");
    o = l(
      n.map((u) => {
        (u = Am(u));

        if (u in Vi) {
          return;
        }

        Vi[u] = true;
        const f = u.endsWith(".css");
        const d = f ? '[rel="stylesheet"]' : "";
        if (document.querySelector(`link[href="${u}"]${d}`)) {
          return;
        }
        const p = document.createElement("link");
        (p.rel = f ? "stylesheet" : Pm);

        if (!f) {
          (p.as = "script");
        }

        (p.crossOrigin = "");
        (p.href = u);

        if (c) {
          p.setAttribute("nonce", c);
        }

        document.head.appendChild(p);

        if (f) {
          return new Promise((h, m) => {
            p.addEventListener("load", h);

            p.addEventListener("error", () => m(new Error(`Unable to preload CSS for ${u}`))
            );
          });
        }
      })
    );
  }
  function s(a) {
    const c = new Event("vite:preloadError", { cancelable: true });
    (c.payload = a);
    window.dispatchEvent(c);

    if (!c.defaultPrevented) {
      throw a;
    }
  }
  return o.then((a) => {
    for (const c of a || []) {
      if (c.status === "rejected") {
        s(c.reason);
      }
    }
    return t().catch(s);
  });
};

const Om = {};
function nr(e, t) {
  for (const n in t) {
    e[n] = t[n];
  }
  return e;
}
function nl(e, t, n) {
  let r;
  const o = /(?:\?([^#]*))?(#.*)?$/;
  const s = e.match(o);
  const a = {};
  if (s && s[1]) {
    for (let c = s[1].split("&"), l = 0; l < c.length; l++) {
      const u = c[l].split("=");
      a[decodeURIComponent(u[0])] = decodeURIComponent(u.slice(1).join("="));
    }
  }
  (e = xo(e.replace(o, "")));
  (t = xo(t || ""));
  for (let f = Math.max(e.length, t.length), d = 0; d < f; d++) {
    if (t[d] && t[d].charAt(0) === ":") {
      const p = t[d].replace(/(^:|[+*?]+$)/g, "");
      const h = (t[d].match(/[+*?]+$/) || Om)[0] || "";
      const m = ~h.indexOf("+");
      const g = ~h.indexOf("*");
      const T = e[d] || "";
      if (!T && !g && (!h.includes("?") || m)) {
        r = false;
        break;
      }
      (a[p] = decodeURIComponent(T));

      if (m || g) {
        a[p] = e.slice(d).map(decodeURIComponent).join("/");
        break;
      }
    } else if (t[d] !== e[d]) {
      r = false;
      break;
    }
  }
  return (n.default === true || r !== false) && a;
}
function xm(e, t) {
  return e.rank < t.rank ? 1 : e.rank > t.rank ? -1 : e.index - t.index;
}
function Lm(e, t) {
  (e.index = t);

  (e.rank = (n => n.props.default ? 0 : xo(n.props.path).map(Mm).join(""))(e));

  return e.props;
}
function xo(e) {
  return e.replace(/(^\/+|\/+$)/g, "").split("/");
}
function Mm(e) {
  return e.charAt(0) == ":"
    ? 1 + "*+?".indexOf(e.charAt(e.length - 1)) || 4
    : 5;
}
const $m = {};
const qt = [];
const xn = [];
let Be = null;
const ls = { url: us() };
const rl = fn(ls);
function Xn() {
  const e = Lr(rl);
  if (e === ls) {
    const t = A()[1];
    D(() => {
      xn.push(t);

      return () => xn.splice(xn.indexOf(t), 1);
    }, []);
  }
  return [e, Ye];
}
function us() {
  let e;
  return `${(e =
  Be && Be.location
    ? Be.location
    : Be && Be.getCurrentLocation
    ? Be.getCurrentLocation()
    : typeof location !== "undefined"
    ? location
    : $m).pathname || ""}${e.search || ""}`;
}
function Ye(e, t = false) {
  if (typeof e != "string" && e.url) {
    (t = e.replace);
    (e = e.url);
  }

  if ((n => {
    for (let r = qt.length; r--; ) {
      if (qt[r].canRoute(n)) {
        return true;
      }
    }
    return false;
  })(e)) {
    ((n, r = "push") => {
      if (Be && Be[r]) {
        Be[r](n);
      } else if (typeof history !== "undefined" &&
          history[`${r}State`]) {
        history[`${r}State`](null, null, n);
      }
    })(e, t ? "replace" : "push");
  }

  return ol(e);
}
function ol(e) {
  let t = false;
  for (let n = 0; n < qt.length; n++) {
    if (qt[n].routeTo(e)) {
      (t = true);
    }
  }
  return t;
}
function Dm(e) {
  if (e && e.getAttribute) {
    const t = e.getAttribute("href");
    const n = e.getAttribute("target");
    if (t && t.match(/^\//g) && (!n || n.match(/^_?self$/i))) {
      return Ye(t);
    }
  }
}
function Um(e) {
  if (e.stopImmediatePropagation) {
    e.stopImmediatePropagation();
  }

  if (e.stopPropagation) {
    e.stopPropagation();
  }

  e.preventDefault();
  return false;
}
function Fm(e) {
  if (!(e.ctrlKey || e.metaKey || e.altKey || e.shiftKey || e.button)) {
    let e_target = e.target;
    do {
      if (e_target.localName === "a" && e_target.getAttribute("href")) {
        if (e_target.hasAttribute("data-native") || e_target.hasAttribute("native")) {
          return;
        }
        if (Dm(e_target)) {
          return Um(e);
        }
      }
    } while ((e_target = e_target.parentNode));
  }
}
function sl(e) {
  if (e.history) {
    (Be = e.history);
  }

  (this.state = { url: e.url || us() });
}
nr((sl.prototype = new qe()), {
  shouldComponentUpdate(e) {
    return (e.static !== true ||
    e.url !== this.props.url || e.onChange !== this.props.onChange);
  },
  canRoute(e) {
    const t = Ke(this.props.children);
    return this.g(t, e) !== undefined;
  },
  routeTo(e) {
    this.setState({ url: e });
    const t = this.canRoute(e);

    if (!this.p) {
      this.forceUpdate();
    }

    return t;
  },
  componentWillMount() {
    this.p = true;
  },
  componentDidMount() {
    const e = this;

    if (!Wi) {
      (Wi = true);

      Be ||
        addEventListener("popstate", () => {
          ol(us());
        });

      addEventListener("click", Fm);
    }

    qt.push(this);

    if (Be) {
      (this.u = Be.listen(t => {
          const n = t.location || t;
          e.routeTo(`${n.pathname || ""}${n.search || ""}`);
        }));
    }

    (this.p = false);
  },
  componentWillUnmount() {
    if (typeof this.u == "function") {
      this.u();
    }

    qt.splice(qt.indexOf(this), 1);
  },
  componentWillUpdate() {
    this.p = true;
  },
  componentDidUpdate() {
    this.p = false;
  },
  g(e, t) {
    e = e.filter(Lm).sort(xm);

    for (const r of e) {
      const o = nl(t, r.props.path, r.props);
      if (o) {
        return [r, o];
      }
    }
  },
  render(e, t) {
    let n;
    let r;
    const e_onChange = e.onChange;
    const t_url = t.url;
    let a = this.c;
    const c = this.g(Ke(e.children), t_url);

    if (c) {
      (r = Ma(
            c[0],
            nr(nr({ url: t_url, matches: (n = c[1]) }, n), {
              key: undefined,
              ref: undefined,
            })
          ));
    }

    if (t_url !== (a && a.url)) {
      nr(
        ls,
        (a = this.c =
          {
            url: t_url,
            previous: a && a.url,
            current: r,
            path: r ? r.props.path : null,
            matches: n,
          })
      );

      (a.router = this);
      (a.active = r ? [r] : []);
      for (let l = xn.length; l--; ) {
        xn[l]({});
      }

      if (typeof e_onChange == "function") {
        e_onChange(a);
      }
    }

    return Je(rl.Provider, { value: a }, r);
  },
});

const ji = (e) => {
  let t;
  const n = new Set();

  const r = (u, f) => {
    const d = typeof u == "function" ? u(t) : u;
    if (!Object.is(d, t)) {
      const p = t;

      (t = f ?? (typeof d != "object" || d === null)
        ? d
        : Object.assign({}, t, d));

      n.forEach(h => h(t, p));
    }
  };

  const o = () => t;

  const c = {
    setState: r,
    getState: o,
    getInitialState: () => l,
    subscribe: u => {
      n.add(u);

      return () => n.delete(u);
    },
  };

  const l = (t = e(r, o, c));
  return c;
};

const Hm = e => e ? ji(e) : ji;

const Bm = e => e;

function Vm(e, t = Bm) {
  const n = Rn.useSyncExternalStore(
    e.subscribe,
    Rn.useCallback(() => t(e.getState()), [e, t]),
    Rn.useCallback(() => t(e.getInitialState()), [e, t])
  );
  Rn.useDebugValue(n);
  return n;
}

const zi = (e) => {
  const t = Hm(e);

  const n = r => Vm(t, r);

  Object.assign(n, t);
  return n;
};

const at = e => e ? zi(e) : zi;

function ds(e, t) {
  let n;
  try {
    n = e();
  } catch {
    return;
  }
  return {
    getItem: (o) => {
      let s;

      const a = l => l === null ? null : JSON.parse(l, undefined);

      const c = (s = n.getItem(o)) != null ? s : null;
      return c instanceof Promise ? c.then(a) : a(c);
    },
    setItem: (o, s) => n.setItem(o, JSON.stringify(s, undefined)),
    removeItem: o => n.removeItem(o),
  };
}

const Lo = e => (t) => {
    try {
      const n = e(t);
      return n instanceof Promise
        ? n
        : {
            then(r) {
              return Lo(r)(n);
            },
            catch(r) {
              return this;
            },
          };
    } catch (n) {
      return {
        then(r) {
          return this;
        },
        catch(r) {
          return Lo(r)(n);
        },
      };
    }
  };

const il = (e, t) => (n, r, o) => {
  let s = {
      storage: ds(() => localStorage),
      partialize: g => g,
      version: 0,
      merge: (g, T) => ({
        ...T,
        ...g
      }),
      ...t,
    };

  let a = false;
  const c = new Set();
  const l = new Set();
  let s_storage = s.storage;
  if (!s_storage) {
    return e(
      (...g) => {
        console.warn(
          `[zustand persist middleware] Unable to update item '${s.name}', the given storage is currently unavailable.`
        );

        n(...g);
      },
      r,
      o
    );
  }

  const f = () => {
      const g = s.partialize({ ...r() });
      return s_storage.setItem(s.name, { state: g, version: s.version });
    };

  const o_setState = o.setState;
  o.setState = (g, T) => {
    o_setState(g, T);
    return f();
  };
  const p = e((...g) => {
    n(...g);
    return f();
  }, r, o);
  o.getInitialState = () => p;
  let h;
  const m = () => {
    let T;
    if (!s_storage) {
      return;
    }
    (a = false);

    c.forEach((y) => {
      let w;
      return y((w = r()) != null ? w : p);
    });

    const _ =
      ((T = s.onRehydrateStorage) == null
        ? undefined
        : T.call(s, (g = r()) != null ? g : p)) || undefined;
    return Lo(s_storage.getItem.bind(s_storage))(s.name)
      .then((y) => {
        if (y) {
          if (typeof y.version == "number" && y.version !== s.version) {
            if (s.migrate) {
              const w = s.migrate(y.state, y.version);
              return w instanceof Promise ? w.then(I => [true, I]) : [true, w];
            }
            console.error(
              "State loaded from storage couldn't be migrated since no migrate function was provided"
            );
          } else {
            return [false, y.state];
          }
        }
        return [false, undefined];
      })
      .then((y) => {
      let w;
      const [I, S] = y;
      (h = s.merge(S, (w = r()) != null ? w : p));
      n(h, true);

      if (I) {
        return f();
      }
    })
      .then(() => {
      _?.(h, undefined);
      (h = r());
      (a = true);

      l.forEach(y => y(h));
    })
      .catch((y) => {
        _?.(undefined, y);
      });
  };

  (o.persist = {
    setOptions: (g) => {
      (s = { ...s, ...g });

      if (g.storage) {
        (s_storage = g.storage);
      }
    },
    clearStorage: () => {
      s_storage?.removeItem(s.name);
    },
    getOptions: () => s,
    rehydrate: () => m(),
    hasHydrated: () => a,
    onHydrate: g => {
      c.add(g);

      return () => {
        c.delete(g);
      };
    },
    onFinishHydration: g => {
      l.add(g);

      return () => {
        l.delete(g);
      };
    },
  });

  if (!s.skipHydration) {
    m();
  }

  return h || p;
};

const al = at(e => ({
  isOpen: false,
  images: [],
  initialIndex: 0,
  open: (t, n = 0) => e({ isOpen: true, images: t, initialIndex: n }),
  close: () => e({ isOpen: false, images: [], initialIndex: 0 })
}));

const jm = at(e => ({
  isOpen: false,
  url: null,
  open: t => e({ isOpen: true, url: t }),
  close: () => e({ isOpen: false, url: null })
}));

const Fe = {
  OK: 200,
  CREATED: 201,
  NO_CONTENT: 204,
  BAD_REQUEST: 400,
  UNAUTHORIZED: 401,
  FORBIDDEN: 403,
  NOT_FOUND: 404,
  CONFLICT: 409,
  UNPROCESSABLE_ENTITY: 422,
  TOO_MANY_REQUESTS: 429,
  INTERNAL_SERVER_ERROR: 500,
};

function Pe(e) {
  return (
    e instanceof Error && "status" in e && "code" in e && e.name === "ApiError"
  );
}

const oe = {
    BAD_REQUEST: "BAD_REQUEST",
    UNAUTHORIZED: "UNAUTHORIZED",
    ACCESS_DENIED: "ACCESS_DENIED",
    ENTITY_NOT_FOUND: "ENTITY_NOT_FOUND",
    ENTITY_ALREADY_EXISTS: "ENTITY_ALREADY_EXISTS",
    VALIDATION_ERROR: "VALIDATION_ERROR",
    BUSINESS_RULE_VIOLATION: "BUSINESS_RULE_VIOLATION",
    RATE_LIMIT_EXCEEDED: "RATE_LIMIT_EXCEEDED",
    UNKNOWN_ERROR: "UNKNOWN_ERROR",
    NETWORK_ERROR: "NETWORK_ERROR",
    TIMEOUT: "TIMEOUT",
    CAPTCHA_FAILED: "CAPTCHA_FAILED",
    OTP_INVALID: "OTP_INVALID",
    ACCOUNT_DEACTIVATED: "ACCOUNT_DEACTIVATED",
    ACCOUNT_EMAIL_DOMAIN_NOT_ALLOWED: "ACCOUNT_EMAIL_DOMAIN_NOT_ALLOWED",
    ACCOUNT_INVALID_CREDENTIALS: "ACCOUNT_INVALID_CREDENTIALS",
    ACCOUNT_TEMPORARILY_LOCKED: "ACCOUNT_TEMPORARILY_LOCKED",
    ACCOUNT_CURRENT_PASSWORD_INCORRECT: "ACCOUNT_CURRENT_PASSWORD_INCORRECT",
    SESSION_EXPIRED: "SESSION_EXPIRED",
    SESSION_REVOKED: "SESSION_REVOKED",
    SESSION_INVALID_REFRESH_TOKEN: "SESSION_INVALID_REFRESH_TOKEN",
    MISSING_FLOW_TOKEN: "MISSING_FLOW_TOKEN",
    PROFILE_USERNAME_TAKEN: "PROFILE_USERNAME_TAKEN",
    PROFILE_RESTRICTION_ACTIVE: "PROFILE_RESTRICTION_ACTIVE",
    PROFILE_MODIFICATION_RESTRICTED: "PROFILE_MODIFICATION_RESTRICTED",
    CONTENT_MODERATION_FAILED: "CONTENT_MODERATION_FAILED",
    FILE_TOO_LARGE: "FILE_TOO_LARGE",
    UNSUPPORTED_FILE_TYPE: "UNSUPPORTED_FILE_TYPE",
    UPLOAD_FAILED: "UPLOAD_FAILED",
    VIDEO_REQUIRES_VERIFICATION: "VIDEO_REQUIRES_VERIFICATION",
  };

const zm = 4000/* 4e3 */;

const Sr = at((e, t) => ({
  toasts: [],

  addToast: (n) => {
    const r = `toast-${Date.now()}-${Math.random().toString(36).slice(2)}`;
    const o = n.duration ?? zm;

    e(s => ({
      toasts: [...s.toasts, { ...n, id: r, duration: o }]
    }));

    if (o > 0) {
      setTimeout(() => {
        t().removeToast(r);
      }, o);
    }

    return r;
  },

  removeToast: (n) => {
    e(r => ({
      toasts: r.toasts.filter(o => o.id !== n)
    }));
  }
}));

const mt = {
  success: (e, t) => Sr.getState().addToast({ type: "success", message: e, duration: t }),
  error: (e, t) => Sr.getState().addToast({ type: "error", message: e, duration: t }),
};

function qm() {
  const e = "device_id";
  let t = localStorage.getItem(e);

  if (!t) {
    (t = crypto.randomUUID());
    localStorage.setItem(e, t);
  }

  return t;
}
const qi = qm();
class cl {
  baseURL;
  defaultTimeout;
  defaultHeaders;
  accessToken = null;
  refreshTokenCallback = null;
  onUnauthorizedCallback = null;
  isRefreshing = false;
  refreshPromise = null;
  constructor(t) {
    (this.baseURL = t.baseURL);
    (this.defaultTimeout = t.timeout ?? 30000/* 3e4 */);

    (this.defaultHeaders = {
        "Content-Type": "application/json",
        "X-Requested-With": "XMLHttpRequest",
        ...t.headers,
      });
  }
  setAccessToken(t) {
    this.accessToken = t;
  }
  getAccessToken() {
    return this.accessToken;
  }
  setRefreshTokenCallback(t) {
    this.refreshTokenCallback = t;
  }
  setOnUnauthorizedCallback(t) {
    this.onUnauthorizedCallback = t;
  }
  notifyError(t) {
    if (t.status !== Fe.UNAUTHORIZED) {
      if (t.code === "PHONE_VERIFICATION_REQUIRED") {
        window.dispatchEvent(new Event("phone-verification-required"));
        return;
      }
      if (t.code === "WRITE_ACCESS_RESTRICTED") {
        mt.error("Вы не можете сделать это сегодня. Попробуйте завтра.");
        return;
      }
      mt.error(t.message || "Произошла ошибка");
    }
  }
  buildUrl(t) {
    const n = this.baseURL.replace(/\/$/, "");
    const r = t.startsWith("/") ? t : `/${t}`;
    return `${n}${r}`;
  }
  buildHeaders(t) {
    const n = new Headers({ ...this.defaultHeaders, ...t });

    if (this.accessToken) {
      n.set("Authorization", `Bearer ${this.accessToken}`);
    }

    n.set("X-Device-Id", qi);
    return n;
  }
  async handleResponse(t) {
    if (t.status === Fe.NO_CONTENT) {
      return null;
    }
    let n;
    try {
      n = await t.json();
    } catch {
      if (!t.ok) {
        throw this.createApiError(
          t.status,
          "Invalid response format",
          "INVALID_RESPONSE"
        );
      }
      return null;
    }
    if (!t.ok) {
      const r = n;
      const r_error = r.error;
      const s = r_error && typeof r_error == "object" ? r_error : r;
      let s_errors = s.errors;
      if (s.violations && Array.isArray(s.violations)) {
        s_errors = {};
        for (const c of s.violations) {
          if (!s_errors[c.field]) {
            (s_errors[c.field] = []);
          }

          s_errors[c.field].push(c.message);
        }
      }
      throw this.createApiError(
        t.status,
        s.detail || s.message || s.title || "Request failed",
        s.code || this.mapStatusToErrorCode(t.status),
        s_errors
      );
    }
    return n;
  }
  mapStatusToErrorCode(t) {
    switch (t) {
      case Fe.BAD_REQUEST:
        {
          return oe.BAD_REQUEST;
        }
      case Fe.UNAUTHORIZED:
        {
          return oe.UNAUTHORIZED;
        }
      case Fe.FORBIDDEN:
        {
          return oe.ACCESS_DENIED;
        }
      case Fe.NOT_FOUND:
        {
          return oe.ENTITY_NOT_FOUND;
        }
      case Fe.CONFLICT:
        {
          return oe.ENTITY_ALREADY_EXISTS;
        }
      case Fe.UNPROCESSABLE_ENTITY:
        {
          return oe.VALIDATION_ERROR;
        }
      case Fe.TOO_MANY_REQUESTS:
        {
          return oe.RATE_LIMIT_EXCEEDED;
        }
      default:
        {
          return oe.UNKNOWN_ERROR;
        }
    }
  }
  createApiError(t, n, r, o) {
    const s = new Error(n);
    (s.status = t);
    (s.code = r);
    (s.errors = o);
    (s.name = "ApiError");
    return s;
  }
  async refreshAccessToken() {
    return this.refreshTokenCallback
      ? this.isRefreshing && this.refreshPromise
        ? this.refreshPromise
        : ((this.isRefreshing = true),
          (this.refreshPromise = this.refreshTokenCallback().finally(() => {
      (this.isRefreshing = false);
      (this.refreshPromise = null);
    })),
          this.refreshPromise)
      : null;
  }
  async executeRequest(t, n, r, o, s = false) {
    const a = this.buildUrl(n);
    const c = this.buildHeaders(o?.headers);
    const l = new AbortController();
    const u = o?.timeout ?? this.defaultTimeout;

    const f = setTimeout(() => l.abort(), u);

    try {
      const d = await fetch(a, {
        method: t,
        headers: c,
        body: r ? JSON.stringify(r) : undefined,
        signal: l.signal,
        credentials: "include",
        ...o,
      });
      clearTimeout(f);
      const p =
        n.startsWith("/auth/") ||
        n.startsWith("/sign-") ||
        n.startsWith("/verify-") ||
        n.startsWith("/resend-") ||
        n.startsWith("/refresh") ||
        n.startsWith("/forgot-") ||
        n.startsWith("/reset-") ||
        n.startsWith("/login/");
      if (d.status === Fe.UNAUTHORIZED && !s && !p && this.accessToken) {
        const h = await this.refreshAccessToken();
        if (h) {
          this.setAccessToken(h);
          return this.executeRequest(t, n, r, o, true);
        }
        this.onUnauthorizedCallback?.();

        throw this.createApiError(
          Fe.UNAUTHORIZED,
          "Session expired",
          oe.UNAUTHORIZED
        );
      }
      return await this.handleResponse(d);
    } catch (d) {
      clearTimeout(f);

      if (d instanceof Error) {
        if (d.name === "AbortError") {
          const h = this.createApiError(0, "Request timeout", oe.TIMEOUT);

          if (!s) {
            this.notifyError(h);
          }

          throw h;
        }
        if (d.name === "ApiError") {
          if (!s) {
            this.notifyError(d);
          }

          throw d;
        }
        const p = this.createApiError(
          0,
          d.message || "Network error",
          oe.NETWORK_ERROR
        );

        if (!s) {
          this.notifyError(p);
        }

        throw p;
      }

      throw d;
    }
  }
  async get(t, n) {
    return this.executeRequest("GET", t, undefined, n);
  }
  async post(t, n, r) {
    return this.executeRequest("POST", t, n, r);
  }
  async put(t, n, r) {
    return this.executeRequest("PUT", t, n, r);
  }
  async patch(t, n, r) {
    return this.executeRequest("PATCH", t, n, r);
  }
  async delete(t, n) {
    return this.executeRequest("DELETE", t, undefined, n);
  }
  async head(t, n) {
    const r = this.buildUrl(t);
    const o = this.buildHeaders(n?.headers);
    const s = new AbortController();
    const a = n?.timeout ?? this.defaultTimeout;

    const c = setTimeout(() => s.abort(), a);

    try {
      const l = await fetch(r, {
        method: "HEAD",
        headers: o,
        signal: s.signal,
        credentials: "include",
      });
      clearTimeout(c);
      return l.status;
    } catch (l) {
      clearTimeout(c);

      throw l instanceof Error && l.name === "AbortError"
        ? this.createApiError(0, "Request timeout", oe.TIMEOUT)
        : this.createApiError(0, "Network error", oe.NETWORK_ERROR);
    }
  }
  async uploadFormData(t, n, r, o = false) {
    const s = this.buildUrl(t);
    const a = { "X-Requested-With": "XMLHttpRequest", "X-Device-Id": qi };

    if (this.accessToken) {
      (a.Authorization = `Bearer ${this.accessToken}`);
    }

    const c = new AbortController();
    const l = r?.timeout ?? this.defaultTimeout;

    const u = setTimeout(() => c.abort(), l);

    try {
      const f = await fetch(s, {
        method: "POST",
        headers: a,
        body: n,
        signal: c.signal,
        credentials: "include",
      });
      clearTimeout(u);

      if (f.status === Fe.UNAUTHORIZED && !o && this.accessToken) {
        const d = await this.refreshAccessToken();
        if (d) {
          this.setAccessToken(d);
          return this.uploadFormData(t, n, r, true);
        }
        this.onUnauthorizedCallback?.();

        throw this.createApiError(
          Fe.UNAUTHORIZED,
          "Session expired",
          oe.UNAUTHORIZED
        );
      }

      return await this.handleResponse(f);
    } catch (f) {
      clearTimeout(u);

      if (f instanceof Error) {
        if (f.name === "AbortError") {
          const p = this.createApiError(0, "Request timeout", oe.TIMEOUT);

          if (!o) {
            this.notifyError(p);
          }

          throw p;
        }
        if (f.name === "ApiError") {
          if (!o) {
            this.notifyError(f);
          }

          throw f;
        }
        const d = this.createApiError(
          0,
          f.message || "Network error",
          oe.NETWORK_ERROR
        );

        if (!o) {
          this.notifyError(d);
        }

        throw d;
      }

      throw f;
    }
  }
}
const P = new cl({ baseURL: "/api", timeout: 30000/* 3e4 */ });
const Ce = new cl({ baseURL: "/api/v1/auth", timeout: 30000/* 3e4 */ });

const fs = at((e, t) => ({
  portal: { active: false },
  loaded: false,

  fetchPortal: async () => {
    if (!t().loaded) {
      try {
        const n = await P.get("/v1/portal");
        e({ portal: n, loaded: true });
      } catch {
        e({ loaded: true });
      }
    }
  }
}));

const ll = () => fs(e => e.portal);

const Hn = new Set();
let Ln = null;
const Gm = 30000/* 3e4 */;
function Ym() {
  if (Ln === null) {
    (Ln = window.setInterval(() => {
        Hn.forEach(e => e());
      }, Gm));
  }
}
function Xm() {
  if (Ln !== null) {
    clearInterval(Ln);
    (Ln = null);
  }
}
function Km(e) {
  Hn.add(e);

  if (Hn.size === 1) {
    Ym();
  }
}
function Zm(e) {
  Hn.delete(e);

  if (Hn.size === 0) {
    Xm();
  }
}
function Zr(e) {
  const t = Date.now();
  const n = Math.floor((t - e.getTime()) / 1000/* 1e3 */);
  return n < 60
    ? "сейчас"
    : n < 3600
    ? `${Math.floor(n / 60)} мин.`
    : n < 86400
    ? `${Math.floor(n / 3600)} ч.`
    : n < 604800
    ? `${Math.floor(n / 86400)} дн.`
    : n < 2419200
    ? `${Math.floor(n / 604800)} нед.`
    : e.toLocaleDateString("ru-RU", { day: "numeric", month: "short" });
}
function ul(e) {
  const t = new Date(e).getTime();
  const n = !isNaN(t);
  const r = n ? t : 0;

  const [o, s] = A(() => n ? Zr(new Date(r)) : "");

  D(() => {
    if (!n) {
      s("");
      return;
    }
    const a = new Date(r);
    s(Zr(a));
    const c = () => {
      s(Zr(a));
    };
    Km(c);

    return () => Zm(c);
  }, [r, n]);

  return o;
}
const Gi = 1174;
function Ot() {
  const [e, t] = A(() => typeof window === "undefined" ? false : window.innerWidth < Gi);

  D(() => {
    const n = window.matchMedia(`(max-width: ${Gi - 1}px)`);

    const r = (o) => {
      t(o.matches);
    };

    t(n.matches);
    n.addEventListener("change", r);

    return () => {
      n.removeEventListener("change", r);
    };
  }, []);

  return e;
}
const Jm = fn({ isHidden: false });

const Qm = () => {
  const [e, t] = A(false);
  const n = N(0);

  D(() => {
    const r = () => {
      const window_scrollY = window.scrollY;
      const s = window_scrollY - n.current;

      if (s > 10 && window_scrollY > 50) {
        t(true);
      } else if (s < -10) {
        t(false);
      }

      (n.current = window_scrollY);
    };
    window.addEventListener("scroll", r, { passive: true });

    return () => window.removeEventListener("scroll", r);
  }, []);

  return e;
};

function $r(e = "", t = []) {
  const [n, r] = A(e);
  const [o, s] = A(t);
  const a = N(null);

  const c = k((f, d) => {
    r(f);
    s(d);
  }, []);

  const l = k((f) => {
    a.current?.insertText(f);
  }, []);

  const u = k(() => {
    r("");
    s([]);
  }, []);

  return {
    text: n,
    spans: o,
    editorRef: a,
    handleChange: c,
    insertText: l,
    reset: u,
    setText: r,
    setSpans: s,
  };
}
function eg({
  sentinelRef: e,
  hasMore: t,
  isLoading: n,
  onLoadMore: r,
  rootMargin: o = "100px",
}) {
  D(() => {
    if (!t || n) {
      return;
    }
    const e_current = e.current;
    if (!e_current) {
      return;
    }
    const a = new IntersectionObserver(
      (c) => {
        if (c[0].isIntersecting) {
          r();
        }
      },
      { rootMargin: o }
    );
    a.observe(e_current);

    return () => a.disconnect();
  }, [t, n, r, o, e]);
}
function tg({
  itemCount: e,
  estimatedItemHeight: t,
  overscan: n = 5,
  gap: r = 0,
  getItemKey: o = l => l,
  initialMeasuredHeights: s,
  scrollElement: a,
  initialScrollTop: c,
}) {
  const [, l] = A(0);

  const u = () => a
    ? Math.max(0, a.scrollTop)
    : c !== undefined
    ? c
    : typeof window !== "undefined"
    ? Math.max(0, window.scrollY)
    : 0;

  const f = () => a ? a.clientHeight : typeof window !== "undefined" ? window.innerHeight : 0;

  const d = N(s ?? new Map());
  const p = N(null);
  const h = N(null);
  const m = N(new Map());

  const g = v => d.current.get(v) ?? t;

  const T = (v) => {
    let C = 0;
    for (let b = 0; b < v; b++) {
      C += g(b) + r;
    }
    return C;
  };

  const _ = () => {
    if (e === 0) {
      return 0;
    }
    let v = 0;
    for (let C = 0; C < e; C++) {
      v += g(C);
    }
    (v += Math.max(0, e - 1) * r);
    return v;
  };

  const y = () => {
    if (e === 0) {
      return { start: 0, end: 0 };
    }
    const v = u();
    const C = f();
    let b = 0;
    let E = 0;
    for (let $ = 0; $ < e; $++) {
      const V = g($) + r;
      if (E + V > v) {
        b = $;
        break;
      }
      E += V;
    }
    let R = b;
    let O = 0;
    for (let $ = b; $ < e && ((O += g($) + r), (R = $), !(O >= C)); $++)
      {}
    return { start: Math.max(0, b - n), end: Math.min(e - 1, R + n) };
  };

  const w = () => {
    if (e === 0) {
      return [];
    }
    const { start, end } = y();
    const b = [];
    for (let E = start; E <= end; E++) {
      b.push({ index: E, key: o(E), start: T(E) });
    }
    return b;
  };

  if (!h.current) {
    (h.current = new ResizeObserver((v) => {
      let C = false;
      for (const b of v) {
        const b_target = b.target;
        const R = m.current.get(b_target);
        if (R === undefined) {
          continue;
        }
        const O = b.contentRect.height;

        if (O > 0 && d.current.get(R) !== O) {
          d.current.set(R, O);
          (C = true);
        }
      }

      if (C) {
        l(b => b + 1);
      }
    }));
  }

  const I = k((v, C) => {
    if (!v) {
      return;
    }
    m.current.set(v, C);
    h.current?.observe(v);
    const b = v.getBoundingClientRect().height;

    if (b > 0 && d.current.get(C) !== b) {
      d.current.set(C, b);
      l(E => E + 1);
    }
  }, []);

  vn(() => {
    const v = a ?? window;

    const C = () => {
      if (!p.current) {
        (p.current = requestAnimationFrame(() => {
          (p.current = null);

          l(b => b + 1);
        }));
      }
    };

    v.addEventListener("scroll", C, { passive: true });

    l(b => b + 1);

    return () => {
      v.removeEventListener("scroll", C);

      if (p.current) {
        cancelAnimationFrame(p.current);
      }
    };
  }, [a]);

  D(
    () => () => {
      h.current?.disconnect();
      m.current.clear();
    },
    []
  );

  const S = k(() => new Map(d.current), []);
  return {
    virtualItems: w(),
    totalSize: _(),
    measureElement: I,
    getMeasuredHeights: S,
  };
}

const ng = ({ size: e = 18 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  strokeWidth: "2",
  strokeLinecap: "round",
  strokeLinejoin: "round",
  children: [
    i("path", { d: "M6 4h8a4 4 0 0 1 4 4 4 4 0 0 1-4 4H6z" }),
    i("path", { d: "M6 12h9a4 4 0 0 1 4 4 4 4 0 0 1-4 4H6z" }),
  ],
});

const rg = ({ size: e = 18 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  strokeWidth: "2",
  strokeLinecap: "round",
  strokeLinejoin: "round",
  children: [
    i("polyline", { points: "16 18 22 12 16 6" }),
    i("polyline", { points: "8 6 2 12 8 18" }),
  ],
});

const og = ({ size: e = 18 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  strokeWidth: "2",
  strokeLinecap: "round",
  strokeLinejoin: "round",
  children: [
    i("line", { x1: "19", y1: "4", x2: "10", y2: "4" }),
    i("line", { x1: "14", y1: "20", x2: "5", y2: "20" }),
    i("line", { x1: "15", y1: "4", x2: "9", y2: "20" }),
  ],
});

const dl = ({ size: e = 18 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  strokeWidth: "2",
  strokeLinecap: "round",
  strokeLinejoin: "round",
  children: [
    i("path", {
      d: "M10 13a5 5 0 0 0 7.54.54l3-3a5 5 0 0 0-7.07-7.07l-1.72 1.71",
    }),
    i("path", {
      d: "M14 11a5 5 0 0 0-7.54-.54l-3 3a5 5 0 0 0 7.07 7.07l1.71-1.71",
    }),
  ],
});

const sg = ({ size: e = 18 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "currentColor",
  children: i("path", {
    d: "M10 8c-2.2 0-4 1.8-4 4v6h6v-6H8c0-1.1.9-2 2-2V8zm8 0c-2.2 0-4 1.8-4 4v6h6v-6h-4c0-1.1.9-2 2-2V8z",
  }),
});

const ig = ({ size: e = 18 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  strokeWidth: "2",
  strokeLinecap: "round",
  strokeLinejoin: "round",
  children: [
    i("path", {
      d: "M17.94 17.94A10.07 10.07 0 0 1 12 20c-7 0-11-8-11-8a18.45 18.45 0 0 1 5.06-5.94",
    }),
    i("path", {
      d: "M9.9 4.24A9.12 9.12 0 0 1 12 4c7 0 11 8 11 8a18.5 18.5 0 0 1-2.16 3.19",
    }),
    i("line", { x1: "1", y1: "1", x2: "23", y2: "23" }),
    i("path", { d: "M14.12 14.12a3 3 0 1 1-4.24-4.24" }),
  ],
});

const ag = ({ size: e = 18 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  strokeWidth: "2",
  strokeLinecap: "round",
  strokeLinejoin: "round",
  children: [
    i("path", { d: "M16 4H9a3 3 0 0 0-3 3c0 1.66 1.34 3 3 3h6" }),
    i("path", { d: "M8 20h7a3 3 0 0 0 3-3c0-1.66-1.34-3-3-3H4" }),
    i("line", { x1: "4", y1: "12", x2: "20", y2: "12" }),
  ],
});

const cg = ({ size: e = 18 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  strokeWidth: "2",
  strokeLinecap: "round",
  strokeLinejoin: "round",
  children: [
    i("path", { d: "M6 3v7a6 6 0 0 0 6 6 6 6 0 0 0 6-6V3" }),
    i("line", { x1: "4", y1: "21", x2: "20", y2: "21" }),
  ],
});

const lg = ({ size: e = 18 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  fill: "none",
  viewBox: "0 0 18 18",
  children: i("g", {
    stroke: "currentColor",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    strokeWidth: "2",
    children: [
      i("path", {
        d: "M9 2c-.53 0-1.04.219-1.414.608C7.21 2.998 7 3.526 7 4.077v4.846c0 .55.21 1.08.586 1.469.375.39.884.608 1.414.608.53 0 1.04-.219 1.414-.608.375-.39.586-.918.586-1.469V4.077c0-.55-.21-1.08-.586-1.469A1.963 1.963 0 0 0 9 2Z",
      }),
      i("path", {
        d: "M14 8v1.333c0 1.238-.527 2.425-1.464 3.3C11.598 13.508 10.326 14 9 14s-2.598-.492-3.536-1.367C4.527 11.758 4 10.571 4 9.333V8M9 14v2",
      }),
    ],
  }),
});

const ug = ({ size: e = 24 }) => i("svg", {
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "currentColor",
  children: i("path", { d: "M8 5v14l11-7z" }),
});

const dg = ({ size: e = 24 }) => i("svg", {
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "none",
  xmlns: "http://www.w3.org/2000/svg",
  children: [
    i("path", {
      d: "M5 12L12 5L19 12",
      stroke: "currentColor",
      strokeWidth: "2",
      strokeLinecap: "round",
      strokeLinejoin: "round",
    }),
    i("path", {
      d: "M12 19V5",
      stroke: "currentColor",
      strokeWidth: "2",
      strokeLinecap: "round",
      strokeLinejoin: "round",
    }),
  ],
});

const fl = ({ size: e = 20 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  fill: "none",
  viewBox: "0 0 20 20",
  children: i("path", {
    stroke: "currentColor",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    strokeWidth: "1.833",
    d: "m17.867 9.208-7.659 7.659a5.003 5.003 0 1 1-7.075-7.075l7.659-7.659a3.335 3.335 0 1 1 4.716 4.717l-7.666 7.658a1.667 1.667 0 1 1-2.359-2.358l7.075-7.067",
  }),
});

const fg = ({ size: e = 8 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  fill: "none",
  viewBox: "0 0 8 8",
  children: [
    i("g", {
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "2",
      clipPath: "url(#af)",
      children: i("path", { d: "M1 4h6M4 1v6" }),
    }),
    i("defs", {
      children: i("clipPath", {
        id: "af",
        children: i("path", { fill: "#fff", d: "M0 0h8v8H0z" }),
      }),
    }),
  ],
});

const pg = ({ size: e = 8 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  fill: "none",
  viewBox: "0 0 8 8",
  children: i("path", {
    stroke: "currentColor",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    strokeWidth: "2",
    d: "M1 4h6",
  }),
});

const hg = () => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: "36",
  height: "18",
  fill: "none",
  children: [
    i("path", { fill: "currentColor", d: "M12 3V0h12v3h-4v11h-4V3h-4Z" }),
    i("path", {
      fill: "currentColor",
      d: "M12 3V0h12v3h-4v11h-4V3h-4ZM9 0 3 9V0H0v14h3l6-9v9h3V0H9Z",
    }),
    i("path", {
      fill: "currentColor",
      "fill-rule": "evenodd",
      d: "M34 11h2v7h-3v-4h-9v4h-3v-7c3 0 3-4 3-11h10v11Zm-7-8v8h4V3h-4Z",
      "clip-rule": "evenodd",
    }),
  ],
});

const ot = ({ size: e = 24 }) => i("svg", {
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "none",
  xmlns: "http://www.w3.org/2000/svg",
  children: [
    i("path", {
      d: "M18 6L6 18",
      stroke: "currentColor",
      strokeWidth: "2",
      strokeLinecap: "round",
      strokeLinejoin: "round",
    }),
    i("path", {
      d: "M6 6L18 18",
      stroke: "currentColor",
      strokeWidth: "2",
      strokeLinecap: "round",
      strokeLinejoin: "round",
    }),
  ],
});

const ps = ({ filled: e = false, size: t = 20 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: t,
  height: t,
  fill: "none",
  viewBox: "0 0 20 20",
  children: i("path", {
    stroke: "currentColor",
    strokeWidth: 2,
    d: "M14.953 5.046c-2.73-2.728-7.173-2.728-9.903 0-2.07 2.07-2.634 5.247-1.41 7.888.136.336.232.59.232.798 0 .247-.105.553-.205.849-.195.573-.416 1.222.058 1.696.475.475 1.125.251 1.697.055.294-.1.598-.205.84-.205.215 0 .486.109.798.235a7.034 7.034 0 0 0 7.893-1.412c2.73-2.73 2.73-7.172 0-9.904Z",
    clipRule: "evenodd",
    fill: e ? "currentColor" : "none",
  }),
});

const pl = ({ size: e = 18 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  strokeWidth: "2",
  strokeLinecap: "round",
  strokeLinejoin: "round",
  children: [
    i("path", {
      d: "M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7",
    }),
    i("path", {
      d: "M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z",
    }),
  ],
});

const mg = ({ size: e = 24 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  fill: "none",
  viewBox: "0 0 24 24",
  children: [
    i("circle", {
      cx: "12",
      cy: "12",
      r: "10",
      stroke: "currentColor",
      strokeWidth: "2",
    }),
    i("path", {
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeWidth: "2",
      d: "M15 9l-6 6m0-6l6 6",
    }),
  ],
});

const hl = () => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: "24",
  height: "24",
  fill: "none",
  children: i("path", {
    fill: "currentColor",
    "fill-rule": "evenodd",
    d: "M20.689 10.968a2.806 2.806 0 0 0-2.244-1.108H5.555c-.887 0-1.705.404-2.244 1.107a2.808 2.808 0 0 0-.485 2.455l1.65 6.112a2.83 2.83 0 0 0 2.729 2.09h9.589a2.832 2.832 0 0 0 2.729-2.09l1.65-6.111a2.804 2.804 0 0 0-.484-2.455ZM8.436 3.875h7.125a.75.75 0 0 0 0-1.5H8.436a.75.75 0 0 0 0 1.5ZM5.682 7.253h12.634a.75.75 0 0 0 0-1.5H5.682a.75.75 0 0 0 0 1.5Z",
    "clip-rule": "evenodd",
  }),
});

const ml = ({ size: e = 18 }) => i("svg", {
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "none",
  xmlns: "http://www.w3.org/2000/svg",
  children: i("path", {
    fillRule: "evenodd",
    clipRule: "evenodd",
    d: "M17.0463 8.361L19.6973 3.35C19.8203 3.118 19.8133 2.839 19.6773 2.613C19.5413 2.387 19.2973 2.25 19.0343 2.25H4.96533C4.55133 2.25 4.21533 2.586 4.21533 3V21C4.21533 21.414 4.55133 21.75 4.96533 21.75C5.37933 21.75 5.71533 21.414 5.71533 21V14.544L19.0443 14.365C19.3073 14.361 19.5483 14.221 19.6813 13.995C19.8143 13.768 19.8183 13.489 19.6943 13.258L17.0463 8.361Z",
    fill: "currentColor",
  }),
});

const gl = ({ size: e = 24 }) => i("svg", {
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  strokeWidth: "1.5",
  strokeLinecap: "round",
  strokeLinejoin: "round",
  children: [
    i("rect", {
      x: "3",
      y: "3",
      width: "18",
      height: "18",
      rx: "2",
      ry: "2",
    }),
    i("circle", { cx: "8.5", cy: "8.5", r: "1.5" }),
    i("polyline", { points: "21 15 16 10 5 21" }),
  ],
});

const Dr = ({ filled: e = false, size: t = 20, className: n }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: t,
  height: t,
  viewBox: "0 0 20 20",
  fill: "none",
  className: n,
  children: i("path", {
    fill: e ? "currentColor" : "none",
    stroke: "currentColor",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    strokeWidth: "1.667",
    d: "M10 4.583C8.75 2.917 6.25 2.5 4.583 3.75 2.917 5 2.083 7.5 3.333 10S10 16.667 10 16.667 15.417 12.5 16.667 10s0-5-1.667-6.25-4.167-.833-5 .833Z",
  }),
});

const hs = ({ size: e = 24 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  strokeWidth: "3",
  strokeLinecap: "round",
  strokeLinejoin: "round",
  style: { animation: "spin 1s linear infinite" },
  children: i("path", { d: "M19 12a7 7 0 1 1-4.83-6.66" }),
});

const gg = ({ size: e = 24 }) => i("svg", {
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "none",
  xmlns: "http://www.w3.org/2000/svg",
  children: [
    i("path", {
      d: "M9 21H5C4.46957 21 3.96086 20.7893 3.58579 20.4142C3.21071 20.0391 3 19.5304 3 19V5C3 4.46957 3.21071 3.96086 3.58579 3.58579C3.96086 3.21071 4.46957 3 5 3H9",
      stroke: "currentColor",
      strokeWidth: "2",
      strokeLinecap: "round",
      strokeLinejoin: "round",
    }),
    i("path", {
      d: "M16 17L21 12L16 7",
      stroke: "currentColor",
      strokeWidth: "2",
      strokeLinecap: "round",
      strokeLinejoin: "round",
    }),
    i("path", {
      d: "M21 12H9",
      stroke: "currentColor",
      strokeWidth: "2",
      strokeLinecap: "round",
      strokeLinejoin: "round",
    }),
  ],
});

const _l = ({ size: e = 18 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  fill: "none",
  viewBox: "0 0 18 18",
  children: i("path", {
    fill: "currentColor",
    stroke: "currentColor",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    strokeWidth: "2",
    d: "M9 9.75a.75.75 0 1 0 0-1.5.75.75 0 0 0 0 1.5ZM14.25 9.75a.75.75 0 1 0 0-1.5.75.75 0 0 0 0 1.5ZM3.75 9.75a.75.75 0 1 0 0-1.5.75.75 0 0 0 0 1.5Z",
  }),
});

const yl = ({ size: e = 24 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  fill: "none",
  viewBox: "0 0 24 24",
  children: i("path", {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M19.742 13.807c-.86-1.832-.837-2.52-.798-3.773.01-.296.02-.617.02-.986C18.964 6.122 16.804 2 12 2 7.197 2 5.036 6.122 5.036 9.048c0 .368.01.69.02.986.04 1.252.062 1.941-.807 3.797-.372.928-.327 1.73.135 2.382C5.492 17.783 8.7 18 12 18s6.508-.216 7.616-1.787c.463-.653.508-1.454.125-2.406Zm-4.686 5.198c-1.848.193-3.852.192-6.13-.002a.873.873 0 0 0-.835.437.763.763 0 0 0 .125.893C9.236 21.407 10.578 22 11.994 22h.002c1.42 0 2.765-.592 3.788-1.667a.765.765 0 0 0 .122-.9c-.162-.294-.495-.458-.85-.428Z",
    clipRule: "evenodd",
  }),
});

const _g = ({ size: e = 20 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  viewBox: "0 0 20 20",
  fill: "none",
  children: i("path", {
    fill: "currentColor",
    d: "M10.004 1C14.92 1 18.976 4.61 19 8.955c0 2.747-2.255 5-5.002 5h-1.797a1.477 1.477 0 0 0-1.502 1.501c0 .426.134.753.395 1.013.231.26.393.618.393 1.011 0 .848-.65 1.52-1.483 1.52C5.052 19 1 14.95 1 10s4.052-9 9.004-9ZM5.25 9a1.25 1.25 0 1 0 0 2.5 1.25 1.25 0 0 0 0-2.5Zm9-2a1.25 1.25 0 1 0 0 2.5 1.25 1.25 0 0 0 0-2.5Zm-7-2a1.25 1.25 0 1 0 0 2.5 1.25 1.25 0 0 0 0-2.5Zm4-1a1.25 1.25 0 1 0 0 2.5 1.25 1.25 0 0 0 0-2.5Z",
  }),
});

const vl = ({ size: e = 24 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  fill: "none",
  viewBox: "0 0 24 24",
  children: i("path", {
    fill: "currentColor",
    fillRule: "evenodd",
    d: "M11.998 11a3.996 3.996 0 0 0 4-4c.084-2.213-1.702-4-4-4A3.995 3.995 0 0 0 8 7c0 2.213 1.787 4 3.998 4Zm6.94 6.878c-.3-1.04-.9-1.986-2.097-2.743C15.843 14.473 14.246 14 12.05 14c-4.292 0-6.39 1.892-6.987 3.878-.2.568.1 1.136.598 1.42C7.458 20.431 9.654 21 12.05 21c2.296 0 4.492-.662 6.288-1.703.5-.284.8-.851.6-1.419Z",
    clipRule: "evenodd",
  }),
});

const Yi = ({ size: e = 18 }) => i("svg", {
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "none",
  xmlns: "http://www.w3.org/2000/svg",
  children: i("path", {
    fillRule: "evenodd",
    clipRule: "evenodd",
    d: "M20.0397 9.25349L14.7397 3.95349C13.9837 3.19649 12.6657 3.19649 11.9097 3.95349L11.3187 4.54549C10.7487 5.11449 10.5767 5.96749 10.8957 6.75249C11.0497 7.12649 10.9647 7.55249 10.6797 7.83949L9.34373 9.17449C9.22773 9.28849 9.08673 9.37449 8.93473 9.42249L5.77073 10.4125C5.46773 10.5085 5.18573 10.6795 4.95673 10.9065C4.57773 11.2855 4.36973 11.7875 4.36973 12.3225C4.36973 12.8575 4.57873 13.3585 4.95673 13.7355L7.07573 15.8545L3.59573 19.3345C3.30273 19.6275 3.30273 20.1025 3.59573 20.3955C3.74173 20.5415 3.93373 20.6145 4.12573 20.6145C4.31773 20.6145 4.50973 20.5415 4.65573 20.3955L8.13573 16.9145L10.2577 19.0365C10.6467 19.4255 11.1587 19.6195 11.6707 19.6195C12.1837 19.6195 12.6957 19.4245 13.0867 19.0355C13.3147 18.8055 13.4847 18.5235 13.5797 18.2205L14.5687 15.0605C14.6187 14.9045 14.7037 14.7635 14.8167 14.6505L16.1537 13.3125C16.4387 13.0265 16.8627 12.9415 17.2737 13.1085C18.0197 13.4155 18.8747 13.2465 19.4477 12.6745L20.0397 12.0815C20.8187 11.3015 20.8187 10.0325 20.0397 9.25349Z",
    fill: "currentColor",
  }),
});

const ms = ({ size: e = 24 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  fill: "none",
  viewBox: "0 0 24 24",
  children: i("path", {
    stroke: "currentColor",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    strokeWidth: "2",
    d: "M5 12h14M12 5v14",
  }),
});

const yg = ({ size: e = 20 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  fill: "none",
  viewBox: "0 0 24 24",
  children: i("path", {
    stroke: "currentColor",
    "stroke-linecap": "round",
    "stroke-linejoin": "round",
    "stroke-width": "2",
    d: "M18 20V10M12 20V4M6 20v-6",
  }),
});

const br = ({ size: e = 20 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  fill: "none",
  viewBox: "0 0 24 24",
  children: [
    i("path", {
      stroke: "currentColor",
      "stroke-linecap": "round",
      "stroke-linejoin": "round",
      "stroke-width": "2",
      d: "m2 9 3-3 3 3",
    }),
    i("path", {
      stroke: "currentColor",
      "stroke-linecap": "round",
      "stroke-linejoin": "round",
      "stroke-width": "2",
      d: "M13 18H7a2 2 0 0 1-2-2V6M22 15l-3 3-3-3",
    }),
    i("path", {
      stroke: "currentColor",
      "stroke-linecap": "round",
      "stroke-linejoin": "round",
      "stroke-width": "2",
      d: "M11 6h6a2 2 0 0 1 2 2v10",
    }),
  ],
});

const wl = ({ size: e = 24 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "none",
  children: i("path", {
    stroke: "currentColor",
    strokeLinecap: "round",
    strokeLinejoin: "round",
    strokeWidth: "3",
    d: "m19.5 19.5-3-3M11 4.5a6.5 6.5 0 1 1 0 13 6.5 6.5 0 0 1 0-13Z",
  }),
});

const vg = ({ size: e = 20, color: t = "currentColor" }) => i("svg", {
  width: e,
  height: e,
  viewBox: "0 0 20 20",
  fill: "none",
  xmlns: "http://www.w3.org/2000/svg",
  children: [
    i("path", {
      d: "M10 17.5a7.5 7.5 0 1 0 0-15 7.5 7.5 0 0 0 0 15Z",
      stroke: t,
      strokeWidth: "2",
      strokeLinecap: "round",
      strokeLinejoin: "round",
    }),
    i("path", {
      d: "M7.6 11.908c.585.76 1.445 1.234 2.4 1.234.956 0 1.816-.474 2.4-1.234M7.308 7.504v-.043m-.038-.127a.188.188 0 1 0 .002.374.188.188 0 0 0-.002-.374ZM12.692 7.504v-.043m-.005-.127a.188.188 0 1 0 .002.374.188.188 0 0 0-.002-.374Z",
      stroke: t,
      strokeWidth: "2",
      strokeLinecap: "round",
      strokeLinejoin: "round",
    }),
  ],
});

const wg = ({ size: e = 24 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  fill: "none",
  viewBox: "0 0 24 24",
  children: [
    i("circle", {
      cx: "12",
      cy: "12",
      r: "10",
      stroke: "currentColor",
      strokeWidth: "2",
    }),
    i("path", {
      stroke: "currentColor",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "2",
      d: "M8 12l3 3 5-6",
    }),
  ],
});

const Eg = ({ size: e = 48 }) => i("svg", {
  width: e,
  height: e,
  viewBox: "0 0 48 48",
  fill: "none",
  xmlns: "http://www.w3.org/2000/svg",
  children: [
    i("circle", {
      cx: "24",
      cy: "24",
      r: "24",
      fill: "#2AABEE",
      fillOpacity: "0.12",
    }),
    i("svg", {
      x: "8",
      y: "8",
      width: "32",
      height: "32",
      viewBox: "0 0 1000 1000",
      children: i("path", {
        d: "M226.328419,494.722069 C372.088573,431.216685 469.284839,389.350049 517.917216,369.122161 C656.772535,311.36743 685.625481,301.334815 704.431427,301.003532 C708.567621,300.93067 717.815839,301.955743 723.806446,306.816707 C728.864797,310.92121 730.256552,316.46581 730.922551,320.357329 C731.588551,324.248848 732.417879,333.113828 731.758626,340.040666 C724.234007,419.102486 691.675104,610.964674 675.110982,699.515267 C668.10208,736.984342 654.301336,749.547532 640.940618,750.777006 C611.904684,753.448938 589.856115,731.588035 561.733393,713.153237 C517.726886,684.306416 492.866009,666.349181 450.150074,638.200013 C400.78442,605.66878 432.786119,587.789048 460.919462,558.568563 C468.282091,550.921423 596.21508,434.556479 598.691227,424.000355 C599.00091,422.680135 599.288312,417.758981 596.36474,415.160431 C593.441168,412.561881 589.126229,413.450484 586.012448,414.157198 C581.598758,415.158943 511.297793,461.625274 375.109553,553.556189 C355.154858,567.258623 337.080515,573.934908 320.886524,573.585046 C303.033948,573.199351 268.692754,563.490928 243.163606,555.192408 C211.851067,545.013936 186.964484,539.632504 189.131547,522.346309 C190.260287,513.342589 202.659244,504.134509 226.328419,494.722069 Z",
        fill: "#2AABEE",
      }),
    }),
  ],
});

const El = ({ size: e = 18 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  viewBox: "0 0 24 24",
  fill: "none",
  stroke: "currentColor",
  strokeWidth: "2",
  strokeLinecap: "round",
  strokeLinejoin: "round",
  children: [
    i("polyline", { points: "3 6 5 6 21 6" }),
    i("path", {
      d: "M19 6v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6m3 0V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2",
    }),
    i("line", { x1: "10", y1: "11", x2: "10", y2: "17" }),
    i("line", { x1: "14", y1: "11", x2: "14", y2: "17" }),
  ],
});

const Sg = ({ size: e = 16 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  fill: "none",
  viewBox: "0 0 16 16",
  children: [
    i("path", {
      fill: "#0080FF",
      d: "M6.724.821a1.63 1.63 0 0 1 2.858.051l.556 1.042a1.634 1.634 0 0 0 1.672.856l1.155-.166c1.263-.181 2.238 1.108 1.742 2.303L14.253 6a1.69 1.69 0 0 0 .385 1.863l.847.815c.927.891.544 2.47-.685 2.821l-1.122.32a1.663 1.663 0 0 0-1.192 1.468l-.098 1.181c-.108 1.294-1.56 1.974-2.596 1.216l-.946-.693a1.62 1.62 0 0 0-1.872-.033l-.969.658c-1.06.721-2.49-.01-2.552-1.306l-.058-1.184a1.666 1.666 0 0 0-1.141-1.51l-1.11-.36C-.073 10.864-.402 9.272.556 8.413l.874-.783a1.69 1.69 0 0 0 .448-1.849l-.416-1.108c-.454-1.212.565-2.466 1.821-2.24l1.148.207a1.632 1.632 0 0 0 1.7-.796L6.724.82Z",
    }),
    i("path", {
      stroke: "#fff",
      strokeLinecap: "round",
      strokeLinejoin: "round",
      strokeWidth: "1.333",
      d: "M10.667 6.667 7.11 10.222 5.334 8.444",
    }),
  ],
});

const Sl = ({ size: e = 20 }) => i("svg", {
  xmlns: "http://www.w3.org/2000/svg",
  width: e,
  height: e,
  fill: "none",
  viewBox: "0 0 20 20",
  children: [
    i("path", {
      stroke: "currentColor",
      strokeWidth: "1.5",
      d: "M2 10s2.91-6 8-6 8 6 8 6-2.91 6-8 6-8-6-8-6Z",
    }),
    i("path", {
      stroke: "currentColor",
      strokeWidth: "1.5",
      d: "M10 12a2 2 0 1 0 0-4 2 2 0 0 0 0 4Z",
    }),
  ],
});

function bg(e) {
  const t = Xn()[0];
  return e.children({
    url: t.url,
    path: t.path,
    matches: nl(t.path || t.url, e.path, {}) !== false,
  });
}
const Cg = "sAbT";
const Tg = "wb03";
const Ig = "VwNT";
const kg = "ZQJP";
const Ng = "efuY";
const Rg = "J8iq";
const Pg = "A45D";
const Ag = "q62g";
const Og = "kEfY";
const xg = "ZPao";
const Lg = "qe1B";
const Mg = "xpde";
const $g = "pe3e";

const Oe = {
  aside: Cg,
  asideBottom: Tg,
  logoutButton: Ig,
  asideBrand: kg,
  asideBrandVersion: Ng,
  nav: Rg,
  navItem: Pg,
  active: Ag,
  iconWrapper: Og,
  portalButton: xg,
  portalActive: Lg,
  portalImage: Mg,
  badge: $g,
};

const ue = {
  HOME: "/",
  LOGIN: "/login",
  REGISTER: "/register",
  FORGOT_PASSWORD: "/forgot-password",
  VERIFY_EMAIL: "/verify-email",
  RESET_PASSWORD: "/reset-password",
  ONBOARDING: "/onboarding",
  TERMS: "/terms",
  PRIVACY: "/privacy",
  COOKIES: "/cookies",
  EXTERNAL: "/external",
  SUPPORT: "/support",
  CHILD_SAFETY: "/child-safety",
  SUBSCRIPTION_TERMS: "/subscription-terms",
};

const Xi = [
  ue.LOGIN,
  ue.REGISTER,
  ue.FORGOT_PASSWORD,
  ue.RESET_PASSWORD,
  ue.VERIFY_EMAIL,
  ue.TERMS,
  ue.PRIVACY,
  ue.COOKIES,
  ue.EXTERNAL,
  ue.SUPPORT,
  ue.CHILD_SAFETY,
  ue.SUBSCRIPTION_TERMS,
];

const gs = [
  ue.LOGIN,
  ue.REGISTER,
  ue.FORGOT_PASSWORD,
  ue.RESET_PASSWORD,
  ue.VERIFY_EMAIL,
  ue.ONBOARDING,
];

const x = {
  auth: {
    signUp: "/sign-up",
    signIn: "/sign-in",
    verifyOtp: "/verify-otp",
    resendOtp: "/resend-otp",
    refresh: "/refresh",
    logout: "/logout",
    changePassword: "/change-password",
    forgotPassword: "/forgot-password",
    resetPassword: "/reset-password",
    loginYandex: "/login/yandex",
    loginGoogle: "/login/google",
  },
  users: {
    me: "/users/me",
    profile: e => `/users/${e}`,
    updateProfile: "/users/me",
    privacy: "/users/me/privacy",
    follow: e => `/users/${e}/follow`,
    followers: e => `/users/${e}/followers`,
    following: e => `/users/${e}/following`,
    whoToFollow: "/users/suggestions/who-to-follow",
    topClans: "/users/stats/top-clans",
    search: "/users/search",
    pins: "/users/me/pins",
    setPin: "/users/me/pin",
    followStatus: "/users/follow-status",
    block: e => `/users/${e}/block`,
    blocked: "/users/me/blocked",
    checkUsername: "/users/check-username",
    deleteAccount: "/users/me",
    restoreAccount: "/users/me/restore",
  },
  posts: {
    list: "/posts",
    single: e => `/posts/${e}`,
    create: "/posts",
    update: e => `/posts/${e}`,
    delete: e => `/posts/${e}`,
    restore: e => `/posts/${e}/restore`,
    like: e => `/posts/${e}/like`,
    repost: e => `/posts/${e}/repost`,
    view: e => `/posts/${e}/view`,
    pin: e => `/posts/${e}/pin`,
    pollVote: e => `/posts/${e}/poll/vote`,
    byUser: e => `/posts/user/${e}`,
    likedByUser: e => `/posts/user/${e}/liked`,
    comments: e => `/posts/${e}/comments`,
  },
  comments: {
    edit: e => `/comments/${e}`,
    delete: e => `/comments/${e}`,
    restore: e => `/comments/${e}/restore`,
    like: e => `/comments/${e}/like`,
    replies: e => `/comments/${e}/replies`,
  },
  notifications: {
    list: "/notifications/",
    count: "/notifications/count",
    markRead: e => `/notifications/${e}/read`,
    markReadBatch: "/notifications/read-batch",
    markAllRead: "/notifications/read-all",
    stream: "/notifications/stream",
    settings: "/notifications/settings",
  },
  files: {
    upload: "/files/upload",
    get: e => `/files/${e}`,
    delete: e => `/files/${e}`,
  },
  reports: { create: "/reports" },
  hashtags: {
    search: "/hashtags",
    trending: "/hashtags/trending",
    posts: e => `/hashtags/${encodeURIComponent(e)}/posts`,
  },
  search: { global: "/search" },
  subscription: {
    status: "/v1/subscription/",
    pay: "/v1/subscription/pay",
    autoRenewal: "/v1/subscription/auto-renewal",
    bindCard: "/v1/subscription/bind-card",
    methods: "/v1/subscription/methods",
    methodDefault: e => `/v1/subscription/methods/${e}/default`,
    methodDelete: e => `/v1/subscription/methods/${e}`,
  },
  verification: {
    status: "/verification/status",
    submit: "/verification/submit",
  },
  platform: { changelog: "/platform/changelog" },
};

const Dg = [
  "image/jpeg",
  "image/png",
  "image/gif",
  "image/webp",
  "image/avif",
  "image/heic",
  "image/heif",
];

const Ug = ["video/mp4", "video/webm", "video/quicktime"];
const Mo = ".jpg,.jpeg,.png,.gif,.webp,.avif,.heic,.heif";
const Fg = ".mp4,.webm,.mov";

const rn = {
  async uploadMedia(e) {
    const t = new FormData();
    t.append("file", e);
    return await P.uploadFormData(x.files.upload, t, { timeout: 300 * 1000/* 1e3 */ });
  },
  async deleteFile(e) {
    await P.delete(x.files.delete(e));
  },
  isValidImageType(e) {
    return Dg.includes(e.type);
  },
  isValidVideoType(e) {
    return Ug.includes(e.type);
  },
  isValidMediaType(e) {
    return this.isValidImageType(e) || this.isValidVideoType(e);
  },
};

const Hg = {
  [oe.CONTENT_MODERATION_FAILED]: "Изображение содержит запрещённый контент",
  [oe.FILE_TOO_LARGE]: "Файл слишком большой",
  [oe.UNSUPPORTED_FILE_TYPE]: "Неподдерживаемый формат файла",
  [oe.UPLOAD_FAILED]: "Не удалось загрузить файл",
  [oe.VIDEO_REQUIRES_VERIFICATION]:
    "Загрузка видео доступна только верифицированным пользователям",
  [oe.RATE_LIMIT_EXCEEDED]: "Слишком много запросов. Попробуйте позже",
  [oe.UNAUTHORIZED]: "Требуется авторизация",
  [oe.ACCESS_DENIED]: "Доступ запрещён",
  [oe.NETWORK_ERROR]: "Ошибка сети. Проверьте подключение",
  [oe.TIMEOUT]: "Превышено время ожидания",
};

function Bg(e, t = "Произошла ошибка") {
  return e ? Hg[e] ?? t : t;
}
class _s {
  cache = new Map();
  maxSize;
  ttl;
  constructor(t = 100, n = 300 * 1000/* 1e3 */) {
    (this.maxSize = t);
    (this.ttl = n);
  }
  get(t) {
    const n = this.cache.get(t);
    if (n) {
      if (Date.now() - n.timestamp > this.ttl) {
        this.cache.delete(t);
        return;
      }
      this.cache.delete(t);
      this.cache.set(t, n);
      return n.value;
    }
  }
  set(t, n) {
    if (this.cache.has(t)) {
      this.cache.delete(t);
    }

    if (this.cache.size >= this.maxSize) {
      const r = this.cache.keys().next().value;

      if (r) {
        this.cache.delete(r);
      }
    }

    this.cache.set(t, { value: n, timestamp: Date.now() });
  }
  has(t) {
    const n = this.cache.get(t);
    return n
      ? Date.now() - n.timestamp > this.ttl
        ? (this.cache.delete(t), false)
        : true
      : false;
  }
  delete(t) {
    return this.cache.delete(t);
  }
  clear() {
    this.cache.clear();
  }
  getAge(t) {
    const n = this.cache.get(t);
    if (n) {
      return Date.now() - n.timestamp;
    }
  }
  isFresh(t, n = this.ttl) {
    const r = this.getAge(t);
    return r !== undefined && r < n;
  }
  get size() {
    return this.cache.size;
  }
  cleanup() {
    const t = Date.now();
    for (const [n, r] of this.cache.entries()) {
      if (t - r.timestamp > this.ttl) {
        this.cache.delete(n);
      }
    }
  }
}
function Ki(e) {
  const t = { ...e };

  if ("verified" in t && !("isVerified" in t)) {
    (t.isVerified = t.verified);
  }

  if ("isVerified" in t) {
    (t.isVerified = false);
  }

  if ((!("isPrivate" in t) || t.isPrivate === undefined)) {
    (t.isPrivate = false);
  }

  if (typeof t.banner == "string") {
    (t.banner = { url: t.banner });
  }

  if (!t.stats &&
    ("followersCount" in t || "followingCount" in t)) {
    (t.stats = {
        followers: t.followersCount ?? 0,
        following: t.followingCount ?? 0,
      });
  }

  if (!t.interaction &&
    ("isFollowing" in t ||
      "isFollowedBy" in t ||
      "isBlockedByMe" in t ||
      "isBlocking" in t)) {
    (t.interaction = {
        isFollowing: t.isFollowing ?? false,
        isFollowedBy: t.isFollowedBy ?? false,
        hasOutgoingRequest: t.hasOutgoingRequest ?? false,
        hasIncomingRequest: t.hasIncomingRequest ?? false,
        isBlocking: t.isBlocking ?? t.isBlockedByMe ?? false,
        isBlockedBy: t.isBlockedBy ?? false,
      });
  }

  if (!t.privacySettings &&
    ("wallAccess" in t || "likesVisibility" in t)) {
    (t.privacySettings = {
        whoCanPostOnWall: t.wallAccess ?? "everyone",
        whoCanSeeMyPostReactions: t.likesVisibility ?? "everyone",
      });
  }

  return t;
}
const Ft = new _s(100, 300 * 1000/* 1e3 */);
const Vg = 60 * 1000/* 1e3 */;
setInterval(() => Ft.cleanup(), 120 * 1000/* 1e3 */);
const rr = {
  async checkUsername(e) {
    return (
      await P.get(`/users/check-username?username=${encodeURIComponent(e)}`)
    ).available;
  },
  async createProfile(e) {
    return await P.post("/users/profile", e);
  },
  async getMyProfile() {
    const e = await P.get(x.users.me);
    return Ki(e);
  },
  async updateProfile(e) {
    return await P.put(x.users.updateProfile, e);
  },
  async getProfileByUsername(e) {
    const t = e.toLowerCase();
    const n = Ft.get(t);

    if (n && Ft.isFresh(t, Vg)) {
      this._fetchAndCacheProfile(e, t).catch(() => {});
      return n;
    }

    if (n) {
      this._fetchAndCacheProfile(e, t).catch(() => {});
      return n;
    }

    return this._fetchAndCacheProfile(e, t);
  },
  async _fetchAndCacheProfile(e, t) {
    const n = await P.get(x.users.profile(e));
    const r = Ki(n);
    Ft.set(t, r);
    return r;
  },
  invalidateProfileCache(e) {
    Ft.delete(e.toLowerCase());
  },
  updateProfileCache(e, t) {
    const n = e.toLowerCase();
    const r = Ft.get(n);

    if (r) {
      Ft.set(n, { ...r, ...t });
    }
  },
  async followUser(e) {
    await P.post(x.users.follow(e), {});
  },
  async unfollowUser(e) {
    await P.delete(x.users.follow(e));
  },
  async pinPost(e) {
    await P.post(x.posts.pin(e));
  },
  async unpinPost(e) {
    await P.delete(x.posts.pin(e));
  },
  async getPrivacySettings() {
    const e = await P.get(x.users.privacy);
    return {
      isPrivate: e.isPrivate ?? false,
      showLastSeen: e.showLastSeen ?? true,
      whoCanPostOnWall: e.whoCanPostOnWall ?? e.wallAccess ?? "everyone",
      whoCanSeeMyPostReactions:
        e.whoCanSeeMyPostReactions ?? e.likesVisibility ?? "everyone",
    };
  },
  async updatePrivacySettings(e) {
    const t = {};

    if (e.whoCanPostOnWall) {
      (t.wallAccess = e.whoCanPostOnWall);
    }

    if (e.whoCanSeeMyPostReactions) {
      (t.likesVisibility = e.whoCanSeeMyPostReactions);
    }

    if (e.showLastSeen !== undefined) {
      (t.showLastSeen = e.showLastSeen);
    }

    await P.put(x.users.privacy, t);
  },
  async getVerificationStatus() {
    try {
      return await P.get(x.verification.status);
    } catch (e) {
      if (e && typeof e == "object" && "status" in e && e.status === 404) {
        return null;
      }
      throw e;
    }
  },
  async submitVerificationRequest(e) {
    return await P.post(x.verification.submit, { videoUrl: e });
  },
  async getMyPins() {
    const e = await P.get(x.users.pins);
    const t = e.data ?? e;
    return { pins: t.pins ?? [], activePin: t.activePin ?? null };
  },
  async setActivePin(e) {
    await P.put(x.users.setPin, { slug: e });
  },
  async removeActivePin() {
    await P.delete(x.users.setPin);
  },
  async deleteAccount() {
    await P.delete(x.users.deleteAccount);
  },
  async restoreAccount() {
    await P.post(x.users.restoreAccount);
  },
};
function Zi(e) {
  const t = e.user ?? e;
  const n = t.id ?? e.id;
  return {
    id: e.id,
    userId: n,
    displayName: t.displayName ?? "",
    username: t.username ?? null,
    avatar: t.avatar ?? "",
    isVerified: t.isVerified ?? t.verified ?? false,
    isPrivate: t.isPrivate ?? false,
    interaction: e.interaction ?? {
      isFollowing: e.isFollowing ?? false,
      isFollowedBy: e.isFollowedBy ?? false,
      hasOutgoingRequest: e.hasOutgoingRequest ?? false,
      hasIncomingRequest: e.hasIncomingRequest ?? false,
      isBlocking: e.isBlocking ?? false,
      isBlockedBy: e.isBlockedBy ?? false,
    },
  };
}
const _t = at(e => ({
  statuses: {},

  setStatuses: t => e(n => ({
    statuses: { ...n.statuses, ...t }
  })),

  setStatus: (t, n) => e(r => ({
    statuses: { ...r.statuses, [t]: n }
  })),

  clear: () => e({ statuses: {} })
}));
let $o = new Set();
function Wg() {
  if (!Jr) {
    (null = setTimeout(async () => {
      Jr = null;
      const e = Array.from($o);
      $o.clear();

      if (e.length !== 0) {
        for (let t = 0; t < e.length; t += 20) {
          const n = e.slice(t, t + 20);
          try {
            const r = await Do.batchFollowStatus(n);
            _t.getState().setStatuses(r);
          } catch {}
        }
      }
    }, 50));
  }
}
function jg(e) {
  const t = he(s => s.profile?.id);

  const n = _t(s => s.statuses);

  const r = N("");

  D(() => {
    if (!t) {
      return;
    }

    const s = e.filter(c => c !== t && n[c] === undefined);

    const a = s.sort().join(",");
    if (!(a === r.current || a === "")) {
      r.current = a;
      for (const c of s) {
        $o.add(c);
      }
      Wg();
    }
  }, [e, t]);

  return {
    getStatus: k(
      (s) => {
        if (s !== t) {
          return n[s];
        }
      },
      [n, t]
    ),
    statuses: n,
  };
}
const Ht = new _s(500, 120 * 1000/* 1e3 */);
setInterval(() => Ht.cleanup(), 60 * 1000/* 1e3 */);
const Do = {
  async followUser(e) {
    const t = await P.post(x.users.follow(e), {});
    Ht.delete(e);
    _t.getState().setStatus(e, true);
    return t.following ? "following" : t.status ?? "following";
  },
  async unfollowUser(e) {
    await P.delete(x.users.follow(e));
    Ht.delete(e);
    _t.getState().setStatus(e, false);
  },
  async getFollowers(e, t = {}) {
    const n = new URLSearchParams();
    const r = t.limit ?? 20;
    n.set("limit", r.toString());
    const o = t.cursor ? parseInt(t.cursor) : t.page ?? 1;
    n.set("page", o.toString());
    const s = n.toString();
    const a = `${x.users.followers(e)}${s ? `?${s}` : ""}`;
    const c = await P.get(a);
    const l = c.data ?? c;
    const u = l.users ?? l.followers ?? [];
    const d = l.pagination?.hasMore ?? false ? String(o + 1) : null;
    return { data: u.map(Zi), nextCursor: d };
  },
  async getFollowing(e, t = {}) {
    const n = new URLSearchParams();
    const r = t.limit ?? 20;
    n.set("limit", r.toString());
    const o = t.cursor ? parseInt(t.cursor) : t.page ?? 1;
    n.set("page", o.toString());
    const s = n.toString();
    const a = `${x.users.following(e)}${s ? `?${s}` : ""}`;
    const c = await P.get(a);
    const l = c.data ?? c;
    const u = l.users ?? l.following ?? [];
    const d = l.pagination?.hasMore ?? false ? String(o + 1) : null;
    return { data: u.map(Zi), nextCursor: d };
  },
  async blockUser(e) {
    await P.post(x.users.block(e), {});
    Ht.delete(e);
  },
  async unblockUser(e) {
    await P.delete(x.users.block(e));
    Ht.delete(e);
  },
  async getBlockedUsers(e = {}) {
    const t = new URLSearchParams();
    const n = e.limit ?? 20;
    t.set("limit", n.toString());
    const r = e.cursor ? parseInt(e.cursor) : e.page ?? 1;
    t.set("page", r.toString());
    const o = t.toString();
    const s = `${x.users.blocked}${o ? `?${o}` : ""}`;
    const a = await P.get(s);
    const c = a.data ?? a;
    let l = [];

    if (Array.isArray(c.users)) {
      (l = c.users);
    } else if (Array.isArray(c)) {
      (l = c);
    }

    const u = l.map((p) => {
        const h = p.user ?? p;
        return {
          id: h.id,
          username: h.username ?? null,
          displayName: h.displayName ?? "",
          avatar: h.avatar ?? null,
          isVerified: h.isVerified ?? h.verified ?? false,
          isPrivate: h.isPrivate ?? false,
          isBlocked: true,
        };
      });

    const f = c.pagination?.hasMore ?? false;
    const d = f ? String(r + 1) : null;
    return { users: u, nextCursor: d, hasMore: f };
  },
  async batchFollowStatus(e) {
    if (e.length === 0) {
      return {};
    }

    return (await P.post(x.users.followStatus, { userIds: e })).data ?? {};
  },
  invalidateSocialCache(e) {
    Ht.delete(e);
  },
  clearSocialCache() {
    Ht.clear();
  },
};
function zg(e) {
  const t = _t(o => o.statuses[e]);

  const n = k(async () => {
    _t.getState().setStatus(e, true);
    try {
      await Do.followUser(e);
    } catch {
      _t.getState().setStatus(e, false);
    }
  }, [e]);

  const r = k(async () => {
    _t.getState().setStatus(e, false);
    try {
      await Do.unfollowUser(e);
    } catch {
      _t.getState().setStatus(e, true);
    }
  }, [e]);

  return { isFollowing: t, follow: n, unfollow: r };
}
const qg = "xgyL";
const Gg = "Qxj3";
const Yg = "R4cU";
const Xg = "C24C";
const Kg = "qUGI";
const Zg = "rThH";
const Jg = "hzH7";
const Qg = "c9yn";
const e_ = "GFCO";
const t_ = "YhB8";
const n_ = "JVJg";
const r_ = "SWTC";
const o_ = "EkjW";
const s_ = "w1rD";

const Le = {
  overlay: qg,
  modalWrapper: Gg,
  wide: Yg,
  modal: Xg,
  frameless: Kg,
  header: Zg,
  title: Jg,
  closeButton: Qg,
  externalCloseButton: e_,
  mobileOverlay: t_,
  closing: n_,
  bottomSheet: r_,
  dragHandle: o_,
  dragIndicator: s_,
};

const i_ = fn(null);
const a_ = 100;
const c_ = 0.5;
function Qt({
  children: e,
  onClose: t,
  title: n,
  showHeader: r = true,
  showCloseButton: o = true,
  frameless: s = false,
  className: a,
  contentClassName: c,
  size: l = "default",
  onBeforeClose: u,
}) {
  const f = N(null);
  const d = N(null);
  const p = N(null);
  const h = Ot();
  const m = N(0);
  const g = N(false);
  const [T, _] = A(false);
  const y = N(0);
  const w = N(0);
  const I = N(0);
  D(() => {
    const j = (Y) => {
        if (Y.key === "Escape") {
          if (u && !u()) {
            return;
          }
          t();
        }
      };

    const K = document.documentElement.style.overflow;
    (document.documentElement.style.overflow = "hidden");
    document.addEventListener("keydown", j);

    return () => {
      document.removeEventListener("keydown", j);
      (document.documentElement.style.overflow = K);
    };
  }, [t]);

  const S = (j) => {
      p.current = j.target;
    };

  const v = (j) => {
    if (p.current === f.current && j.target === f.current) {
      if (h) {
        C();
      } else {
        if (u && !u()) {
          return;
        }
        t();
      }
    }
    p.current = null;
  };

  const C = k(() => {
    if (u && !u()) {
      O(0, "transform 0.2s ease-out");
      $(0);
      (m.current = 0);
      return;
    }
    _(true);

    setTimeout(() => {
      t();
    }, 200);
  }, [t, u]);

  const b = N(false);
  const E = N(false);

  const R = (j) => {
    let K = j;

    while (K && K !== d.current) {
      const U = window.getComputedStyle(K).overflowY;
      if ((U === "auto" || U === "scroll") && K.scrollHeight > K.clientHeight) {
        return K;
      }
      K = K.parentElement;
    }

    return null;
  };

  const O = (j, K) => {
    if (d.current) {
      (d.current.style.transform = j > 0 ? `translateY(${j}px)` : "");
      (d.current.style.transition = K || "");
    }
  };

  const $ = (j) => {
    if (f.current && j > 0) {
      (f.current.style.backgroundColor = `rgba(0, 0, 0, ${Math.max(
            0,
            0.4 - j / 500
          )})`);
    } else if (f.current) {
      (f.current.style.backgroundColor = "");
    }
  };

  const V = (j) => {
    if (!h) {
      return;
    }
    (y.current = j.touches[0].clientY);
    (w.current = Date.now());
    (I.current = j.touches[0].clientY);
    const j_target = j.target;
    if (j_target.closest(`.${Le.dragHandle}`)) {
      (b.current = true);
      (E.current = true);
      (g.current = true);

      if (d.current) {
        (d.current.style.transition = "none");
      }

      return;
    }
    (b.current = false);

    if (j_target.closest('button, a, input, textarea, select, video, [role="button"]')) {
      E.current = false;
      return;
    }

    if (j_target.tagName === "CANVAS" || j_target.closest("canvas")) {
      E.current = false;
      return;
    }
    const ce = R(j_target);
    E.current = !ce || ce.scrollTop === 0;
  };

  const ne = (j) => {
    if (!h) {
      return;
    }
    const K = j.touches[0].clientY;
    const Y = K - y.current;
    (I.current = K);

    if (b.current) {
      if (Y > 0) {
        (m.current = Y);
        O(Y);
        $(Y);
        j.preventDefault();
      }

      return;
    }

    if (E.current) {
      if (g.current && m.current > 0) {
        if (Y > 0) {
          (m.current = Y);
          O(Y);
          $(Y);
          j.preventDefault();
        } else {
          (m.current = 0);
          (g.current = false);
          O(0);
          $(0);
        }

        return;
      }

      if (Y > 0) {
        g.current ||
            ((g.current = true),
            d.current && (d.current.style.transition = "none"));

        (m.current = Y);
        O(Y);
        $(Y);
        j.preventDefault();
      }
    }
  };

  const _e = () => {
    if (!h) {
      return;
    }
    const j = I.current - y.current;
    const K = Date.now() - w.current;
    const Y = j / K;

    if (g.current && (j > a_ || Y > c_)) {
      C();
    } else if (m.current > 0) {
      O(0, "transform 0.2s ease-out");
      $(0);
      (m.current = 0);
    }

    (g.current = false);
    (b.current = false);
    (E.current = false);
  };

  const ye = (() => {
    if (h && T) {
      return {
        transform: "translateY(100%)",
        transition: "transform 0.2s ease-out",
      };
    }
  })();

  const Se = { onClose: t, isMobile: h, isClosing: T, handleClose: C };
  return i(i_.Provider, {
    value: Se,
    children: i("div", {
      ref: f,
      className: `${Le.overlay} ${h ? Le.mobileOverlay : ""} ${
        T ? Le.closing : ""
      }`,
      onMouseDown: S,
      onMouseUp: v,
      children: i("div", {
        ref: d,
        className: `${Le.modalWrapper} ${l === "wide" ? Le.wide : ""} ${
          h ? Le.bottomSheet : ""
        }`,
        style: ye,
        onTouchStart: V,
        onTouchMove: ne,
        onTouchEnd: _e,
        children: [
          s &&
            !h &&
            i("button", {
              type: "button",
              className: Le.externalCloseButton,
              onClick: (j) => {
                j.stopPropagation();
                t();
              },
              children: i(ot, { size: 24 }),
            }),
          h &&
            i("div", {
              className: Le.dragHandle,
              children: i("div", { className: Le.dragIndicator }),
            }),
          i("div", {
            className: `${Le.modal} ${s ? Le.frameless : ""} ${a || ""} ${
              c || ""
            }`,
            children: [
              !s &&
                r &&
                !h &&
                i("div", {
                  className: Le.header,
                  children: [
                    i("span", { className: Le.title, children: n }),
                    o &&
                      i("button", {
                        type: "button",
                        className: Le.closeButton,
                        onClick: (j) => {
                          j.stopPropagation();
                          t();
                        },
                        children: i(ot, { size: 16 }),
                      }),
                  ],
                }),
              e,
            ],
          }),
        ],
      }),
    }),
  });
}
const l_ = "FQzV";
const u_ = "KScw";
const d_ = "nOrT";
const f_ = "eTGp";
const p_ = "Vu6n";
const h_ = "J96q";
const Ji = { spinner: l_, spin: u_, xs: d_, sm: f_, md: p_, lg: h_ };
function Xt({ size: e = "md", className: t }) {
  const n = [Ji.spinner, Ji[e], t].filter(Boolean).join(" ");
  return i("div", { className: n, children: i(hs, {}) });
}
const m_ = "XbzE";
const g_ = "BF5f";
const __ = "yoA3";
const y_ = "JMFf";
const v_ = "TXBP";
const w_ = "DphA";
const E_ = "MHSj";
const S_ = "loq7";
const b_ = "Hg0L";
const C_ = "GFTu";
const T_ = "o6eY";
const I_ = "Zu7x";

const tn = {
  button: m_,
  primary: g_,
  secondary: __,
  ghost: y_,
  accent: v_,
  danger: w_,
  sm: E_,
  md: S_,
  lg: b_,
  fullWidth: C_,
  iconOnly: T_,
  loading: I_,
};

function Xe({
  children: e,
  variant: t = "primary",
  size: n = "md",
  fullWidth: r = false,
  iconOnly: o = false,
  loading: s = false,
  className: a,
  type: c = "button",
  disabled: l,
  ...u
}) {
  const f = [
    tn.button,
    tn[t],
    tn[n],
    r && tn.fullWidth,
    o && tn.iconOnly,
    s && tn.loading,
    a,
  ]
    .filter(Boolean)
    .join(" ");
  return i("button", {
    type: c,
    className: f,
    disabled: l || s,
    ...u,
    children: s ? i(Xt, { size: "sm" }) : e,
  });
}
const k_ = "UTmg";
const N_ = "M5L8";
const R_ = "e8bf";
const P_ = "YYtr";
const or = { content: k_, title: N_, subtitle: R_, actions: P_ };
function A_({ displayName: e, onConfirm: t, onClose: n }) {
  return i(Qt, {
    onClose: n,
    showHeader: false,
    children: i("div", {
      className: or.content,
      children: [
        i("h2", { className: or.title, children: "Отписаться?" }),
        i("p", {
          className: or.subtitle,
          children: [
            "Вы действительно хотите отписаться от ",
            i("strong", { children: e }),
            "?",
          ],
        }),
        i("div", {
          className: or.actions,
          children: [
            i(Xe, {
              variant: "secondary",
              onClick: (r) => {
                r.stopPropagation();
                n();
              },
              children: "Отмена",
            }),
            i(Xe, {
              variant: "danger",
              onClick: (r) => {
                r.stopPropagation();
                t();
                n();
              },
              children: "Отписаться",
            }),
          ],
        }),
      ],
    }),
  });
}
const bl = fn(null);
let O_ = 0;
function x_({ children: e }) {
  const [t, n] = A([]);

  const r = k((a) => {
    const c = `modal-${++O_}`;

    n(l => [...l, { id: c, component: a }]);

    return c;
  }, []);

  const o = k((a) => {
    n(c => a ? c.filter(l => l.id !== a) : c.slice(0, -1));
  }, []);

  const s = k(() => {
    n([]);
  }, []);

  D(() => {
    let a = window.location.pathname + window.location.search;
    const c = () => {
      const f = window.location.pathname + window.location.search;

      if (f !== a) {
        (a = f);
        n([]);
      }
    };
    window.addEventListener("popstate", c);

    const {
      pushState,
      replaceState
    } = history;

    history.pushState = function (...f) {
      pushState.apply(this, f);
      c();
    };

    (history.replaceState = function (...f) {
      replaceState.apply(this, f);
      c();
    });

    return () => {
      window.removeEventListener("popstate", c);
      (history.pushState = pushState);
      (history.replaceState = replaceState);
    };
  }, []);

  return i(bl.Provider, {
    value: { openModal: r, closeModal: o, closeAllModals: s },
    children: [e, t.length > 0 && i(L_, { modals: t })],
  });
}
function L_({ modals: e }) {
  return $(
    i(Te, {
      children: e.map(({ id: t, component: n }) => i(Ee, { fallback: null, children: n }, t)
      ),
    }),
    document.body
  );
}
function wn() {
  const e = Lr(bl);
  if (!e) {
    throw new Error("useModal must be used within a ModalProvider");
  }
  return e;
}
const M_ = "eHUv";
const $_ = "jkr5";
const D_ = "wGLu";
const U_ = "XLtm";
const F_ = "xGuN";
const H_ = "ARWq";
const B_ = "JTBC";
const V_ = "I5So";
const W_ = "c31P";
const j_ = "Ux2Y";
const z_ = "qFC3";
const q_ = "GAwo";

const bt = {
  avatar: M_,
  xs: $_,
  emoji: D_,
  onlineDot: U_,
  sm: F_,
  md: H_,
  lg: B_,
  xl: V_,
  badge: W_,
  followBadge: j_,
  notFollowing: z_,
  following: q_,
};

function G_(e) {
  return (
    e.startsWith("http://") || e.startsWith("https://") || e.startsWith("/")
  );
}
function Qe({
  src: e,
  alt: t,
  size: n = "md",
  badge: r,
  online: o,
  followBadge: s,
  onFollowBadgeClick: a,
  className: c,
}) {
  const l = e ? G_(e) : false;
  return i("div", {
    className: `${bt.avatar} ${bt[n]} ${c || ""}`,
    children: [
      l && e
        ? i("img", { src: e, alt: t || "" })
        : i("span", { className: bt.emoji, children: e || "👤" }),
      r && i("div", { className: bt.badge, children: r }),
      s !== undefined
        ? i("button", {
            type: "button",
            className: `${bt.followBadge} ${
              s ? bt.following : bt.notFollowing
            }`,
            onClick: (u) => {
              u.preventDefault();
              u.stopPropagation();
              a?.(u);
            },
            children: s ? i(pg, { size: 8 }) : i(fg, { size: 8 }),
          })
        : o && i("span", { className: bt.onlineDot }),
    ],
  });
}
const Y_ = "AoFr";
const X_ = "G0av";
const K_ = "B2Nw";
const Z_ = "WSJw";
const J_ = "bzMJ";
const Q_ = "jHZK";
const ey = "yEKH";
const ty = "puRy";
const ny = "Ulla";
const ry = "vNyK";
const oy = "i0kl";
const sy = "w86e";
const iy = "gcnf";
const ay = "lhpx";
const cy = "ygvm";

const $e = {
  userName: Y_,
  pinBadge: X_,
  text: K_,
  nukstaGlow: Z_,
  xs: J_,
  sm: Q_,
  md: ey,
  lg: ty,
  pinWrapper: ny,
  pinClickable: ry,
  pinTooltip: oy,
  pinTooltipFadeIn: sy,
  pinTooltipRow: iy,
  pinTooltipLabel: ay,
  pinTooltipArrow: cy,
};

const ly = Z(() => X(() => import("./index-Cs8C4zj9.js"), __vite__mapDeps([0, 1, 2, 3])).then(
  e => ({
    default: e.SubscriptionModal
  })
)
);

const uy = { xs: 12, sm: 14, md: 16, lg: 22 };
const dy = "subscription_nuksta";
function Kn({
  name: e,
  verified: t,
  hasNuksta: n,
  pin: r,
  size: o = "md",
  className: s,
}) {
  const uy_o = uy[o];
  const c = N(null);
  const [l, u] = A(null);
  const [f, d] = A(false);
  const p = r?.slug === dy;

  const h = k(() => {
    if (!c.current) {
      return;
    }
    const g = c.current.getBoundingClientRect();
    u({ x: g.left + g.width / 2, y: g.top });
  }, []);

  const m = k(() => {
    u(null);
  }, []);

  return i("span", {
    className: `${$e.userName} ${$e[o]} ${s || ""}`,
    children: [
      n
        ? i("span", {
            className: $e.nukstaGlow,
            children: i("span", { className: $e.text, children: e }),
          })
        : i("span", { className: $e.text, children: e }),
      t && i(Sg, {}),
      r &&
        i("span", {
          ref: c,
          className: `${$e.pinWrapper} ${p ? $e.pinClickable : ""}`,
          onMouseEnter: h,
          onMouseLeave: m,
          onClick: p
            ? (g) => {
            g.stopPropagation();
            g.preventDefault();
            d(true);
          }
            : undefined,
          children: [
            i("img", {
              src: r.url,
              alt: r.name,
              className: $e.pinBadge,
              width: uy_o,
              height: uy_o,
            }),
            l &&
              $(
                i("div", {
                  className: $e.pinTooltip,
                  style: { left: `${l.x}px`, top: `${l.y}px` },
                  children: [
                    i("span", {
                      className: $e.pinTooltipRow,
                      children: [
                        i("span", {
                          className: $e.pinTooltipLabel,
                          children: "Пин:",
                        }),
                        " ",
                        r.name,
                      ],
                    }),
                    r.description &&
                      i("span", {
                        className: $e.pinTooltipRow,
                        children: [
                          i("span", {
                            className: $e.pinTooltipLabel,
                            children: "Ивент:",
                          }),
                          " ",
                          r.description,
                        ],
                      }),
                    i("span", { className: $e.pinTooltipArrow }),
                  ],
                }),
                document.body
              ),
          ],
        }),
      f &&
        i(Ee, {
          fallback: null,
          children: i(ly, { isOpen: true, onClose: () => d(false) }),
        }),
    ],
  });
}
function fy(e) {
  return "accessToken" in e;
}
function py(e) {
  return "accessToken" in e;
}
const ft = {
  async register(e) {
    return await Ce.post(x.auth.signUp, e);
  },
  async login(e) {
    return await Ce.post(x.auth.signIn, e);
  },
  async verifyOtp(e) {
    return await Ce.post(x.auth.verifyOtp, e);
  },
  async resendOtp(e) {
    await Ce.post(x.auth.resendOtp, e);
  },
  async refreshSession() {
    return await Ce.post(x.auth.refresh);
  },
  async logout() {
    await Ce.post(x.auth.logout);
  },
  async logoutAll() {
    await Ce.post(`${x.auth.logout}-all`);
  },
  async forgotPassword(e) {
    return await Ce.post(x.auth.forgotPassword, e);
  },
  async resetPassword(e) {
    await Ce.post(x.auth.resetPassword, e);
  },
  async changePassword(e) {
    await Ce.post(x.auth.changePassword, e);
  },
};
function Lt(e, t) {
  if (!e) {
    ei(null);
    return;
  }
  ei({ id: e.id, username: e.username ?? undefined, email: t ?? undefined });
}

const sr = {
    status: "idle",
    profile: null,
    email: null,
    pendingEmail: null,
    pendingPassword: null,
    flowToken: null,
    error: null,
    errorCode: null,
    canRestore: null,
    restoreDeadline: null,
  };

const he = at()(
  il(
    (e, t) => {
      P.setRefreshTokenCallback(async () => {
        try {
          const r = await ft.refreshSession();
          P.setAccessToken(r.accessToken);
          Ce.setAccessToken(r.accessToken);
          return r.accessToken;
        } catch (r) {
          return Pe(r) && r.status >= 500
            ? (e({ status: "service_error" }), null)
            : (t().reset(), null);
        }
      });

      P.setOnUnauthorizedCallback(() => {
        if (t().status !== "service_error") {
          t().reset();
        }
      });

      return {
        ...sr,
        register: async (r) => {
          e({ status: "loading", error: null, errorCode: null });
          try {
            const o = await ft.register(r);

            e({
              status: "needs_verification",
              pendingEmail: r.email,
              pendingPassword: r.password,
              flowToken: o.flowToken ?? null,
            });

            return o.nextStep;
          } catch (o) {
            const s = Pe(o) ? o.message : "Registration failed";
            const a = Pe(o) ? o.code : null;
            e({ status: "unauthenticated", error: s, errorCode: a });
            throw o;
          }
        },
        login: async (r) => {
          e({ status: "loading", error: null, errorCode: null });
          try {
            const o = await ft.login(r);
            if (py(o)) {
              P.setAccessToken(o.accessToken);
              Ce.setAccessToken(o.accessToken);
              try {
                await t().fetchProfile();

                if (t().status !== "account_deleted") {
                  e({
                      status: "authenticated",
                      pendingEmail: null,
                      email: r.email,
                    });

                  Lt(t().profile, r.email);
                }
              } catch (a) {
                if (Pe(a) &&
                (a.code === oe.ENTITY_NOT_FOUND || a.status === 404)) {
                  e({
                    status: "needs_profile",
                    pendingEmail: null,
                    email: r.email,
                  });
                } else {
                  throw a;
                }
              }
              return "authenticated";
            }
            const s = o;

            e({
              status: "needs_verification",
              pendingEmail: r.email,
              pendingPassword: r.password,
              flowToken: s.flowToken ?? null,
            });

            return o.nextStep;
          } catch (o) {
            const s = Pe(o) ? o.message : "Login failed";
            const a = Pe(o) ? o.code : null;
            e({ status: "unauthenticated", error: s, errorCode: a });
            throw o;
          }
        },
        verifyOtp: async (r) => {
          e({ status: "loading", error: null, errorCode: null });
          const { pendingEmail, pendingPassword, flowToken } = t();
          try {
            const c = await ft.verifyOtp({
              email: pendingEmail || "",
              password: pendingPassword || "",
              otp: r,
              flowToken: flowToken || "",
            });
            e({ pendingPassword: null });

            if (fy(c)) {
              P.setAccessToken(c.accessToken);
              Ce.setAccessToken(c.accessToken);
              const l = pendingEmail;
              try {
                await t().fetchProfile();

                if (t().status !== "account_deleted") {
                  e({
                      status: "authenticated",
                      pendingEmail: null,
                      pendingPassword: null,
                      flowToken: null,
                      email: l,
                    });

                  Lt(t().profile, l);
                }
              } catch (u) {
                if (Pe(u) &&
                (u.code === oe.ENTITY_NOT_FOUND || u.status === 404)) {
                  e({
                    status: "needs_profile",
                    pendingEmail: null,
                    pendingPassword: null,
                    flowToken: null,
                    email: l,
                  });
                } else {
                  throw u;
                }
              }
              return "authenticated";
            }

            e({ status: "needs_verification" });
            return "password_reset";
          } catch (c) {
            const l = Pe(c) ? c.message : "Verification failed";
            const u = Pe(c) ? c.code : null;
            e({ status: "needs_verification", error: l, errorCode: u });
            throw c;
          }
        },
        resendOtp: async () => {
          e({ error: null, errorCode: null });
          const { pendingEmail, flowToken } = t();
          try {
            await ft.resendOtp({ email: pendingEmail || "", flowToken: flowToken || "" });
          } catch (s) {
            const a = Pe(s) ? s.message : "Failed to resend code";
            const c = Pe(s) ? s.code : null;
            e({ error: a, errorCode: c });
            throw s;
          }
        },
        createProfile: async (r) => {
          e({ error: null, errorCode: null });
          try {
            await rr.createProfile(r);
            await t().fetchProfile();
            e({ status: "authenticated" });
            Lt(t().profile, t().email);
          } catch (o) {
            const s = Pe(o) ? o.message : "Failed to create profile";
            const a = Pe(o) ? o.code : null;
            e({ error: s, errorCode: a });
            throw o;
          }
        },
        logout: async () => {
          try {
            await ft.logout();
          } catch {
          } finally {
            P.setAccessToken(null);
            Ce.setAccessToken(null);
            e({ ...sr, status: "unauthenticated" });
            Lt(null, null);
          }
        },
        logoutAll: async () => {
          try {
            await ft.logoutAll();
          } catch {
          } finally {
            P.setAccessToken(null);
            Ce.setAccessToken(null);
            e({ ...sr, status: "unauthenticated" });
            Lt(null, null);
          }
        },
        refreshSession: async () => {
          try {
            const r = await ft.refreshSession();
            P.setAccessToken(r.accessToken);
            Ce.setAccessToken(r.accessToken);
            return r.accessToken;
          } catch (r) {
            return Pe(r) && r.status >= 500
              ? (e({ status: "service_error" }), null)
              : (t().reset(), null);
          }
        },
        fetchProfile: async () => {
          const r = await rr.getMyProfile();
          if (r.isDeleted) {
            e({
              status: "account_deleted",
              profile: null,
              canRestore: r.canRestore ?? false,
              restoreDeadline: r.restoreDeadline ?? null,
            });
            return;
          }
          e({ profile: r });
        },
        initialize: async () => {
          if (
            !document.cookie
              .split(";")
              .some(o => o.trim().startsWith("is_auth="))
          ) {
            e({ status: "unauthenticated" });
            return;
          }
          e({ status: "loading" });
          try {
            if (!(await t().refreshSession())) {
              if (t().status === "service_error") {
                return;
              }
              e({ status: "unauthenticated" });
              return;
            }
            try {
              await t().fetchProfile();

              if (t().status !== "account_deleted") {
                e({ status: "authenticated" });
                Lt(t().profile, t().email);
              }
            } catch (s) {
              if (Pe(s) &&
              (s.code === oe.ENTITY_NOT_FOUND || s.status === 404)) {
                e({ status: "needs_profile" });
              } else {
                throw s;
              }
            }
          } catch (o) {
            if (Pe(o) && o.status >= 500) {
              e({ status: "service_error" });
            } else {
              e({ status: "unauthenticated" });
            }
          }
        },
        deleteAccount: async () => {
          await rr.deleteAccount();
          await t().logout();
        },
        restoreAccount: async () => {
          await rr.restoreAccount();
          await t().fetchProfile();
          const { status } = t();

          if (status === "account_deleted") {
            e({
              status: "authenticated",
              canRestore: null,
              restoreDeadline: null,
            });
          }
        },
        clearError: () => {
          e({ error: null, errorCode: null });
        },
        reset: () => {
          P.setAccessToken(null);
          Ce.setAccessToken(null);
          e({ ...sr, status: "unauthenticated" });
          Lt(null, null);
        },
        setProfile: (r) => {
          e({ profile: r });
        },
      };
    },
    {
      name: "auth-storage",
      storage: ds(() => sessionStorage),
      partialize: e => ({
        profile: e.profile,
        email: e.email
      }),
    }
  )
);

const hy = () => he(e => e.status);

const ys = () => he(e => e.profile);

const Cl = () => he(e => e.status === "authenticated");

function my({ children: e, currentPath: t }) {
  const n = hy();

  const r = he(s => s.initialize);

  D(() => {
    if (n === "idle") {
      r();
    }
  }, [n, r]);

  D(() => {
    if (n === "loading" || n === "idle") {
      return;
    }
    const s = Xi.some(a => t.startsWith(a));

    if (n === "unauthenticated" && !s) {
      Ye(ue.LOGIN);
    } else if (n === "needs_profile" && t !== ue.ONBOARDING) {
      Ye(ue.ONBOARDING);
    } else if (n === "authenticated" &&
        (t === ue.LOGIN || t === ue.REGISTER || t === ue.ONBOARDING)) {
      Ye(ue.HOME);
    }
  }, [n, t]);

  const o = Xi.some(s => t.startsWith(s));
  return n === "idle" || (n === "loading" && !o)
    ? null
    : n === "service_error"
    ? i(gy, {})
    : n === "account_deleted"
    ? i(_y, {})
    : i(Te, { children: e });
}
function gy() {
  const e = he(o => o.initialize);

  const [t, n] = A(false);
  return i("div", {
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      minHeight: "100vh",
      padding: "24px",
    },
    children: i("div", {
      style: {
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        gap: "16px",
        maxWidth: "400px",
        width: "100%",
        textAlign: "center",
      },
      children: [
        i("h1", {
          style: {
            fontSize: "24px",
            fontWeight: 600,
            color: "var(--text-primary)",
            margin: 0,
          },
          children: "Сервис недоступен",
        }),
        i("p", {
          style: {
            fontSize: "15px",
            color: "var(--text-secondary)",
            margin: 0,
            lineHeight: 1.5,
          },
          children:
            "Не удалось подключиться к серверу. Попробуйте обновить страницу или повторите попытку позже.",
        }),
        i("div", {
          style: { marginTop: "8px" },
          children: i(Xe, {
            onClick: async () => {
              n(true);
              try {
                await e();
              } finally {
                n(false);
              }
            },
            disabled: t,
            children: t ? "Подключение..." : "Попробовать снова",
          }),
        }),
      ],
    }),
  });
}
function _y() {
  const e = he(l => l.canRestore);

  const t = he(l => l.restoreDeadline);

  const n = he(l => l.restoreAccount);

  const r = he(l => l.logout);

  const [o, s] = A(false);

  const a = t
    ? new Date(t).toLocaleDateString("ru-RU", {
        day: "numeric",
        month: "long",
        year: "numeric",
      })
    : null;

  const c = async () => {
    s(true);
    try {
      await n();
    } catch {
      s(false);
    }
  };

  return i("div", {
    style: {
      display: "flex",
      alignItems: "center",
      justifyContent: "center",
      minHeight: "100vh",
      padding: "24px",
    },
    children: i("div", {
      style: {
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        gap: "16px",
        maxWidth: "400px",
        width: "100%",
        textAlign: "center",
      },
      children: [
        i("h1", {
          style: {
            fontSize: "24px",
            fontWeight: 600,
            color: "var(--text-primary)",
            margin: 0,
          },
          children: "Аккаунт удалён",
        }),
        o
          ? i("p", {
              style: {
                fontSize: "15px",
                color: "var(--text-secondary)",
                margin: 0,
              },
              children: "Восстановление аккаунта...",
            })
          : e
          ? i(Te, {
              children: [
                i("p", {
                  style: {
                    fontSize: "15px",
                    color: "var(--text-secondary)",
                    margin: 0,
                    lineHeight: 1.5,
                  },
                  children: [
                    "Ваш аккаунт был удалён. Вы можете восстановить его",
                    a ? ` до ${a}` : "",
                    ".",
                  ],
                }),
                i("div", {
                  style: {
                    display: "flex",
                    flexDirection: "column",
                    alignItems: "center",
                    gap: "12px",
                    marginTop: "8px",
                    width: "100%",
                  },
                  children: [
                    i(Xe, { onClick: c, children: "Восстановить аккаунт" }),
                    i("button", {
                      type: "button",
                      onClick: () => r(),
                      style: {
                        background: "none",
                        border: "none",
                        color: "var(--text-secondary)",
                        fontSize: "14px",
                        cursor: "pointer",
                        padding: "8px",
                      },
                      children: "Выйти",
                    }),
                  ],
                }),
              ],
            })
          : i(Te, {
              children: [
                i("p", {
                  style: {
                    fontSize: "15px",
                    color: "var(--text-secondary)",
                    margin: 0,
                  },
                  children: "Срок восстановления аккаунта истёк.",
                }),
                i("div", {
                  style: { marginTop: "8px" },
                  children: i(Xe, { onClick: () => r(), children: "Выйти" }),
                }),
              ],
            }),
      ],
    }),
  });
}
const yy = "SAkb";
const vy = "wnm0";
const wy = "Xa20";
const Ey = "mHij";
const ir = { content: yy, icon: vy, text: wy, button: Ey };
const Qi = "phone-verification-required";
function Sy() {
  const [e, t] = A(false);

  const n = he(o => o.profile?.id ?? "");

  D(() => {
    const o = () => t(true);
    window.addEventListener(Qi, o);

    return () => window.removeEventListener(Qi, o);
  }, []);

  if (!e) {
    return null;
  }

  const r = `https://t.me/itd_verification_bot?start=${n}`;
  return i(Qt, {
    onClose: () => t(false),
    title: "Подтверждение телефона",
    children: i("div", {
      className: ir.content,
      children: [
        i("div", { className: ir.icon, children: i(Eg, { size: 48 }) }),
        i("p", {
          className: ir.text,
          children:
            "Для публикации постов и комментариев необходимо подтвердить номер телефона через Telegram-бота.",
        }),
        i("a", {
          href: r,
          target: "_blank",
          rel: "noopener noreferrer",
          className: ir.button,
          onClick: () => t(false),
          children: "Подтвердить через Telegram",
        }),
      ],
    }),
  });
}
const by = {
  like: "post_reaction",
  comment: "post_comment",
  reply: "comment_reply",
  repost: "post_repost",
  mention: "post_mention",
  follow: "follow",
  follow_request: "follow_request",
  follow_accepted: "follow_accepted",
  post_reaction: "post_reaction",
  post_comment: "post_comment",
  post_repost: "post_repost",
  comment_reaction: "comment_reaction",
  comment_reply: "comment_reply",
  post_mention: "post_mention",
  comment_mention: "comment_mention",
  wall_post: "wall_post",
};
function Tl(e) {
  const t = by[e.type] ?? "follow";
  const n = e.entityId ?? e.targetId ?? null;
  const r = e.isRead ?? e.read ?? false;
  let o = [];

  if (e.payload?.actors) {
    (o = e.payload.actors);
  } else if (e.actor) {
    (o = [
          {
            id: e.actor.id,
            username: e.actor.username,
            displayName: e.actor.displayName,
            avatar: e.actor.avatar,
            isFollowing: e.actor.isFollowing,
            isFollowedBy: e.actor.isFollowedBy,
          },
        ]);
  }

  const s = e.payload?.entityPreview ?? e.preview ?? null;
  return {
    id: e.id,
    type: t,
    entityId: n,
    parentEntityId: e.parentEntityId ?? null,
    isRead: r,
    payload: {
      actors: o,
      count: e.payload?.count ?? 1,
      clickUrl: e.payload?.clickUrl,
      entityPreview: s,
    },
    createdAt: e.createdAt,
    updatedAt: e.updatedAt ?? e.readAt ?? e.createdAt,
  };
}

const Mt = {
    async getNotifications(e = {}) {
      const t = new URLSearchParams();
      const n = e.limit ?? 20;
      t.set("limit", n.toString());
      const r = e.cursor ? parseInt(e.cursor) : e.offset ?? 0;

      if (r > 0) {
        t.set("offset", r.toString());
      }

      const o = t.toString();
      const s = `${x.notifications.list}${o ? `?${o}` : ""}`;
      const a = await P.get(s);
      const c = a.notifications ?? a.data ?? [];
      const u = a.hasMore ?? false ? String(r + c.length) : null;
      return { notifications: c.map(Tl), nextCursor: u };
    },
    async getUnreadCount() {
      return (await P.get(x.notifications.count)).count;
    },
    async markAsRead(e) {
      if (e.length === 1) {
        const r = await P.post(x.notifications.markRead(e[0]));
        return { markedCount: r.markedCount ?? r.marked ?? 1 };
      }
      const t = 20;
      let n = 0;
      for (let r = 0; r < e.length; r += t) {
        const o = e.slice(r, r + t);
        const s = await P.post(x.notifications.markReadBatch, { ids: o });
        n += s.markedCount ?? s.marked ?? o.length;
      }
      return { markedCount: n };
    },
    async markAllAsRead() {
      const e = await P.post(x.notifications.markAllRead);
      return { markedCount: e.markedCount ?? e.marked ?? 0 };
    },
    async getSettings() {
      const e = await P.get(x.notifications.settings);
      return {
        webEnabled: e.webEnabled ?? e.enabled ?? true,
        soundEnabled: e.soundEnabled ?? e.sound ?? true,
        preferences: e.preferences ?? {
          follows: e.follows ?? true,
          reactions: e.reactions ?? e.likes ?? true,
          replies: e.replies ?? e.comments ?? true,
          mentions: e.mentions ?? true,
          wallPosts: e.wallPosts ?? true,
        },
      };
    },
    async updateSettings(e) {
      const t = { ...e };

      if (e.webEnabled !== undefined) {
        (t.enabled = e.webEnabled);
      }

      if (e.soundEnabled !== undefined) {
        (t.sound = e.soundEnabled);
      }

      if (e.preferences) {
        e.preferences.follows !== undefined &&
            (t.follows = e.preferences.follows);

        e.preferences.reactions !== undefined &&
          ((t.reactions = e.preferences.reactions),
          (t.likes = e.preferences.reactions));

        e.preferences.replies !== undefined &&
          ((t.replies = e.preferences.replies),
          (t.comments = e.preferences.replies));

        e.preferences.mentions !== undefined &&
          (t.mentions = e.preferences.mentions);

        e.preferences.wallPosts !== undefined &&
          (t.wallPosts = e.preferences.wallPosts);
      }

      await P.put(x.notifications.settings, t);
    },
  };

const ea = [1000/* 1e3 */, 2000/* 2e3 */, 4000/* 4e3 */, 8000/* 8e3 */, 16000/* 16e3 */, 30000/* 3e4 */];
const Cy = 0.3;
const Ty = 15;
function ta(e) {
  const t = ea[Math.min(e, ea.length - 1)];
  const n = t * Cy * (Math.random() * 2 - 1);
  return Math.round(t + n);
}
let lt = null;
let Sn = null;
let $t = 0;
let Dt = null;
function Iy(e) {
  const {
    url,
    getToken,
    onRefreshToken,
    onMessage,
    onStatusChange: s,
  } = e;
  function a() {
    if (lt) {
      return;
    }
    const l = getToken();
    if (!l) {
      s("error");
      return;
    }
    s("connecting");
    (lt = new AbortController());

    (async () => {
      try {
        const f = await fetch(url, {
          method: "GET",
          headers: {
            Accept: "text/event-stream",
            Authorization: `Bearer ${l}`,
            "Cache-Control": "no-cache",
          },
          signal: lt?.signal,
        });
        if (!f.ok) {
          if (f.status === 401) {
            try {
              await onRefreshToken();
              (lt = null);
              const m = ta($t);
              $t++;
              (Sn = setTimeout(a, m));
              return;
            } catch {
              s("error");
              return;
            }
          }
          throw new Error(`SSE connection failed: ${f.status}`);
        }
        if (!f.body) {
          throw new Error("SSE response has no body");
        }
        ($t = 0);
        s("connected");

        if (Dt) {
          Dt.cancel().catch(() => {});
          (Dt = null);
        }

        const d = f.body.getReader();
        Dt = d;
        const p = new TextDecoder();
        let h = "";

        while (true) {
          const { done: m, value: g } = await d.read();
          if (m) {
            break;
          }
          h += p.decode(g, { stream: true });
          const T = h.split(`
`);
          h = T.pop() || "";
          let _ = "";
          let y = "";
          for (const w of T) {
            if (w.startsWith("event: ")) {
              _ = w.slice(7);
            } else if (w.startsWith("data: ")) {
              y = w.slice(6);
            } else if (w === "" && y) {
              try {
                const I = JSON.parse(y);
                const S = _ || I.type;
                onMessage(S, I);
              } catch (I) {
                console.error("SSE message parse error:", I, y);
              }
              (_ = "");
              (y = "");
            }
          }
        }
      } catch (f) {
        if (f.name === "AbortError") {
          return;
        }
        s("error");

        if ($t >= Ty) {
          console.warn(
            "SSE: Max reconnect attempts reached, stopping reconnection"
          );

          (lt = null);
          return;
        }

        const d = ta($t);
        $t++;

        (Sn = setTimeout(() => {
          (lt = null);
          a();
        }, d));
      }
    })();
  }
  function c() {
    if (Sn) {
      clearTimeout(Sn);
      (Sn = null);
    }

    if (Dt) {
      Dt.cancel().catch(() => {});
      (Dt = null);
    }

    if (lt) {
      lt.abort();
      (lt = null);
    }

    ($t = 0);
    s("disconnected");
  }
  return { connect: a, disconnect: c };
}
const na = {
  notifications: [],
  unreadCount: 0,
  nextCursor: null,
  status: "idle",
  sseStatus: "disconnected",
  error: null,
  settings: null,
  settingsLoading: false,
  isInitialized: false,
  lastSseToast: null,
};
function ky(e) {
  const t = {
    id: e.id,
    type: e.type,
    targetId: e.targetId ?? e.entityId ?? null,
    entityId: e.entityId ?? e.targetId ?? null,
    parentEntityId: e.parentEntityId ?? null,
    actor: e.actor ?? e.actors?.[0] ?? null,
    actors: e.actors,
    preview: e.preview ?? e.entityPreview ?? null,
    read: e.read ?? false,
    createdAt: e.createdAt ?? new Date().toISOString(),
    readAt: e.readAt ?? null,
    payload: e.actors
      ? {
          actors: e.actors,
          count: e.count ?? 1,
          clickUrl: e.clickUrl,
          entityPreview: e.entityPreview ?? e.preview ?? null,
        }
      : undefined,
  };
  return Tl(t);
}

const ar = Iy({
    url: "/api/notifications/stream",
    getToken: () => P.getAccessToken(),
    onRefreshToken: async () => {
      const e = await ft.refreshSession();
      P.setAccessToken(e.accessToken);
      Ce.setAccessToken(e.accessToken);
      return e.accessToken;
    },
    onMessage: (e, t) => {
      if (e === "notification") {
        const n = t.payload ?? t;
        const r = ky(n);
        const o = r.payload.actors[0];

        const s = {
          id: r.id,
          type: r.type,
          actorName: o?.displayName || "Пользователь",
          actorUsername: o?.username || "",
          actorAvatar: o?.avatar || "",
          count: r.payload.count,
          message: Ry(
            r.type,
            o?.displayName || "Пользователь",
            r.payload.count
          ),
          clickUrl: r.payload.clickUrl,
          entityId: r.entityId,
          parentEntityId: r.parentEntityId,
        };

        kt.setState(a => ({
          notifications: [r, ...a.notifications],
          unreadCount: t.unreadCount ?? a.unreadCount,
          lastSseToast: s
        }));

        if (t.sound) {
          Py();
        }
      } else {
        if (e === "unread_count") {
          kt.setState({ unreadCount: t.payload?.count ?? 0 });
        }
      }
    },
    onStatusChange: (e) => {
      kt.setState({
        sseStatus: e,
        error: e === "error" ? "SSE connection error" : null,
      });
    },
  });

const kt = at()((e, t) => ({
  ...na,

  initialize: () => {
    if (!t().isInitialized) {
      e({ isInitialized: true });
      ar.connect();
      t().fetchUnreadCount();
    }
  },

  fetchNotifications: async (n = false) => {
    const { status: r, nextCursor: o, notifications: s } = t();
    if (r !== "loading" && !(!n && o === null && s.length > 0)) {
      e({ status: "loading", error: null });
      try {
        const a = n ? undefined : o ?? undefined;
        const c = await Mt.getNotifications({ cursor: a, limit: 20 });
        e({
          notifications: n ? c.notifications : [...s, ...c.notifications],
          nextCursor: c.nextCursor,
          status: "success",
        });
      } catch (a) {
        const c =
          a instanceof Error ? a.message : "Failed to fetch notifications";
        e({ status: "error", error: c });
      }
    }
  },

  fetchUnreadCount: async () => {
    try {
      const n = await Mt.getUnreadCount();
      e({ unreadCount: n });
    } catch {}
  },

  markAsRead: async (n) => {
    if (n.length === 0) {
      return;
    }
    const r = t();

    const o = n.filter((s) => {
      const a = r.notifications.find(c => c.id === s);
      return a && !a.isRead;
    });

    e(s => ({
      notifications: s.notifications.map(a => n.includes(a.id) ? { ...a, isRead: true } : a
      ),

      unreadCount: Math.max(0, s.unreadCount - o.length)
    }));
    try {
      await Mt.markAsRead(n);
    } catch {}
  },

  markAsReadSilent: async (n) => {
    if (n.length !== 0) {
      e(r => ({
        unreadCount: Math.max(0, r.unreadCount - n.length)
      }));
      try {
        await Mt.markAsRead(n);
      } catch {}
    }
  },

  markAllAsRead: async () => {
    e(n => ({
      notifications: n.notifications.map(r => ({
        ...r,
        isRead: true
      })),

      unreadCount: 0
    }));
    try {
      await Mt.markAllAsRead();
    } catch {}
  },

  connectSSE: () => ar.connect(),
  disconnectSSE: () => ar.disconnect(),

  fetchSettings: async () => {
    e({ settingsLoading: true });
    try {
      const n = await Mt.getSettings();
      e({ settings: n, settingsLoading: false });
    } catch {
      e({ settingsLoading: false });
    }
  },

  updateSettings: async (n) => {
    const { settings: r } = t();
    if (r) {
      const o = {
        webEnabled: n.webEnabled ?? r.webEnabled,
        soundEnabled: n.soundEnabled ?? r.soundEnabled,
        preferences: { ...r.preferences, ...n.preferences },
      };
      e({ settings: o });
    }
    try {
      await Mt.updateSettings(n);
    } catch {
      e({ settings: r });
    }
  },

  reset: () => {
    ar.disconnect();
    e(na);
  }
}));

const Ny = {
  follow: (e, t) => t > 1
    ? `${e} и ещё ${t - 1} подписались на вас`
    : `${e} подписался(-ась) на вас`,
  follow_request: e => `${e} хочет подписаться на вас`,
  follow_accepted: e => `${e} принял(а) вашу заявку`,
  post_reaction: (e, t) => t > 1
    ? `${e} и ещё ${t - 1} оценили ваш пост`
    : `${e} оценил(а) ваш пост`,
  post_comment: e => `${e} прокомментировал(а) ваш пост`,
  post_repost: (e, t) => t > 1 ? `${e} и ещё ${t - 1} сделали репост` : `${e} сделал(а) репост`,
  comment_reaction: (e, t) => t > 1
    ? `${e} и ещё ${t - 1} оценили ваш комментарий`
    : `${e} оценил(а) ваш комментарий`,
  comment_reply: e => `${e} ответил(а) на ваш комментарий`,
  post_mention: e => `${e} упомянул(а) вас в посте`,
  comment_mention: e => `${e} упомянул(а) вас в комментарии`,
  wall_post: e => `${e} написал(а) на вашей стене`,
  like: (e, t) => t > 1
    ? `${e} и ещё ${t - 1} оценили ваш пост`
    : `${e} оценил(а) ваш пост`,
  comment: e => `${e} прокомментировал(а) ваш пост`,
  reply: e => `${e} ответил(а) на ваш комментарий`,
  repost: (e, t) => t > 1 ? `${e} и ещё ${t - 1} сделали репост` : `${e} сделал(а) репост`,
  mention: e => `${e} упомянул(а) вас в посте`,
};

function Ry(e, t, n) {
  const Ny_e = Ny[e];
  return Ny_e ? Ny_e(t, n) : "Новое уведомление";
}
function Py() {
  try {
    const e = new Audio("/assets/notification.ogg");
    (e.volume = 0.5);
    e.play().catch(() => {});
  } catch {}
}

const Il = () => kt(e => e.unreadCount);

const Ay = () => kt(e => e.lastSseToast);

const Oy = 0.5;
const xy = 500;
const Ly = 5000/* 5e3 */;
const ra = 5000/* 5e3 */;
class My {
  tokens = new Map();
  legacyViewed = new Set();
  sessions = new Map();
  elementToPostId = new WeakMap();
  observer = null;
  constructor() {
    if (typeof window !== "undefined") {
      this.initObserver();
      this.bindLifecycleHandlers();
    }
  }
  registerPostTokens(t) {
    if (!(!t || t.length === 0)) {
      for (const n of t) {
        if (n.vt) {
          this.tokens.set(n.id, n.vt);
        }
      }
      if (this.tokens.size > ra) {
        const n = this.tokens.size - ra;
        const r = this.tokens.keys();
        for (let o = 0; o < n; o++) {
          const s = r.next().value;

          if (s) {
            this.tokens.delete(s);
          }
        }
      }
    }
  }
  observe(t, n) {
    if (this.observer) {
      this.elementToPostId.set(n, t);
      this.observer.observe(n);
    }
  }
  unobserve(t) {
    if (!this.observer) {
      return;
    }
    const n = this.elementToPostId.get(t);

    if (n) {
      this.endSession(n, true);
    }

    this.observer.unobserve(t);
  }
  isViewed(t) {
    return this.legacyViewed.has(t) || this.sessions.has(t);
  }
  clearViewedCache() {
    this.legacyViewed.clear();
    this.tokens.clear();
    for (const t of this.sessions.keys()) {
      this.endSession(t, false);
    }
  }
  destroy() {
    for (const t of this.sessions.keys()) {
      this.endSession(t, false);
    }

    if (this.observer) {
      this.observer.disconnect();
      (this.observer = null);
    }
  }
  initObserver() {
    this.observer = new IntersectionObserver(
      (t) => {
        for (const n of t) {
          const r = this.elementToPostId.get(n.target);

          if (r) {
            if (n.isIntersecting) {
              this.beginSession(r);
            } else {
              this.endSession(r, false);
            }
          }
        }
      },
      { threshold: Oy }
    );
  }
  bindLifecycleHandlers() {
    document.addEventListener("visibilitychange", () => {
      if (document.hidden) {
        for (const n of Array.from(this.sessions.keys())) {
          this.endSession(n, true);
        }
      }
    });
    const t = () => {
      for (const n of Array.from(this.sessions.keys())) {
        this.flushFinalHeartbeat(n);
      }
    };
    window.addEventListener("pagehide", t);
    window.addEventListener("beforeunload", t);
  }
  beginSession(t) {
    if (this.sessions.has(t)) {
      return;
    }
    const n = this.tokens.get(t);
    if (!n) {
      if (!this.legacyViewed.has(t)) {
        this.legacyViewed.add(t);

        P.post(x.posts.view(t)).catch(() => {
          this.legacyViewed.delete(t);
        });
      }

      return;
    }
    const r = { postId: t, token: n, startedAt: Date.now(), hbInFlight: false };
    this.sessions.set(t, r);

    (r.initialDelayId = window.setTimeout(() => {
      (r.initialDelayId = undefined);
      this.sendHeartbeat(r, false);

      (r.intervalId = window.setInterval(() => {
          this.sendHeartbeat(r, false);
        }, Ly));
    }, xy));
  }
  endSession(t, n) {
    const r = this.sessions.get(t);

    if (r) {
      r.initialDelayId !== undefined &&
          (clearTimeout(r.initialDelayId), (r.initialDelayId = undefined));

      r.intervalId !== undefined &&
        (clearInterval(r.intervalId), (r.intervalId = undefined));

      n && this.sendHeartbeat(r, true);
      this.sessions.delete(t);
    }
  }
  async sendHeartbeat(t, n) {
    if (!(t.hbInFlight && !n)) {
      t.hbInFlight = true;
      try {
        await P.post(`${x.posts.view(t.postId)}/heartbeat`, {
          vt: t.token,
          f: n || undefined,
        });
      } catch {
      } finally {
        t.hbInFlight = false;
      }
    }
  }
  flushFinalHeartbeat(t) {
    const n = this.sessions.get(t);
    if (n) {
      if (typeof navigator !== "undefined" && navigator.sendBeacon) {
        const r = `${x.posts.view(t)}/heartbeat`;
        const o = r.startsWith("http") ? r : `${window.location.origin}/api${r}`;
        const s = JSON.stringify({ vt: n.token, f: true });
        navigator.sendBeacon(o, new Blob([s], { type: "application/json" }));
      }

      if (n.initialDelayId !== undefined) {
        clearTimeout(n.initialDelayId);
      }

      if (n.intervalId !== undefined) {
        clearInterval(n.intervalId);
      }

      this.sessions.delete(t);
    }
  }
}
const gt = new My();
function bn(e) {
  return e.pagination?.nextCursor ?? e.cursor ?? null;
}
const pt = new _s(50, 300 * 1000/* 1e3 */);
const $y = 60 * 1000/* 1e3 */;
setInterval(() => pt.cleanup(), 120 * 1000/* 1e3 */);
function Dy(e) {
  return {
    id: e.id,
    username: e.username,
    displayName: e.displayName,
    avatar: e.avatar,
    isVerified: e.isVerified ?? e.verified ?? false,
    hasNuksta: e.hasNuksta ?? false,
    pin: e.pin ?? null,
  };
}
function Bt(e) {
  const n = (e.attachments ?? []).map((u) => {
    if (u.type === "poll") {
      const f = u;

      const d = f.options.map(p => ({
        id: p.id,
        text: p.text,
        votes: p.votesCount ?? p.voteCount ?? p.votes ?? 0
      }));

      return {
        ...u,
        options: d,
        totalVotes: f.totalVotes ?? 0,
        multipleChoice: f.multipleChoice ?? false,
        myVotes: f.votedOptionIds?.length
          ? f.votedOptionIds
          : e.viewerStatus?.pollVote
          ? [e.viewerStatus.pollVote]
          : [],
        myVote: f.votedOptionIds?.[0] ?? e.viewerStatus?.pollVote ?? null,
      };
    }
    return u;
  });
  if (e.poll && !n.some(u => u.type === "poll")) {
    const e_poll = e.poll;

    const f = {
      id: e_poll.id,
      type: "poll",
      question: e_poll.question,
      multipleChoice: e_poll.multipleChoice ?? false,
      options: (e_poll.options ?? []).map(d => ({
        id: d.id,
        text: d.text,
        votes: d.votesCount ?? d.voteCount ?? 0
      })),
      totalVotes: e_poll.totalVotes ?? 0,
      myVotes: e_poll.votedOptionIds ?? [],
      myVote: e_poll.votedOptionIds?.length > 0 ? e_poll.votedOptionIds[0] : null,
    };

    n.push(f);
  }
  const r = e.stats?.reactions ?? e.likesCount ?? 0;
  const o = e.stats?.views ?? e.viewsCount ?? 0;
  const s = e.stats?.comments ?? e.commentsCount ?? 0;
  const a = e.stats?.reposts ?? e.repostsCount ?? 0;
  const c = e.viewerStatus?.reaction ?? (e.isLiked ? "like" : null);
  const l = e.text ?? e.content ?? "";
  return {
    id: e.id,
    author: Dy(e.author),
    wallOwnerId: e.wallOwnerId ?? e.authorId ?? e.author?.id,
    text: l,
    spans: e.spans ?? [],
    attachments: n,
    reactions: { total: r, myReaction: c },
    stats: { views: o, comments: s, reposts: a },
    originalPost: e.originalPost ? Bt(e.originalPost) : null,
    dominantEmoji: e.dominantEmoji ?? null,
    createdAt: e.createdAt,
    editedAt: e.editedAt ?? null,
  };
}

const Ue = {
    async getFeed(e, t = {}) {
      const n = new URLSearchParams();
      n.set("limit", String(t.limit || 20));

      n.set(
        "tab",
        e === "global" ? "popular" : e === "clan" ? "clan" : "following"
      );

      if (t.cursor) {
        n.set("cursor", t.cursor);
      }

      const r = n.toString();
      const o = `${x.posts.list}${r ? `?${r}` : ""}`;
      const s = await P.get(o);
      gt.registerPostTokens(s.data.posts);
      return { data: s.data.posts.map(Bt), nextCursor: bn(s.data) };
    },
    async getPost(e) {
      const t = await P.get(x.posts.single(e));
      gt.registerPostTokens([t.data]);
      return Bt(t.data);
    },
    async getUserWall(e, t = {}) {
      if (!t.cursor) {
        const n = e;
        const r = pt.get(n);
        const o = r && r.pinnedPostId === (t.pinnedPostId ?? null);

        if (r && o && pt.isFresh(n, $y)) {
          this._fetchAndCacheWall(e, t, n).catch(() => {});
          return { data: r.posts, nextCursor: r.nextCursor };
        }

        if (r && o) {
          this._fetchAndCacheWall(e, t, n).catch(() => {});
          return { data: r.posts, nextCursor: r.nextCursor };
        }

        return this._fetchAndCacheWall(e, t, n);
      }
      return this._fetchWall(e, t);
    },
    async _fetchAndCacheWall(e, t, n) {
      const r = await this._fetchWall(e, t);

      pt.set(n, {
        posts: r.data,
        nextCursor: r.nextCursor,
        pinnedPostId: t.pinnedPostId ?? null,
      });

      return r;
    },
    async _fetchWall(e, t) {
      const n = new URLSearchParams();

      if (t.limit) {
        n.set("limit", t.limit.toString());
      }

      n.set("sort", "new");

      if (t.cursor) {
        n.set("cursor", t.cursor);
      }

      if (t.pinnedPostId) {
        n.set("pinnedPostId", t.pinnedPostId);
      }

      const r = n.toString();
      const o = `${x.posts.byUser(e)}${r ? `?${r}` : ""}`;
      const s = await P.get(o);
      gt.registerPostTokens(s.data.posts);
      return { data: s.data.posts.map(Bt), nextCursor: bn(s.data) };
    },
    invalidateWallCache(e) {
      pt.delete(e);
    },
    updatePostInWallCache(e, t, n) {
      const r = pt.get(e);
      if (r) {
        const o = r.posts.map(s => s.id === t ? { ...s, ...n } : s);
        pt.set(e, { ...r, posts: o });
      }
    },
    removePostFromWallCache(e, t) {
      const n = pt.get(e);
      if (n) {
        const r = n.posts.filter(o => o.id !== t);
        pt.set(e, { ...n, posts: r });
      }
    },
    async likePost(e) {
      await P.post(x.posts.like(e));
    },
    async unlikePost(e) {
      await P.delete(x.posts.like(e));
    },
    async createPost(e) {
      return await P.post(x.posts.create, {
        content: e.text,
        spans: e.spans,
        wallRecipientId: e.wallOwnerId,
        attachmentIds: e.attachmentIds,
        poll: e.poll,
      });
    },
    async createRepost(e, t) {
      return await P.post(x.posts.repost(e), { content: t });
    },
    async editPost(e, t) {
      const n = t.content ?? t.text;
      await P.put(x.posts.update(e), { content: n, spans: t.spans });
    },
    async deletePost(e) {
      await P.delete(x.posts.delete(e));
    },
    async restorePost(e) {
      await P.post(x.posts.restore(e));
    },
    async trackView(e) {
      P.post(x.posts.view(e)).catch(() => {});
    },
    async trackViewsBatch(e) {
      if (e.length !== 0) {
        for (const t of e) {
          P.post(x.posts.view(t)).catch(() => {});
        }
      }
    },
    async getUserPosts(e, t = {}) {
      const n = new URLSearchParams();

      if (t.limit) {
        n.set("limit", t.limit.toString());
      }

      if (t.cursor) {
        n.set("cursor", t.cursor);
      }

      if (t.sort) {
        n.set("sort", t.sort);
      }

      if (t.pinnedPostId) {
        n.set("pinnedPostId", t.pinnedPostId);
      }

      const r = n.toString();
      const o = `${x.posts.byUser(e)}${r ? `?${r}` : ""}`;
      const s = await P.get(o);
      gt.registerPostTokens(s.data.posts);
      return { data: s.data.posts.map(Bt), nextCursor: bn(s.data) };
    },
    async getUserLikedPosts(e, t = {}) {
      const n = new URLSearchParams();

      if (t.limit) {
        n.set("limit", t.limit.toString());
      }

      if (t.cursor) {
        n.set("cursor", t.cursor);
      }

      const r = n.toString();
      const o = `${x.posts.likedByUser(e)}${r ? `?${r}` : ""}`;
      const s = await P.get(o);
      gt.registerPostTokens(s.data.posts);
      return { data: s.data.posts.map(Bt), nextCursor: bn(s.data) };
    },
    async pinPost(e) {
      await P.post(x.posts.pin(e));
    },
    async unpinPost(e) {
      await P.delete(x.posts.pin(e));
    },
    async votePoll(e, t) {
      const n = await P.post(x.posts.pollVote(e), { optionIds: t });
      return n.data ?? n;
    },
    async unrepost(e) {
      await P.delete(x.posts.repost(e));
    },
    async getPostsByHashtag(e, t = {}) {
      const n = new URLSearchParams();

      if (t.limit) {
        n.set("limit", t.limit.toString());
      }

      if (t.cursor) {
        n.set("cursor", t.cursor);
      }

      const r = n.toString();
      const o = `${x.hashtags.posts(e)}${r ? `?${r}` : ""}`;
      const s = await P.get(o);
      gt.registerPostTokens(s.data.posts);
      return { data: s.data.posts.map(Bt), nextCursor: bn(s.data) };
    },
  };

const Uy = { new: "newest", old: "oldest", popular: "popular" };
function Fy(e) {
  return {
    id: e.id,
    username: e.username,
    displayName: e.displayName,
    avatar: e.avatar,
    isVerified: e.isVerified ?? e.verified ?? false,
    hasNuksta: e.hasNuksta ?? false,
    pin: e.pin ?? null,
  };
}
function Uo(e) {
  const t = e.stats?.reactions ?? e.likesCount ?? 0;
  const n = e.stats?.replies ?? e.repliesCount ?? 0;
  const r = e.viewerStatus?.reaction ?? (e.isLiked ? "like" : null);
  const o = e.text ?? e.content ?? "";
  return {
    id: e.id,
    postId: e.postId,
    author: Fy(e.author),
    parentId: e.parentId,
    rootId: e.rootId ?? null,
    text: o,
    spans: e.spans ?? [],
    attachments: e.attachments ?? [],
    reactions: { total: t, myReaction: r },
    stats: { replies: n },
    replyTo: e.replyTo ?? null,
    previewReplies:
      e.previewReplies ?? e.replies
        ? (e.previewReplies ?? e.replies).map(Uo)
        : undefined,
    createdAt: e.createdAt,
    editedAt: e.editedAt ?? null,
  };
}

const et = {
    async getComments(e, t = {}) {
      const n = new URLSearchParams();

      if (t.limit) {
        n.set("limit", t.limit.toString());
      }

      if (t.sort) {
        n.set("sort", Uy[t.sort]);
      }

      if (t.cursor) {
        n.set("cursor", t.cursor);
      }

      const r = n.toString();
      const o = `${x.posts.comments(e)}${r ? `?${r}` : ""}`;
      const s = await P.get(o);
      let a = [];
      let c = null;

      if (Array.isArray(s.data)) {
        (a = s.data);
      } else if (s.data && "comments" in s.data) {
        (a = s.data.comments);
        (c = s.data.nextCursor ?? null);
      } else if (s.comments) {
        (a = s.comments);
      }

      (c = c ?? s.cursor ?? s.meta?.cursor?.next ?? null);
      return { data: a.map(Uo), nextCursor: c };
    },
    async getReplies(e, t = {}) {
      const n = new URLSearchParams();

      if (t.limit) {
        n.set("limit", t.limit.toString());
      }

      if (t.cursor) {
        n.set("cursor", t.cursor);
      }

      const r = n.toString();
      const o = `${x.comments.replies(e)}${r ? `?${r}` : ""}`;
      const s = await P.get(o);
      let a = [];
      let c = null;

      if (Array.isArray(s.data)) {
        (a = s.data);
      } else if (s.data && "replies" in s.data) {
        (a = s.data.replies);
        (c = s.data.nextCursor ?? null);
      } else if (s.replies) {
        (a = s.replies);
      }

      (c = c ?? s.cursor ?? s.meta?.cursor?.next ?? null);
      return { data: a.map(Uo), nextCursor: c };
    },
    async createComment(e, t, n, r, o) {
      return await P.post(x.posts.comments(e), {
        content: t,
        attachmentIds: o?.map(s => s.mediaId),
      });
    },
    async createReply(e, t, n, r, o) {
      return await P.post(x.comments.replies(e), {
        content: t,
        replyToUserId: r,
        attachmentIds: o?.map(s => s.mediaId),
      });
    },
    async editComment(e, t, n) {
      await P.patch(x.comments.edit(e), { content: t });
    },
    async deleteComment(e) {
      await P.delete(x.comments.delete(e));
    },
    async likeComment(e) {
      await P.post(x.comments.like(e));
    },
    async unlikeComment(e) {
      await P.delete(x.comments.like(e));
    },
  };

const se = at((e, t) => ({
  posts: [],
  activeFeed: "global",
  isLoading: false,
  isLoadingMore: false,
  isRefreshing: false,
  hasMore: true,
  nextCursor: null,
  error: null,
  feedScrollPosition: 0,
  feedMeasuredHeights: new Map(),
  profileScrollByUser: {},
  profileMeasuredHeightsByUser: {},
  highlightedPostId: null,
  _lastPostEdit: null,
  _lastLikeUpdate: null,
  currentPost: null,
  currentPostLoading: false,
  currentPostError: false,

  setActiveFeed: (n) => {
    if (t().activeFeed !== n) {
      t().feedMeasuredHeights.clear();

      e({
        activeFeed: n,
        posts: [],
        hasMore: true,
        nextCursor: null,
        isLoading: false,
        error: null,
        feedScrollPosition: 0,
      });
    }
  },

  fetchFeed: async (n = false) => {
    const { activeFeed: r, isLoading: o, isRefreshing: s } = t();
    if (!(o || s)) {
      e({ isLoading: !n, isRefreshing: n, error: null });
      try {
        const a = await Ue.getFeed(r, { limit: 20 });
        e({
          posts: a.data,
          nextCursor: a.nextCursor,
          hasMore: a.nextCursor !== null,
          isLoading: false,
          isRefreshing: false,
        });
      } catch (a) {
        e({
          isLoading: false,
          isRefreshing: false,
          error: a instanceof Error ? a.message : "Ошибка загрузки",
        });
      }
    }
  },

  loadMoreFeed: async () => {
    const {
      activeFeed: n,
      isLoadingMore: r,
      hasMore: o,
      nextCursor: s,
    } = t();
    if (!(r || !o)) {
      e({ isLoadingMore: true });
      try {
        const a = await Ue.getFeed(n, { limit: 20, cursor: s ?? undefined });
        e(c => ({
          posts: [...c.posts, ...a.data],
          nextCursor: a.nextCursor,
          hasMore: a.nextCursor !== null,
          isLoadingMore: false
        }));
      } catch {
        e({ isLoadingMore: false });
      }
    }
  },

  createPost: async ({
    wallOwnerId: n,
    text: r,
    spans: o = [],
    attachments: s = [],
    poll: a,
  }) => {
    const c = he.getState().profile;
    if (!c) {
      throw new Error("Not authenticated");
    }
    try {
      const l = s.map(({ mediaId: p }) => p);

      const { id: u } = await Ue.createPost({
        wallOwnerId: n !== c.id ? n : undefined,
        text: r,
        spans: o.length > 0 ? o : undefined,
        attachmentIds: l.length > 0 ? l : undefined,
        poll: a
          ? {
              question: a.question,
              options: a.options,
              multipleChoice: a.multipleChoice ?? false,
            }
          : undefined,
      });

      const f = s.map(({ url: p }, h) => ({
        id: `temp-${h}`,
        url: p,
        type: "image"
      }));

      if (a) {
        f.push({
          id: `temp-poll-${Date.now()}`,
          type: "poll",
          question: a.question,
          options: a.options.map((p, h) => ({
            id: `temp-opt-${h}`,
            text: p.text,
            votes: 0
          })),
          totalVotes: 0,
          myVote: null,
        });
      }

      const d = {
        id: u,
        author: {
          id: c.id,
          username: c.username,
          displayName: c.displayName,
          avatar: c.avatar,
          isVerified: c.isVerified,
        },
        wallOwnerId: n,
        text: r,
        spans: o,
        attachments: f,
        reactions: { total: 0, myReaction: null },
        stats: { views: 0, comments: 0, reposts: 0 },
        originalPost: null,
        dominantEmoji: null,
        createdAt: new Date().toISOString(),
        editedAt: null,
      };

      e(p => ({
        posts: [d, ...p.posts],
        highlightedPostId: u
      }));

      Ue.invalidateWallCache(n);
      try {
        const p = await Ue.getPost(u);
        e(h => ({
          posts: h.posts.map(m => m.id === u ? p : m)
        }));
      } catch {}
    } catch (l) {
      console.error("Failed to create post:", l);
      throw l;
    }
  },

  clearHighlightedPost: () => {
    e({ highlightedPostId: null });
  },

  fetchPost: async (n) => {
    const r = t().posts.find(o => o.id === n);
    if (r) {
      e({ currentPost: r, currentPostLoading: false, currentPostError: false });
      return;
    }
    if (t().currentPost?.id !== n) {
      e({ currentPostLoading: true, currentPost: null, currentPostError: false });
      try {
        const o = await Ue.getPost(n);
        e({ currentPost: o, currentPostLoading: false, currentPostError: false });
      } catch {
        e({
          currentPost: null,
          currentPostLoading: false,
          currentPostError: true,
        });
      }
    }
  },

  setCurrentPost: (n) => {
    e({ currentPost: n, currentPostLoading: false, currentPostError: false });
  },

  editPost: async (n, r, o) => {
    await Ue.editPost(n, { text: r, spans: o });
    const { posts: s, currentPost: a } = t();
    const c = new Date().toISOString();

    const l = s.find(u => u.id === n);

    e({
      posts: s.map(u => u.id === n ? { ...u, text: r, spans: o ?? u.spans, editedAt: c } : u
      ),
      currentPost:
        a?.id === n ? { ...a, text: r, spans: o ?? a.spans, editedAt: c } : a,
      _lastPostEdit: { postId: n, text: r, spans: o ?? [], editedAt: c },
    });

    if (l?.wallOwnerId) {
      Ue.updatePostInWallCache(l.wallOwnerId, n, {
        text: r,
        spans: o ?? l.spans,
        editedAt: c,
      });
    }
  },

  deletePost: async (n) => {
    const { posts: r, currentPost: o } = t();
    const s = r;

    const a = r.find(c => c.id === n);

    e({
      posts: r.filter(c => c.id !== n),
      currentPost: o?.id === n ? null : o,
    });
    try {
      await Ue.deletePost(n);
    } catch (c) {
      console.error("Failed to delete post:", c);

      if (a) {
        e({ posts: s });
      }

      throw c;
    }
  },

  updatePostLike: (n, r, o) => {
    e((s) => {
      const a = s.posts.map(l => l.id === n
        ? {
            ...l,
            reactions: {
              ...l.reactions,
              myReaction: r,
              total: Math.max(0, l.reactions.total + o),
            },
          }
        : l
        );

      const c =
        s.currentPost?.id === n
          ? {
              ...s.currentPost,
              reactions: {
                ...s.currentPost.reactions,
                myReaction: r,
                total: Math.max(0, s.currentPost.reactions.total + o),
              },
            }
          : s.currentPost;

      return {
        posts: a,
        currentPost: c,
        _lastLikeUpdate: { postId: n, myReaction: r, totalDelta: o },
      };
    });
  },

  updatePollVote: (n, r, o) => {
    const s = (a) => {
      const c = a.attachments.findIndex(h => h.type === "poll");
      if (c === -1) {
        return a;
      }
      const l = a.attachments[c];

      const u = l.options.map(h => h.id === r
        ? { ...h, votes: (h.votes ?? 0) + 1 }
        : h.id === o
        ? { ...h, votes: Math.max(0, (h.votes ?? 0) - 1) }
        : h
      );

      const f = o ? 0 : 1;

      const d = {
        ...l,
        options: u,
        totalVotes: (l.totalVotes ?? 0) + f,
        myVote: r,
      };

      const p = [...a.attachments];
      (p[c] = d);
      return { ...a, attachments: p };
    };
    e((a) => {
      const c = a.posts.map(u => u.id === n ? s(u) : u);

      const l = a.currentPost?.id === n ? s(a.currentPost) : a.currentPost;
      return { posts: c, currentPost: l };
    });
  },

  updatePollData: (n, r) => {
    const o = (s) => {
      const a = s.attachments.findIndex(u => u.type === "poll");
      if (a === -1) {
        return s;
      }

      const c = {
          ...s.attachments[a],
          options: (r.options ?? []).map(u => ({
            id: u.id,
            text: u.text,
            votes: u.votesCount ?? u.voteCount ?? 0
          })),
          totalVotes: r.totalVotes ?? 0,
          myVote:
            (r.votedOptionIds?.length ?? 0) > 0 ? r.votedOptionIds[0] : null,
        };

      const l = [...s.attachments];
      (l[a] = c);
      return { ...s, attachments: l };
    };
    e((s) => {
      const a = s.posts.map(l => l.id === n ? o(l) : l);

      const c = s.currentPost?.id === n ? o(s.currentPost) : s.currentPost;
      return { posts: a, currentPost: c };
    });
  },

  setFeedScrollPosition: (n) => {
    e({ feedScrollPosition: n });
  },

  setFeedMeasuredHeights: (n) => {
    e({ feedMeasuredHeights: n });
  },

  setProfileScrollPosition: (n, r) => {
    e(o => ({
      profileScrollByUser: { ...o.profileScrollByUser, [n]: r }
    }));
  },

  setProfileMeasuredHeights: (n, r) => {
    e(o => ({
      profileMeasuredHeightsByUser: {
        ...o.profileMeasuredHeightsByUser,
        [n]: r,
      }
    }));
  },

  reset: () => {
    t().feedMeasuredHeights.clear();

    X(async () => {
      const { useCommentsStore: n } = await Promise.resolve().then(
        () => Wy
      );
      return { useCommentsStore: n };
    }, undefined).then(({ useCommentsStore: n }) => {
      n.getState().reset();
    });

    e({
      posts: [],
      isLoading: false,
      isLoadingMore: false,
      isRefreshing: false,
      hasMore: true,
      nextCursor: null,
      error: null,
      feedScrollPosition: 0,
      profileScrollByUser: {},
      profileMeasuredHeightsByUser: {},
      highlightedPostId: null,
      _lastPostEdit: null,
      _lastLikeUpdate: null,
      currentPost: null,
      currentPostLoading: false,
      currentPostError: false,
    });
  }
}));

const Qr = Object.freeze(
  Object.defineProperty(
    { __proto__: null, usePostsStore: se },
    Symbol.toStringTag,
    { value: "Module" }
  )
);

const _r = at()(
  il(
    e => ({
      commentsSort: "popular",

      setCommentsSort: (t) => {
        e({ commentsSort: t });
      }
    }),
    { name: "settings", storage: ds(() => localStorage) }
  )
);

const je = new Map();
const Hy = 60 * 1000/* 1e3 */;
const By = 300 * 1000/* 1e3 */;
const oa = 20;
const sa = 500;
function Vy() {
  const e = Date.now();
  for (const [t, n] of je.entries()) {
    if (e - n.timestamp > By) {
      je.delete(t);
    }
  }
  if (je.size > oa) {
    const t = Array.from(je.entries()).sort(
      (r, o) => r[1].timestamp - o[1].timestamp
    );
    t.slice(0, t.length - oa).forEach(([r]) => je.delete(r));
  }
}

const It = at((e, t) => ({
  comments: [],
  commentsLoading: false,
  commentsLoadingMore: false,
  commentsHasMore: true,
  commentsNextCursor: null,
  highlightedCommentId: null,

  clearComments: () => {
    e({
      comments: [],
      commentsLoading: true,
      commentsHasMore: true,
      commentsNextCursor: null,
    });
  },

  fetchComments: async (n) => {
    const { usePostsStore: r } = await X(async () => {
        const { usePostsStore: u } = await Promise.resolve().then(() => Qr);
        return { usePostsStore: u };
      }, undefined);

    const o = r.getState().currentPost;
    if (o && o.id === n && o.stats.comments === 0) {
      e({
        comments: [],
        commentsLoading: false,
        commentsHasMore: false,
        commentsNextCursor: null,
      });
      return;
    }
    Vy();
    const s = _r.getState().commentsSort;
    const a = n;
    const c = je.get(a);
    const l = Date.now();
    if (c && c.sort === s) {
      if (l - c.timestamp < Hy) {
        e({
          comments: c.comments,
          commentsNextCursor: c.nextCursor,
          commentsHasMore: c.hasMore,
          commentsLoading: false,
        });

        et
          .getComments(n, { limit: 100, sort: s })
          .then((f) => {
          const f_data = f.data;

          je.set(a, {
            comments: f_data,
            hasMore: f.nextCursor !== null,
            nextCursor: f.nextCursor,
            timestamp: Date.now(),
            sort: s,
          });

          e(p => p.comments.length > 0 && p.comments[0]?.postId === n
            ? {
                comments: f_data,
                commentsNextCursor: f.nextCursor,
                commentsHasMore: f.nextCursor !== null,
              }
            : p
          );
        })
          .catch(() => {});

        return;
      } else {
        e({
          comments: c.comments,
          commentsNextCursor: c.nextCursor,
          commentsHasMore: c.hasMore,
          commentsLoading: true,
        });
      }
    } else {
      e({
        commentsLoading: true,
        comments: [],
        commentsHasMore: true,
        commentsNextCursor: null,
      });
    }
    try {
      const u = await et.getComments(n, { limit: 100, sort: s });
      const u_data = u.data;

      je.set(a, {
        comments: u_data,
        hasMore: u.nextCursor !== null,
        nextCursor: u.nextCursor,
        timestamp: Date.now(),
        sort: s,
      });

      e({
        comments: u_data,
        commentsNextCursor: u.nextCursor,
        commentsHasMore: u.nextCursor !== null,
        commentsLoading: false,
      });
    } catch {
      e({ commentsLoading: false });
    }
  },

  loadMoreComments: async (n) => {
    const {
      commentsLoadingMore: r,
      commentsHasMore: o,
      commentsNextCursor: s,
    } = t();
    if (r || !o) {
      return;
    }
    const a = _r.getState().commentsSort;
    e({ commentsLoadingMore: true });
    try {
      const c = await et.getComments(n, {
        limit: 100,
        sort: a,
        cursor: s ?? undefined,
      });
      e((l) => {
        const u = [...l.comments, ...c.data];
        return {
          comments: u.length > sa ? u.slice(-sa) : u,
          commentsNextCursor: c.nextCursor,
          commentsHasMore: c.nextCursor !== null,
          commentsLoadingMore: false,
        };
      });
    } catch {
      e({ commentsLoadingMore: false });
    }
  },

  toggleCommentLike: async (n) => {
    const { comments: r } = t();

    let o = r.findIndex(g => g.id === n);

    let s = false;
    let a = -1;
    let c = -1;
    if (o === -1) {
      for (let g = 0; g < r.length; g++) {
        const _ = (r[g].previewReplies ?? []).findIndex(y => y.id === n);
        if (_ !== -1) {
          (s = true);
          (a = g);
          (c = _);
          break;
        }
      }
    }
    if (o === -1 && !s) {
      return;
    }
    const l = s ? r[a].previewReplies[c] : r[o];
    const u = l.reactions.myReaction;
    const f = l.reactions.total;
    const d = u !== null;
    const p = d ? Math.max(0, f - 1) : f + 1;
    const h = d ? null : "love";
    const m = [...r];
    if (s) {
      const g = [...(m[a].previewReplies ?? [])];
      (g[c] = { ...g[c], reactions: { total: p, myReaction: h } });
      (m[a] = { ...m[a], previewReplies: g });
    } else {
      m[o] = { ...m[o], reactions: { total: p, myReaction: h } };
    }
    e({ comments: m });
    try {
      if (d) {
        await et.unlikeComment(n);
      } else {
        await et.likeComment(n);
      }
    } catch (g) {
      console.error("Failed to toggle comment like:", g);
      const T = [...t().comments];
      if (s) {
        const _ = T.findIndex(y => y.previewReplies?.some(w => w.id === n)
        );
        if (_ !== -1) {
          const y = T[_].previewReplies.findIndex(w => w.id === n);
          if (y !== -1) {
            const w = [...T[_].previewReplies];
            (w[y] = { ...w[y], reactions: { total: f, myReaction: u } });
            (T[_] = { ...T[_], previewReplies: w });
          }
        }
      } else {
        const _ = T.findIndex(y => y.id === n);

        if (_ !== -1) {
          (T[_] = { ...T[_], reactions: { total: f, myReaction: u } });
        }
      }
      e({ comments: T });
    }
  },

  addComment: async (
    n,
    {
      text: r,
      spans: o = [],
      parentId: s,
      attachments: a,
      replyToUserId: c,
      replyToInfo: l,
    }
  ) => {
    try {
      const u = s
          ? await et.createReply(s, r, o, c, a)
          : await et.createComment(n, r, o, undefined, a);

      const f = he.getState().profile;
      if (f) {
        const g = (u.attachments || [])
            .filter(_ => typeof _ == "object" && _ !== null)
            .map(_ => _.type === "media" && _.media
          ? {
              id: _.media.id,
              type: _.media.type,
              url: _.media.url,
              duration: _.media.duration,
            }
          : { id: _.id, type: _.type, url: _.url, duration: _.duration }
            );

        const T = {
          id: u.id,
          postId: n,
          author: {
            id: f.id,
            username: f.username,
            displayName: f.displayName,
            avatar: f.avatar,
            isVerified: f.isVerified,
          },
          parentId: s ?? null,
          rootId: s ?? null,
          text: u.text ?? u.content ?? r,
          spans: u.spans,
          attachments: g,
          replyTo: u.replyTo ?? l ?? null,
          reactions: { total: 0, myReaction: null },
          stats: { replies: 0 },
          createdAt: new Date().toISOString(),
          editedAt: null,
        };

        e(
          s
            ? _ => ({
            comments: _.comments.map(y => y.id === s
              ? {
                  ...y,
                  previewReplies: [...(y.previewReplies || []), T],
                  stats: { ...y.stats, replies: y.stats.replies + 1 },
                }
              : y
            ),

            highlightedCommentId: u.id
          })
            : _ => ({
            comments: [T, ..._.comments],
            highlightedCommentId: u.id
          })
        );
      }

      const { usePostsStore: d } = await X(async () => {
          const { usePostsStore: g } = await Promise.resolve().then(() => Qr);
          return { usePostsStore: g };
        }, undefined);

      const p = d.getState();

      if (p.currentPost &&
        p.currentPost.id === n) {
        d.setState({
          currentPost: {
            ...p.currentPost,
            stats: {
              ...p.currentPost.stats,
              comments: p.currentPost.stats.comments + 1,
            },
          },
        });
      }

      const h = _r.getState().commentsSort;
      const m = je.get(n);

      if (m) {
        je.set(n, { ...m, comments: t().comments, timestamp: Date.now() });
      } else {
        je.set(n, {
              comments: t().comments,
              hasMore: t().commentsHasMore,
              nextCursor: t().commentsNextCursor,
              timestamp: Date.now(),
              sort: h,
            });
      }
    } catch (u) {
      console.error("Failed to add comment:", u);
      throw u;
    }
  },

  loadReplies: async (n) => {
    const { comments: r } = t();
    if (r.findIndex(s => s.id === n) !== -1) {
      try {
        const s = await et.getReplies(n, { limit: 100 });
        e(a => ({
          comments: a.comments.map(c => c.id === n ? { ...c, previewReplies: s.data } : c
          )
        }));
      } catch (s) {
        console.error("Failed to load replies:", s);
      }
    }
  },

  editComment: async (n, r, o) => {
    const { comments: s } = t();

    const a = l => l.map(u => u.id === n
      ? {
          ...u,
          text: r,
          spans: o ?? u.spans,
          editedAt: new Date().toISOString(),
        }
      : u.previewReplies
      ? {
          ...u,
          previewReplies: u.previewReplies.map(f => f.id === n
            ? {
                ...f,
                text: r,
                spans: o ?? f.spans,
                editedAt: new Date().toISOString(),
              }
            : f
          ),
        }
      : u
    );

    const c = s;
    e({ comments: a(s) });
    try {
      await et.editComment(n, r, o);
    } catch (l) {
      console.error("Failed to edit comment:", l);
      e({ comments: c });
      throw l;
    }
  },

  deleteComment: async (n) => {
    const { comments: r } = t();

    const o = r.some(u => u.id === n);

    const s = r.find(u => u.previewReplies?.some(f => f.id === n));

    const a = r;

    if (o) {
      e({ comments: r.filter(u => u.id !== n) });
    } else if (s) {
      e({
        comments: r.map(u => u.id === s.id
          ? {
              ...u,
              previewReplies: u.previewReplies?.filter(f => f.id !== n),
              stats: { ...u.stats, replies: u.stats.replies - 1 },
            }
          : u
        ),
      });
    }

    const { usePostsStore: c } = await X(async () => {
        const { usePostsStore: u } = await Promise.resolve().then(() => Qr);
        return { usePostsStore: u };
      }, undefined);

    const l = c.getState().currentPost;

    if (l) {
      c.setState({
        currentPost: {
          ...l,
          stats: { ...l.stats, comments: Math.max(0, l.stats.comments - 1) },
        },
      });
    }

    try {
      await et.deleteComment(n);
    } catch (u) {
      console.error("Failed to delete comment:", u);
      e({ comments: a });
      throw u;
    }
  },

  clearHighlightedComment: () => {
    e({ highlightedCommentId: null });
  },

  setHighlightedCommentId: (n) => {
    e({ highlightedCommentId: n });
  },

  reset: () => {
    je.clear();

    e({
      comments: [],
      commentsLoading: false,
      commentsLoadingMore: false,
      commentsHasMore: true,
      commentsNextCursor: null,
      highlightedCommentId: null,
    });
  }
}));

const Wy = Object.freeze(
  Object.defineProperty(
    { __proto__: null, useCommentsStore: It },
    Symbol.toStringTag,
    { value: "Module" }
  )
);

function jy(e, t) {
  D(() => {
    const t_current = t.current;
    if (!(!t_current || gt.isViewed(e))) {
      gt.observe(e, t_current);

      return () => {
        gt.unobserve(t_current);
      };
    }
  }, [e, t]);
}

const zy = Z(() => X(() => import("./index-DDf9w1fw.js"), __vite__mapDeps([4, 5])).then(
  e => ({
    default: e.ChangelogModal
  })
)
  );

const qy = Z(() => X(() => import("./index-Cs8C4zj9.js"), __vite__mapDeps([0, 1, 2, 3])).then(
  e => ({
    default: e.SubscriptionModal
  })
)
);

const kl = ({
  href: e,
  icon: t,
  children: n,
  badge: r,
  onActiveClick: o,
  isActive: s = false,
}) => {
  const [a] = Xn();
  const c = a.url || "/";
  const u = c === e || c.startsWith(`${e}/`) || s;
  return i("a", {
    href: e,
    className: `${Oe.navItem} ${u ? Oe.active : ""}`,
    onClick: (f) => {
      if (u && o) {
        f.preventDefault();
        o();
      }
    },
    children: [
      i("span", {
        className: Oe.iconWrapper,
        children: [
          t,
          r !== undefined &&
            r > 0 &&
            i("span", { className: Oe.badge, children: r > 99 ? "99+" : r }),
        ],
      }),
      i("span", { children: n }),
    ],
  });
};

const eo = e => i(kl, { ...e });

const Gy = () => {
  const [e] = Xn();

  const t = se(I => I.fetchFeed);

  const n = se(I => I.isRefreshing);

  const r = he(I => I.logout);

  const o = Cl();
  const s = ys();
  const a = Il();
  const { initialize: c, disconnectSSE: l } = kt();
  const [u, f] = A(false);
  const [d, p] = A(false);
  const h = ll();

  const m = fs(I => I.fetchPortal);

  const g = s?.username ? `/@${s.username}` : "/profile";

  D(
    () => {
      if (o) {
        c();
      }

      return () => {
        l();
      };
    },
    [o, c, l]
  );

  D(() => {
    m();
  }, [m]);

  const T = k(() => {
    window.scrollTo({ top: 0, behavior: "instant" });
    t(true);
  }, [t]);

  const _ = k(() => {
    r();
  }, [r]);

  const y = ge(() => {
    const I = e.url || "/";
    return gs.some(S => I.startsWith(S));
  }, [e.url]);

  const w = ge(() => {
    const I = e.url || "/";
    return s?.username
      ? I === `/@${s.username}` || I.startsWith(`/@${s.username}/`)
      : false;
  }, [e.url, s?.username]);

  return y
    ? null
    : i("aside", {
        className: Oe.aside,
        children: [
          i("div", {
            className: Oe.asideTop,
            children: [
              i("div", {
                className: Oe.asideBrand,
                children: [
                  i(hg, {}),
                  i("button", {
                    className: Oe.asideBrandVersion,
                    onClick: () => f(true),
                    title: "Что нового",
                    children: "v1.1",
                  }),
                ],
              }),
              i("nav", {
                className: Oe.nav,
                children: [
                  i(eo, {
                    href: "/",
                    icon: n ? i(hs, {}) : i(hl, {}),
                    onActiveClick: T,
                    children: "Лента",
                  }),
                  i(eo, {
                    href: "/search",
                    icon: i(wl, {}),
                    children: "Поиск",
                  }),
                  i(bg, {
                    path: "/event",
                    children: ({ matches: I }) => i("a", {
                      href: h.active && h.url ? h.url : "/event",
                      target: h.active && h.url ? "_blank" : undefined,
                      rel:
                        h.active && h.url ? "noopener noreferrer" : undefined,
                      className: `${Oe.portalButton} ${
                        h.active ? Oe.portalActive : ""
                      } ${I ? Oe.active : ""}`,
                      title: "Ивент",
                      children: [
                        i("img", {
                          src: h.active
                            ? "/assets/portal/portal-active.gif"
                            : "/assets/portal/portal-inactive.png",
                          alt: "Ивент",
                          className: Oe.portalImage,
                        }),
                        i("span", { children: "Ивент" }),
                      ],
                    }),
                  }),
                  i(eo, {
                    href: "/notifications",
                    icon: i(yl, {}),
                    badge: a,
                    children: "Уведомления",
                  }),
                  i(kl, {
                    href: g,
                    icon: i(vl, {}),
                    isActive: w,
                    children: "Профиль",
                  }),
                ],
              }),
            ],
          }),
          i("div", {
            className: Oe.asideBottom,
            children: [
              !s?.subscription?.isActive &&
                i("button", {
                  className: Oe.logoutButton,
                  onClick: () => p(true),
                  children: [
                    i("span", { children: "⭐" }),
                    i("span", { children: "ИТД НУКСТА" }),
                  ],
                }),
              i("button", {
                className: Oe.logoutButton,
                onClick: _,
                children: [
                  i(gg, { size: 20 }),
                  i("span", { children: "Выйти" }),
                ],
              }),
            ],
          }),
          u &&
            i(Ee, {
              fallback: null,
              children: i(zy, { isOpen: u, onClose: () => f(false) }),
            }),
          d &&
            i(Ee, {
              fallback: null,
              children: i(qy, { isOpen: d, onClose: () => p(false) }),
            }),
        ],
      });
};

const Yy = "TWZf";
const Xy = "iOAx";
const Ky = "omRp";
const Zy = "omOO";
const Cn = { sidebar: Yy, sidebarContent: Xy, sidebarBottom: Ky, legalLinks: Zy };

const Jy = () => {
  const [e] = Xn();
  return ge(() => {
    const n = e.url || "/";
    return gs.some(r => n.startsWith(r));
  }, [e.url])
    ? null
    : i("aside", {
        className: Cn.sidebar,
        children: [
          i("div", { className: Cn.sidebarContent }),
          i("div", {
            className: Cn.sidebarBottom,
            children: [
              i("ul", {
                className: Cn.legalLinks,
                children: [
                  i("li", {
                    children: i("a", {
                      href: "https://статус.итд.com",
                      target: "_blank",
                      rel: "noopener noreferrer",
                      children: "Статус серверов",
                    }),
                  }),
                  i("li", {
                    children: i("a", {
                      href: "/terms",
                      children: "Условия использования",
                    }),
                  }),
                  i("li", {
                    children: i("a", {
                      href: "/privacy",
                      children: "Конфиденциальность",
                    }),
                  }),
                  i("li", {
                    children: i("a", {
                      href: "/cookies",
                      children: "Политика Cookies",
                    }),
                  }),
                ],
              }),
              i("span", {
                className: Cn.copyright,
                children: "© 2026 ООО «ИТД»",
              }),
            ],
          }),
        ],
      });
};

const Qy = "ediY";
const ev = "liP0";
const tv = "N0ZZ";
const nv = "bT7v";
const rv = "AnhZ";
const ov = "Jfl6";
const sv = "t7i5";
const iv = "FEV3";
const av = "nQw1";
const cv = "DeUf";
const lv = "DcOO";
const uv = "aSVJ";

const De = {
  mobileNavigationWrapper: Qy,
  navigation: ev,
  indicator: tv,
  indicatorHidden: nv,
  navItem: rv,
  label: ov,
  active: sv,
  createButton: iv,
  iconWrapper: av,
  portalImage: cv,
  portalImageActive: lv,
  badge: uv,
};

const ia = e => Symbol.iterator in e;

const aa = e => "entries" in e;

const ca = (e, t) => {
  const n = e instanceof Map ? e : new Map(e.entries());
  const r = t instanceof Map ? t : new Map(t.entries());
  if (n.size !== r.size) {
    return false;
  }
  for (const [o, s] of n) {
    if (!r.has(o) || !Object.is(s, r.get(o))) {
      return false;
    }
  }
  return true;
};

const dv = (e, t) => {
  const n = e[Symbol.iterator]();
  const r = t[Symbol.iterator]();
  let o = n.next();
  let s = r.next();

  while (!o.done && !s.done) {
    if (!Object.is(o.value, s.value)) {
      return false;
    }
    (o = n.next());
    (s = r.next());
  }

  return !!o.done && !!s.done;
};

function fv(e, t) {
  return Object.is(e, t)
    ? true
    : typeof e != "object" ||
      e === null ||
      typeof t != "object" ||
      t === null ||
      Object.getPrototypeOf(e) !== Object.getPrototypeOf(t)
    ? false
    : ia(e) && ia(t)
    ? aa(e) && aa(t)
      ? ca(e, t)
      : dv(e, t)
    : ca(
        { entries: () => Object.entries(e) },
        { entries: () => Object.entries(t) }
      );
}
function la(e) {
  const t = Rn.useRef(undefined);
  return (n) => {
    const r = e(n);
    return fv(t.current, r) ? t.current : (t.current = r);
  };
}

const yt = {
    MAX_CHARS: 1000/* 1e3 */,
    MIN_POLL_OPTIONS: 2,
    MAX_POLL_OPTIONS: 10,
    MAX_POLL_QUESTION_LENGTH: 200,
    MAX_POLL_OPTION_LENGTH: 100,
    MAX_TEXTAREA_HEIGHT: 400,
  };

const to = {
  question: "",
  options: [
    { id: "1", text: "" },
    { id: "2", text: "" },
  ],
  multipleChoice: false,
};

function pv() {
  const [e, t] = A(false);
  const [n, r] = A(to);

  const o = k((m) => {
    if (m.length <= yt.MAX_POLL_QUESTION_LENGTH) {
      r(g => ({
        ...g,
        question: m
      }));
    }
  }, []);

  const s = k((m, g) => {
    if (g.length <= yt.MAX_POLL_OPTION_LENGTH) {
      r(T => ({
        ...T,
        options: T.options.map(_ => _.id === m ? { ..._, text: g } : _)
      }));
    }
  }, []);

  const a = k(() => {
    if (n.options.length < yt.MAX_POLL_OPTIONS) {
      r(m => ({
        ...m,
        options: [...m.options, { id: Date.now().toString(), text: "" }]
      }));
    }
  }, [n.options.length]);

  const c = k(
    (m) => {
      if (n.options.length > yt.MIN_POLL_OPTIONS) {
        r(g => ({
          ...g,
          options: g.options.filter(T => T.id !== m)
        }));
      }
    },
    [n.options.length]
  );

  const l = k(() => {
    r(m => ({
      ...m,
      multipleChoice: !m.multipleChoice
    }));
  }, []);

  const u = k(() => {
    t(false);
    r(to);
  }, []);

  const f = k(() => {
    t(m => !m);
  }, []);

  const d = k(() => {
    if (!e) {
      return true;
    }
    const m = n.question.trim().length > 0;

    const g = n.options.filter(T => T.text.trim().length > 0);

    return m && g.length >= yt.MIN_POLL_OPTIONS;
  }, [e, n]);

  const p = k(() => {
    if (!(!e || !d())) {
      return {
        question: n.question.trim(),
        options: n.options
          .filter(m => m.text.trim().length > 0)
          .map(m => ({
          text: m.text.trim()
        })),
        multipleChoice: n.multipleChoice,
      };
    }
  }, [e, d, n]);

  const h = k(() => {
    t(false);
    r(to);
  }, []);

  return {
    isPollOpen: e,
    poll: n,
    togglePoll: f,
    handlePollQuestionChange: o,
    handlePollOptionChange: s,
    handleAddPollOption: a,
    handleRemovePollOption: c,
    handleMultipleChoiceToggle: l,
    handleClosePoll: u,
    isPollValid: d,
    getPollData: p,
    resetPoll: h,
  };
}
function Nl(e = 10, t = false) {
  const [n, r] = A([]);
  const [o, s] = A([]);
  const a = N(null);
  const c = N(n);
  const l = N(o);
  (c.current = n);
  (l.current = o);

  D(
    () => () => {
      c.current.forEach(w => URL.revokeObjectURL(w.previewUrl));

      l.current.forEach(w => URL.revokeObjectURL(w.previewUrl));
    },
    []
  );

  const u = o.length > 0;

  const f = n.some(w => w.type === "video") || o.some(w => w.type === "video");

  const d = n.some(w => w.type === "image") || o.some(w => w.type === "image");

  const p = k(() => {
    a.current?.click();
  }, []);

  const h = k(
    async (w) => {
      const I = rn.isValidVideoType(w);
      const S = rn.isValidImageType(w);
      if (I && !t) {
        mt.error(
          "Загрузка видео доступна только верифицированным пользователям"
        );
        return;
      }
      if (!S && !I) {
        mt.error("Неподдерживаемый формат файла");
        return;
      }
      const c_current = c.current;
      const l_current = l.current;

      const b =
        c_current.some(V => V.type === "video") ||
        l_current.some(V => V.type === "video");

      const E =
        c_current.some(V => V.type === "image") ||
        l_current.some(V => V.type === "image");

      if (I && E) {
        mt.error("Нельзя добавить видео вместе с изображениями");
        return;
      }
      if (S && b) {
        mt.error("Нельзя добавить изображения вместе с видео");
        return;
      }
      if (I && b) {
        mt.error("Можно загрузить только 1 видео");
        return;
      }
      const R = `upload-${Date.now()}-${Math.random().toString(36).slice(2)}`;
      const O = URL.createObjectURL(w);
      const $ = I ? "video" : "image";
      s(V => [
        ...V,
        { id: R, file: w, previewUrl: O, progress: 0, type: $ },
      ]);
      try {
        const V = await rn.uploadMedia(w);

        s(ne => ne.filter(_e => _e.id !== R));

        r(ne => [
          ...ne,
          {
            id: `img-${Date.now()}-${Math.random().toString(36).slice(2)}`,
            mediaId: V.id,
            url: V.url,
            previewUrl: O,
            type: $,
          },
        ]);
      } catch (V) {
        let ne = "Ошибка загрузки";

        if (Pe(V)) {
          (ne = Bg(V.code, V.message));
        } else if (V instanceof Error) {
          (ne = V.message);
        }

        mt.error(ne);

        s(_e => _e.filter(ae => ae.id !== R));

        URL.revokeObjectURL(O);
      }
    },
    [t]
  );

  const m = k(
    (w) => {
      const w_target = w.target;
      const w_target_files = w_target.files;
      if (!w_target_files || w_target_files.length === 0) {
        return;
      }
      const v = n.length + o.length;
      const C = e - v;
      if (C <= 0) {
        return;
      }
      Array.from(w_target_files).slice(0, C).forEach(h);
      (w_target.value = "");
    },
    [n.length, o.length, e, h]
  );

  const g = k((w) => {
    r((I) => {
      const S = I.find(v => v.id === w);

      if (S) {
        URL.revokeObjectURL(S.previewUrl);
      }

      return I.filter(v => v.id !== w);
    });

    s((I) => {
      const S = I.find(v => v.id === w);

      if (S) {
        URL.revokeObjectURL(S.previewUrl);
      }

      return I.filter(v => v.id !== w);
    });
  }, []);

  const T = k(
    (w) => {
      const I = w.filter(C => t ? rn.isValidMediaType(C) : rn.isValidImageType(C)
      );
      if (I.length === 0) {
        return;
      }
      const S = c.current.length + l.current.length;
      const v = e - S;

      if (v > 0) {
        I.slice(0, v).forEach(h);
      }
    },
    [e, h, t]
  );

  const _ = k(
    async (w) => {
      const [I, S] = w.split(",");
      const v = I.match(/:(.*?);/)?.[1] || "image/png";
      const C = atob(S);
      const b = new Uint8Array(C.length);
      for (let O = 0; O < C.length; O++) {
        b[O] = C.charCodeAt(O);
      }
      const E = new Blob([b], { type: v });
      const R = new File([E], `drawing-${Date.now()}.png`, { type: "image/png" });
      h(R);
    },
    [h]
  );

  const y = k(() => {
    n.forEach(w => URL.revokeObjectURL(w.previewUrl));

    o.forEach(w => URL.revokeObjectURL(w.previewUrl));

    r([]);
    s([]);
  }, [n, o]);

  return {
    images: n,
    uploadingImages: o,
    isUploading: u,
    hasVideo: f,
    hasImages: d,
    openFilePicker: p,
    removeImage: g,
    addImage: _,
    uploadFiles: T,
    clearAll: y,
    fileInputRef: a,
    handleFileChange: m,
  };
}
const hv = "hO06";
const mv = "lUOw";
const gv = "gZD6";
const _v = "VI3o";
const yv = "kCNi";
const vv = "zpKZ";
const wv = "ZzVZ";
const Ev = "h9BK";
const Sv = "E5bj";
const bv = "hQhs";
const Cv = "UsVU";
const Tv = "EfOb";
const Iv = "qRTk";
const kv = "PlF9";
const Nv = "aeNH";
const Rv = "asrP";
const Pv = "ttfJ";
const Av = "BjQW";
const Ov = "W9uB";
const xv = "Ec2D";
const Lv = "ft87";
const Mv = "jqTh";
const $v = "YBFV";
const Dv = "KKRT";
const Uv = "VTgv";
const Fv = "UJMf";
const Hv = "CG2H";
const Bv = "QG3V";
const Vv = "YL8J";
const Wv = "WNoT";
const jv = "cl5N";
const zv = "hl4Q";
const qv = "tvog";
const Gv = "YWZg";
const Yv = "Z9SL";

const G = {
  form: hv,
  dragActive: mv,
  whatsNew: gv,
  editor: _v,
  dragOverlay: yv,
  attachments: vv,
  attachmentPreview: wv,
  uploading: Ev,
  uploadError: Sv,
  videoPreviewWrapper: bv,
  videoPlayIcon: Cv,
  uploadOverlay: Tv,
  spinner: Iv,
  errorOverlay: kv,
  errorText: Nv,
  removeAttachment: Rv,
  actions: Pv,
  mediaButtons: Av,
  mediaButton: Ov,
  submitGroup: xv,
  charCount: Lv,
  error: Mv,
  pollContainer: $v,
  pollHeader: Dv,
  pollTitle: Uv,
  pollClose: Fv,
  pollQuestion: Hv,
  pollOptions: Bv,
  pollOptionRow: Vv,
  pollOption: Wv,
  removeOption: jv,
  addOption: zv,
  pollFooter: qv,
  pollToggle: Gv,
  active: Yv,
};

function ua({ src: e, type: t }) {
  return t === "video"
    ? i("div", {
        className: G.videoPreviewWrapper,
        children: [
          i("video", { src: e, preload: "metadata" }),
          i("div", {
            className: G.videoPlayIcon,
            children: i(ug, { size: 24 }),
          }),
        ],
      })
    : i("img", { src: e, alt: "" });
}
function Rl({ images: e, uploadingImages: t, onRemove: n }) {
  return e.length > 0 || t.length > 0
    ? i("div", {
        className: G.attachments,
        children: [
          e.map(o => i(
            "div",
            {
              className: G.attachmentPreview,
              children: [
                i(ua, { src: o.previewUrl, type: o.type }),
                i("button", {
                  className: G.removeAttachment,
                  onClick: () => n(o.id),
                  children: i(ot, {}),
                }),
              ],
            },
            o.id
          )
          ),
          t.map(o => i(
            "div",
            {
              className: `${G.attachmentPreview} ${
                o.error ? G.uploadError : G.uploading
              }`,
              children: [
                i(ua, { src: o.previewUrl, type: o.type }),
                !o.error &&
                  i("div", {
                    className: G.uploadOverlay,
                    children: i("div", { className: G.spinner }),
                  }),
                o.error &&
                  i("div", {
                    className: G.errorOverlay,
                    children: i("span", {
                      className: G.errorText,
                      children: o.error,
                    }),
                  }),
                i("button", {
                  className: G.removeAttachment,
                  onClick: () => n(o.id),
                  children: i(ot, {}),
                }),
              ],
            },
            o.id
          )
          ),
        ],
      })
    : null;
}
function Xv({
  poll: e,
  onQuestionChange: t,
  onOptionChange: n,
  onAddOption: r,
  onRemoveOption: o,
  onMultipleChoiceToggle: s,
  onClose: a,
}) {
  return i("div", {
    className: G.pollContainer,
    children: [
      i("div", {
        className: G.pollHeader,
        children: [
          i("span", { className: G.pollTitle, children: "Опрос" }),
          i("button", {
            className: G.pollClose,
            onClick: a,
            children: i(ot, {}),
          }),
        ],
      }),
      i("input", {
        type: "text",
        className: G.pollQuestion,
        placeholder: "Вопрос опроса",
        value: e.question,
        onInput: c => t(c.target.value),
      }),
      i("div", {
        className: G.pollOptions,
        children: e.options.map((c, l) => i(
          "div",
          {
            className: G.pollOptionRow,
            children: [
              i("input", {
                type: "text",
                className: G.pollOption,
                placeholder: `Вариант ${l + 1}`,
                value: c.text,
                maxLength: 50,
                onInput: u => n(c.id, u.target.value),
              }),
              e.options.length > yt.MIN_POLL_OPTIONS &&
                i("button", {
                  className: G.removeOption,
                  onClick: () => o(c.id),
                  children: i(ot, {}),
                }),
            ],
          },
          c.id
        )
        ),
      }),
      e.options.length < yt.MAX_POLL_OPTIONS &&
        i("button", {
          className: G.addOption,
          onClick: r,
          children: [i(ms, {}), i("span", { children: "Добавить вариант" })],
        }),
      i("div", {
        className: G.pollFooter,
        children: i("label", {
          className: G.pollToggle,
          children: [
            i("input", {
              type: "checkbox",
              checked: e.multipleChoice,
              onChange: s,
            }),
            i("span", { children: "Несколько вариантов ответа" }),
          ],
        }),
      }),
    ],
  });
}
const Kv = "hK7L";
const Zv = "PCh0";
const Jv = "xbUE";
const Qv = "aTaO";
const e0 = "cp16";
const t0 = "ovv8";
const n0 = "AOdw";
const r0 = "XKNB";
const o0 = "yAma";
const s0 = "gOKS";
const i0 = "WXHn";
const a0 = "VIiS";
const c0 = "b2Jh";
const l0 = "YBUI";
const u0 = "cW5m";
const d0 = "gZsI";

const xe = {
  editor: Kv,
  empty: Zv,
  bold: Jv,
  italic: Qv,
  underline: e0,
  strike: t0,
  spoiler: n0,
  monospace: r0,
  quote: o0,
  link: s0,
  menu: i0,
  buttons: a0,
  button: c0,
  linkForm: l0,
  linkInput: u0,
  linkSubmit: d0,
};

const Cr = {
  bold: xe.bold,
  italic: xe.italic,
  underline: xe.underline,
  strike: xe.strike,
  spoiler: xe.spoiler,
  monospace: xe.monospace,
  quote: xe.quote,
  link: xe.link,
};

function no(e) {
  return e
    .replace(/&/g, "&amp;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;")
    .replace(/\n/g, "<br>");
}
function f0(e) {
  return e
    .replace(/&/g, "&amp;")
    .replace(/"/g, "&quot;")
    .replace(/</g, "&lt;")
    .replace(/>/g, "&gt;");
}
function p0(e) {
  return e !== "mention" && e !== "hashtag";
}
function da(e, t) {
  if (t.length === 0) {
    return e;
  }
  let n = e;
  for (const r of t) {
    if (!p0(r.type)) {
      continue;
    }
    const o = Cr[r.type];
    const s = r.type === "link" ? ` data-url="${f0(r.url)}"` : "";
    n = `<span class="${o}"${s}>${n}</span>`;
  }
  return n;
}
function h0(e, t, n) {
  let r = 0;
  const o = document.createTreeWalker(e, NodeFilter.SHOW_TEXT);
  let s = o.nextNode();

  while (s) {
    if (s === t) {
      return r + n;
    }
    (r += s.textContent?.length || 0);
    (s = o.nextNode());
  }

  return r;
}
function m0(e, t) {
  return e
    .map((n) => {
      const r = n.offset + n.length;
      return n.offset >= t.length
        ? null
        : r > t.length
        ? { ...n, length: t.length - n.offset }
        : n;
    })
    .filter(n => n !== null);
}
function g0(e) {
  const t = [];

  const n = (r, o) => {
    if (r.nodeType === Node.TEXT_NODE) {
      return o + (r.textContent?.length || 0);
    }
    if (r.nodeType === Node.ELEMENT_NODE) {
      const s = r;
      if (s.tagName === "BR") {
        return o + 1;
      }
      let a = null;
      for (const [u, f] of Object.entries(Cr)) {
        if (s.classList.contains(f)) {
          a = u;
          break;
        }
      }
      const c = o;
      let l = o;
      for (const u of Array.from(r.childNodes)) {
        l = n(u, l);
      }
      if (a && l > c) {
        const u =
          a === "link"
            ? {
                type: "link",
                url: s.dataset.url || "",
                offset: c,
                length: l - c,
              }
            : { type: a, offset: c, length: l - c };
        t.push(u);
      }
      return l;
    }
    return o;
  };

  n(e, 0);
  return t;
}
function _0(e, t) {
  let n = e;

  while (n && n.nodeType !== Node.DOCUMENT_NODE) {
    if (n.nodeType === Node.ELEMENT_NODE) {
      const r = n;
      if (r.classList.contains(t)) {
        return r;
      }
    }
    n = n.parentNode;
  }

  return null;
}
function y0(e) {
  const e_parentNode = e.parentNode;
  if (e_parentNode) {
    while (e.firstChild) {
      e_parentNode.insertBefore(e.firstChild, e);
    }

    e_parentNode.removeChild(e);
  }
}

const v0 = [
    { type: "bold", icon: ng, title: "Жирный" },
    { type: "italic", icon: og, title: "Курсив" },
    { type: "underline", icon: cg, title: "Подчёркнутый" },
    { type: "strike", icon: ag, title: "Зачёркнутый" },
    { type: "spoiler", icon: ig, title: "Спойлер" },
    { type: "monospace", icon: rg, title: "Моноширинный" },
    { type: "quote", icon: sg, title: "Цитата" },
    { type: "link", icon: dl, title: "Ссылка" },
  ];

const Ur = Gc((
  {
    value: t,
    spans: n,
    onChange: r,
    placeholder: o = "Написать...",
    maxLength: s = 5000/* 5e3 */,
    autoFocus: a = false,
    className: c = "",
    minHeight: l = 40,
    maxHeight: u = 400,
    onSubmit: f,
    disableFormatting: d = false,
    onImagePaste: p,
  },
  h
) => {
  const m = N(null);
  const [g, T] = A(false);
  const [_, y] = A({ x: 0, y: 0 });
  const [w, I] = A(false);
  const [S, v] = A("");
  const C = N(null);
  const b = N(null);
  const E = N(null);
  const R = N(false);
  const O = N(false);
  const $ = N(t);
  const V = N(n);
  const ne = N(r);

  D(() => {
    ($.current = t);
    (V.current = n);
    (ne.current = r);
  }, [t, n, r]);

  is(
    h,
    () => ({
      insertText: (L) => {
        const m_current = m.current;
        if (!m_current) {
          return;
        }
        m_current.focus();
        const H = window.getSelection();
        if (!H) {
          return;
        }
        let J = 0;
        if (H.rangeCount > 0) {
          const Q = H.getRangeAt(0);
          J = h0(m_current, Q.startContainer, Q.startOffset);
        }
        const $_current = $.current;
        const V_current = V.current;
        const le = $_current.slice(0, J) + L + $_current.slice(J);

        const be = V_current.map(Q => Q.offset >= J
          ? { ...Q, offset: Q.offset + L.length }
          : Q.offset + Q.length > J
          ? { ...Q, length: Q.length + L.length }
          : Q
        );

        (O.current = true);
        ($.current = le);
        (V.current = be);
        const ke = document.createTextNode(L);
        if (H.rangeCount > 0) {
          const Q = H.getRangeAt(0);
          Q.deleteContents();
          Q.insertNode(ke);
          Q.setStartAfter(ke);
          Q.setEndAfter(ke);
          H.removeAllRanges();
          H.addRange(Q);
        }
        ne.current(le, be);
      },

      focus: () => {
        m.current?.focus();
      }
    }),
    []
  );

  const _e = k(() => {
    if (!t) {
      return "";
    }
    if (n.length === 0) {
      return no(t);
    }

    const L = [...n].sort((W, le) => W.offset - le.offset);

    const M = [];
    for (const W of L) {
      M.push({ pos: W.offset, type: "start", span: W });
      M.push({ pos: W.offset + W.length, type: "end", span: W });
    }
    M.sort((W, le) => W.pos !== le.pos
      ? W.pos - le.pos
      : W.type !== le.type
      ? W.type === "end"
        ? -1
        : 1
      : 0
    );
    let H = "";
    let J = 0;
    const B = [];
    for (const W of M) {
      if (W.pos > J) {
        const le = t.substring(J, W.pos);
        (H += da(no(le), B));
        (J = W.pos);
      }
      if (W.type === "start") {
        B.push(W.span);
      } else {
        const le = B.indexOf(W.span);

        if (le !== -1) {
          B.splice(le, 1);
        }
      }
    }
    if (J < t.length) {
      const W = t.substring(J);
      H += da(no(W), B);
    }
    return H || "<br>";
  }, [t, n]);

  D(() => {
    if (O.current) {
      O.current = false;
      return;
    }
    const m_current = m.current;
    if (!m_current || (document.activeElement === m_current && t !== "")) {
      return;
    }
    const M = _e();

    if (m_current.innerHTML !== M) {
      (m_current.innerHTML = M);
    }
  }, [_e, t]);

  D(() => {
    if (a && m.current) {
      const m_current = m.current;
      m_current.focus();

      if (m_current.childNodes.length > 0) {
        const M = window.getSelection();
        if (M) {
          const H = document.createRange();
          H.selectNodeContents(m_current);
          H.collapse(false);
          M.removeAllRanges();
          M.addRange(H);
        }
      }
    }
  }, [a]);

  D(() => {
    if (w && b.current) {
      b.current.focus();
    }
  }, [w]);

  const ae = k(
      (L) => {
        if (R.current) {
          return;
        }
        const m_current = m.current;
        if (!m_current) {
          return;
        }
        if (L?.data === " ") {
          const B = window.getSelection();
          if (B && B.rangeCount > 0) {
            const le = B.getRangeAt(0).startContainer;
            let be = null;
            let ke = le;

            while (ke && ke !== m_current) {
              if (ke.nodeType === Node.ELEMENT_NODE) {
                const Q = ke;
                if (Q.tagName === "SPAN" && Q.className) {
                  be = Q;
                  break;
                }
              }
              ke = ke.parentNode;
            }

            if (be) {
              const Q = be.textContent || "";
              if (Q.endsWith(" ")) {
                be.textContent = Q.slice(0, -1);
                const Et = document.createTextNode(" ");
                be.parentNode?.insertBefore(Et, be.nextSibling);
                const Ne = document.createRange();
                Ne.setStartAfter(Et);
                Ne.setEndAfter(Et);
                B.removeAllRanges();
                B.addRange(Ne);
              }
            }
          }
        }
        const H = m_current.innerText.replace(/\n$/, "");
        if (H.length > s) {
          const B = H.substring(0, s);
          (O.current = true);
          r(B, m0(n, B));
          return;
        }
        const J = g0(m_current);
        (O.current = true);
        r(H, J);
      },
      [s, r, n]
    );

  const ye = k(
    (L) => {
      if (d) {
        return;
      }
      const M = window.getSelection();
      if (!M || M.isCollapsed) {
        return;
      }
      L.preventDefault();
      (E.current = M.getRangeAt(0).cloneRange());

      const H = Math.max(
          10,
          Math.min(L.clientX - 150, window.innerWidth - 310)
        );

      const J = Math.max(10, L.clientY - 50);
      y({ x: H, y: J });
      T(true);
    },
    [d]
  );

  const Se = k(
    (L) => {
      L.preventDefault();

      if (p && L.clipboardData?.files?.length) {
        const W = Array.from(L.clipboardData.files).filter(le => le.type.startsWith("image/")
        );
        if (W.length > 0) {
          p(W);
          return;
        }
      }

      const M = L.clipboardData?.getData("text/plain") || "";
      if (!M) {
        return;
      }
      const H = window.getSelection();
      if (!H || !H.rangeCount) {
        return;
      }
      const J = H.getRangeAt(0);
      J.deleteContents();
      const B = document.createTextNode(M);
      J.insertNode(B);
      J.setStartAfter(B);
      J.setEndAfter(B);
      H.removeAllRanges();
      H.addRange(J);
      ae();
    },
    [ae, p]
  );

  const j = k(() => {
    const m_current = m.current;
    if (m_current && !$.current) {
      const M = window.getSelection();
      if (M) {
        const H = document.createRange();
        H.setStart(m_current, 0);
        H.collapse(true);
        M.removeAllRanges();
        M.addRange(H);
      }
    }
  }, []);

  const K = k(
    (L) => {
      if (L.key === "Enter" && !L.shiftKey && f) {
        L.preventDefault();
        f();
        return;
      }
      if (!d && (L.ctrlKey || L.metaKey)) {
        let M = null;
        switch (L.key.toLowerCase()) {
          case "b":
            {
              M = "bold";
              break;
            }
          case "i":
            {
              M = "italic";
              break;
            }
          case "u":
            {
              M = "underline";
              break;
            }
        }

        if (M) {
          L.preventDefault();
          Y(M);
        }
      }
    },
    [f, d]
  );

  const Y = k(
    (L, M) => {
      const m_current = m.current;
      if (!m_current) {
        return;
      }
      const J = window.getSelection();
      if (!J ||
      (E.current && (J.removeAllRanges(), J.addRange(E.current)),
      J.isCollapsed)) {
        return;
      }
      const B = J.getRangeAt(0);
      const W = document.createElement("span");
      (W.className = Cr[L]);

      if (L === "link" && M) {
        (W.dataset.url = M);
      }

      const le = _0(B.commonAncestorContainer, Cr[L]);
      if (le) {
        y0(le);
      } else {
        try {
          B.surroundContents(W);
        } catch {
          const be = B.extractContents();
          W.appendChild(be);
          B.insertNode(W);
        }
      }
      ae();
      T(false);
      I(false);
      v("");
      (E.current = null);
      m_current.focus();
    },
    [ae]
  );

  const U = k(
    (L) => {
      if (L === "link") {
        I(true);
      } else {
        Y(L);
      }
    },
    [Y]
  );

  const ve = k(
    (L) => {
      L.preventDefault();

      if (S.trim()) {
        Y("link", S.trim());
      }
    },
    [Y, S]
  );

  D(() => {
    if (!g) {
      return;
    }

    const L = (H) => {
      if (C.current &&
        !C.current.contains(H.target)) {
        T(false);
        I(false);
        v("");
        (E.current = null);
      }
    };

    const M = () => {
      T(false);
      I(false);
      v("");
      (E.current = null);
    };

    document.addEventListener("mousedown", L);
    window.addEventListener("scroll", M, true);

    return () => {
      document.removeEventListener("mousedown", L);
      window.removeEventListener("scroll", M, true);
    };
  }, [g]);
  const ce = !t;
  return i(Te, {
    children: [
      i("div", {
        ref: m,
        className: `${xe.editor} ${c} ${ce ? xe.empty : ""}`,
        contentEditable: true,
        "data-placeholder": o,
        onInput: L => ae(L),
        onFocus: j,
        onPaste: Se,
        onContextMenu: ye,
        onKeyDown: K,
        onCompositionStart: () => {
          R.current = true;
        },
        onCompositionEnd: () => {
          (R.current = false);
          ae();
        },
        style: { minHeight: l, maxHeight: u },
      }),
      g &&
        $(
          i("div", {
            ref: C,
            className: xe.menu,
            style: { left: _.x, top: _.y },
            children: w
              ? i("form", {
                  className: xe.linkForm,
                  onSubmit: ve,
                  children: [
                    i("input", {
                      ref: b,
                      type: "url",
                      className: xe.linkInput,
                      placeholder: "https://...",
                      value: S,
                      onInput: L => v(L.target.value),
                    }),
                    i("button", {
                      type: "submit",
                      className: xe.linkSubmit,
                      disabled: !S.trim(),
                      children: "OK",
                    }),
                  ],
                })
              : i("div", {
                  className: xe.buttons,
                  children: v0.map(({ type: L, icon: M, title: H }) => i(
                    "button",
                    {
                      type: "button",
                      className: xe.button,
                      onClick: () => U(L),
                      title: H,
                      children: i(M, { size: 16 }),
                    },
                    L
                  )
                  ),
                }),
          }),
          document.body
        ),
    ],
  });
});

const w0 = "q2wH";
const E0 = "tZIF";
const S0 = "rPbO";
const b0 = "n0jU";
const C0 = "LXIj";
const T0 = "FE1F";
const I0 = "WDfE";
const k0 = "LuDQ";
const N0 = "KPMg";
const R0 = "hWUL";
const P0 = "Esz6";
const A0 = "uenP";
const O0 = "wSPh";
const x0 = "S3kD";
const L0 = "pmGr";
const M0 = "hYxM";
const $0 = "JXrG";
const D0 = "G0hr";
const U0 = "r4AH";
const F0 = "wflh";
const H0 = "rCER";

const fe = {
  commentInput: w0,
  replyMode: E0,
  inputRow: S0,
  attachmentStrip: b0,
  circleButton: C0,
  micButton: T0,
  sendButton: I0,
  submitting: k0,
  textareaContainer: N0,
  expanded: R0,
  voiceMode: P0,
  inputWrapper: A0,
  commentCharCount: O0,
  error: x0,
  input: L0,
  replyHeader: M0,
  replyText: $0,
  replyName: D0,
  replyClose: U0,
  dragActive: F0,
  dragOverlay: H0,
};

const B0 = "bd1f";
const V0 = "Sig1";
const W0 = "HVsu";
const ro = { textInput: B0, entering: V0, sendButton: W0 };
const j0 = 1000/* 1e3 */;
function z0({
  text: e,
  spans: t,
  onChange: n,
  placeholder: r,
  onSubmit: o,
  isEntering: s,
  autoFocus: a,
  isSubmitting: c,
  sendDisabled: l,
  onImagePaste: u,
}) {
  const f = j0 - e.length;
  const d = f < 0;
  const p = [ro.textInput, s ? ro.entering : ""].filter(Boolean).join(" ");
  return i("div", {
    className: p,
    children: [
      i("div", {
        className: fe.inputWrapper,
        children: [
          i(Ur, {
            value: e,
            spans: t,
            onChange: n,
            placeholder: r,
            autoFocus: a,
            className: fe.input,
            minHeight: 24,
            maxHeight: 200,
            onSubmit: d ? undefined : o,
            disableFormatting: true,
            onImagePaste: u,
          }),
          d &&
            i("span", {
              className: `${fe.commentCharCount} ${fe.error}`,
              children: f,
            }),
        ],
      }),
      i("button", {
        className: `${fe.circleButton} ${fe.sendButton} ${ro.sendButton} ${
          c ? fe.submitting : ""
        }`,
        onClick: o,
        disabled: c || l || d,
        children: c ? i(Xt, { size: "xs" }) : i(dg, { size: 20 }),
      }),
    ],
  });
}
const q0 = Z(() => X(() => import("./VoiceInput-CnkjU1-_.js"), __vite__mapDeps([6, 7, 8])).then(
  e => ({
    default: e.VoiceInput
  })
)
);
function Pl({
  onSubmit: e,
  onVoiceSend: t,
  placeholder: n = "Написать комментарий...",
  replyTo: r,
  onCancelReply: o,
  autoFocus: s,
}) {
  const { text: a, spans: c, handleChange: l, reset: u } = $r();
  const [f, d] = A("text");
  const [p, h] = A(false);
  const [m, g] = A(false);
  const [T, _] = A(false);
  const [y, w] = A(false);
  const I = N(false);
  const S = N(null);
  const v = N(0);

  const {
    images: C,
    uploadingImages: b,
    isUploading: E,
    openFilePicker: R,
    removeImage: O,
    uploadFiles: $,
    clearAll: V,
    fileInputRef: ne,
    handleFileChange: _e,
  } = Nl(4);

  D(
    () => () => {
      if (S.current) {
        clearTimeout(S.current);
      }
    },
    []
  );
  const ae = C.length > 0 || b.length > 0;
  const ye = a.length > 0 || T || ae;
  const Se = f === "voice";
  const j = 1000/* 1e3 */;

  const K = async () => {
    const B = a.trim().length > 0;
    const W = C.length > 0;
    if ((!B && !W) || T || E || a.length > j) {
      return;
    }
    const le = a.trim();
    const be = [...c];

    const ke = C.map(Q => ({
      mediaId: Q.mediaId
    }));

    _(true);
    try {
      await e(le, be, ke.length > 0 ? ke : undefined);
      u();
      V();
    } catch (Q) {
      console.error("Failed to submit comment:", Q);
    } finally {
      _(false);
    }
  };

  const Y = () => {
    (I.current = true);
    d("voice");
    g(false);
  };

  const U = () => {
    h(true);
  };

  const ve = () => {
    h(false);
    d("text");
    g(true);

    if (S.current) {
      clearTimeout(S.current);
    }

    (S.current = window.setTimeout(() => {
      (S.current = null);
      g(false);
    }, 300));
  };

  const ce = k((B) => {
    B.preventDefault();
    B.stopPropagation();
    v.current++;

    if (B.dataTransfer?.types.includes("Files")) {
      w(true);
    }
  }, []);

  const L = k((B) => {
    B.preventDefault();
    B.stopPropagation();
  }, []);

  const M = k((B) => {
    B.preventDefault();
    B.stopPropagation();
    v.current--;

    if (v.current === 0) {
      w(false);
    }
  }, []);

  const H = k(
    (B) => {
      B.preventDefault();
      B.stopPropagation();
      (v.current = 0);
      w(false);
      const W = B.dataTransfer?.files;

      if (W && W.length > 0) {
        $(Array.from(W));
      }
    },
    [$]
  );

  const J = [
    fe.commentInput,
    ye ? fe.expanded : "",
    Se ? fe.voiceMode : "",
    r ? fe.replyMode : "",
    y ? fe.dragActive : "",
  ]
    .filter(Boolean)
    .join(" ");

  return i("div", {
    className: J,
    onDragEnter: ce,
    onDragOver: L,
    onDragLeave: M,
    onDrop: H,
    children: [
      y &&
        i("div", {
          className: fe.dragOverlay,
          children: [
            i(gl, { size: 24 }),
            i("span", { children: "Перетащите изображение" }),
          ],
        }),
      r &&
        i("div", {
          className: fe.replyHeader,
          children: [
            i("span", {
              className: fe.replyText,
              children: [
                "Ответ для ",
                i("span", { className: fe.replyName, children: r.authorName }),
              ],
            }),
            i("button", {
              className: fe.replyClose,
              onClick: o,
              children: i(ot, { size: 16 }),
            }),
          ],
        }),
      !Se &&
        !p &&
        ae &&
        i("div", {
          className: fe.attachmentStrip,
          children: i(Rl, { images: C, uploadingImages: b, onRemove: O }),
        }),
      i("div", {
        className: fe.inputRow,
        children: [
          i("button", {
            className: fe.circleButton,
            onClick: Se ? U : R,
            children: Se ? i(ot, { size: 20 }) : i(fl, { size: 20 }),
          }),
          i("div", {
            className: fe.textareaContainer,
            children:
              Se || p
                ? i(Ee, {
                    fallback: null,
                    children: i(q0, {
                      onCancel: U,
                      onSend: t,
                      isExiting: p,
                      onExitComplete: ve,
                    }),
                  })
                : i(z0, {
                    text: a,
                    spans: c,
                    onChange: l,
                    placeholder: n,
                    onSubmit: K,
                    isEntering: m,
                    autoFocus: s,
                    isSubmitting: T,
                    sendDisabled: E,
                    onImagePaste: $,
                  }),
          }),
          !Se &&
            !p &&
            i("button", {
              className: `${fe.circleButton} ${fe.micButton}`,
              onClick: Y,
              children: i(lg, { size: 20 }),
            }),
        ],
      }),
      i("input", {
        ref: ne,
        type: "file",
        accept: Mo,
        multiple: true,
        onChange: _e,
        style: { display: "none" },
      }),
    ],
  });
}
const G0 = "OLBS";
const Y0 = "B7rG";
const X0 = "AcpV";
const K0 = "YuXr";
const Z0 = "KS8T";
const J0 = "I9vf";
const Q0 = "BLhH";

const Ut = {
  counter: G0,
  digit: Y0,
  prev: X0,
  current: K0,
  animating: Z0,
  up: J0,
  down: Q0,
};

function ew(e) {
  if (e >= 1000000/* 1e6 */) {
    const t = e / 1000000/* 1e6 */;
    return t % 1 === 0 ? `${t}M` : `${t.toFixed(1)}M`;
  }
  if (e >= 1000/* 1e3 */) {
    const t = e / 1000/* 1e3 */;
    return t % 1 === 0 ? `${t}K` : `${t.toFixed(1)}K`;
  }
  return e.toString();
}
function Pn({ value: e }) {
  const t = ew(e);
  const n = N(e);
  const r = N(t.length);
  const o = N(Date.now());

  const [s, a] = A(() => t
    .split("")
    .map((d, p) => ({
    char: d,
    prevChar: d,
    isAnimating: false,
    key: p
  }))
  );

  const [c, l] = A(null);

  D(() => {
    if (Date.now() - o.current < 100) {
      n.current = e;
      return;
    }
    if (e === n.current) {
      return;
    }
    const p = e > n.current ? "up" : "down";
    l(p);
    (n.current = e);
    const h = t.split("");

    const m = s.map(I => I.char);

    const g = Math.max(h.length, m.length);
    const T = m.join("").padStart(g, " ").split("");

    const y = h
      .join("")
      .padStart(g, " ")
      .split("")
      .map((I, S) => {
      const v = T[S] || " ";
      const C = s[S - (g - s.length)];
      return I !== v
        ? (r.current++,
          { char: I, prevChar: v, isAnimating: true, key: r.current })
        : { char: I, prevChar: I, isAnimating: false, key: C?.key ?? S };
    })
      .filter(I => I.char !== " " || I.isAnimating);

    a(y);
    const w = setTimeout(() => {
      a(I => I.map(S => ({
        ...S,
        isAnimating: false
      })));

      l(null);
    }, 300);
    return () => clearTimeout(w);
  }, [e]);

  if (!s.some(d => d.isAnimating)) {
    return i("span", { children: t });
  }

  const f = c === "up" ? Ut.up : c === "down" ? Ut.down : "";
  return i("span", {
    className: Ut.counter,
    children: s.map(d => d.isAnimating
      ? i(
          "span",
          {
            className: `${Ut.digit} ${Ut.animating} ${f}`,
            children: [
              i("span", { className: Ut.prev, children: d.prevChar }),
              i("span", { className: Ut.current, children: d.char }),
            ],
          },
          d.key
        )
      : i("span", { children: d.char }, d.key)
    ),
  });
}
const tw = "GX6x";
const nw = "N7bG";
const rw = "xgCN";
const ow = "y7mQ";
const sw = "z1X4";
const iw = "XeDf";
const aw = "Jqab";
const cw = "cERd";
const lw = "Rn1Y";

const ut = {
  dropdownWrapper: tw,
  trigger: nw,
  menu: rw,
  hidden: ow,
  menuItem: sw,
  danger: iw,
  itemIcon: aw,
  itemLabel: cw,
  divider: lw,
};

function Al({
  trigger: e,
  items: t,
  position: n = "bottom-right",
  dividerAfter: r = [],
  className: o,
}) {
  const [s, a] = A(false);
  const [c, l] = A(false);
  const [u, f] = A({ top: 0, left: 0 });
  const d = N(null);
  const p = N(null);

  const h = k(() => {
    if (!d.current) {
      return;
    }
    const _ = d.current.getBoundingClientRect();
    const y = p.current?.offsetHeight || 150;
    const w = p.current?.offsetWidth || 160;
    let I = 0;
    let S = 0;

    if (n.startsWith("bottom")) {
      (I = _.bottom + 4);
    } else {
      (I = _.top - y - 4);
    }

    if (n.endsWith("right")) {
      (S = _.right - w);
    } else {
      (S = _.left);
    }

    const {
      innerWidth,
      innerHeight
    } = window;

    if (S + w > innerWidth) {
      (S = innerWidth - w - 8);
    }

    if (S < 8) {
      (S = 8);
    }

    if (I + y > innerHeight) {
      (I = _.top - y - 4);
    }

    if (I < 8) {
      (I = _.bottom + 4);
    }

    f({ top: I, left: S });
    l(true);
  }, [n]);

  const m = k((_) => {
    const _target = _.target;

    if (d.current &&
      !d.current.contains(_target) &&
      p.current &&
      !p.current.contains(_target)) {
      a(false);
      l(false);
    }
  }, []);

  D(() => {
    if (s) {
      h();
      document.addEventListener("mousedown", m);
      window.addEventListener("scroll", h, true);
      window.addEventListener("resize", h);

      return () => {
        document.removeEventListener("mousedown", m);
        window.removeEventListener("scroll", h, true);
        window.removeEventListener("resize", h);
      };
    }
  }, [s, m, h]);

  const g = (_) => {
    _.stopPropagation();

    if (s) {
      a(false);
      l(false);
    } else {
      a(true);
    }
  };

  const T = (_, y) => {
    _.stopPropagation();
    y.onClick();
    a(false);
    l(false);
  };

  return i("div", {
    className: `${ut.dropdownWrapper} ${o || ""}`,
    children: [
      i("div", { ref: d, className: ut.trigger, onClick: g, children: e }),
      s &&
        $(
          i("div", {
            ref: p,
            className: `${ut.menu} ${c ? "" : ut.hidden}`,
            style: { top: u.top, left: u.left },
            children: t.map((_, y) => i(
              "div",
              {
                children: [
                  i("button", {
                    type: "button",
                    className: `${ut.menuItem} ${_.danger ? ut.danger : ""}`,
                    onClick: w => T(w, _),
                    children: [
                      _.icon &&
                        i("span", {
                          className: ut.itemIcon,
                          children: _.icon,
                        }),
                      i("span", {
                        className: ut.itemLabel,
                        children: _.label,
                      }),
                    ],
                  }),
                  r.includes(_.id) &&
                    y < t.length - 1 &&
                    i("div", { className: ut.divider }),
                ],
              },
              _.id
            )
            ),
          }),
          document.body
        ),
    ],
  });
}
const uw = "R3pX";
const dw = "RwQL";
const fw = "gP2R";
const pw = "CMdI";
const hw = "X7CB";
const mw = "P5DI";
const gw = "d4YF";
const _w = "eKOW";

const Ct = {
  underline: uw,
  monospace: dw,
  quote: fw,
  spoiler: pw,
  revealed: hw,
  link: mw,
  mention: gw,
  hashtag: _w,
};

function yw(e) {
  try {
    const t = new URL(e);
    return t.protocol === "http:" || t.protocol === "https:";
  } catch {
    return false;
  }
}
function vw(e) {
  if (!yw(e)) {
    return "#";
  }
  const n = new TextEncoder().encode(e);
  const r = String.fromCharCode(...n);
  const o = btoa(r);
  return `/external?url=${encodeURIComponent(o)}`;
}
function Ol({ text: e, spans: t = [], className: n = "" }) {
  const [r, o] = A(new Set());

  const s = ge(() => {
    if (t.length === 0) {
      return [{ text: e, styles: new Set() }];
    }
    const l = [];

    t.forEach((p, h) => {
      l.push({ pos: p.offset, type: "start", span: p, index: h });
      l.push({ pos: p.offset + p.length, type: "end", span: p, index: h });
    });

    l.sort((p, h) => p.pos !== h.pos
      ? p.pos - h.pos
      : p.type !== h.type
      ? p.type === "end"
        ? -1
        : 1
      : 0
    );

    const u = [];
    let f = 0;
    const d = new Map();
    for (const p of l) {
      if (p.pos > f) {
        const h = e.substring(f, p.pos);
        const m = new Set();
        let g;
        let T;
        let _;

        d.forEach((y) => {
          m.add(y.type);

          if (y.type === "link" && y.url) {
            (g = y.url);
          }

          if (y.type === "mention" &&
            (y.username || y.id)) {
            (T = y.username || y.id);
          }

          if (y.type === "hashtag" && y.tag) {
            (_ = y.tag);
          }
        });

        u.push({ text: h, styles: m, url: g, mentionId: T, hashtag: _ });
      }

      if (p.type === "start") {
        d.set(p.index, p.span);
      } else {
        d.delete(p.index);
      }

      (f = p.pos);
    }

    if (f < e.length) {
      u.push({ text: e.substring(f), styles: new Set() });
    }

    return u;
  }, [e, t]);

  const a = (l, u) => {
    l.stopPropagation();

    o((f) => {
      const d = new Set(f);

      if (d.has(u)) {
        d.delete(u);
      } else {
        d.add(u);
      }

      return d;
    });
  };

  const c = (l, u) => {
    let l_text = l.text;

    if (l.styles.has("bold")) {
      (l_text = i("strong", { children: l_text }));
    }

    if (l.styles.has("italic")) {
      (l_text = i("em", { children: l_text }));
    }

    if (l.styles.has("underline")) {
      (l_text = i("span", { className: Ct.underline, children: l_text }));
    }

    if (l.styles.has("strike")) {
      (l_text = i("s", { children: l_text }));
    }

    if (l.styles.has("monospace")) {
      (l_text = i("code", { className: Ct.monospace, children: l_text }));
    }

    if (l.styles.has("quote")) {
      (l_text = i("span", { className: Ct.quote, children: l_text }));
    }

    if (l.styles.has("spoiler")) {
      const d = r.has(u);
      l_text = i("span", {
        className: `${Ct.spoiler} ${d ? Ct.revealed : ""}`,
        onClick: p => a(p, u),
        children: l_text,
      });
    }

    if (l.styles.has("link") && l.url) {
      const d = vw(l.url);
      l_text = i("a", {
        href: d,
        target: "_blank",
        rel: "noopener noreferrer",
        className: Ct.link,
        onClick: p => p.stopPropagation(),
        children: l_text,
      });
    }
    if (l.styles.has("mention") && l.mentionId) {
      const d = `/@${l.mentionId}`;
      l_text = i("a", {
        href: d,
        className: Ct.mention,
        onClick: (p) => {
          p.preventDefault();
          p.stopPropagation();
          Ye(d);
        },
        children: l_text,
      });
    }
    if (l.styles.has("hashtag") && l.hashtag) {
      const d = `/hashtag/${encodeURIComponent(l.hashtag)}`;
      l_text = i("a", {
        href: d,
        className: Ct.hashtag,
        onClick: (p) => {
          p.preventDefault();
          p.stopPropagation();
          Ye(d);
        },
        children: l_text,
      });
    }
    return i("span", { children: l_text }, u);
  };

  return i("span", { className: n, children: s.map((l, u) => c(l, u)) });
}
function ww(e, t, n) {
  const { isVisible: r, isRevealing: o, onRevealComplete: s } = n;
  const a = N([]);
  const c = N(null);
  const l = N(null);
  const u = N({ width: 0, height: 0 });
  const f = N(1);

  const d = k((g, T) => {
    const _ = Math.random() * 80 + 60;
    return {
      x: Math.random() * g,
      y: Math.random() * T,
      size: Math.random() * 1.2 + 0.5,
      speedX: (Math.random() - 0.5) * 0.4,
      speedY: (Math.random() - 0.5) * 0.4,
      opacity: Math.random() * 0.5 + 0.2,
      life: Math.random() * _,
      maxLife: _,
    };
  }, []);

  const p = k(
    (g, T) => {
      const _ = Math.floor((g * T) / 600);
      const y = [];
      for (let w = 0; w < _; w++) {
        y.push(d(g, T));
      }
      a.current = y;
    },
    [d]
  );

  const h = k(() => {
    const e_current = e.current;
    const t_current = t.current;
    if (!e_current || !t_current) {
      return;
    }
    const _ = t_current.getBoundingClientRect();
    if (_.width === 0 || _.height === 0) {
      return;
    }
    const y = window.devicePixelRatio || 1;
    if (u.current.width !== _.width || u.current.height !== _.height) {
      (u.current = { width: _.width, height: _.height });
      (e_current.width = _.width * y);
      (e_current.height = _.height * y);
      (e_current.style.width = `${_.width}px`);
      (e_current.style.height = `${_.height}px`);
      const w = e_current.getContext("2d");

      if (w) {
        w.setTransform(y, 0, 0, y, 0, 0);
        (l.current = w);
      }

      p(_.width, _.height);
    }
  }, [e, t, p]);

  const m = k(() => {
    f.current = 1;
  }, []);

  D(() => {
    if (!r) {
      if (c.current) {
        cancelAnimationFrame(c.current);
        (c.current = null);
      }

      return;
    }
    h();
    const g = () => {
      const l_current = l.current;
      const { width: _, height: y } = u.current;
      if (!l_current || _ === 0 || y === 0) {
        c.current = requestAnimationFrame(g);
        return;
      }
      if (o && ((f.current -= 0.05), f.current <= 0)) {
        s();
        return;
      }
      l_current.clearRect(0, 0, _, y);

      a.current.forEach((w, I) => {
        (w.x += w.speedX);
        (w.y += w.speedY);
        w.life--;

        if (w.x < 0) {
          (w.x = _);
        }

        if (w.x > _) {
          (w.x = 0);
        }

        if (w.y < 0) {
          (w.y = y);
        }

        if (w.y > y) {
          (w.y = 0);
        }

        if (w.life <= 0) {
          a.current[I] = d(_, y);
          return;
        }

        const S = w.life / w.maxLife;
        const v = S < 0.3 ? S / 0.3 : 1;
        const C = w.opacity * v * f.current;
        l_current.beginPath();
        l_current.arc(w.x, w.y, w.size, 0, Math.PI * 2);
        (l_current.fillStyle = `rgba(255, 255, 255, ${C})`);
        l_current.fill();
      });

      (c.current = requestAnimationFrame(g));
    };
    (c.current = requestAnimationFrame(g));
    window.addEventListener("resize", h);

    return () => {
      if (c.current) {
        cancelAnimationFrame(c.current);
      }

      window.removeEventListener("resize", h);
    };
  }, [r, o, d, h, s]);

  return { resetOpacity: m };
}
const Ew = "ttWw";
const Sw = "QXjI";
const bw = "zpPo";
const Cw = "IZET";
const Tw = "Ch4j";
const Tn = { container: Ew, hidden: Sw, image: bw, revealing: Cw, canvas: Tw };
const fa = 5;
function pa({
  src: e,
  alt: t = "",
  spoiler: n = false,
  width: r,
  height: o,
  className: s = "",
  onClick: a,
}) {
  const [c, l] = A(!n);
  const [u, f] = A(false);
  const [d, p] = A(false);
  const h = N(null);
  const m = N(null);
  const g = N(null);
  const T = N(false);

  const { resetOpacity: _ } = ww(h, m, {
    isVisible: d && !c && n,
    isRevealing: u,
    onRevealComplete: () => l(true),
  });

  D(() => {
    const m_current = m.current;
    if (!m_current) {
      return;
    }
    const b = new IntersectionObserver(
      (E) => {
        E.forEach((R) => {
          p(R.isIntersecting);
        });
      },
      { threshold: 0, rootMargin: "0px 200px 0px 200px" }
    );
    b.observe(m_current);

    return () => {
      b.disconnect();
    };
  }, []);

  const y = (C) => {
    (g.current = { x: C.clientX, y: C.clientY });
    (T.current = false);
  };

  const w = (C) => {
    if (!g.current) {
      return;
    }
    const b = Math.abs(C.clientX - g.current.x);
    const E = Math.abs(C.clientY - g.current.y);

    if ((b > fa || E > fa)) {
      (T.current = true);
    }
  };

  const I = (C) => {
    if (T.current) {
      (T.current = false);
      C.stopPropagation();
      return;
    }

    if (!c && !u && n) {
      C.stopPropagation();
      f(true);
      _();
    } else if (a) {
      a(C);
    }
  };

  const S = !c && n;
  const v = r && o ? { aspectRatio: `${r} / ${o}` } : undefined;
  return n
    ? i("div", {
        ref: m,
        className: `${Tn.container} ${s} ${S ? Tn.hidden : ""} ${
          u ? Tn.revealing : ""
        }`,
        style: v,
        onPointerDown: y,
        onPointerMove: w,
        onClick: I,
        children: [
          i("img", {
            src: e,
            alt: t,
            className: Tn.image,
            loading: "lazy",
            width: r,
            height: o,
            draggable: false,
          }),
          S && i("canvas", { ref: h, className: Tn.canvas }),
        ],
      })
    : i("img", {
        src: e,
        alt: t,
        className: s,
        loading: "lazy",
        draggable: false,
        onClick: a,
      });
}
const Iw = "LjKe";
const kw = "qudP";
const Nw = "stcR";
const Rw = "JBr6";
const Pw = "C9fb";
const Aw = "xaS8";
const Ow = "wgFB";

const Re = {
  mediaWrapper: Iw,
  isFeed: kw,
  single: Nw,
  image: Rw,
  singleVideo: Pw,
  media: Aw,
  dragging: Ow,
};

const ha = Z(() => X(
  () => import("./PostMediaVideo-DPEVDT0F.js"),
  __vite__mapDeps([9, 10])
).then(e => ({
  default: e.PostMediaVideo
}))
);

const xw = 5;
const Lw = 0.95;
const oo = 0.5;
function vs({ media: e, isFeed: t = false }) {
  const n = e?.filter(C => C.type === "image") ?? [];

  const r = e?.filter(C => C.type === "video") ?? [];

  const o = N(null);

  const s = al(C => C.open);

  const a = N(false);
  const c = N(0);
  const l = N(0);
  const u = N(false);
  const f = N(0);
  const d = N(0);
  const p = N(0);
  const h = N(null);

  const m = () => {
    if (h.current) {
      cancelAnimationFrame(h.current);
      (h.current = null);
    }
  };

  const g = () => {
    const o_current = o.current;
    if (o_current) {
      (p.current *= Lw);

      if (Math.abs(p.current) < oo) {
        m();
        return;
      }

      (o_current.scrollLeft += p.current);
      (h.current = requestAnimationFrame(g));
    }
  };

  const T = (C) => {
    const o_current = o.current;

    if (o_current && n.length + r.length > 1) {
      m();
      (a.current = true);
      o_current.classList.add(Re.dragging);
      (c.current = C.clientX);
      (f.current = C.clientX);
      (d.current = Date.now());
      (l.current = o_current.scrollLeft);
      (u.current = false);
      (p.current = 0);
      C.preventDefault();
    }
  };

  const _ = (C) => {
    if (!a.current) {
      return;
    }
    const o_current = o.current;
    if (!o_current) {
      return;
    }
    const E = Date.now();
    const R = C.clientX - c.current;
    const O = C.clientX - f.current;
    const $ = E - d.current;

    if (Math.abs(R) > xw) {
      (u.current = true);
    }

    if ($ > 0) {
      (p.current = (-O / $) * 16);
    }

    (f.current = C.clientX);
    (d.current = E);
    (o_current.scrollLeft = l.current - R);
  };

  const y = () => {
    if (a.current && Math.abs(p.current) > oo) {
      g();
    }

    (a.current = false);
    o.current?.classList.remove(Re.dragging);
  };

  const w = () => {
    if (a.current) {
      Math.abs(p.current) > oo && g();
      (a.current = false);
      o.current?.classList.remove(Re.dragging);
    }
  };

  D(
    () => () => {
      m();
      o.current?.classList.remove(Re.dragging);
    },
    []
  );

  const I = (C, b) => {
      if (u.current) {
        (u.current = false);
        b.stopPropagation();
        return;
      }
      s(
        n.map(E => ({
          id: E.id,
          url: E.url,
          width: E.width || 800,
          height: E.height || 600
        })),
        C
      );
    };

  const S = (C) => {
    C.stopPropagation();
    (u.current = false);
  };

  if (n.length === 0 && r.length === 0) {
    return null;
  }
  const v = n.length + r.length;
  if (v === 1) {
    if (n.length === 1) {
      const [C] = n;
      return i("div", {
        className: `${Re.mediaWrapper} ${t ? Re.isFeed : ""}`,
        "data-count": 1,
        children: i("div", {
          className: Re.single,
          onClick: (b) => {
            b.stopPropagation();
            I(0, b);
          },
          children: i(
            pa,
            {
              src: C.url,
              spoiler: C.spoiler,
              width: C.width,
              height: C.height,
              className: Re.image,
              onClick: b => I(0, b),
            },
            C.id
          ),
        }),
      });
    }
    if (r.length === 1) {
      const [C] = r;
      return i("div", {
        className: `${Re.mediaWrapper} ${t ? Re.isFeed : ""}`,
        "data-count": 1,
        onClick: b => b.stopPropagation(),
        children: i("div", {
          className: Re.singleVideo,
          children: i(Ee, {
            fallback: null,
            children: i(
              ha,
              {
                src: C.url,
                spoiler: C.spoiler,
                width: C.width,
                height: C.height,
                duration: C.duration,
              },
              C.id
            ),
          }),
        }),
      });
    }
  }
  return i("div", {
    className: `${Re.mediaWrapper} ${t ? Re.isFeed : ""}`,
    "data-count": v,
    children: i("div", {
      ref: o,
      className: `${Re.media} ${t ? Re.isFeed : ""}`,
      "data-count": v,
      onClick: S,
      onMouseDown: T,
      onMouseMove: _,
      onMouseUp: y,
      onMouseLeave: w,
      children: [
        r.map(C => i(
          Ee,
          {
            fallback: null,
            children: i(ha, {
              src: C.url,
              spoiler: C.spoiler,
              width: C.width,
              height: C.height,
              duration: C.duration,
              className: Re.image,
            }),
          },
          C.id
        )
        ),
        n.map((C, b) => i(
          pa,
          {
            src: C.url,
            spoiler: C.spoiler,
            width: C.width,
            height: C.height,
            className: Re.image,
            onClick: E => I(b, E),
          },
          C.id
        )
        ),
      ],
    }),
  });
}
const Mw = "gjHT";
const $w = "xKnw";
const Dw = "wToh";
const so = { hint: Mw, multiline: $w, arrow: Dw };
function Fo({ text: e, children: t, className: n, multiline: r }) {
  const o = N(null);
  const [s, a] = A(null);

  const c = k(() => {
    if (!o.current) {
      return;
    }
    const f = o.current.getBoundingClientRect();
    a({ x: f.left + f.width / 2, y: f.top });
  }, []);

  const l = k(() => {
    a(null);
  }, []);

  const u = k(
    (f) => {
      f.stopPropagation();

      if (s) {
        l();
      } else {
        c();
      }
    },
    [s, c, l]
  );

  D(() => {
    if (!s) {
      return;
    }
    const f = (d) => {
      if (o.current && !o.current.contains(d.target)) {
        l();
      }
    };
    document.addEventListener("touchstart", f);
    document.addEventListener("mousedown", f);
    window.addEventListener("scroll", l, true);

    return () => {
      document.removeEventListener("touchstart", f);
      document.removeEventListener("mousedown", f);
      window.removeEventListener("scroll", l, true);
    };
  }, [s, l]);

  return i("span", {
    ref: o,
    className: n,
    onMouseEnter: c,
    onMouseLeave: l,
    onClick: u,
    children: [
      t,
      s &&
        $(
          i("div", {
            className: `${so.hint} ${r ? so.multiline : ""}`,
            style: { left: `${s.x}px`, top: `${s.y}px` },
            children: [e, i("span", { className: so.arrow })],
          }),
          document.body
        ),
    ],
  });
}
const Uw = "so2s";
const Fw = "fUy4";
const Hw = "Nboe";
const Bw = "DreZ";
const Vw = "oICA";
const Ww = "m8Ma";
const jw = "ILpO";
const zw = "YqWy";

const Tt = {
  header: Uw,
  headerMain: Fw,
  moreDropdown: Hw,
  pinnedBadge: Bw,
  authorInfo: Vw,
  authorLink: Ww,
  time: jw,
  edited: zw,
};

function qw({
  author: e,
  createdAt: t,
  editedAt: n,
  postId: r,
  showAvatar: o = true,
  isOnOwnProfile: s = false,
  isPinned: a = false,
  onReport: c,
  onEdit: l,
  onDelete: u,
  onPin: f,
  onCopyLink: d,
}) {
  const p = ul(t);

  const h = he(y => y.profile?.id);

  const m = e.id === h;
  const g = 2880 * 60 * 1000/* 1e3 */;
  const T = m && Date.now() - new Date(t).getTime() < g;

  const _ = ge(() => {
    const y = [];

    y.push({
      id: "copy-link",
      label: "Скопировать ссылку",
      icon: i(dl, { size: 16 }),
      onClick: () => d?.(r),
    });

    if (T) {
      y.push({
        id: "edit",
        label: "Редактировать",
        icon: i(pl, { size: 16 }),
        onClick: () => l?.(r),
      });
    }

    if (s) {
      y.push({
        id: "pin",
        label: a ? "Открепить" : "Закрепить",
        icon: i(Yi, { size: 16 }),
        onClick: () => f?.(r),
      });
    }

    if ((m || s)) {
      y.push({
        id: "delete",
        label: "Удалить",
        icon: i(El, { size: 16 }),
        danger: true,
        onClick: () => u?.(r),
      });
    }

    if (!m) {
      y.push({
        id: "report",
        label: "Пожаловаться",
        icon: i(ml, { size: 16 }),
        danger: true,
        onClick: () => c?.(r),
      });
    }

    return y;
  }, [m, T, s, a, r, l, u, f, c, d]);

  return i("header", {
    className: Tt.header,
    children: [
      a &&
        i("div", {
          className: Tt.pinnedBadge,
          children: [
            i(Yi, { size: 14 }),
            i("span", { children: "Закреплённый пост" }),
          ],
        }),
      i("div", {
        className: Tt.headerMain,
        children: [
          o &&
            i("a", {
              href: `/@${e.username}`,
              children: i(Qe, {
                src: e.avatar,
                alt: e.displayName,
                size: "sm",
                online: e.online,
              }),
            }),
          i("div", {
            className: Tt.authorInfo,
            children: [
              i("a", {
                href: `/@${e.username}`,
                className: Tt.authorLink,
                children: i(Kn, {
                  name: e.displayName,
                  verified: e.isVerified,
                  hasNuksta: e.hasNuksta,
                  pin: e.pin,
                  size: "sm",
                }),
              }),
              i("time", {
                dateTime: t,
                className: Tt.time,
                children: [
                  p,
                  n &&
                    i(Fo, {
                      text: new Date(n).toLocaleString("ru-RU"),
                      children: i("span", {
                        className: Tt.edited,
                        children: " (ред.)",
                      }),
                    }),
                ],
              }),
            ],
          }),
          i(Al, {
            trigger: i(_l, { size: 18 }),
            items: _,
            position: "bottom-right",
            className: Tt.moreDropdown,
          }),
        ],
      }),
    ],
  });
}
const Gw = "eA5A";
const Yw = "LMqE";
const Xw = "GJWx";
const Kw = "q9Qo";
const Zw = "Hkd0";
const Jw = "mqzz";
const Qw = "nsbC";
const eE = "uHNP";
const tE = "IkvX";
const nE = "nj3o";
const rE = "IF7U";
const oE = "VDLf";
const sE = "sjio";
const iE = "g3BX";

const Ae = {
  actions: Gw,
  actionsLeft: Yw,
  action: Xw,
  views: Kw,
  liked: Zw,
  reposted: Jw,
  disabled: Qw,
  noAnimation: eE,
  reactionWrapper: tE,
  actionsRight: nE,
  captured: rE,
  capturedEmoji: oE,
  capturedText: sE,
  capturedMobile: iE,
};

function aE({
  liked: e,
  reposted: t,
  likesCount: n,
  repostsCount: r,
  commentsCount: o,
  viewsCount: s,
  dominantEmoji: a,
  onLike: c,
  onRepost: l,
  onComment: u,
  disableRepost: f = false,
}) {
  const d = N(false);
  return i("footer", {
    className: Ae.actions,
    children: [
      i("div", {
        className: Ae.actionsLeft,
        children: [
          i("div", {
            className: Ae.reactionWrapper,
            onClick: p => p.stopPropagation(),
            children: i("button", {
              className: `${Ae.action} ${e ? Ae.liked : ""} ${
                !d.current && e ? Ae.noAnimation : ""
              }`,
              onClick: (p) => {
                p.stopPropagation();
                (d.current = true);
                c();
              },
              "aria-label": "Нравится",
              children: [
                i(Dr, { filled: e }, e ? "liked" : "not-liked"),
                i(Pn, { value: n }),
              ],
            }),
          }),
          i("button", {
            className: Ae.action,
            onClick: (p) => {
              p.stopPropagation();
              u();
            },
            "aria-label": "Комментировать",
            children: [i(ps, {}), i(Pn, { value: o })],
          }),
          i("button", {
            className: `${Ae.action} ${t ? Ae.reposted : ""} ${
              f ? Ae.disabled : ""
            }`,
            onClick: (p) => {
              p.stopPropagation();

              if (!f) {
                l();
              }
            },
            disabled: f,
            "aria-label": "Репост",
            children: [i(br, {}), i(Pn, { value: r })],
          }),
        ],
      }),
      i("div", {
        className: Ae.actionsRight,
        children: [
          a &&
            i(Te, {
              children: [
                i(Fo, {
                  text: "Эмоджи, которое чаще всего лайкало этот пост",
                  className: Ae.captured,
                  children: [
                    i("span", { className: Ae.capturedEmoji, children: a }),
                    i("span", {
                      className: Ae.capturedText,
                      children: "Пост захвачен",
                    }),
                  ],
                }),
                i(Fo, {
                  text: "Эмоджи, которое чаще всего лайкало этот пост",
                  className: Ae.capturedMobile,
                  children: i("span", { children: a }),
                }),
              ],
            }),
          i("span", {
            className: Ae.views,
            children: [i(Sl, {}), i(Pn, { value: s })],
          }),
        ],
      }),
    ],
  });
}
const cE = "NLRq";
const lE = "ozPS";
const uE = "zgUL";
const dE = "xOfb";
const fE = "wWUc";
const pE = "b0ZW";
const hE = "wUNi";
const mE = "zJgZ";
const gE = "B2MJ";
const _E = "SY6G";
const yE = "ktHU";
const vE = "Stov";
const wE = "RkoF";
const EE = "YeK4";
const SE = "HGbu";
const bE = "D6AT";

const we = {
  post: cE,
  postInner: lE,
  isFeed: uE,
  postContent: dE,
  postBody: fE,
  textWrapper: pE,
  text: hE,
  collapsed: mE,
  expandButton: gE,
  originalPost: _E,
  originalPostHeader: yE,
  originalPostTime: vE,
  originalPostText: wE,
  originalPostMedia: EE,
  originalPostStats: SE,
  originalPostStat: bE,
};

function cr(e) {
  if (e >= 1000000/* 1e6 */) {
    const t = e / 1000000/* 1e6 */;
    return t % 1 === 0 ? `${t}M` : `${t.toFixed(1)}M`;
  }
  if (e >= 1000/* 1e3 */) {
    const t = e / 1000/* 1e3 */;
    return t % 1 === 0 ? `${t}K` : `${t.toFixed(1)}K`;
  }
  return e.toString();
}
function CE(e) {
  if (!e) {
    return "";
  }
  const t = new Date(e);
  return isNaN(t.getTime())
    ? ""
    : t.toLocaleDateString("ru-RU", { day: "numeric", month: "short" });
}
function TE({ attachments: e }) {
  const t = ge(() => Tr(e), [e]);
  return t.length === 0
    ? null
    : i("div", {
        className: we.originalPostMedia,
        children: i(vs, { media: t }),
      });
}
function IE({ originalPost: e }) {
  const t = CE(e.createdAt);

  const n = k(
    (r) => {
      r.stopPropagation();
      const o = e.author.username ?? e.author.id;
      Ye(`/@${o}/post/${e.id}`);
    },
    [e.author.username, e.author.id, e.id]
  );

  return i("div", {
    className: we.originalPost,
    onClick: n,
    children: [
      i("div", {
        className: we.originalPostHeader,
        children: [
          i(br, { size: 14 }),
          i(Qe, {
            src: e.author.avatar ?? "",
            alt: e.author.displayName,
            size: "xs",
          }),
          i(Kn, {
            name: e.author.displayName,
            verified: e.author.isVerified,
            hasNuksta: e.author.hasNuksta,
            pin: e.author.pin,
            size: "xs",
          }),
          i("span", { className: we.originalPostTime, children: t }),
        ],
      }),
      e.text && i("div", { className: we.originalPostText, children: e.text }),
      e.attachments &&
        e.attachments.length > 0 &&
        i(TE, { attachments: e.attachments }),
      i("div", {
        className: we.originalPostStats,
        children: [
          i("span", {
            className: we.originalPostStat,
            children: [i(Dr, { size: 16 }), cr(e.reactions?.total ?? 0)],
          }),
          i("span", {
            className: we.originalPostStat,
            children: [i(ps, { size: 16 }), cr(e.stats?.comments ?? 0)],
          }),
          i("span", {
            className: we.originalPostStat,
            children: [i(br, { size: 16 }), cr(e.stats?.reposts ?? 0)],
          }),
          i("span", {
            className: we.originalPostStat,
            children: [i(Sl, { size: 16 }), cr(e.stats?.views ?? 0)],
          }),
        ],
      }),
    ],
  });
}
const kE = Z(() => X(() => import("./index-CpKibEpf.js"), __vite__mapDeps([11, 12, 13])).then(
  e => ({
    default: e.ReportModal
  })
)
);
function NE(e, t) {
  const { isOwnPost: n, openModal: r, closeModal: o, onDelete: s } = t;
  const a = Ot();

  const c = se(b => b.deletePost);

  const l = se(b => b.updatePostLike);

  const u = se(b => b.updatePollVote);

  const f = se(b => b.updatePollData);

  const d = e.reactions.myReaction !== null;
  const p = e.reactions.total;

  const h = ge(() => xl(e.attachments), [e.attachments]);

  const m = k(async () => {
    const b = e.reactions.myReaction !== null;
    const E = b ? -1 : 1;
    l(e.id, b ? null : "love", E);
    try {
      if (b) {
        await Ue.unlikePost(e.id);
      } else {
        await Ue.likePost(e.id);
      }
    } catch (R) {
      l(e.id, b ? "love" : null, -E);
      console.error("Failed to toggle like:", R);
    }
  }, [e.id, e.reactions.myReaction, l]);

  const g = k(() => {
    if (!d) {
      m();
    }
  }, [d, m]);

  const T = k(
    (b) => {
      const E = e.author.username ?? e.author.id;
      const R = `${window.location.origin}/@${E}/post/${b}`;
      navigator.clipboard.writeText(R);
      mt.success("Ссылка скопирована");
    },
    [e.author.username, e.author.id]
  );

  const _ = k(
    (b) => {
      r(i(kE, { targetType: "post", targetId: b, onClose: o }));
    },
    [r, o]
  );

  const y = k(
    (b) => {
      r(
        i(cS, {
          postId: e.id,
          initialText: e.text ?? "",
          initialSpans: e.spans ?? [],
        })
      );
    },
    [r, e.id, e.text, e.spans]
  );

  const w = k(
    async (b) => {
      if (confirm("Вы уверены, что хотите удалить этот пост?")) {
        try {
          await c(b);
          s?.(b);
        } catch (E) {
          console.error("Failed to delete post:", E);
        }
      }
    },
    [c, s]
  );

  const I = k(() => {
    if (a) {
      r(i(G1, { postId: e.id, onClose: o }));
    } else {
      const b = e.author.username ?? e.author.id;
      Ye(`/@${b}/post/${e.id}`);
    }
  }, [e.author.username, e.author.id, e.id, a, r, o]);

  const S = k(() => {
    if (!n) {
      r(i(yS, { post: e, onClose: o }));
    }
  }, [r, o, e, n]);

  const v = k(
    async (b) => {
      const E = h?.myVote ?? null;
      u(e.id, b, E);
      try {
        const R = await Ue.votePoll(e.id, [b]);
        if (R) {
          f(e.id, R);
          return R;
        }
      } catch (R) {
        console.error("[Poll] Failed to vote:", R);

        if (E) {
          u(e.id, E, b);
        }
      }
      return null;
    },
    [e.id, h?.myVote, u, f]
  );

  const C = k(
    async (b) => {
      try {
        const E = await Ue.votePoll(e.id, b);
        if (E) {
          f(e.id, E);
          return E;
        }
      } catch (E) {
        console.error("[Poll] Failed to vote multiple:", E);
      }
      return null;
    },
    [e.id, f]
  );

  return {
    liked: d,
    totalLikes: p,
    handleLike: m,
    handleDoubleTap: g,
    handleComment: I,
    handleRepost: S,
    handleReport: _,
    handleEdit: y,
    handleDelete: w,
    handleCopyLink: T,
    handlePollVote: v,
    handlePollVoteMultiple: C,
  };
}
const RE = Z(() => X(() => import("./index-BfAQ7MuJ.js"), __vite__mapDeps([14, 15])).then(
  e => ({
    default: e.Poll
  })
)
);
function PE(e) {
  return {
    id: e.id,
    username: e.username ?? "",
    displayName: e.displayName,
    avatar: e.avatar ?? "",
    isVerified: e.isVerified,
    hasNuksta: e.hasNuksta ?? false,
    pin: e.pin ?? null,
  };
}
function Tr(e) {
  return e
    .filter(
      t => t.type === "image" ||
      t.type === "video" ||
      (t.type === "media" && "media" in t)
    )
    .map(t => t.type === "media" && "media" in t ? t.media : t);
}
function xl(e) {
  return e.find(t => t.type === "poll");
}
const AE = 300;
const OE = 500;

const xE = Mr(
  (
    {
      post: t,
      variant: n = "feed",
      className: r,
      isOnOwnProfile: o = false,
      isPinned: s = false,
      isHighlighted: a = false,
      onEdit: c,
      onPin: l,
      onDelete: u,
    }
  ) => {
    const f = n === "feed";
    const d = Ot();
    const { openModal: p, closeModal: h } = wn();

    const m = se(me => me.setCurrentPost);

    const g = se(me => me.posts.find(ee => ee.id === t.id));

    const T = he(me => me.profile);

    const { isFollowing: _, follow: y, unfollow: w } = zg(t.author.id);
    const I = f && T?.id !== t.author.id ? _ : undefined;
    const S = T?.id === t.author.id;

    const {
      liked: v,
      totalLikes: C,
      handleLike: b,
      handleDoubleTap: E,
      handleComment: R,
      handleRepost: O,
      handleReport: $,
      handleEdit: V,
      handleDelete: ne,
      handleCopyLink: _e,
      handlePollVote: ae,
      handlePollVoteMultiple: ye,
    } = NE(t, { isOwnPost: S, openModal: p, closeModal: h, onDelete: u });

    const Se = k(() => {
      if (I !== undefined) {
        if (I) {
          p(
                  i(A_, {
                    displayName: t.author.displayName,
                    onConfirm: w,
                    onClose: h,
                  })
                );
        } else {
          y();
        }
      }
    }, [I, t.author.displayName, y, w, p, h]);

    const j = N(null);
    const K = N(null);
    const Y = N(null);
    const [U, ve] = A(AE);
    const [ce, L] = A(0);
    const M = ce > U;
    D(
      () => () => {
        if (Y.current) {
          cancelAnimationFrame(Y.current);
          (Y.current = null);
        }
      },
      []
    );

    const H = k(
        (me) => {
          if (me &&
            f) {
            Y.current && cancelAnimationFrame(Y.current);

            (Y.current = requestAnimationFrame(() => {
              (Y.current = null);
              L(me.scrollHeight);
            }));
          }

          if (K) {
            (K.current = me);
          }
        },
        [f]
      );

    const J = k((me) => {
      me.stopPropagation();

      ve(ee => ee + OE);
    }, []);

    jy(t.id, j);
    const B = N(null);
    const W = N(0);

    const le = k((me) => {
      B.current = me.target;
    }, []);

    const be = k(() => {
      m(g ?? t);
      const ee = t.author.username ?? t.author.id;
      Ye(`/@${ee}/post/${t.id}`);
    }, [t, g, m]);

    const ke = k(
      (me) => {
        const me_target = me.target;
        if (me_target.closest("button") ||
        me_target.closest("a") ||
        me_target.closest("video") ||
        me_target.closest("img")) {
          return;
        }
        if (d) {
          const Es = Date.now();
          if (Es - W.current < 300) {
            (W.current = 0);
            E();
            return;
          }
          W.current = Es;
          return;
        }
        if (B.current !== me_target) {
          B.current = null;
          return;
        }
        B.current = null;
        const ct = window.getSelection();

        if (!ct || ct.toString().length <= 0) {
          be();
        }
      },
      [d, E, be]
    );

    const Q = ge(() => PE(t.author), [t.author]);

    const Et = ge(() => Tr(t.attachments), [t.attachments]);

    const Ne = ge(() => xl(t.attachments), [t.attachments]);

    const Zn = i("div", {
      className: `${we.postInner} ${f ? we.isFeed : ""} ${r || ""}`,
      children: [
        f &&
          i("a", {
            href: `/@${t.author.username ?? t.author.id}`,
            children: i(Qe, {
              src: t.author.avatar ?? "",
              alt: t.author.displayName,
              size: "sm",
              followBadge: I,
              onFollowBadgeClick: Se,
            }),
          }),
        i("div", {
          className: we.postContent,
          children: [
            i(qw, {
              author: Q,
              createdAt: t.createdAt,
              editedAt: t.editedAt,
              postId: t.id,
              showAvatar: !f,
              isOnOwnProfile: o,
              isPinned: s,
              onReport: $,
              onEdit: c ?? V,
              onDelete: ne,
              onPin: l,
              onCopyLink: _e,
            }),
            i("div", {
              className: we.postBody,
              children: [
                t.text &&
                  i("div", {
                    className: we.textWrapper,
                    children: [
                      i("div", {
                        ref: H,
                        className: `${we.text} ${M ? we.collapsed : ""}`,
                        style: f && M ? { maxHeight: `${U}px` } : undefined,
                        children: i(Ol, {
                          text: t.text,
                          spans: t.spans ?? [],
                        }),
                      }),
                      f &&
                        M &&
                        i("button", {
                          type: "button",
                          className: we.expandButton,
                          onClick: J,
                          children: "Читать далее",
                        }),
                    ],
                  }),
                Et.length > 0 && i(vs, { media: Et, isFeed: f }),
                Ne &&
                  i(Ee, {
                    fallback: null,
                    children: i(RE, {
                      title: Ne.question,
                      options: Ne.options.map(me => ({
                        id: me.id,
                        text: me.text,
                        votes: me.votes ?? 0
                      })),
                      totalVotes: Ne.totalVotes ?? 0,
                      voted:
                        (Ne.myVotes ?? []).length > 0 ||
                        (Ne.myVote !== undefined && Ne.myVote !== null),
                      selectedOptionId: Ne.myVote,
                      selectedOptionIds: Ne.myVotes ?? [],
                      multipleChoice: Ne.multipleChoice ?? false,
                      onVote: ae,
                      onVoteMultiple: ye,
                      disabled: Ne.id.startsWith("temp-"),
                    }),
                  }),
                t.originalPost && i(IE, { originalPost: t.originalPost }),
                i(aE, {
                  liked: v,
                  reposted: false,
                  likesCount: C,
                  repostsCount: t.stats.reposts,
                  commentsCount: t.stats.comments,
                  viewsCount: t.stats.views,
                  dominantEmoji: t.dominantEmoji,
                  onLike: b,
                  onRepost: O,
                  onComment: R,
                  disableRepost: S,
                }),
              ],
            }),
          ],
        }),
      ],
    });

    return f
      ? i("article", {
          ref: j,
          className: `${we.post} ${a ? "flash-highlight" : ""}`,
          onMouseDown: le,
          onClick: ke,
          children: Zn,
        })
      : i("div", { ref: j, children: Zn });
  },
  (e, t) => e.post.id === t.post.id &&
  e.post.text === t.post.text &&
  e.post.reactions.total === t.post.reactions.total &&
  e.post.reactions.myReaction === t.post.reactions.myReaction &&
  e.post.stats.comments === t.post.stats.comments &&
  e.post.stats.reposts === t.post.stats.reposts &&
  e.post.attachments === t.post.attachments &&
  e.variant === t.variant &&
  e.isOnOwnProfile === t.isOnOwnProfile &&
  e.isPinned === t.isPinned &&
  e.isHighlighted === t.isHighlighted
);

const LE = "SZrk";
const ME = "KTL1";
const $E = "nW60";
const DE = "QsvX";
const UE = "bR54";
const FE = "IVtc";
const HE = "KJck";
const BE = "n5Vt";
const VE = "fYFF";
const WE = "tuD1";
const jE = "iOM2";
const zE = "UJva";
const qE = "h946";
const GE = "bAc2";
const YE = "eR0U";
const XE = "ujBJ";
const KE = "cC8r";
const ZE = "eck7";
const JE = "JVPC";
const QE = "BzBP";
const e1 = "E9AN";
const t1 = "VaxP";
const n1 = "iJ0H";
const r1 = "uMpP";

const re = {
  commentWrapper: LE,
  threadItem: ME,
  avatarWrapper: $E,
  threadLine: DE,
  commentBody: UE,
  showMoreBtn: FE,
  avatarPlaceholder: HE,
  comment: BE,
  small: VE,
  commentTime: WE,
  commentText: jE,
  commentActions: zE,
  commentContent: qE,
  avatarLink: GE,
  authorLink: YE,
  commentHeader: XE,
  moreButton: KE,
  commentHeaderLeft: ZE,
  replyMention: JE,
  commentMedia: QE,
  reactionWrapper: e1,
  commentAction: t1,
  liked: n1,
  replyButton: r1,
};

const o1 = Z(() => X(() => import("./index-DxaTj9Qm.js"), __vite__mapDeps([16, 7, 17])).then(
  e => ({
    default: e.VoiceMessage
  })
)
);

const s1 = Mr((
  {
    author: t,
    commentId: n,
    text: r,
    spans: o = [],
    attachments: s = [],
    createdAt: a,
    reactionsCount: c,
    isReacted: l,
    size: u = "sm",
    onLike: f,
    onReply: d,
    onReport: p,
    onEdit: h,
    onDelete: m,
    replyTo: g,
    hideAvatar: T = false,
    isWallOwner: _ = false,
  }
) => {
  const y = ul(a);

  const w = he(E => E.profile?.id);

  const I = t.id === w;
  const S = I || _;
  const v = u === "xs";

  const C = ge(() => {
    const E = [];

    if (I &&
      h) {
      E.push({
        id: "edit",
        label: "Редактировать",
        icon: i(pl, { size: 16 }),
        onClick: () => h(n),
      });
    }

    if (S &&
      m) {
      E.push({
        id: "delete",
        label: "Удалить",
        icon: i(El, { size: 16 }),
        danger: true,
        onClick: () => m(n),
      });
    }

    if (!I) {
      E.push({
        id: "report",
        label: "Пожаловаться",
        icon: i(ml, { size: 16 }),
        danger: true,
        onClick: () => p(n),
      });
    }

    return E;
  }, [I, S, n, h, m, p]);

  const b = `/@${t.username ?? t.id}`;
  return i("div", {
    className: `${re.comment} ${v ? re.small : ""}`,
    children: [
      !T &&
        i("a", {
          href: b,
          className: re.avatarLink,
          children: i(Qe, { src: t.avatar, alt: t.displayName, size: u }),
        }),
      i("div", {
        className: re.commentContent,
        children: [
          i("div", {
            className: re.commentHeader,
            children: [
              i("div", {
                className: re.commentHeaderLeft,
                children: [
                  i("a", {
                    href: b,
                    className: re.authorLink,
                    children: i(Kn, {
                      name: t.displayName,
                      verified: t.isVerified,
                      hasNuksta: t.hasNuksta,
                      pin: t.pin,
                      size: u,
                    }),
                  }),
                  i("span", { className: re.commentTime, children: y }),
                ],
              }),
              i(Al, {
                trigger: i(_l, { size: v ? 14 : 16 }),
                items: C,
                position: "bottom-right",
                className: re.moreButton,
              }),
            ],
          }),
          (g || r) &&
            i("div", {
              className: re.commentText,
              children: [
                g &&
                  i(Te, {
                    children: [
                      i("a", {
                        href: `/@${g.username}`,
                        className: re.replyMention,
                        children: ["@", g.displayName],
                      }),
                      ", ",
                    ],
                  }),
                r && i(Ol, { text: r, spans: o }),
              ],
            }),
          Tr(s).length > 0 &&
            i("div", {
              className: re.commentMedia,
              children: i(vs, { media: Tr(s) }),
            }),
          s
            .filter(E => E.type === "audio")
            .map(E => i(
            Ee,
            {
              fallback: null,
              children: i(o1, { src: E.url, duration: E.duration }),
            },
            E.id
          )
            ),
          i("div", {
            className: re.commentActions,
            children: [
              i("button", {
                className: re.replyButton,
                onClick: d,
                children: "Ответить",
              }),
              i("div", {
                className: re.reactionWrapper,
                children: i("button", {
                  className: `${re.commentAction} ${l ? re.liked : ""}`,
                  onClick: () => f(),
                  children: [
                    i(Dr, { size: 14, filled: l }),
                    i(Pn, { value: c }),
                  ],
                }),
              }),
            ],
          }),
        ],
      }),
    ],
  });
});

const Ll = Mr((
  {
    comment: t,
    onLike: n,
    onLikeReply: r,
    replyingTo: o,
    onStartReply: s,
    onCancelReply: a,
    onSubmitReply: c,
    onVoiceSend: l,
    onLoadReplies: u,
    onReport: f,
    onEdit: d,
    onDelete: p,
    isLoadingReplies: h = false,
    flashingCommentId: m,
    isWallOwner: g = false,
  }
) => {
  const T = o?.commentId === t.id;

  const _ = {
    id: t.author.id,
    username: t.author.username,
    avatar: t.author.avatar ?? "",
    displayName: t.author.displayName,
    isVerified: t.author.isVerified,
    pin: t.author.pin,
  };

  const y = t.previewReplies ?? [];
  const w = m === t.id;
  const I = t.stats.replies > y.length;

  const S = [
    { type: "parent", data: t, author: _ },
    ...y.map(v => ({
      type: "reply",
      data: v,

      author: {
        id: v.author.id,
        username: v.author.username,
        avatar: v.author.avatar ?? "",
        displayName: v.author.displayName,
        isVerified: v.author.isVerified,
        pin: v.author.pin,
      }
    })),
  ];

  return i("div", {
    className: `${re.commentWrapper} ${w ? "flash-highlight" : ""}`,
    "data-comment-id": t.id,
    children: [
      S.map((v, C) => {
        const E = !(C === S.length - 1 && !T && !I);
        const R = m === v.data.id;
        return i(
          "div",
          {
            "data-comment-id": v.data.id,
            className: `${re.threadItem} ${R ? "flash-highlight" : ""}`,
            children: [
              i("div", {
                className: re.avatarWrapper,
                children: [
                  i("a", {
                    href: `/@${v.author.username ?? v.author.id}`,
                    className: re.avatarLink,
                    children: i(Qe, {
                      src: v.author.avatar,
                      alt: v.author.displayName,
                      size: "sm",
                    }),
                  }),
                  E && i("div", { className: re.threadLine }),
                ],
              }),
              i("div", {
                className: re.commentBody,
                children: i(s1, {
                  author: v.author,
                  commentId: v.data.id,
                  text: v.data.text,
                  spans: v.data.spans ?? [],
                  attachments: v.data.attachments ?? [],
                  replyTo: v.data.replyTo,
                  createdAt: v.data.createdAt,
                  reactionsCount: v.data.reactions.total,
                  isReacted: v.data.reactions.myReaction !== null,
                  size: "sm",
                  onLike: v.type === "parent" ? n : () => r(v.data.id),
                  onReply: () => v.type === "parent"
                    ? s(
                        t.id,
                        t.author.username ?? t.author.id,
                        t.author.displayName,
                        t.author.id
                      )
                    : s(
                        t.id,
                        v.data.author.username ?? v.data.author.id,
                        v.data.author.displayName,
                        v.data.author.id,
                        v.data.id
                      ),
                  onReport: f,
                  onEdit: d,
                  onDelete: p,
                  hideAvatar: true,
                  isWallOwner: g,
                }),
              }),
            ],
          },
          v.data.id
        );
      }),
      T &&
        i("div", {
          className: re.threadItem,
          children: [
            i("div", {
              className: re.avatarWrapper,
              children: [
                i("div", { className: re.avatarPlaceholder }),
                I && i("div", { className: re.threadLine }),
              ],
            }),
            i("div", {
              className: re.commentBody,
              children: i(Pl, {
                placeholder: "Написать ответ...",
                replyTo: { id: o.commentId, authorName: o.displayName },
                onCancelReply: a,
                onSubmit: c,
                onVoiceSend: l,
                autoFocus: true,
              }),
            }),
          ],
        }),
      I &&
        !h &&
        i("button", {
          className: re.showMoreBtn,
          onClick: () => u(t.id),
          children: ["Показать ещё ", t.stats.replies - y.length, " ответов"],
        }),
    ],
  });
});

function i1({
  itemCount: e,
  estimatedItemHeight: t,
  overscan: n = 5,
  getItemKey: r = o => o,
}) {
  const o = N(null);
  const [s, a] = A(0);
  const [c, l] = A(0);
  const u = N(new Map());
  const f = N(new Map());
  const d = N(0);

  if (d.current !== e) {
    (d.current = e);
    f.current.clear();
  }

  const p = k(
      (S) => {
        const v = r(S);
        return u.current.get(v) ?? t;
      },
      [r, t]
    );

  const h = k(
    (S) => {
      if (S === 0) {
        return 0;
      }
      const v = f.current.get(S);
      if (v !== undefined) {
        return v;
      }
      let C = 0;
      let b = 0;
      for (let E = S - 1; E >= 0; E--) {
        const R = f.current.get(E);
        if (R !== undefined) {
          (C = E);
          (b = R);
          break;
        }
      }
      for (let E = C; E < S; E++) {
        b += p(E);
      }
      f.current.set(S, b);
      return b;
    },
    [p]
  );

  const m = ge(() => e === 0 ? 0 : h(e - 1) + p(e - 1), [e, h, p]);

  const { startIndex: g, endIndex: T } = ge(() => {
    if (e === 0 || c === 0) {
      return { startIndex: 0, endIndex: 0 };
    }
    let S = 0;
    let v = e - 1;

    while (S < v) {
      const R = Math.floor((S + v) / 2);
      const O = h(R);
      const $ = p(R);

      if (O + $ < s) {
        (S = R + 1);
      } else {
        (v = R);
      }
    }

    const C = Math.max(0, S - n);
    let b = S;
    let E = h(S) - s;

    while (b < e && E < c + t * n) {
      (E += p(b));
      b++;
    }

    (b = Math.min(e - 1, b + n));
    return { startIndex: C, endIndex: b };
  }, [e, s, c, h, p, n, t]);

  const _ = ge(() => {
    if (e === 0) {
      return [];
    }
    const S = [];
    for (let v = g; v <= T; v++) {
      S.push({ index: v, key: r(v), start: h(v), size: p(v) });
    }
    return S;
  }, [g, T, r, h, p, e]);

  const y = k(
    (S, v) => {
      if (!S) {
        return;
      }
      const C = r(v);
      const b = S.getBoundingClientRect().height;
      if (b <= 0) {
        return;
      }
      const E = u.current.get(C);

      if ((E === undefined || Math.abs(E - b) > 2)) {
        u.current.set(C, b);
        f.current.clear();
      }
    },
    [r]
  );

  const w = k(() => {
    if (o.current) {
      a(o.current.scrollTop);
    }
  }, []);

  const I = k(
    (S) => {
      if (o.current) {
        o.current.removeEventListener("scroll", w);
      }

      (o.current = S);

      if (S) {
        l(S.clientHeight);
        a(S.scrollTop);
        S.addEventListener("scroll", w, { passive: true });
      }
    },
    [w]
  );

  D(() => {
    if (!o.current) {
      return;
    }
    const S = new ResizeObserver((v) => {
      for (const C of v) {
        l(C.contentRect.height);
      }
    });
    S.observe(o.current);

    return () => S.disconnect();
  }, []);

  D(
    () => () => {
      if (o.current) {
        o.current.removeEventListener("scroll", w);
      }
    },
    [w]
  );

  return { containerRef: I, virtualItems: _, totalSize: m, measureElement: y };
}
const a1 = "x82I";
const c1 = "piYF";
const l1 = "qRUd";
const u1 = "iFz3";
const d1 = "EAor";
const f1 = "sWb6";
const p1 = "eEBg";
const h1 = "tnk0";
const m1 = "vWRn";
const g1 = "iM48";
const _1 = "eJVB";
const y1 = "FQDD";
const v1 = "AXTv";

const He = {
  comments: a1,
  sortWrapper: c1,
  sortSelect: l1,
  commentsList: u1,
  commentItem: d1,
  empty: f1,
  loading: p1,
  loadMoreSentinel: h1,
  virtualContainer: m1,
  virtualContent: g1,
  virtualItem: _1,
  loadMoreSpinner: y1,
  inputWrapper: v1,
};

const w1 = 120;
function E1({
  comments: e,
  hasMore: t,
  isLoadingMore: n,
  onLoadMore: r,
  replyingTo: o,
  flashingCommentId: s,
  loadingRepliesId: a,
  isWallOwner: c,
  onLikeComment: l,
  onLikeReply: u,
  onStartReply: f,
  onCancelReply: d,
  onSubmitReply: p,
  onVoiceSend: h,
  onLoadReplies: m,
  onReport: g,
  onEdit: T,
  onDelete: _,
}) {
  const y = N(false);

  const {
    containerRef: w,
    virtualItems: I,
    totalSize: S,
    measureElement: v,
  } = i1({
    itemCount: e.length,
    estimatedItemHeight: w1,
    overscan: 3,
    getItemKey: b => e[b]?.id ?? b,
  });

  D(() => {
    if (!t || n || I.length === 0) {
      y.current = false;
      return;
    }
    const b = I[I.length - 1]?.index ?? 0;
    const E = e.length - 5;

    if (b >= E && !y.current) {
      (y.current = true);
      r();
    }
  }, [I, e.length, t, n, r]);

  D(() => {
    if (!n) {
      (y.current = false);
    }
  }, [n]);

  const C = k(
    (b, E) => {
      v(b, E);
    },
    [v]
  );
  return i("div", {
    ref: w,
    className: He.virtualContainer,
    "data-comments-scroll": true,
    children: [
      i("div", {
        className: He.virtualContent,
        style: { height: `${S}px` },
        children: I.map((b) => {
          const E = e[b.index];
          return E
            ? i(
                "div",
                {
                  ref: R => C(R, b.index),
                  className: He.virtualItem,
                  style: {
                    position: "absolute",
                    top: 0,
                    left: 0,
                    width: "100%",
                    transform: `translateY(${b.start}px)`,
                  },
                  children: i(Ll, {
                    comment: E,
                    onLike: () => l(E.id),
                    onLikeReply: u,
                    replyingTo: o?.commentId === E.id ? o : null,
                    onStartReply: f,
                    onCancelReply: d,
                    onSubmitReply: p,
                    onVoiceSend: h,
                    onLoadReplies: m,
                    onReport: g,
                    onEdit: T,
                    onDelete: _,
                    isLoadingReplies: a === E.id,
                    flashingCommentId: s,
                    isWallOwner: c,
                  }),
                },
                b.key
              )
            : null;
        }),
      }),
      n &&
        i("div", {
          className: He.loadMoreSpinner,
          children: i(Xt, { size: "sm" }),
        }),
    ],
  });
}
const S1 = "BeHb";
const b1 = "NWQO";
const C1 = "KHb3";
const io = { wrapper: S1, popup: b1, closing: C1 };

const T1 = Z(() => X(() => import("./index-BL62Y3xN.js"), __vite__mapDeps([18, 19])).then(
  e => ({
    default: e.EmojiPicker
  })
)
);

const ma = 280;
const ga = 380;
const ao = 8;
const I1 = 100;
const _a = 150;
const k1 = 150;
function ws({ onEmojiSelect: e, buttonClassName: t, size: n = 20 }) {
  const [r, o] = A(false);
  const [s, a] = A(false);
  const [c, l] = A(null);
  const u = N(null);
  const f = N(null);
  const d = N(null);
  const p = N(null);
  const h = N(null);
  const m = N(null);

  const g = k(() => {
    const u_current = u.current;
    if (!u_current) {
      return;
    }
    const b = u_current.getBoundingClientRect();

    const {
      innerHeight,
      innerWidth
    } = window;

    const O = innerHeight - b.bottom;
    const $ = innerWidth - b.left;
    const b_right = b.right;
    const ne = O >= ga + ao ? "bottom" : "top";
    const _e = $ >= ma || $ > b_right ? "left" : "right";
    let ae;
    let ye;

    if (ne === "top") {
      (ae = b.top - ga - ao);
    } else {
      (ae = b.bottom + ao);
    }

    if (_e === "left") {
      (ye = b.left);
    } else {
      (ye = b.right - ma);
    }

    l({
      top: ae,
      left: ye,
      transformOrigin: `${ne === "top" ? "bottom" : "top"} ${
        _e === "left" ? "left" : "right"
      }`,
    });
  }, []);

  const T = k(() => {
    if (!r && !s) {
      g();
      o(true);
    }
  }, [r, s, g]);

  const _ = k(() => {
    if (r && !s) {
      a(true);

      (m.current = window.setTimeout(() => {
        o(false);
        a(false);
      }, k1));
    }
  }, [r, s]);

  const y = () => {
    if (h.current) {
      clearTimeout(h.current);
      (h.current = null);
    }

    if (m.current) {
      clearTimeout(m.current);
      (m.current = null);
    }

    if (s) {
      a(false);
    }

    if (!r) {
      (p.current = window.setTimeout(() => {
          T();
        }, I1));
    }
  };

  const w = () => {
    if (p.current) {
      clearTimeout(p.current);
      (p.current = null);
    }

    (h.current = window.setTimeout(() => {
        _();
      }, _a));
  };

  D(
    () => () => {
      if (p.current) {
        clearTimeout(p.current);
      }

      if (h.current) {
        clearTimeout(h.current);
      }

      if (m.current) {
        clearTimeout(m.current);
      }
    },
    []
  );

  const I = () => {
    if (h.current) {
      clearTimeout(h.current);
      (h.current = null);
    }

    if (m.current) {
      clearTimeout(m.current);
      (m.current = null);
    }

    if (s) {
      a(false);
    }
  };

  const S = () => {
    h.current = window.setTimeout(() => {
      _();
    }, _a);
  };

  const v = (C) => {
    C.preventDefault();
  };

  return i("div", {
    ref: f,
    className: io.wrapper,
    onMouseEnter: y,
    onMouseLeave: w,
    onMouseDown: v,
    children: [
      i("button", {
        ref: u,
        className: t,
        title: "Добавить эмоджи",
        children: i(vg, { size: n }),
      }),
      r &&
        c &&
        $(
          i("div", {
            ref: d,
            className: `${io.popup} ${s ? io.closing : ""}`,
            style: {
              position: "fixed",
              top: c.top,
              left: c.left,
              transformOrigin: c.transformOrigin,
            },
            onMouseEnter: I,
            onMouseLeave: S,
            onMouseDown: v,
            children: i(Ee, {
              fallback: null,
              children: i(T1, { onEmojiSelect: e }),
            }),
          }),
          document.body
        ),
    ],
  });
}
const N1 = "jupe";
const R1 = "fjFm";
const P1 = "SJBt";
const A1 = "zQDp";
const O1 = "HI8m";
const x1 = "RCjH";
const L1 = "acAw";
const M1 = "kFOA";
const $1 = "vU6U";
const D1 = "UFde";
const U1 = "ePkw";
const F1 = "zNVX";

const We = {
  editCommentModal: N1,
  form: R1,
  header: P1,
  title: A1,
  content: O1,
  editor: x1,
  actions: L1,
  mediaButtons: M1,
  mediaButton: $1,
  submitGroup: D1,
  charCount: U1,
  error: F1,
};

const ya = 2000/* 2e3 */;
function H1({ commentId: e, initialText: t, initialSpans: n = [] }) {
  const { closeModal: r } = wn();

  const o = It(S => S.editComment);

  const s = he(S => S.profile);

  const a = Ot();

  const {
    text: c,
    spans: l,
    editorRef: u,
    handleChange: f,
    insertText: d,
  } = $r(t, n);

  const [p, h] = A(false);
  const m = ya - c.length;
  const g = m < 0;
  const T = c !== t;
  const _ = JSON.stringify(l) !== JSON.stringify(n);
  const y = T || _;

  const w = k(
    (S) => {
      d(S.emoji);
    },
    [d]
  );

  const I = k(async () => {
    if (!(!c.trim() || g || !y || p)) {
      h(true);
      try {
        await o(e, c, l);
        r();
      } catch (S) {
        console.error("Failed to update comment:", S);
      } finally {
        h(false);
      }
    }
  }, [c, l, g, y, p, o, e, r]);

  return i(Qt, {
    frameless: true,
    onClose: r,
    className: We.editCommentModal,
    children: i("div", {
      className: We.form,
      children: [
        i("div", {
          className: We.header,
          children: i("span", {
            className: We.title,
            children: "Редактирование комментария",
          }),
        }),
        i("div", {
          className: We.content,
          children: [
            i(Qe, { src: s?.avatar ?? "", size: "sm" }),
            i(Ur, {
              ref: u,
              value: c,
              spans: l,
              onChange: f,
              placeholder: "Комментарий...",
              maxLength: ya,
              autoFocus: true,
              className: We.editor,
              minHeight: 40,
              maxHeight: 300,
              disableFormatting: true,
            }),
          ],
        }),
        i("div", {
          className: We.actions,
          children: [
            i("div", {
              className: We.mediaButtons,
              children:
                !a &&
                i(ws, { onEmojiSelect: w, buttonClassName: We.mediaButton }),
            }),
            i("div", {
              className: We.submitGroup,
              children: [
                g &&
                  i("span", {
                    className: `${We.charCount} ${We.error}`,
                    children: m,
                  }),
                i(Xe, {
                  size: "md",
                  variant: "ghost",
                  onClick: () => r(),
                  children: "Отмена",
                }),
                i(Xe, {
                  size: "md",
                  disabled: !c.trim() || g || !y || p,
                  onClick: I,
                  children: p ? "Сохранение..." : "Сохранить",
                }),
              ],
            }),
          ],
        }),
      ],
    }),
  });
}
const B1 = Z(() => X(() => import("./index-CpKibEpf.js"), __vite__mapDeps([11, 12, 13])).then(
  e => ({
    default: e.ReportModal
  })
)
);
function V1({
  comments: e,
  isLoading: t,
  isLoadingMore: n,
  hasMore: r,
  sort: o,
  onSortChange: s,
  onLikeComment: a,
  onAddComment: c,
  onVoiceSend: l,
  onLoadMore: u,
  isWallOwner: f = false,
  variant: d = "modal",
  hideInput: p = false,
}) {
  const m = Ot() && d === "modal";
  const [g, T] = A(null);
  const [_, y] = A(null);
  const [w, I] = A(null);
  const [S, v] = A(null);
  const C = N(null);
  const { openModal: b } = wn();

  const E = It(U => U.highlightedCommentId);

  const R = It(U => U.clearHighlightedComment);

  const O = It(U => U.loadReplies);

  const $ = It(U => U.deleteComment);

  const V = It(U => U.toggleCommentLike);

  eg({
    sentinelRef: C,
    hasMore: r,
    isLoading: n,
    onLoadMore: u,
    rootMargin: "200px",
  });

  D(() => {
    if (!E) {
      return;
    }
    const U =
      document.querySelector("[data-comments-scroll]") ||
      document.querySelector("[data-comments-modal]");
    if (U) {
      U.scrollTo({ top: 0, behavior: "smooth" });
    } else {
      const ce = document.querySelector("[data-comments-section]");

      if (ce) {
        ce.scrollIntoView({ behavior: "smooth", block: "start" });
      }
    }
    const ve = window.setTimeout(() => {
      y(E);
      R();
      const ce = window.setTimeout(() => {
        y(null);
      }, 600);
      return () => clearTimeout(ce);
    }, 300);
    return () => clearTimeout(ve);
  }, [E, R]);

  const ne = async (U, ve, ce) => {
    if (g) {
      await c({
          text: U,
          spans: ve,
          parentId: g.commentId,
          replyToUserId: g.userId,
          replyToInfo: {
            id: g.userId,
            username: g.username,
            displayName: g.displayName,
          },
          attachments: ce,
        });

      T(null);
    }
  };

  const _e = (U, ve, ce, L, M) => {
    T({ commentId: U, username: ve, displayName: ce, userId: L, replyId: M });
  };

  const ae = () => {
    T(null);
  };

  const ye = k(
    (U) => {
      V(U);
    },
    [V]
  );

  const Se = k(
    async (U) => {
      I(U);
      try {
        await O(U);
      } finally {
        I(null);
      }
    },
    [O]
  );

  const j = k((U) => {
    v(U);
  }, []);

  const K = k(
    (U) => {
      let ve = "";
      let ce = [];
      for (const L of e) {
        if (L.id === U) {
          (ve = L.text);
          (ce = L.spans ?? []);
          break;
        }
        const M = L.previewReplies?.find(H => H.id === U);
        if (M) {
          (ve = M.text);
          (ce = M.spans ?? []);
          break;
        }
      }
      b(i(H1, { commentId: U, initialText: ve, initialSpans: ce }));
    },
    [e, b]
  );

  const Y = k(
    (U) => {
      if (confirm("Вы уверены, что хотите удалить этот комментарий?")) {
        $(U);
      }
    },
    [$]
  );

  return i("div", {
    className: He.comments,
    children: [
      i("div", {
        className: He.sortWrapper,
        children: i("select", {
          value: o,
          onChange: U => s(U.target.value),
          className: He.sortSelect,
          children: [
            i("option", { value: "new", children: "Новые" }),
            i("option", { value: "old", children: "Старые" }),
            i("option", { value: "popular", children: "Популярные" }),
          ],
        }),
      }),
      t
        ? i("div", { className: He.loading, children: i(Xt, {}) })
        : e.length === 0
        ? i("div", { className: He.empty, children: "Нет комментариев" })
        : m
        ? i(E1, {
            comments: e,
            hasMore: r,
            isLoadingMore: n,
            onLoadMore: u,
            replyingTo: g,
            flashingCommentId: _,
            loadingRepliesId: w,
            isWallOwner: f,
            onLikeComment: a,
            onLikeReply: ye,
            onStartReply: _e,
            onCancelReply: ae,
            onSubmitReply: ne,
            onVoiceSend: l,
            onLoadReplies: Se,
            onReport: j,
            onEdit: K,
            onDelete: Y,
          })
        : i("div", {
            className: He.commentsList,
            children: [
              e.map(U => i(
                "div",
                {
                  className: He.commentItem,
                  children: i(Ll, {
                    comment: U,
                    onLike: () => a(U.id),
                    onLikeReply: ye,
                    replyingTo: g?.commentId === U.id ? g : null,
                    onStartReply: _e,
                    onCancelReply: ae,
                    onSubmitReply: ne,
                    onVoiceSend: l,
                    onLoadReplies: Se,
                    onReport: j,
                    onEdit: K,
                    onDelete: Y,
                    isLoadingReplies: w === U.id,
                    flashingCommentId: _,
                    isWallOwner: f,
                  }),
                },
                U.id
              )
              ),
              r &&
                i("div", {
                  ref: C,
                  className: He.loadMoreSentinel,
                  children: n && i(Xt, { size: "sm" }),
                }),
            ],
          }),
      !p &&
        i("div", {
          className: He.inputWrapper,
          children: i(Pl, {
            onSubmit: (U, ve, ce) => c({ text: U, spans: ve, attachments: ce }),
            onVoiceSend: l,
          }),
        }),
      S &&
        i(Ee, {
          fallback: null,
          children: i(B1, {
            targetType: "comment",
            targetId: S,
            onClose: () => v(null),
          }),
        }),
    ],
  });
}
const W1 = "FztL";
const j1 = "GOHo";
const z1 = "OID7";
const q1 = "TWMi";
const lr = { commentsModal: W1, header: j1, title: z1, content: q1 };
function G1({ postId: e, onClose: t }) {
  const n = N(null);

  const {
    comments: r,
    commentsLoading: o,
    commentsLoadingMore: s,
    commentsHasMore: a,
    clearComments: c,
    fetchComments: l,
    loadMoreComments: u,
    toggleCommentLike: f,
    addComment: d,
  } = It(
    la(w => ({
      comments: w.comments,
      commentsLoading: w.commentsLoading,
      commentsLoadingMore: w.commentsLoadingMore,
      commentsHasMore: w.commentsHasMore,
      clearComments: w.clearComments,
      fetchComments: w.fetchComments,
      loadMoreComments: w.loadMoreComments,
      toggleCommentLike: w.toggleCommentLike,
      addComment: w.addComment
    }))
  );

  const { commentsSort: p, setCommentsSort: h } = _r(
    la(w => ({
      commentsSort: w.commentsSort,
      setCommentsSort: w.setCommentsSort
    }))
  );

  if (n.current !== e) {
    (n.current = e);
    c();
  }

  D(() => {
    l(e);
  }, [e, l]);

  const m = k(
      (w) => {
        h(w);
        l(e);
      },
      [h, l, e]
    );

  const g = k(() => {
    if (a && !s) {
      u(e);
    }
  }, [a, s, u, e]);

  const T = k(
    (w) => {
      f(w);
    },
    [f]
  );

  const _ = k(
    async (w) => {
      await d(e, w);
    },
    [d, e]
  );

  const y = k(
    async (w) => {
      const I = `voice_${Date.now()}.webm`;
      const S = new File([w], I, { type: w.type || "audio/webm" });
      const v = await rn.uploadMedia(S);
      await d(e, { text: "", attachments: [{ mediaId: v.id }] });
    },
    [d, e]
  );

  return i(Qt, {
    frameless: true,
    onClose: t,
    className: lr.commentsModal,
    children: [
      i("div", {
        className: lr.header,
        children: i("span", { className: lr.title, children: "Комментарии" }),
      }),
      i("div", {
        className: lr.content,
        "data-comments-modal": true,
        children: i(V1, {
          comments: r,
          isLoading: o,
          isLoadingMore: s,
          hasMore: a,
          sort: p,
          onSortChange: m,
          onLikeComment: T,
          onAddComment: _,
          onVoiceSend: y,
          onLoadMore: g,
        }),
      }),
    ],
  });
}
const Y1 = Z(() => X(() => import("./index-CtxhrCFP.js"), __vite__mapDeps([20, 21])).then(
  e => ({
    default: e.DrawingCanvas
  })
)
);
function Ml({
  onSubmit: e,
  autoFocus: t = false,
  placeholder: n = "Что нового?",
}) {
  const {
      text: r,
      spans: o,
      editorRef: s,
      handleChange: a,
      insertText: c,
      reset: l,
    } = $r();

  const [u, f] = A(false);
  const [d, p] = A(false);
  const [h, m] = A(false);
  const g = N(0);
  const T = Ot();
  const y = ys()?.subscription?.isActive ?? false;

  const {
    images: w,
    uploadingImages: I,
    isUploading: S,
    hasVideo: v,
    openFilePicker: C,
    removeImage: b,
    addImage: E,
    uploadFiles: R,
    clearAll: O,
    fileInputRef: $,
    handleFileChange: V,
  } = Nl(10, y);

  const {
    isPollOpen: ne,
    poll: _e,
    togglePoll: ae,
    handlePollQuestionChange: ye,
    handlePollOptionChange: Se,
    handleAddPollOption: j,
    handleRemovePollOption: K,
    handleMultipleChoiceToggle: Y,
    handleClosePoll: U,
    isPollValid: ve,
    getPollData: ce,
    resetPoll: L,
  } = pv();

  const M = yt.MAX_CHARS - r.length;
  const H = M < 0;
  const J = ne && ve();
  const B = w.length > 0 || I.length > 0;
  const W = r.trim().length > 0 || J || B;
  const le = y ? `${Mo},${Fg}` : Mo;

  const be = k(async () => {
    if (!(!W || H || S || d)) {
      p(true);
      try {
        const ee = w.map(ct => ({
          mediaId: ct.mediaId,
          url: ct.url
        }));
        await e?.(r, o, ee, ce());
        l();
        O();
        L();
      } catch {
      } finally {
        p(false);
      }
    }
  }, [W, H, S, d, r, o, w, ce, e, l, O, L]);

  const ke = k(
    (ee) => {
      E(ee);
    },
    [E]
  );

  const Q = k(
    (ee) => {
      c(ee.emoji);
    },
    [c]
  );

  const Et = k((ee) => {
    ee.preventDefault();
    ee.stopPropagation();
    g.current++;

    if (ee.dataTransfer?.types.includes("Files")) {
      m(true);
    }
  }, []);

  const Ne = k((ee) => {
    ee.preventDefault();
    ee.stopPropagation();
  }, []);

  const Zn = k((ee) => {
    ee.preventDefault();
    ee.stopPropagation();
    g.current--;

    if (g.current === 0) {
      m(false);
    }
  }, []);

  const me = k(
    (ee) => {
      ee.preventDefault();
      ee.stopPropagation();
      (g.current = 0);
      m(false);
      const ct = ee.dataTransfer?.files;

      if (ct && ct.length > 0) {
        R(Array.from(ct));
      }
    },
    [R]
  );

  return i("div", {
    className: `${G.form} ${h ? G.dragActive : ""}`,
    onDragEnter: Et,
    onDragOver: Ne,
    onDragLeave: Zn,
    onDrop: me,
    children: [
      h &&
        i("div", {
          className: G.dragOverlay,
          children: [
            i(gl, { size: 32 }),
            i("span", {
              children: y ? "Перетащите файл" : "Перетащите изображение",
            }),
          ],
        }),
      i("div", {
        className: G.whatsNew,
        children: i(Ur, {
          ref: s,
          value: r,
          spans: o,
          onChange: a,
          placeholder: n,
          autoFocus: t,
          className: G.editor,
          minHeight: 40,
          maxHeight: yt.MAX_TEXTAREA_HEIGHT,
          onImagePaste: R,
        }),
      }),
      i(Rl, { images: w, uploadingImages: I, onRemove: b }),
      i("input", {
        ref: $,
        type: "file",
        accept: le,
        multiple: !v,
        onChange: V,
        style: { display: "none" },
      }),
      ne &&
        i(Xv, {
          poll: _e,
          onQuestionChange: ye,
          onOptionChange: Se,
          onAddOption: j,
          onRemoveOption: K,
          onMultipleChoiceToggle: Y,
          onClose: U,
        }),
      i("div", {
        className: G.actions,
        children: [
          i("div", {
            className: G.mediaButtons,
            children: [
              i("button", {
                className: G.mediaButton,
                onClick: C,
                title: y ? "Добавить медиа" : "Добавить изображение",
                children: i(fl, {}),
              }),
              !T && i(ws, { onEmojiSelect: Q, buttonClassName: G.mediaButton }),
              i("button", {
                className: G.mediaButton,
                onClick: () => f(true),
                title: "Нарисовать",
                disabled: v,
                children: i(_g, {}),
              }),
              i("button", {
                className: `${G.mediaButton} ${ne ? G.active : ""}`,
                onClick: ae,
                title: "Добавить опрос",
                children: i(yg, {}),
              }),
            ],
          }),
          i("div", {
            className: G.submitGroup,
            children: [
              H &&
                i("span", {
                  className: `${G.charCount} ${G.error}`,
                  children: M,
                }),
              i(Xe, {
                size: "lg",
                disabled: !W || H || S || d,
                loading: d,
                onClick: be,
                children: "Опубликовать",
              }),
            ],
          }),
        ],
      }),
      u &&
        i(Ee, {
          fallback: null,
          children: i(Y1, {
            isOpen: u,
            onClose: () => f(false),
            onSave: ke,
            mode: "post",
          }),
        }),
    ],
  });
}
const X1 = "datX";
const K1 = "m4wM";
const va = { createPostModal: X1, title: K1 };
function Z1({ wallOwnerId: e, placeholder: t, onPostCreated: n }) {
  const { closeModal: r } = wn();

  const o = he(c => c.profile);

  const s = se(c => c.createPost);

  const a = async (c, l, u, f) => {
    if (!o) {
      return;
    }
    const d = e ?? o.id;
    await s({ wallOwnerId: d, text: c, spans: l, attachments: u, poll: f });
    await n?.();
    r();
  };

  return i(Qt, {
    frameless: true,
    onClose: r,
    className: va.createPostModal,
    children: [
      i("h2", { className: va.title, children: "Создать пост" }),
      i(Ml, { onSubmit: a, autoFocus: true, placeholder: t }),
    ],
  });
}
const J1 = "SbRx";
const Q1 = "VUOS";
const eS = "sFeW";
const tS = "Gx3D";
const nS = "poAn";
const rS = "SE2b";
const oS = "uLW9";
const sS = "eGnh";
const iS = "OGOh";
const aS = "hwGv";

const tt = {
  editPostModal: J1,
  form: Q1,
  whatsNew: eS,
  editor: tS,
  actions: nS,
  mediaButtons: rS,
  mediaButton: oS,
  submitGroup: sS,
  charCount: iS,
  error: aS,
};

const wa = 5000/* 5e3 */;
function cS({ postId: e, initialText: t, initialSpans: n = [] }) {
  const { closeModal: r } = wn();

  const o = se(S => S.editPost);

  const s = he(S => S.profile);

  const a = Ot();

  const {
    text: c,
    spans: l,
    editorRef: u,
    handleChange: f,
    insertText: d,
  } = $r(t, n);

  const [p, h] = A(false);
  const m = wa - c.length;
  const g = m < 0;
  const T = c !== t;
  const _ = JSON.stringify(l) !== JSON.stringify(n);
  const y = T || _;

  const w = k(
    (S) => {
      d(S.emoji);
    },
    [d]
  );

  const I = k(async () => {
    if (!(!c.trim() || g || !y || p)) {
      h(true);
      try {
        await o(e, c, l);
        r();
      } catch (S) {
        console.error("Failed to update post:", S);
      } finally {
        h(false);
      }
    }
  }, [c, l, g, y, p, o, e, r]);

  return i(Qt, {
    frameless: true,
    onClose: r,
    className: tt.editPostModal,
    children: i("div", {
      className: tt.form,
      children: [
        i("div", {
          className: tt.whatsNew,
          children: [
            i(Qe, { src: s?.avatar ?? "", size: "md" }),
            i(Ur, {
              ref: u,
              value: c,
              spans: l,
              onChange: f,
              placeholder: "Что нового?",
              maxLength: wa,
              autoFocus: true,
              className: tt.editor,
              minHeight: 40,
              maxHeight: 400,
            }),
          ],
        }),
        i("div", {
          className: tt.actions,
          children: [
            i("div", {
              className: tt.mediaButtons,
              children:
                !a &&
                i(ws, { onEmojiSelect: w, buttonClassName: tt.mediaButton }),
            }),
            i("div", {
              className: tt.submitGroup,
              children: [
                g &&
                  i("span", {
                    className: `${tt.charCount} ${tt.error}`,
                    children: m,
                  }),
                i(Xe, {
                  size: "lg",
                  disabled: !c.trim() || g || !y,
                  loading: p,
                  onClick: I,
                  children: "Сохранить",
                }),
              ],
            }),
          ],
        }),
      ],
    }),
  });
}
const lS = "PnrU";
const uS = "w24v";
const dS = "V619";
const fS = "e6Y1";
const pS = "CnA6";
const hS = "Jaiw";
const mS = "zsVf";
const gS = "OkeH";
const _S = "znlA";

const dt = {
  repostModal: lS,
  content: uS,
  title: dS,
  inputSection: fS,
  textarea: pS,
  originalPost: hS,
  postHeader: mS,
  postText: gS,
  actions: _S,
};

function yS({ post: e, onClose: t, onSuccess: n }) {
  const [r, o] = A("");
  const [s, a] = A(false);

  const c = he(u => u.profile);

  const l = async () => {
    a(true);
    try {
      await Ue.createRepost(e.id, r.trim() || undefined);
      n?.();
      t();
    } catch (u) {
      console.error("Failed to create repost:", u);
    } finally {
      a(false);
    }
  };

  return i(Qt, {
    onClose: t,
    showHeader: false,
    frameless: false,
    className: dt.repostModal,
    children: i("div", {
      className: dt.content,
      children: [
        i("h2", { className: dt.title, children: "Репост" }),
        i("div", {
          className: dt.inputSection,
          children: [
            c && i(Qe, { src: c.avatar, alt: c.displayName, size: "sm" }),
            i("textarea", {
              className: dt.textarea,
              placeholder: "Добавьте комментарий к репосту...",
              value: r,
              onInput: u => o(u.target.value),
              rows: 3,
            }),
          ],
        }),
        i("div", {
          className: dt.originalPost,
          children: [
            i("div", {
              className: dt.postHeader,
              children: [
                i(Qe, {
                  src: e.author.avatar ?? "",
                  alt: e.author.displayName,
                  size: "xs",
                }),
                i(Kn, {
                  name: e.author.displayName,
                  verified: e.author.isVerified,
                  hasNuksta: e.author.hasNuksta,
                  pin: e.author.pin,
                  size: "xs",
                }),
              ],
            }),
            i("p", { className: dt.postText, children: e.text }),
          ],
        }),
        i("div", {
          className: dt.actions,
          children: [
            i(Xe, {
              variant: "secondary",
              onClick: (u) => {
                u.stopPropagation();
                t();
              },
              disabled: s,
              children: "Отмена",
            }),
            i(Xe, {
              variant: "primary",
              onClick: (u) => {
                u.stopPropagation();
                l();
              },
              disabled: s,
              children: s ? "Репост..." : "Репостнуть",
            }),
          ],
        }),
      ],
    }),
  });
}

const vS = () => {
  const e = ys();
  const t = Cl();
  const { initialize: n, disconnectSSE: r } = kt();
  const o = ll();

  const s = fs(E => E.fetchPortal);

  D(
    () => {
      if (t) {
        n();
      }

      return () => {
        r();
      };
    },
    [t, n, r]
  );

  D(() => {
    s();
  }, [s]);

  const a = e?.username ? `/@${e.username}` : "/profile";

  const c = ge(
    () => [
      { id: "feed", label: "Лента", icon: hl, href: "/" },
      { id: "event", label: "Ивент", icon: null, href: "/event" },
      {
        id: "notifications",
        label: "Уведы",
        icon: yl,
        href: "/notifications",
      },
      { id: "profile", label: "Профиль", icon: vl, href: a },
    ],
    [a]
  );

  const [l, u] = A({});
  const [f, d] = A(true);
  const p = N([]);
  const h = N(null);
  const [m] = Xn();
  const { openModal: g } = wn();

  const T = se(E => E.fetchFeed);

  const _ = se(E => E.isRefreshing);

  const y = Il();

  const w = k(() => {
    window.scrollTo({ top: 0, behavior: "instant" });
    T(true);
  }, [T]);

  const I = ge(() => {
    const E = m.url || "/";
    return gs.some(R => E.startsWith(R));
  }, [m.url]);

  const S = ge(() => {
    const E = m.url || "/";
    return e?.username
      ? E === `/@${e.username}` || E.startsWith(`/@${e.username}/`)
      : false;
  }, [m.url, e?.username]);

  const v = N(null);

  const C = k((E, R = false) => {
    if (!R && v.current === E) {
      return;
    }
    v.current = E;
    const O = p.current[E];
    const h_current = h.current;
    if (O && h_current) {
      const V = parseFloat(getComputedStyle(h_current).paddingLeft) || 0;

      u({
        width: O.offsetWidth,
        transform: `translateX(${O.offsetLeft - V}px)`,
      });

      d(true);
    }
  }, []);

  D(() => {
    const E = m.url || "/";

    if (!c.some(O => O.href === E)) {
      d(false);
    }
  }, [m.url, c]);

  D(() => {
    const h_current = h.current;
    if (!h_current) {
      return;
    }
    const R = h_current.querySelector(`.${De.active}`);
    if (R) {
      const O = p.current.indexOf(R);

      if (O !== -1) {
        (v.current = null);
        C(O);
      }
    }
  }, []);

  D(() => {
    const h_current = h.current;
    if (!h_current) {
      return;
    }

    const R = () => {
        const $ = h_current.querySelector(`.${De.active}`);
        if ($) {
          const V = p.current.indexOf($);

          if (V !== -1) {
            C(V, true);
          }
        }
      };

    const O = new ResizeObserver(R);
    O.observe(h_current);
    window.addEventListener("resize", R);

    return () => {
      O.disconnect();
      window.removeEventListener("resize", R);
    };
  }, [C]);

  const b = () => {
    g(i(Z1, {}));
  };
  return I
    ? null
    : i("div", {
        className: De.mobileNavigationWrapper,
        children: [
          i("nav", {
            ref: h,
            className: De.navigation,
            children: [
              i("div", {
                className: `${De.indicator} ${f ? "" : De.indicatorHidden}`,
                style: l,
              }),
              c.map((E, R) => {
                const E_icon = E.icon;
                const $ = E.id === "event";
                const V = $ && o.active && !!o.url;
                const ne = m.url || "/";

                const ae =
                  ne === E.href ||
                  ne.startsWith(`${E.href}/`) ||
                  (E.id === "profile" && S);

                return i(
                  "a",
                  {
                    href: V ? o.url : E.href,
                    target: V ? "_blank" : undefined,
                    rel: V ? "noopener noreferrer" : undefined,
                    ref: (ye) => {
                      (p.current[R] = ye);

                      if (ye && ae) {
                        C(R);
                      }
                    },
                    className: `${De.navItem} ${ae ? De.active : ""}`,
                    onClick: (ye) => {
                      if (ae && E.id === "feed") {
                        ye.preventDefault();
                        w();
                      }
                    },
                    children: [
                      i("span", {
                        className: De.iconWrapper,
                        children: $
                          ? i("img", {
                              src: o.active
                                ? "/assets/portal/portal-active.gif"
                                : "/assets/portal/portal-inactive.png",
                              alt: "Ивент",
                              className: `${De.portalImage} ${
                                o.active ? De.portalImageActive : ""
                              }`,
                            })
                          : i(Te, {
                              children: [
                                E.id === "feed" && _ ? i(hs, {}) : i(E_icon, {}),
                                E.id === "notifications" &&
                                  y > 0 &&
                                  i("span", {
                                    className: De.badge,
                                    children: y > 99 ? "99+" : y,
                                  }),
                              ],
                            }),
                      }),
                      i("span", { className: De.label, children: E.label }),
                    ],
                  },
                  E.id
                );
              }),
            ],
          }),
          i("button", {
            className: De.createButton,
            onClick: b,
            "aria-label": "Создать пост",
            children: i(ms, {}),
          }),
        ],
      });
};

const wS = "NQF7";
const ES = "FIwq";
const SS = "wmdx";
const bS = "Uhcx";
const ur = { badge: wS, red: ES, green: SS, blue: bS };
function CS({ type: e }) {
  const t =
    e === "like"
      ? ur.red
      : ["wall_post", "reply", "repost"].includes(e)
      ? ur.green
      : ur.blue;
  return i("div", {
    className: `${ur.badge} ${t}`,
    children: [
      e === "follow" && i(ms, { size: 12 }),
      ["wall_post", "reply"].includes(e) && i(ps, { size: 12, filled: true }),
      e === "like" && i(Dr, { size: 12, filled: true }),
      e === "repost" && i(br, { size: 12 }),
    ],
  });
}
const TS = "iXuj";
const IS = "JIRi";
const kS = "VOpa";
const NS = "ja8j";
const RS = "kZxH";
const PS = "CZGA";
const AS = "EU0L";
const OS = "VOGo";
const xS = "s6Lf";
const LS = "KXKI";
const MS = "SZgD";

const ze = {
  container: TS,
  clearAllButton: IS,
  toastList: kS,
  toast: NS,
  toastLeft: RS,
  toastData: PS,
  title: AS,
  message: OS,
  dragging: xS,
  closeButton: LS,
  belowTabs: MS,
};

const $S = fn(null);
function DS({ children: e }) {
  const [t, n] = A([]);

  const r = k((c) => {
    const l = `${Date.now()}-${Math.random().toString(36).slice(2, 9)}`;
    n(u => [
      ...u,
      {
        id: l,
        message: c.message,
        notificationType: c.notificationType || "follow",
        actorName: c.actorName,
        actorUsername: c.actorUsername,
        actorAvatar: c.actorAvatar,
        clickUrl: c.clickUrl,
        apiType: c.apiType,
        entityId: c.entityId,
        parentEntityId: c.parentEntityId,
      },
    ]);
  }, []);

  const o = k((c) => {
    n(l => l.filter(u => u.id !== c));
  }, []);

  const s = k(() => {
    n([]);
  }, []);

  const a = Ay();

  D(() => {
    if (a) {
      const c = HS(a.type);

      r({
        message: a.message,
        notificationType: c,
        actorName: a.actorName,
        actorUsername: a.actorUsername,
        actorAvatar: a.actorAvatar,
        clickUrl: a.clickUrl,
        apiType: a.type,
        entityId: a.entityId,
        parentEntityId: a.parentEntityId,
      });

      kt.setState({ lastSseToast: null });
    }
  }, [a, r]);

  return i($S.Provider, {
    value: { toasts: t, addToast: r, removeToast: o, clearAll: s },
    children: [e, i(US, { toasts: t, onRemove: o, onClearAll: s })],
  });
}
function US({ toasts: e, onRemove: t, onClearAll: n }) {
  const [r, o] = A(false);

  const s =
    typeof window !== "undefined" &&
    (window.location.pathname === "/" || window.location.pathname === "");

  if (e.length === 0) {
    return null;
  }
  const a = e.slice(-4);

  const c = () => {
    o(true);

    setTimeout(() => {
      n();
      o(false);
    }, 300);
  };

  return i("div", {
    className: `${ze.container} ${s ? ze.belowTabs : ""} ym-hide-content`,
    children: [
      i("div", {
        className: `${ze.toastList} ${r ? ze.clearing : ""}`,
        children: a.map((l, u) => i(
          VS,
          {
            toast: l,
            onRemove: t,
            clearingDelay: r ? u * 50 : 0,
            isClearing: r,
          },
          l.id
        )
        ),
      }),
      e.length > 1 &&
        i(Xe, {
          className: ze.clearAllButton,
          onClick: c,
          children: "Скрыть все",
        }),
    ],
  });
}
const FS = 80;
function HS(e) {
  return (
    {
      follow: "follow",
      follow_request: "follow",
      follow_accepted: "follow",
      post_reaction: "like",
      post_comment: "reply",
      post_repost: "repost",
      comment_reaction: "like",
      comment_reply: "reply",
      post_mention: "reply",
      comment_mention: "reply",
      wall_post: "reply",
    }[e] || "follow"
  );
}
function BS(e) {
  const {
      apiType: t,
      entityId: n,
      parentEntityId: r,
      actorUsername: o,
      clickUrl: s,
    } = e;

  const a = ["post_reaction", "post_repost", "post_mention", "wall_post"];

  const c = [
    "post_comment",
    "comment_reaction",
    "comment_reply",
    "comment_mention",
  ];

  if (t && n && o) {
    if (a.includes(t)) {
      return `/@${o}/post/${n}`;
    }
    if (c.includes(t)) {
      return r ? `/@${o}/post/${r}?comment=${n}` : `/@${o}/post/${n}`;
    }
  }
  return t && ["follow", "follow_request", "follow_accepted"].includes(t) && o
    ? `/@${o}`
    : s || "/notifications";
}
function VS({
  toast: e,
  onRemove: t,
  clearingDelay: n = 0,
  isClearing: r = false,
}) {
  const o = N(null);
  const [s, a] = A(0);
  const [c, l] = A(false);
  const [u, f] = A(false);
  const d = N(0);
  const p = N(false);

  const h = (S) => {
    (d.current = S.clientX);
    (p.current = false);
    l(true);
  };

  const m = k(
    (S) => {
      if (!c) {
        return;
      }
      const v = S.clientX - d.current;

      if (Math.abs(v) > 5) {
        (p.current = true);
      }

      a(v);
    },
    [c]
  );

  const g = k(() => {
    if (c) {
      l(false);

      if (Math.abs(s) > FS) {
        f(true);
        a(s > 0 ? 400 : -400);

        setTimeout(() => t(e.id), 200);
      } else {
        a(0);

        if (!p.current) {
          const S = BS(e);

          if (S) {
            Ye(S);
            t(e.id);
          }
        }
      }
    }
  }, [c, s, t, e]);

  D(() => {
    if (c) {
      document.addEventListener("mousemove", m);
      document.addEventListener("mouseup", g);

      return () => {
        document.removeEventListener("mousemove", m);
        document.removeEventListener("mouseup", g);
      };
    }
  }, [c, m, g]);

  const T = (S) => {
    (d.current = S.touches[0].clientX);
    l(true);
  };

  const _ = (S) => {
    if (!c) {
      return;
    }
    const v = S.touches[0].clientX - d.current;
    a(v);
  };

  const y = () => {
    g();
  };

  const w = u || r ? 0 : Math.max(0, 1 - Math.abs(s) / 200);
  const I = r ? 400 : s;
  return i("div", {
    ref: o,
    className: `${ze.toast} ${c ? ze.dragging : ""}`,
    style: {
      transform: `translateX(${I}px)`,
      opacity: w,
      transition: c
        ? "none"
        : `transform 0.3s ease ${n}ms, opacity 0.3s ease ${n}ms`,
    },
    onMouseDown: h,
    onTouchStart: T,
    onTouchMove: _,
    onTouchEnd: y,
    children: [
      i("div", {
        className: ze.toastLeft,
        children: [
          i(Qe, {
            src: e.actorAvatar || "",
            badge: i(CS, { type: e.notificationType }),
          }),
          i("div", {
            className: ze.toastData,
            children: [
              e.actorName &&
                i("div", {
                  className: ze.title,
                  children: i(Kn, { name: e.actorName }),
                }),
              i("p", { className: ze.message, children: e.message }),
            ],
          }),
        ],
      }),
      i("button", {
        className: ze.closeButton,
        onClick: (S) => {
          S.stopPropagation();
          t(e.id);
        },
        onMouseDown: S => S.stopPropagation(),
        onTouchStart: S => S.stopPropagation(),
        children: i(ot, { size: 16 }),
      }),
    ],
  });
}
const WS = "KVKw";
const jS = "XyFk";
const zS = "rGzB";
const qS = "tl3r";
const GS = "ttFy";
const YS = "e58O";
const XS = "ecj0";
const KS = "pI9W";
const ZS = "NDd9";
const JS = "BLAi";

const Vt = {
  container: WS,
  toast: jS,
  slideUp: zS,
  leaving: qS,
  fadeOut: GS,
  success: YS,
  icon: XS,
  message: KS,
  closeButton: ZS,
  error: JS,
};

const QS = { success: wg, error: mg };
function eb({ id: e, type: t, message: n, onRemove: r }) {
  const [o, s] = A(false);
  const QS_t = QS[t];

  const c = k(() => {
    s(true);

    setTimeout(() => {
      r(e);
    }, 300);
  }, [e, r]);

  return i("div", {
    className: `${Vt.toast} ${Vt[t]} ${o ? Vt.leaving : ""}`,
    children: [
      i("span", { className: Vt.icon, children: i(QS_t, { size: 20 }) }),
      i("span", { className: Vt.message, children: n }),
      i("button", {
        className: Vt.closeButton,
        onClick: c,
        children: i(ot, { size: 14 }),
      }),
    ],
  });
}
function tb() {
  const e = Sr(n => n.toasts);

  const t = Sr(n => n.removeToast);

  return e.length === 0
    ? null
    : i("div", {
        className: Vt.container,
        children: e.map(n => i(
          eb,
          { id: n.id, type: n.type, message: n.message, onRemove: t },
          n.id
        )
        ),
      });
}
const nb = "dUmz";
const rb = "WDmG";
const ob = "ZHR4";
const sb = "tMC6";
const dr = { tabs: nb, indicator: rb, button: ob, active: sb };
function ib({
  tabs: e,
  defaultTab: t = 0,
  activeIndex: n,
  onChange: r,
  className: o = "",
}) {
  const [s, a] = A(t);
  const c = n !== undefined ? n : s;
  const [l, u] = A({});
  const f = N([]);
  const d = N(null);
  const p = N(false);

  const h = k(() => {
    const _ = f.current[c];
    if (_) {
      const _parentElement = _.parentElement;
      const w = _parentElement ? parseFloat(getComputedStyle(_parentElement).paddingLeft) : 0;
      const I = !p.current;

      u({
        width: _.offsetWidth,
        transform: `translateX(${_.offsetLeft - w}px)`,
        ...(I ? { transition: "none" } : {}),
      });

      if (I) {
        requestAnimationFrame(() => {
          (p.current = true);

          u((S) => {
            const { transition: v, ...C } = S;
            return C;
          });
        });
      }
    }
  }, [c]);

  D(() => {
    h();
  }, [h]);

  D(() => {
    const d_current = d.current;
    if (!d_current) {
      return;
    }
    const y = new ResizeObserver(() => {
      h();
    });
    y.observe(d_current);

    return () => {
      y.disconnect();
    };
  }, [h]);

  const m = (_) => {
    if (n === undefined) {
      a(_);
    }

    r?.(_, e[_]);
  };

  const g = _ => typeof _ == "string" ? _ : _.label;

  const T = (_, y) => typeof _ == "string" ? `${y}` : _.id;

  return i("div", {
    ref: d,
    className: `${dr.tabs} ${o}`,
    children: [
      i("div", { className: dr.indicator, style: l }),
      e.map((_, y) => i(
        "button",
        {
          ref: (w) => {
            f.current[y] = w;
          },
          onClick: () => m(y),
          className: `${dr.button} ${c === y ? dr.active : ""}`,
          children: g(_),
        },
        T(_, y)
      )
      ),
    ],
  });
}
const ab = Z(() => X(() => import("./index-B9669l9n.js"), __vite__mapDeps([22, 23, 24])).then(
  e => ({
    default: e.ImageViewer
  })
)
);
function cb() {
  const { isOpen: e, images: t, initialIndex: n, close: r } = al();
  return e
    ? i(Ee, {
        fallback: null,
        children: i(ab, { images: t, initialIndex: n, onClose: r }),
      })
    : null;
}
const lb = "Ly9I";
const ub = "yKcj";
const db = "MvLi";
const co = { layout: lb, wrapper: ub, content: db };
const fb = ["/login", "/register", "/forgot-password", "/verify-email"];

const pb = [
  "/terms",
  "/privacy",
  "/cookies",
  "/external",
  "/support",
  "/delete-account",
  "/child-safety",
];

const hb = [];

const mb = ({ children: e }) => {
  const t = Ot();
  const n = Qm();
  const [r, o] = A(window.location.pathname);
  D(() => {
    const l = () => {
      o(window.location.pathname);
    };
    window.addEventListener("popstate", l);
    const u = history.pushState.bind(history);
    const f = history.replaceState.bind(history);

    (history.pushState = (...d) => {
      u(...d);
      l();
    });

    (history.replaceState = (...d) => {
      f(...d);
      l();
    });

    return () => {
      window.removeEventListener("popstate", l);
      (history.pushState = u);
      (history.replaceState = f);
    };
  }, []);
  const s = fb.includes(r);
  const a = pb.includes(r);
  const c = hb.includes(r);
  return s
    ? i(Te, { children: e })
    : i(Jm.Provider, {
        value: { isHidden: n },
        children: i("div", {
          className: co.layout,
          children: i("div", {
            className: co.wrapper,
            children: [
              !a && !(t && c) && (t ? i(vS, {}) : i(Gy, {})),
              !a && !t && i(Jy, {}),
              i("div", { className: co.content, children: e }),
            ],
          }),
        }),
      });
};

const $l = e => {
  Ye("/", true);
  return null;
};

const gb = "hUhR";
const _b = "RQUX";
const yb = "W1cR";
const vb = "eeG5";

const fr = {
  virtualFeed: gb,
  virtualContent: _b,
  virtualItem: yb,
  loadingMore: vb,
};

function wb({
  posts: e,
  renderPost: t,
  isLoadingMore: n = false,
  hasMore: r = false,
  onLoadMore: o,
  estimatedPostHeight: s = 300,
  overscan: a = 5,
  gap: c = 10,
  initialMeasuredHeights: l,
  onMeasuredHeightsChange: u,
}) {
  const f = N(null);
  const d = N(false);
  const [p, h] = A(null);
  const [m, g] = A(window.innerWidth < 1174);

  const T = se(E => E.highlightedPostId);

  const _ = se(E => E.clearHighlightedPost);

  D(() => {
    const E = () => g(window.innerWidth < 1174);
    window.addEventListener("resize", E);

    return () => window.removeEventListener("resize", E);
  }, []);
  const y = m ? 0 : c;

  const w = k(
    (E) => {
      const e_E = e[E];
      if (!e_E) {
        return E;
      }
      const O = e_E.attachments?.[0]?.id ?? "";
      return `${e_E.id}-${O}`;
    },
    [e]
  );

  const {
    virtualItems: I,
    totalSize: S,
    measureElement: v,
    getMeasuredHeights: C,
  } = tg({
    itemCount: e.length,
    estimatedItemHeight: s,
    overscan: a,
    gap: y,
    getItemKey: w,
    initialMeasuredHeights: l,
  });

  D(
    () => () => {
      if (u) {
        u(C());
      }
    },
    [u, C]
  );

  D(() => {
    if (!T) {
      return;
    }
    f.current?.scrollIntoView({ behavior: "smooth", block: "start" });
    const E = setTimeout(() => {
      h(T);
      _();

      setTimeout(() => h(null), 600);
    }, 300);
    return () => clearTimeout(E);
  }, [T, _]);

  const b = k(() => {
    if (!o || !r || n) {
      return;
    }
    const E =
      document.documentElement.scrollHeight -
      window.scrollY -
      window.innerHeight;

    if (E < 500 && !d.current) {
      (d.current = true);
      o();
    }

    if (E > 600) {
      (d.current = false);
    }
  }, [o, r, n]);

  D(() => {
    if (!n) {
      (d.current = false);
    }
  }, [n]);

  D(
    () => {
      window.addEventListener("scroll", b, { passive: true });

      return () => window.removeEventListener("scroll", b);
    },
    [b]
  );

  return i("div", {
    ref: f,
    className: fr.virtualFeed,
    children: [
      i("div", {
        className: fr.virtualContent,
        style: { height: `${S}px` },
        children: I.map((E) => {
          const R = e[E.index];
          return R
            ? i(
                "div",
                {
                  ref: O => v(O, E.index),
                  className: fr.virtualItem,
                  style: { transform: `translateY(${E.start}px)` },
                  children: t(R, E.index, R.id === p),
                },
                E.key
              )
            : null;
        }),
      }),
      n &&
        i("div", {
          className: fr.loadingMore,
          children: i(Xt, { size: "sm" }),
        }),
    ],
  });
}
const Eb = "CpDR";
const Sb = "AQ4z";
const bb = "bYHr";
const Cb = "uTne";
const Tb = "U9Ky";
const Ib = "tMpY";

const nn = {
  page: Eb,
  createPostWrapper: Sb,
  tabsWrapper: bb,
  searchButton: Cb,
  error: Tb,
  empty: Ib,
};

const kb = (e) => {
  const t = se(v => v.posts);

  const n = se(v => v.activeFeed);

  const r = se(v => v.isLoading);

  const o = se(v => v.isLoadingMore);

  const s = se(v => v.hasMore);

  const a = se(v => v.error);

  const c = se(v => v.feedScrollPosition);

  const l = se(v => v.feedMeasuredHeights);

  const u = se(v => v.setActiveFeed);

  const f = se(v => v.fetchFeed);

  const d = se(v => v.loadMoreFeed);

  const p = se(v => v.createPost);

  const h = se(v => v.setFeedMeasuredHeights);

  const m = he(v => v.profile);

  const g = he(v => v.status);

  const T = N(false);

  const _ = ge(() => t.map(v => v.author.id), [t]);

  jg(_);

  D(() => {
    if (g === "authenticated" && t.length === 0 && !r) {
      f();
    }
  }, [n, g]);

  D(() => {
    if (t.length > 0 &&
      c > 0 &&
      !T.current) {
      (T.current = true);

      requestAnimationFrame(() => {
        window.scrollTo(0, c);
      });
    }
  }, [t.length, c]);

  const y = (v) => {
    const b = ["global", "clan", "following"][v] ?? "global";

    if (b !== n) {
      u(b);
    }
  };

  const w = async (v, C, b, E) => {
    if (m) {
      (await p({
          wallOwnerId: m.id,
          text: v,
          spans: C,
          attachments: b,
          poll: E,
        }));
    }
  };

  const I = k(() => {
    if (s && !o) {
      d();
    }
  }, [s, o, d]);

  const S = k((v, C, b) => i(xE, { post: v, isHighlighted: b }, v.id), []);

  return i("div", {
    className: nn.page,
    children: [
      i("div", {
        className: nn.tabsWrapper,
        children: [
          i(ib, {
            tabs: ["Для вас", "Лента кланов", "Подписки"],
            activeIndex: n === "global" ? 0 : n === "clan" ? 1 : 2,
            onChange: y,
          }),
          i("a", {
            href: "/search",
            className: nn.searchButton,
            "aria-label": "Поиск",
            children: i(wl, {}),
          }),
        ],
      }),
      i("div", {
        className: nn.createPostWrapper,
        children: [
          m && i(Qe, { src: m.avatar ?? "", alt: m.displayName, size: "sm" }),
          i(Ml, { onSubmit: w }),
        ],
      }),
      a
        ? i("div", {
            className: nn.error,
            children: [
              i("p", { children: a }),
              i("button", { onClick: () => f(), children: "Повторить" }),
            ],
          })
        : r && t.length === 0
        ? i(Xt, {})
        : t.length === 0
        ? i("div", { className: nn.empty, children: "Нет постов" })
        : t.length > 0
        ? i(wb, {
            posts: t,
            renderPost: S,
            isLoadingMore: o,
            hasMore: s,
            onLoadMore: I,
            estimatedPostHeight: 250,
            overscan: 3,
            initialMeasuredHeights: l,
            onMeasuredHeightsChange: h,
          })
        : null,
    ],
  });
};

const Nb = Z(() => X(() => import("./index-2izXOrDU.js"), __vite__mapDeps([25, 26])).then(
  e => ({
    default: e.GlobalVideoPlayer
  })
)
);

const Rb = Z(() => X(() => import("./index-DrHNb29y.js"), __vite__mapDeps([27, 23, 28])).then(
  e => ({
    default: e.Hashtag
  })
)
);

const Pb = Z(() => X(() => import("./index-BH27YdR9.js"), __vite__mapDeps([29, 30, 31])).then(
  e => ({
    default: e.Profile
  })
)
);

const Ab = Z(() => X(() => import("./index-DDpAeEm4.js"), __vite__mapDeps([32, 23, 33])).then(
  e => ({
    default: e.PostPage
  })
)
);

const Ob = Z(() => X(
  () => import("./index-CiWj-mK_.js"),
  __vite__mapDeps([34, 1, 30, 35])
).then(e => ({
  default: e.Notifications
}))
);

const xb = Z(() => X(() => import("./index-Cwi6RGS5.js"), __vite__mapDeps([36, 37])).then(
  e => ({
    default: e.Search
  })
)
);

const Lb = Z(() => X(() => import("./index-be4I7QTS.js"), __vite__mapDeps([38, 23, 39])).then(
  e => ({
    default: e.DeleteAccount
  })
)
);

const Mb = Z(() => X(() => import("./index-CqMzHFVy.js"), __vite__mapDeps([40, 23, 41])).then(
  e => ({
    default: e.Terms
  })
)
);

const $b = Z(() => X(() => import("./index-BGzFHrA0.js"), __vite__mapDeps([42, 23, 43])).then(
  e => ({
    default: e.Privacy
  })
)
);

const Db = Z(() => X(() => import("./index-B3jmvXK3.js"), __vite__mapDeps([44, 23, 45])).then(
  e => ({
    default: e.Cookies
  })
)
);

const Ub = Z(() => X(
  () => import("./index-DKGMn3gR.js"),
  __vite__mapDeps([46, 23, 2, 47])
).then(e => ({
  default: e.ExternalLink
}))
);

const Fb = Z(() => X(() => import("./index-BpMxfl41.js"), __vite__mapDeps([48, 23, 49])).then(
  e => ({
    default: e.Support
  })
)
);

const Hb = Z(() => X(() => import("./index-B2WBWaZg.js"), __vite__mapDeps([50, 23, 51])).then(
  e => ({
    default: e.ChildSafety
  })
)
);

const Bb = Z(() => X(() => import("./index-LYjm6vTJ.js"), __vite__mapDeps([52, 53])).then(
  e => ({
    default: e.Event
  })
)
);

const Vb = Z(() => X(() => import("./index-C6ljCNWs.js"), __vite__mapDeps([54, 23, 55])).then(
  e => ({
    default: e.SubscriptionTerms
  })
)
);

const Wb = Z(() => X(
  () => import("./index-Bz7li-X9.js"),
  __vite__mapDeps([56, 57, 58, 59, 60, 61, 62])
).then(e => ({
  default: e.Login
}))
);

const jb = Z(() => X(
  () => import("./index-BbSCDQFq.js"),
  __vite__mapDeps([63, 57, 58, 59, 60, 61, 64])
).then(e => ({
  default: e.Register
}))
);

const zb = Z(() => X(
  () => import("./index-iy_jnniV.js"),
  __vite__mapDeps([65, 57, 58, 61, 66])
).then(e => ({
  default: e.ForgotPassword
}))
);

const qb = Z(() => X(() => import("./index-_I6Z1HoX.js"), __vite__mapDeps([67, 61, 68])).then(
  e => ({
    default: e.ResetPassword
  })
)
);

const Gb = Z(() => X(() => import("./index-CMTOGEQ4.js"), []).then(e => ({
  default: e.VerifyEmail
}))
);

const Yb = Z(() => X(() => import("./index-tLqCcjk5.js"), __vite__mapDeps([69, 70])).then(
  e => ({
    default: e.Onboarding
  })
)
);

const Xb = Z(() => X(() => import("./index-Ci3hM5EN.js"), []).then(e => ({
  default: e.Verification
}))
);

function Ea(e) {
  const t = e.match(/^\/@([^/]+)\/?$/);
  return t ? t[1] : null;
}
const Kb = ({ slug: e }) => {
  if (!e?.startsWith("@")) {
    return i($l, {});
  }
  const t = e.slice(1);
  return i(Pb, { username: t });
};
function Zb() {
  const [e, t] = A(window.location.pathname);

  const n = jm(o => o.isOpen);

  return i(DS, {
    children: i(x_, {
      children: i(my, {
        currentPath: e,
        children: [
          i(cb, {}),
          n && i(Ee, { fallback: null, children: i(Nb, {}) }),
          i(tb, {}),
          i(Sy, {}),
          i(mb, {
            children: i(Ee, {
              fallback: null,
              children: i(sl, {
                onChange: (o) => {
                  const s = e;
                  t(o.url);

                  if (o.url === s) {
                    return;
                  }

                  const a = se.getState();
                  if (s === "/" || s === "") {
                    a.setFeedScrollPosition(window.scrollY);
                  } else {
                    const u = Ea(s);

                    if (u) {
                      a.setProfileScrollPosition(u, window.scrollY);
                    }
                  }
                  const c = o.url === "/";
                  const l = !!Ea(o.url);

                  if (!c && !l) {
                    window.scrollTo(0, 0);
                  }
                },
                children: [
                  i(kb, { path: "/" }),
                  i(Ob, { path: "/notifications" }),
                  i(Wb, { path: "/login" }),
                  i(jb, { path: "/register" }),
                  i(zb, { path: "/forgot-password" }),
                  i(qb, { path: "/reset-password" }),
                  i(Gb, { path: "/verify-email" }),
                  i(Mb, { path: "/terms" }),
                  i($b, { path: "/privacy" }),
                  i(Db, { path: "/cookies" }),
                  i(Yb, { path: "/onboarding" }),
                  i(xb, { path: "/search" }),
                  i(Rb, { path: "/hashtag/:name" }),
                  i(Ub, { path: "/external" }),
                  i(Fb, { path: "/support" }),
                  i(Lb, { path: "/delete-account" }),
                  i(Hb, { path: "/child-safety" }),
                  i(Bb, { path: "/event" }),
                  i(Xb, { path: "/verification" }),
                  i(Vb, { path: "/subscription-terms" }),
                  i(Ab, { path: "/:username/post/:postId" }),
                  i(Kb, { path: "/:slug" }),
                  i($l, { default: true }),
                ],
              }),
            }),
          }),
        ],
      }),
    }),
  });
}
Rm(document.getElementById("root")).render(
  i(tl, {
    children: i(Nm, {
      fallback: i("div", { children: "Something went wrong" }),
      children: i(Zb, {}),
    }),
  })
);
export {
  $ as $,
  N as A,
  Xe as B,
  Ot as C,
  Al as D,
  Cl as E,
  A_ as F,
  Z1 as G,
  ib as H,
  ot as I,
  Ml as J,
  la as K,
  It as L,
  Qt as M,
  _r as N,
  V1 as O,
  xE as P,
  Pl as Q,
  pl as R,
  Xt as S,
  ge as T,
  Kn as U,
  wb as V,
  ps as W,
  Dr as X,
  br as Y,
  kt as Z,
  X as __1,
  i as a,
  Il as a0,
  eg as a1,
  P as a2,
  x as a3,
  _s as a4,
  wl as a5,
  ll as a6,
  fs as a7,
  Pe as a8,
  oe as a9,
  hg as aa,
  ft as ab,
  Qi as ac,
  dg as ad,
  at as ae,
  il as af,
  Gc as ag,
  Xn as ah,
  is as ai,
  gg as aj,
  yl as ak,
  vl as al,
  Fo as am,
  Mr as an,
  hs as ao,
  ww as ap,
  jg as aq,
  _t as ar,
  zg as as,
  ug as b,
  se as c,
  A as d,
  Ye as e,
  Qe as f,
  _l as g,
  Sg as h,
  ml as i,
  ys as j,
  Ee as k,
  Te as l,
  ms as m,
  wn as n,
  rr as o,
  Ue as p,
  k as q,
  rn as r,
  _g as s,
  mt as t,
  jm as u,
  El as v,
  he as w,
  Do as x,
  D as y,
  Z as z,
};
